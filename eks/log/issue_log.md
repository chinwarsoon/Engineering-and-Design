# EKS Issue Log

**Project**: Engineering Knowledge System (EKS)  
**Location**: `eks/log/issue_log.md`  
**Last Updated**: 2026-07-17 (I118 RESOLVED — `_preload_infrastructure()` v2: error-to-stderr safety net + logger/heartbeat instantiation moved to `main()` for true pure-stdlib guarantee. I117 remains ✅ Resolved but refined. I112 remains 🔴 Open.) 

---

## Issue Log Table

| ID | Date | Phase | Severity | Title | Description | Status | Resolution |
| :- | :--- | :---- | :------: | :---- | :---------- | :----: | :--------- |
| I028 | 2026-06-23 | Phase 1 | 🟡 Medium | Config metadata fields break setup schema validation | T1.48 added `$schema`, `$id`, `version`, `title`, `description` to `eks_error_config.json` and `eks_message_config.json`, but setup schemas have `additionalProperties: false` — causing all test_phase1 tests to ERROR with ValidationError. Config files are data instances, not schemas; metadata fields don't belong per 3-layer pattern. | ✅ Resolved | Removed metadata fields from both config files (U077). All 114 tests pass. |
| I001 | 2026-06-15 | Phase 1 | 🟠 High | Missing `__init__.py` files in engine packages | `engine/__init__.py`, `engine/core/__init__.py`, `engine/parsers/__init__.py`, `engine/logging/__init__.py` not created per agent_rule §4.2 and workplan Section 9. Imports work via implicit namespace packages, but convention violated. | ✅ Resolved | Created 4 `__init__.py` files with import statements and version info (U011) |
| I002 | 2026-06-15 | Phase 1 | 🟠 High | Missing Phase 1 test report | `eks/workplan/reports/phase_1_foundation_report.md` not created per workplan Section 13 and agent_rule §9. Reports folder contains only `.gitkeep`. | ✅ Resolved | Generated `phase_1_foundation_report.md` (U014) |
| I003 | 2026-06-15 | Phase 1 | 🟡 Medium | Deprecated `jsonschema.RefResolver` API | `schema_loader.py:7` and `verify_schema_metadata.py:3` use deprecated `RefResolver` (deprecated since jsonschema v4.18.0). Runtime deprecation warning emitted. | ✅ Resolved | Migrated to `referencing` library API (U012) |
| I004 | 2026-06-15 | Phase 1 | 🟢 Low | Schema metadata fields in `properties` | `eks_setup_schema.json` lists `$schema, $id, version, title, description` as data properties. These are schema metadata keywords, not config data. Validates correctly but semantically imprecise. | ✅ Resolved | U013 removed these fields from `eks_setup_schema.json` properties; config strips them before validation. Confirmed in v0.9 workplan review. |
| I005 | 2026-06-18 | Phase 1 | 🟢 Low | `eks_config.json` contains placeholder project data | `project_rules_registry` and `discipline_registry` contain dummy entries (P123, P456) — test data from initial implementation. Not a blocker but misleading for future developers. | ✅ Resolved | T1.46: Replaced P123/P456 with real WSD11 project codes (131101, 131242). Added `$ref` to fragment schemas for discipline, project, department, and facility registries. Config now references external schema files for lookup data. |
| I006 | 2026-06-16 | Phase 1 | 🟠 High | Document Registry Technical Gaps (G1-G3) & Extended Metadata | Critical gaps identified in Phase 1 registry: missing `source_type` (G1) for P&ID references; SQL injection risk in filters (G2); Python-side sorting in `get_revision_history` (G3). Added 11 extended metadata fields for extraction/verification workflow. | ✅ Resolved | Fixed in T1.21/T1.22: Added `source_type` and 11 extended fields to schema/DB; implemented COLUMN_ALLOWLIST for filters; migrated sorting to SQL ORDER BY; added JSON array support for asset_tags. |
| I007 | 2026-06-18 | Phase 1 | 🟠 High | Ontology Schema Refactor to Triple-File Pattern | `eks_ontology_schema.json` did not follow the Base/Setup/Config pattern required by `agent_rule.md`. Modular inheritance was missing. | ✅ Resolved | Split into `eks_ontology_base_schema.json` and `eks_ontology_setup_schema.json` with explicit `allOf` inheritance. Updated `SchemaLoader`. |
| I008 | 2026-06-18 | Phase 1 | 🟡 Medium | Inconsistent Ontology Config Filename | `eks_ontology.json` name was inconsistent with the `_config.json` suffix used by other schema components. | ✅ Resolved | Renamed to `eks_ontology_config.json` and updated all internal/external references across code, tests, and docs. |
| I009 | 2026-06-19 | Phase 3 Prep | 🟠 High | Pre-ingestion metadata extraction gap — 572 TWRP documents not registered | Document registry contains 7 test records only; 0 of 572 real TWRP documents ingested. 7 components missing before bulk ingestion can proceed: (1) FilenameParser — extract discipline/doc_type/area/doc_number from filenames; (2) CoverSheetParser — extract 8 fields from PDF cover sheets (Type A/B/D/E); (3) ScannedPDFHandler — detect/handle Type C image-only PDFs; (4) DatadropCrossRef — fuzzy match asset_tags to datadrop KEYTAGs; (5) WSD11 project config — replace P123/P456 placeholders (linked to I005); (6) Bulk ingestion script — walk directory tree, register all documents; (7) Extraction confidence scoring — compute quality score per document. | ⏸️ Deferred | To be evaluated as sub-phase workplan for Phase 3. Blocks Phase 3 bulk ingestion. See Appendix B B6.3 for TWRP ingestion steps. |
| I010 | 2026-06-22 | Phase 1 | 🟠 High | Schema files not consolidated under `eks/config/schemas/` | Core, asset, and ontology schema/config files (9 JSON files) resided at `eks/config/` root while only error/message schema files (4 JSON files) were in `eks/config/schemas/`. This violated AGENTS.md §9 schema pattern and the DCC project reference pattern. `test_phase1.py` also resolved `config_dir` to `eks/config/` rather than the canonical `eks/config/schemas/`. | ✅ Resolved | T1.33: Verified all 13 schema/config files are in `eks/config/schemas/`; updated `test_phase1.py` to use schemas/-first fallback chain; updated section 7b folder tree in workplan; updated update_log.md (U051). Phase 1 COMPLETE (v2.2). |
| I011 | 2026-06-22 | Phase 1 | 🟡 Medium | Document schema definitions mixed with pipeline config in `eks_base_schema.json` | `document_metadata_def` (11 fields) and `project_metadata_def` (5 fields) are defined in `eks_base_schema.json` alongside pipeline config definitions (`registry_def`, `parsers_def`, `embedding_def`, `vector_store_def`, `logging_def`). This violates SSOT principle — document schema should be independent from pipeline config. Asset schema already follows dedicated 3-layer pattern (`eks_asset_base/setup/config`). | ✅ Resolved | T1.34: Created `eks_doc_base_schema.json`, `eks_doc_setup_schema.json`, `eks_doc_config.json`. Removed doc defs from `eks_base_schema.json`. Added `document_element_def`. Updated `schema_loader.py` with doc schema validation. All 73 tests pass. |
| I012 | 2026-06-22 | Phase 1 | 🟡 Medium | Document schema v1 lacks business logic enums and registries | `eks_doc_base_schema.json` v1.0 has untyped `document_type` (string), no `file_type` definition, no `element_type` enum. Missing `document_type_registry` (ontology mapping), `file_type_registry` (parser mapping), `element_type_registry` (D7.10). `element_expectations` uses cover types A-E instead of document type codes. TWRP assets (DWG/PI-PID/SPC/MAN/OM/RPT) and parser plug-ins (PDF/DGN/DOCX/XLSX/DWG) not represented. | ✅ Resolved | T1.35: Added `document_type_code` (7), `file_type_code` (5), `element_type_code` (8) enums; added `document_type_registry` (7 entries with ontology_class), `file_type_registry` (5 entries with parser_class), `element_type_registry` (8 entries with source/phase2/phase3); refactored `element_expectations` keys from A-E → DWG/PI-PID/SPC/RPT/MAN/DS/OM; added `_validate_doc_registries()` to schema_loader; added 6 tests; created DGN/DWG parser stubs; added DataSheet/OpsManual to ontology. All 31 tests pass. |
| I013 | 2026-06-22 | Phase 1 | 🟠 High | No automated pipeline: database creation, parsing, scoring, and review are disconnected | Current workflow: (1) `registry.py` creates DB with hard-coded DDL, (2) parsers run independently, (3) `HealthScorer.score()` exists but is not wired into a pipeline, (4) no file scanner to discover documents, (5) no parser router to map file types, (6) no manual review workflow. Tables are not auto-generated from schema; DDL is manually maintained separate from `eks_doc_base_schema.json`. | ✅ Resolved | T1.36–T1.40 complete: (1) `SchemaToDDL` auto-generates DDL from JSON schema; (2) `FileScanner` walks directory, validates types, registers placeholders; (3) `ParserRouter` maps file_type → parser class; (4) `PipelineOrchestrator` coordinates 3-phase pipeline; (5) `ManualReviewManager` surfaces flagged docs, supports correction/locking. All 53 tests pass. |
| I014 | 2026-06-22 | Phase 1 | 🟠 High | Error/message schemas missing 3-layer pattern — no setup layer | Error and message schema files violate AGENTS.md §9 3-layer inheritance pattern. (1) No `eks_error_setup_schema.json` exists — base definitions have no setup layer with `allOf`/`$ref`; config file acts as both schema and values; (2) No `eks_message_setup_schema.json` exists — same issue; (3) Config files contain `$schema`/`$id`/`title`/`description`/`version` which belong in setup; (4) Error config has range definitions (setup concern) mixed with actual values. DCC reference pattern: `error_code_base.json` → `error_code_setup.json` (allOf) → `system_error_config.json` + `data_error_config.json`. SchemaLoader validates config against setup, but setup schemas are missing — validation skipped. | ⏳ In Progress | T1.41: Create `eks_error_setup_schema.json` and `eks_message_setup_schema.json`; clean config files; update `SchemaLoader` to validate error/message schemas against setup. |
| I015 | 2026-06-23 | Phase 1 | 🟡 Medium | DGN parser stub — 48 CAD files unparseable | `DGNParserStub` returns "Format supported - Implementation Pending" for all .dgn files. 48 DGN files identified in `design_doc/0363/DGN file/` (42) and `project_spec/Volume 5/Part-II` (5) plus `design_doc/1992/2026 06 18 R1/DGN/` (6). All DGN files will be registered with `extract_status = 'failed'` and flagged for manual review. No CAD parsing library currently in `eks.yml` dependencies. | 🔴 Open | Phase 3: Evaluate OpenDesign SDK, LibreCAD, or commercial parser. Mitigated by ManualReviewManager surfacing flagged files. See workplan §11 risk. |
| I016 | 2026-06-23 | Phase 1 | 🟢 Low | Revision folder hierarchy inconsistency | R0 revisions use 3-subfolder structure (`Client Response/`, `Native/`, `Submission/`); R1+ revisions place files directly in revision folder with optional `DGN/` and `PDF/` sub-subfolders. Some submittals (e.g., 0363) have no revision folders at all — files sit at submittal root. FileScanner must handle flat, subfoldered, and nested patterns. | 🔴 Open | Phase 2: Extend FileScanner to traverse inconsistent folder hierarchies. Current implementation walks recursively which handles most cases; verify edge cases. |
| I017 | 2026-06-23 | Phase 1 | 🟢 Low | Mixed file types per submittal require per-file parser routing | Single submittals contain PDF+DOCX+XLSX+DGN files. E.g., submittal 0016 has PDF submissions, DOCX comment sheets, XLSX datasheets; submittal 0363 has PDF reports, DOCX milestones, 42 DGN drawings. ParserRouter correctly routes by file extension — no action needed. | ✅ Resolved | T1.38 ParserRouter handles per-file routing via `file_type_registry`. Verified with twrp data patterns. |
| I018 | 2026-06-23 | Phase 1 | 🟢 Low | Temp files (Thumbs.db, ~$ lock files) must be filtered | Thumbs.db files found in 6 revision folders; MS Word lock file (`~$ply to Comment sheet...docx`) found in `0092/R2`. FileScanner must exclude system/temp files by extension and naming pattern. | ✅ Resolved | T1.37 FileScanner filters by `file_type_registry` extensions; Thumbs.db (.db), ~$ (temp), .zip, .msg, .lnk not registered. |
| I019 | 2026-06-23 | Phase 1 | 🟡 Medium | Two project codes (131101 vs 131242) — metadata normalization | `project_spec/` documents use project code `131101` (e.g., `131101-WSW41-SP-SG-0101.pdf`); `design_doc/` documents use project code `131242` (e.g., `131242-WST02-DS-M-0001.pdf`). Both are valid WSD11 project documents. Metadata schema must normalize `project_code` as a dynamic field extracted from filename, not a hardcoded constant. Document type codes differ: project_spec uses `SP` (Specification) and `DR` (Drawing); design_doc uses `DS`, `SP`, `CL`, `BQ`, `VI`, `M3`, `QA`, `DR`. | 🔴 Open | Phase 2: FilenameParser must extract project_code from document number pattern. Config should define project_code → project_name mapping. |
| I020 | 2026-06-23 | Phase 1 | 🟡 Medium | Datadrop column range (33–112) — fragment mapping coverage | 7 datadrop sheets have varying column counts: Pipeline (33), Inline Component (56), Motor (73), Equipment (82), Instrument (87), MANUALVALVE (64), CONTROLVALVE (112). Some columns are unique to specific asset types (e.g., `MCC FED FROM` only in Motor; `ACTUATOR TAG NUMBER` only in CONTROLVALVE). 13 asset schema fragments must cover all columns; unmapped columns will be silently dropped during ingestion. | 🔴 Open | Phase 3: Validate fragment coverage against all 7 sheets. Add missing fields to asset schema fragments if needed. CONTROLVALVE has dual manufacturer fields (valve + actuator) requiring special handling. |
| I021 | 2026-06-23 | Phase 1 | 🟠 High | 22–64% data incompleteness across datadrop sheets | Sheet3 summary shows: Equipment 59.9% pending, Inline Component 44.1% pending, Instrument 63.5% pending, Motor 45.1% pending, Pipeline 22.8% pending, CONTROLVALVE 48.1% pending, MANUALVALVE 36.6% pending. Many asset records have null values in critical fields (manufacturer, serial number, process conditions). Health scoring will flag incomplete records. ManualReviewManager workflow is essential for data quality. | 🔴 Open | Phase 3: Asset loader must handle null fields gracefully (skip, don't fail). Health scoring applied per-asset. Manual review surfaces incomplete records for project team correction. |
| I022 | 2026-06-23 | Phase 1 | 🟡 Medium | Duplicate `revision_id` definition in base and doc base | `revision_id` defined identically in `eks_base_schema.json:31` and `eks_doc_base_schema.json:14`. Both are `{"type": "string", "description": "Generic revision identifier placeholder..."}`. Violates SSOT. | ✅ Resolved | Removed from `eks_base_schema.json`; moved to `eks_doc_base_schema.json` per U087 (I032 resolution). |
| I023 | 2026-06-23 | Phase 1 | 🟡 Medium | Duplicate `discipline_code` definition in base and doc base | `discipline_code` defined identically in `eks_base_schema.json:27` and `eks_doc_base_schema.json:18`. Both are `{"type": "string", "description": "Generic discipline code placeholder..."}`. Violates SSOT. | ✅ Resolved | Standalone `discipline_code` removed from `eks_base_schema.json` (v1.3.1). Doc base `$ref` now points to `discipline_entry_def.properties/code`. |
| I024 | 2026-06-23 | Phase 1 | 🟠 High | Parser import path mismatch: `engine.parsers.*` vs `eks.engine.parsers.*` | `eks_config.json:40-42` uses `engine.parsers.*` (e.g., `"engine.parsers.pdf_parser.PDFParser"`) while `eks_doc_config.json:46-50` uses `eks.engine.parsers.*` (e.g., `"eks.engine.parsers.pdf_parser.PDFParser"`). One path will fail at runtime. | ✅ Resolved | Both configs now use `eks.engine.parsers.*` path (verified in eks_config.json v1.3.0). |
| I025 | 2026-06-23 | Phase 1 | 🟡 Medium | Missing DGN/DWG parsers in `eks_config.json` | `eks_config.json` parsers only has 3 entries (.pdf, .docx, .xlsx), but `eks_doc_config.json` file_type_registry has 5 file types including dgn and dwg. DGN/DWG files cannot be routed from core config. | ✅ Resolved | Added DGN/DWG stub parsers to `eks_config.json` parsers (verified in eks_config.json v1.3.0). |
| I026 | 2026-06-23 | Phase 1 | 🟠 High | Missing `$schema` in `eks_error_config.json` and `eks_message_config.json` | Both config files lack `$schema` field to reference their setup schemas. Validation chain `config → setup → base` is broken — these configs cannot be validated against `eks_error_setup_schema.json` and `eks_message_setup_schema.json`. | ✅ Resolved | Added `$schema` field to both config files referencing their respective setup schemas (U090). |
| I027 | 2026-06-23 | Phase 1 | 🟢 Low | URI naming inconsistency in error/message base schemas | `eks_error_code_base.json` uses `https://eks.engineering/schemas/error-code/base` (path-based URI) while all other base schemas use filename-based URIs like `https://eks.engineering/schemas/eks_base_schema.json`. Same for message base. Inconsistent but functional. | ✅ Resolved | Aligned to filename-based pattern: `eks_error_code_base.json` and `eks_message_base.json` now use filename-based URIs. Updated all `$ref` in setup schemas. Added `base_schema` to all `Registry().with_resources()` calls in `schema_loader.py` and tests (U078). |
| I029 | 2026-06-24 | Phase 1 | 🟡 Medium | `discipline_registry` uses `oneOf` violating SSOT | `eks_setup_schema.json` allows `discipline_registry` to be either a `$ref` to `eks_discipline_schema.json` or an inline array — SSOT violation. New disciplines could be embedded in config without updating the canonical registry. | ✅ Resolved | Removed `oneOf`, changed to `$ref`-only. `eks_setup_schema.json` v1.2.0→v1.2.1 (U082). |
| I030 | 2026-06-24 | Phase 1 | 🟡 Medium | `project_rules` inline in config violates SSOT | `eks_config.json` contained inline `project_rules` values instead of a dedicated fragment file. Inconsistent with disciplines/projects/departments/facilities pattern. Also inconsistent with strict 3-layer naming (`_config` = actual values). | ✅ Resolved | Created `eks_project_rules_config.json` v1.0.0, updated `eks_setup_schema.json` v1.2.1→v1.2.2 and `eks_config.json` v1.2.0→v1.3.0 to `$ref` it (U083). |
| I031 | 2026-06-24 | Phase 1 | 🟡 Medium | `document_relationship_trigger_map` base layer prescribes actual values | `eks_base_schema.json#/definitions/document_relationship_trigger_map` defines `properties` with `enum` values (document_number→SUPERSEDES, etc.) and `required` — these are actual mapping values, not structural constraints. Base layer should define only shape (`type: object`, `additionalProperties`). Actual entries belong in config files only. | ✅ Resolved | Stripped `properties`/`required` from base definition, keeping only shape (U086). Config files now sole SSOT for trigger values. |
| I032 | 2026-06-24 | Phase 1 | 🟡 Medium | `revision_id` defined in base schema but only used by doc schema set | `eks_base_schema.json#/definitions/revision_id` is a `{type: string}` consumed only by `eks_doc_base_schema.json#/definitions/document_metadata_def.revision` via `$ref`. Not a cross-cutting type like `verbosity_level`. Should follow doc schema set 3-layer pattern. | ✅ Resolved | Moved `revision_id` to `eks_doc_base_schema.json`, added `revision_validation` to doc setup+config, removed `revision_pattern` from project_rules (U087). |
| I033 | 2026-06-25 | Docs | 🟡 Medium | Appendix E: `eks_base_schema.json` version stale (shows v1.3.1, actual v1.5.0) | E5.1 Consolidated Inventory lists `eks_base_schema.json` version as `1.3.1`. Actual file is `v1.5.0` (U087 moved `revision_id` to doc base, changing base to 1.5.0). The mermaid diagram (E2) and E11.1 trace are also misaligned with this. | ✅ Resolved | Updated E5.1 version to `1.5.0` in Appendix E v0.7. |
| I034 | 2026-06-25 | Docs | 🟡 Medium | Appendix E: `eks_setup_schema.json` version stale (shows v1.2.0, actual v1.2.2) | E5.1 lists setup schema as `v1.2.0`. Actual file is `v1.2.2` (v1.2.1 enforced `$ref`-only for discipline_registry; v1.2.2 enforced `$ref`-only for project_rules_registry per I029/I030). Inconsistency also exists within Appendix E — E11.1 column header correctly shows v1.2.2 while E5.1 shows v1.2.0. | ✅ Resolved | Updated E5.1 version to `1.2.2` in Appendix E v0.8. |
| I035 | 2026-06-25 | Docs | 🟡 Medium | Appendix E: `eks_doc_base_schema.json` version stale (shows v1.1.2, actual v1.2.0) | E5.1 lists doc base schema as `v1.1.2`. Actual file is `v1.2.0` — `revision_id` was added here from `eks_base_schema.json` per U087 (I032 resolution). | ✅ Resolved | Updated E5.1 version to `1.2.0` in Appendix E v0.8. |
| I036 | 2026-06-25 | Docs | 🟡 Medium | Appendix E: `eks_doc_setup_schema.json` version stale (shows v1.2.0, actual v1.3.0) | E5.1 lists doc setup schema as `v1.2.0`. Actual file is `v1.3.0` — `revision_validation` property was added to the setup schema and its `required` array per U087. | ✅ Resolved | Updated E5.1 version to `1.3.0` in Appendix E v0.8. |
| I037 | 2026-06-25 | Docs | 🟡 Medium | Appendix E: `eks_base_schema.json` definition count wrong (shows 14, actual 13; `revision_id` removed) | E5.1 states `eks_base_schema.json` has 14 definitions. Since `revision_id` was moved to `eks_doc_base_schema.json` (I032/U087), the actual count is 13. The key content list in E5.1 also still includes `revision_id` under core base — this is stale. | ✅ Resolved | Updated count to 13 in Appendix E v0.8. |
| I038 | 2026-06-25 | Docs | 🟡 Medium | Appendix E: `revision_id` still listed as Core Base definition in E5.1 | Following I032 resolution, `revision_id` now lives exclusively in `eks_doc_base_schema.json`. E5.1 core base key content still lists `revision_id`. E5.4 observation 1 mentions the duplicate was the `last known` duplicate — this should be updated to confirm it is resolved. | ✅ Resolved | Removed `revision_id` from Core Base row in E5.1 in Appendix E v0.7. Updated E5.4 note 2 to confirm resolved. |
| I039 | 2026-06-25 | Docs | 🟡 Medium | Appendix E: Document Setup missing `revision_validation` property (E5.1 shows 6 props, actual is 7) | E5.1 Document Setup key content lists 6 properties: `document_type_registry`, `file_type_registry`, `element_type_registry`, `element_expectations`, `health_scoring`, `ontology_triggers`. Actual `eks_doc_setup_schema.json` (v1.3.0) has a 7th property `revision_validation` which is also in the `required` array. E11.3 trace table does include `revision_validation` but E5.1 count is wrong. | ✅ Resolved | Added `revision_validation` to Document Setup key content in E5.1; updated count to 7 in Appendix E v0.8. |
| I040 | 2026-06-25 | Docs | 🟡 Medium | Appendix E E5.3 Summary Matrix: Document Setup count wrong (shows 6, should be 7) | E5.3 Summary Matrix Document row shows `Setup: 6`. Following I039 above, the correct count is 7 (including `revision_validation`). | ✅ Resolved | Updated Document Setup count in E5.3 from `6` to `7` in Appendix E v0.9. |
| I041 | 2026-06-25 | Docs | 🟢 Low | Appendix E E11.4: Ontology relationship count wrong (shows 14, actual is 15) | E11.4 trace table says `14 relationships` in the Actual Values column. Actual `eks_ontology_config.json` has 15 relationship entries: SUBCLASS_OF, IS_A, CONNECTS_TO, FLOWS_TO, INSTALLED_AT, REFERENCED_BY_DWG, CONTROLS, ENERGIZED_BY, GOVERNED_BY, SET_POINT_IN, SUPERSEDES, SUPPLEMENTS, REFERENCES_DOC, PRODUCED_BY, FEEDS_FROM. E5.1 correctly says 15. Internal inconsistency within Appendix E. | ✅ Resolved | Updated E11.4 to `15 relationships` in Appendix E v0.9. |
| I042 | 2026-06-25 | Docs | 🟢 Low | Appendix E E5.1: Error Setup property count inconsistent (shows 5, actual is 6) | E5.1 Error Setup Count column shows `5`, but the actual properties in `eks_error_setup_schema.json` are 6: `metadata`, `system_error_ranges`, `system_errors`, `data_error_ranges`, `data_logic_errors`, `migration_log`. | ✅ Resolved | Updated Error Setup count to `6` in E5.1 in Appendix E v0.8. |
| I043 | 2026-06-25 | Docs | 🟢 Low | Appendix E E12 Aggregate Summary: Document required count wrong (shows 3 req, should be 4) | E12 Document row shows `7 defs → 7 props(3 req)`. Since `revision_validation` is in `eks_doc_setup_schema.json` required array (per I039), there are 4 required properties: `revision_validation`, `ontology_triggers`, `health_scoring`, `element_expectations`. | ✅ Resolved | Updated E12 Document row to `(4 req)` in Appendix E v0.9. |
| I044 | 2026-06-25 | Docs | 🟢 Low | Appendix E: Internal inconsistency — E5.1 vs E11.1 setup schema version | E5.1 shows `eks_setup_schema.json` as `v1.2.0` while E11.1 column header correctly says `v1.2.2`. Both are in the same document. Resolved by fixing I034 (update E5.1 to v1.2.2). | ✅ Resolved | Resolved when I034 was fixed in Appendix E v0.8. |
| I045 | 2026-06-30 | Phase 1 | 🟠 High | `$schema` field re-introduced in error/message config files, breaking all phase 1 tests | `eks_error_config.json` and `eks_message_config.json` contained `$schema` fields that violate `additionalProperties: false` in their setup schemas. This is a regression from I028 (resolved 2026-06-23 by removing metadata fields). All 63 phase 1 tests errored at setup. | ✅ Resolved | Removed `$schema` from both config files. All 118 tests pass. |
| I046 | 2026-06-30 | Phase 1 | 🟡 Medium | `project_setup.json` violates AGENTS.md §9 3-layer pattern — should integrate into core eks_base/setup/config schemas | `project_setup.json` (created by T1.66) is a standalone schema with 7 compliance violations: (1) missing `project_base_schema.json` for shared definitions; (2) missing `project_config.json` for actual values; (3) no `allOf`/`$ref` to a base schema; (4) definitions embedded inline instead of via `$ref`; (5) default values (config data) stored in the setup layer; (6) schema is never read by `setup_validator.py` (hardcodes all values); (7) workplan claims compliance (R06) but files don't exist. Evaluation confirms the correct fix is to integrate project setup definitions into the core `eks_base_schema.json` (add 4 defs), add `project_setup` property to `eks_setup_schema.json`, populate values in `eks_config.json`, delete the orphan `project_setup.json`, and refactor `setup_validator.py` to load from the schema chain. | ✅ Resolved | T1.67: Added 4 defs to base v1.6.0, project_setup property to setup v1.3.0, values to config v1.4.0. Archived `project_setup.json`. Refactored `setup_validator.py` to load from ConfigRegistry. 118/118 tests pass. U093. |
| I047 | 2026-07-02 | Phase 1.2 | 🟠 High | `eks/server.py` routes `GET /` to static `index.html` — `_build_index()` not wired | `do_GET` in `eks/server.py` calls `_serve_static("index.html")` at root — the `_serve_file_picker()` method exists but is dead code. The dynamic tool-picker required by AGENTS.md §18.12 and Appendix G §10.3 is not implemented. Adding phase 2–5 HTML files will not make them visible from the root URL. | ✅ Resolved | Implemented `_build_index()` in `server.py` with SCAN_DIRS, EXCLUDE_DIRS, live search, card grid, and status bar. `GET /` now returns dynamic tool-picker. All HTML files under `eks/ui/` appear automatically. |
| I048 | 2026-07-02 | Phase 1.2 | 🟠 High | Phase 1 API endpoints lack `/api/v1/` prefix — 404 catch-all cannot be enforced | All 12 Phase 1 backend endpoints (e.g. `/api/documents`, `/api/pipeline/start`) were built without the `/api/v1/` version prefix required by Appendix G §10.4. Enforcing the 404 catch-all for un-versioned paths (G10.4.3) would immediately break all Phase 1 UI calls in `eks.js` and `index.html`. | ✅ Resolved | All 13 Phase 1 endpoints migrated to `/api/v1/` in `phase1_server.py`. All `fetch()` calls in `eks.js` use `/api/v1/`. `server.py` versioned proxy + 404 catch-all for un-versioned `/api/*` paths. |
| I049 | 2026-07-02 | All | 🟠 High | No port availability check — `OSError: address already in use` on corporate machines | `server.py` and all phase servers bind to their default port without checking if it is occupied. On Windows corporate machines, ports 5000–5002 are commonly used by Cisco AnyConnect, IIS, or other corporate tools. The server crashes with `OSError: [Errno 10048]` and no user-friendly message. Derived from code_tracer CT-14. | ✅ Resolved | Added `find_free_port()` with `socket.connect_ex()` probe to both `server.py` and `phase1_server.py`. Auto-increments up to 100 attempts; prints actual bound port. `--phase1-port` arg for overrides. |
| I050 | 2026-07-02 | Phase 1.2 | 🟡 Medium | CDN Chart.js in `index.html` + runtime fallback in `eks.js` — fails on restricted networks; font stack missing `system-ui` | `eks/ui/index.html` line 8 loads Chart.js from `https://cdn.jsdelivr.net/npm/chart.js`. `eks/ui/eks.js` lines 356–364 has a runtime fallback to the same CDN URL. The font stack (`-apple-system, BlinkMacSystemFont, ...`) does not start with `system-ui` as required by AGENTS.md §18.1. (Note: the CSS `@import` for Google Fonts claimed in earlier gap analysis is **not present** in the actual file at v1.7 — this sub-claim is inaccurate.) | ✅ Resolved | Chart.js v4.4.4 self-hosted at `eks/ui/static/chart.min.js`. HTML and JS reference only local path. Font stack uses `system-ui` first via `--font-ui` token. |
| I051 | 2026-07-02 | All | 🟡 Medium | No backend readiness check — conda env not activated produces raw `ModuleNotFoundError` | If `phase1_server.py` is started without the `eks` conda environment activated, `from eks.engine.core import DocumentRegistry` fails with a bare `ModuleNotFoundError` traceback. The launcher in `server.py` starts the phase1 backend as a subprocess and relays stdout — a traceback is printed with no actionable guidance. | ✅ Resolved | All engine imports wrapped in `try/except ImportError` in `phase1_server.py`. Prints clear startup message: `⚠ Engine imports failed: {error}. Run: conda activate eks`. Returns HTTP 503 with install guidance when unavailable. |
| I052 | 2026-07-02 | All | 🟡 Medium | Windows path normalization — proxy URLs and static file security check untested on Windows | `_proxy()` constructs target URLs using `self.path` directly. On Windows, `Path` objects use `\` separators that may not be converted to `/` in URL paths. The static file security check requires `Path.is_relative_to(ROOT)` — not yet implemented; current code has no traversal guard. | ✅ Resolved | `_serve_static()` and `_serve_common()` both implement `Path.is_relative_to(ROOT/COMMON)` traversal guard returning 403. URL construction uses `.as_posix()`. `ROOT` resolved at module import time. |
| I053 | 2026-07-02 | Phases 2–5 | 🟡 Medium | No DuckDB cross-process locking guidance for multi-phase server deployment | When Phase 2, 3, and 4 servers are built, they will open `output/eks_registry.db` alongside Phase 1. DuckDB default mode allows one writer + multiple readers but concurrent writes from multiple processes cause `IOException: database file is locked`. The `_with_retry()` wrapper exists only in `phase1_server.py`. | ✅ Resolved | `_with_retry()` implemented in `phase1_server.py` with 3 retries, 0.5s delay. All DB reads and writes wrapped. Phase 1 has concurrency guard (HTTP 409) preventing overlapping pipeline starts. Read-only vs read-write endpoints documented. |
| I054 | 2026-07-02 | All | 🟢 Low | No restricted-machine deployment guide — no equivalent to code_tracer `download_release.py` | code_tracer solved corporate deployment via a stdlib-only `download_release.py` script that copies all required files to a destination with no git/pip/Docker required. EKS has no equivalent. Users on restricted machines must clone the full repo and build the conda env, which may not be possible. | ⏸️ Deferred | Post Phase 1.2 migration. Create `eks/deploy.py` — stdlib-only script that: (1) copies `server.py` + `ui/` to a destination; (2) generates a minimal `requirements_phase1.txt`; (3) prints next-step instructions. Reference code_tracer R7 pattern. |
| I055 | 2026-07-02 | Phase 1.2 | 🟡 Medium | No cache busting headers on any server response — browser may serve stale pages | Neither `server.py` nor `phase1_server.py` override `end_headers()` to inject `Cache-Control: no-cache, no-store, must-revalidate`. All responses rely on default browser caching. Polling endpoints return stale data. AGENTS.md §18.12, G10.7 #3. | ✅ Resolved | Both servers override `end_headers()` to inject `Cache-Control: no-cache, no-store, must-revalidate` + `Pragma: no-cache` + `Expires: 0` on every response. |
| I056 | 2026-07-02 | Phase 1.2 | 🟡 Medium | No `urllib.parse.unquote()` on path matching — encoded paths fail to route | `server.py` and `phase1_server.py` route using `urlparse(self.path).path` directly. Percent-encoded paths like `DWG%20001` don't match route segments. AGENTS.md §18.12, G10.7 #10. | ✅ Resolved | `unquote()` applied to all path matching in `server.py` and `phase1_server.py` before route segment comparison. |
| I057 | 2026-07-02 | Phase 1.2 | 🟢 Low | `ConnectionResetError` not suppressed — browser polling floods console with tracebacks | Neither server overrides `handle_error()`. Browsers frequently reset idle connections (especially during polling). Each reset prints a full Python traceback. AGENTS.md §18.12, G10.7 #2. | ✅ Resolved | Both servers override `handle_error()` to silently suppress `ConnectionResetError`. |
| I058 | 2026-07-02 | Phase 1.2 | 🟡 Medium | Proxy timeout is 30s not 120s — long pipelines will abort | `server.py:208` uses `urlopen(req, timeout=30)`. Appendix G §3.4 allows 5-minute polling timeout; long pipelines (many large PDFs) need 120s proxy timeout. | ✅ Resolved | `server.py` API proxy uses `timeout=120`. Ollama proxy uses `timeout=30` per design. |
| I059 | 2026-07-02 | Phase 1.2 | 🔴 Critical | `GET /api/files/load` called but only `POST` exists — file loading broken on startup and button click | `eks.js` `loadFilesFromPicker()` calls `apiGet('/files/load')` (GET). Backend `do_GET` has no matching route — only `POST /api/files/load` exists. This function is wired to both the startup sequence (L633) and the "Load Files" button (L575). File loading never works. | ✅ Resolved | `loadFiles()` uses `apiPost('/api/v1/files/load', {data_dir: path})`. Wired to `#btn-load-files` click and startup. No `loadFilesFromPicker` function exists. |
| I060 | 2026-07-02 | Phase 1.2 | 🔴 Critical | `POST /api/review/submit` called but backend only has `PUT /api/review/{lock|recalculate}` — review form broken | `eks.js:342` calls `apiPost('/review/submit', {...})`. Backend has `PUT /api/review/{action}` only for `lock` and `recalculate`. No `submit` action exists. Review submission always 404s. | ✅ Resolved | `eksSubmitReview()` calls `apiPut('/api/v1/review/lock', {doc_id, verified_by, comments})`. `apiPut` helper added to eks.js. |
| I061 | 2026-07-02 | Phase 1.2 | 🔴 Critical | Response format mismatch — backend returns `{discovered, files}` but frontend expects `{documents: [...]}` | After POST `/api/files/load`, backend returns `{discovered, valid, unknown, registered, files}`. Frontend reads `data.documents` which is always `undefined` — table renders "No documents found". | ✅ Resolved | After `POST /api/v1/files/load` succeeds, `loadFiles()` calls `GET /api/v1/documents` to fetch document list. Populates `state.docs` from GET response. |
| I062 | 2026-07-02 | Phase 1.2 | 🔴 Critical | Parameter name mismatch — frontend sends `{dir: ...}` but backend reads `{data_dir: ...}` | `eks.js:79` sends `{dir: dir || 'eks/data'}`. Backend `_handle_files_load` reads `data.get("data_dir", "eks/data")`. Custom directory paths silently ignored. | ✅ Resolved | `loadFiles()` sends `{data_dir: path}` matching backend's expected parameter name. |
| I063 | 2026-07-02 | Phase 1.2 | 🔴 Critical | `loadFiles()` (correct POST) never wired to any UI element — dead code | `loadFiles()` at `eks.js:78` calls `apiPost('/files/load', {dir: ...})` — correct method but wrong param name (I062). Function is defined but never referenced by any event listener. The broken `loadFilesFromPicker()` is the only wired function. | ✅ Resolved | `loadFiles()` wired to `#btn-load-files`, startup, and icon bar 📂 button. Uses correct POST + `data_dir` parameter. |
| I064 | 2026-07-02 | Phase 1.2 | 🟡 Medium | `eks.js` Chart.js runtime fallback still uses CDN path — bypasses self-hosting | `eks.js:356-364` has a runtime fallback that loads Chart.js from `https://cdn.jsdelivr.net/npm/chart.js` when the static `<script>` tag fails. After T1.2.8.5 replaces the static tag, this fallback must be updated to use the self-hosted path `/ui/static/chart.min.js`. | 🔴 Open | Fix in T1.2.8.5 expansion. |
| I065 | 2026-07-02 | Phase 1.2 | 🟡 Medium | CSS design tokens don't match AGENTS.md §18.3 required names — 16 token groups missing or misnamed | Current CSS uses `--bg-primary`, `--bg-secondary`, `--error` instead of `--surface`, `--surface2`, `--surface3`, `--danger`, `--info`, `--text3`. Missing entirely: `--tag-bg`, `--tag-border`, `--row-stripe`, `--row-hover`, `--th-bg`, `--th-hover`, `--icon-bar-w`, `--sidebar-w`, `--right-sidebar-w`, `--titlebar-h`, `--statusbar-h`, `--radius`, `--radius-sm`, `--radius-lg`, `--font-ui`, `--font-mono`. 20+ hardcoded hex values in component rules. | 🔴 Open | Fix in T1.2.9.1. |
| I066 | 2026-07-02 | Phase 1.2 | 🟢 Low | No theme transition CSS | `eks.css` body rule has no `transition` property. Theme switching is instant — no smooth color transition. AGENTS.md §18.4 requires `transition: background 0.25s, color 0.25s` on `body`. | 🔴 Open | Fix in T1.2.9.1. |
| I067 | 2026-07-02 | Phase 1.2 | 🟢 Low | Theme picker is a cycle button, not a dropdown menu | Title bar button cycles through 5 themes sequentially. AGENTS.md §18.4 requires a dropdown menu with color dots, labels, and `.active` class on the current theme. | 🔴 Open | Fix in T1.2.9.2. |
| I068 | 2026-07-02 | Phase 1.2 | 🟡 Medium | Right sidebar only shows Document Detail — no Settings or Help views | Right sidebar is single-purpose (document detail). AGENTS.md §18.2 requires dual-purpose: default "Settings" view and context-sensitive "Help"/"Detail" views. ❓ opens modal overlay, not sidebar. | 🔴 Open | Fix in T1.2.9.3. Refactor right sidebar to support 3 context-sensitive views. |
| I069 | 2026-07-02 | Phase 1.2 | 🟡 Medium | Dashboard has no KPI card grid or stage cards — uses flat table and progress bar | Documents tab shows a table instead of KPI metric tiles. Pipeline tab shows a single progress bar instead of per-stage cards. AGENTS.md §18.10 requires `display: grid` KPI cards and numbered stage cards with emoji, status, progress bar. | 🔴 Open | Fix in T1.2.9.4. |
| I070 | 2026-07-02 | Phase 1.2 | 🟡 Medium | Data table has no sortable columns, no row cap, no active highlight | Column headers have `cursor: pointer` but no JS sort handler. No 50-row cap. Selected row has no visual highlight. AGENTS.md §18.11 requires sortable headers with `▲`/`▼` indicators, 50-row cap, and row count footer. | 🔴 Open | Fix in T1.2.9.5. |
| I071 | 2026-07-02 | Phase 1.2 | 🟢 Low | Icon bar 36px (should be 48px); drag-drop on area not body; sidebar width not persisted; file input missing | Multiple UI polish gaps: icon bar width 36px not 48px (§18.2); drag-and-drop on `#file-drop-area` not full page body (§18.7); sidebar width not saved to localStorage on resize (§18.6); no `<input type="file">` (§18.7); ℹ️ icon has no handler (§18.14); 🔄 refresh icon missing (§18.14); font stack missing `system-ui` first (§18.1); no cache meta tag in HTML (§18.1). | 🔴 Open | Fix in T1.2.9.6. |
| I072 | 2026-07-02 | Phase 1.2 | 🔴 Critical | `/ui/` prefix stripped but `ui/` directory not re-added — all static files return 404 | `server.py:137` calls `_serve_static(path[4:])`, stripping the `/ui/` prefix. For `/ui/index.html`, this resolves to `ROOT/index.html` instead of `ROOT/ui/index.html`. All CSS, JS, and static assets under `/ui/` return 404. | ✅ Resolved | Fixed `server.py:137`: prepend `"ui/"` — `_serve_static("ui/" + path[4:])`. All 28 tests pass. |
| I073 | 2026-07-07 | Phase 1.2 | 🔴 Critical | `#pipeline-progress` element missing from HTML — `TypeError` on every pipeline run | `eks.js` lines 602 and 632 call `document.getElementById('pipeline-progress').style.width` but no element with `id="pipeline-progress"` exists in `phase1_ingestion.html`. The pipeline tab has `#stage-cards` and `#pipeline-status` but no progress bar element. Throws `TypeError: Cannot read properties of null` every time `updatePipelineStatus()` or `showPipelineTab()` is called. | ✅ Resolved | Added `<div class="progress-container">` with `#pipeline-progress` fill element inside `#tab-pipeline` in `phase1_ingestion.html`. |
| I074 | 2026-07-07 | Phase 1.2 | 🔴 Critical | `hideHelp()` called but never defined — `ReferenceError` on help modal click | `eks.js` line 1006 calls `hideHelp()` inside the help modal overlay click handler. `hideHelp` is never defined anywhere in `eks.js`. The help system was migrated to the right sidebar (no modal), leaving this dead event listener referencing a non-existent function. Throws `ReferenceError: hideHelp is not defined` when user clicks the modal overlay background. | ✅ Resolved | Replaced `hideHelp()` call with `comUI.modal.close('help-modal')` which is the correct `comUI` API. |
| I075 | 2026-07-07 | Phase 1.2 | 🔴 Critical | `loadFiles()` has no try/catch — any backend error silently swallows, button appears to do nothing | `eks.js` `loadFiles()` calls `apiPost` and `apiGet` with no try/catch. If the backend is not running, the path is wrong, or any network error occurs, the unhandled promise rejection is silently swallowed — the status bar never updates, no toast appears, and the button gives no feedback. Every other async function (`startPipeline`, `showReview`, `cancelPipeline`) has a try/catch; `loadFiles` was the only one missing it. | ✅ Resolved | Wrapped body in try/catch/finally: catch calls `PU.setStatus` + `comUI.toast.show` with the error message; finally re-enables the button and restores its label. Button is also disabled with "Loading..." text during the request. |
| I076 | 2026-07-07 | Phase 1.2 | 🔴 Critical | Pipeline `progress` never updates mid-run — stays 0% until completion | `_handle_pipeline_start` in `phase1_server.py` runs `orchestrator.run_full_pipeline()` as a black box in a background thread. `_job_state[job_id]["progress"]` is only set to `100` on completion — it stays at `0` throughout all three phases (A/B/C). The JS polls every 2s and renders the progress bar from this value, so the bar shows 0% for the entire run then jumps to 100%. | ✅ Resolved | Split `run_full_pipeline()` into sequential `run_phase_a()` → `run_phase_b()` → `run_phase_c()` calls in `_run()`. Added `_set_phase()` helper that updates `_job_state` progress and `current_stage` after each phase: A=20%/scan, B=75%/parse, C=90%/review, done=100%/register. |
| I077 | 2026-07-07 | Phase 1.2 | 🟠 High | Stage cards infer active stage from `progress %` — wrong mapping, cards show incorrect state | `renderStageCards()` in `eks.js` computed `completedIdx = Math.floor((progress/100) * 5)` to determine which stage card is active. This maps 0–100% linearly across 5 stages, but the orchestrator has only 3 phases with unequal durations. Stage cards showed wrong active/pass/pending states throughout the run. Also, pipeline `error` message was not surfaced in the status label on failure. | ✅ Resolved | Added `current_stage` field to `_job_state` (set per phase in backend). `renderStageCards()` now reads `data.current_stage` via `STAGE_IDX` lookup instead of inferring from progress %. `updatePipelineStatus()` appends `data.error` to the status label when `status === 'failed'`. |
| I078 | 2026-07-08 | Phase 1 / Master | 🟡 Medium | Shared common-library structure is documented but not yet wired into EKS runtime imports | The repository now contains a shared common package layout under `common/library`, but the EKS runtime modules still use their existing local implementations rather than importing the shared packages directly. The workplan update records the milestone, but runtime integration remains pending. | 🔶 Open | Track the next implementation step as a follow-on task: wire EKS engine modules to the shared `common.library.*` packages where appropriate and verify imports/tests. |
| I079 | 2026-07-09 | Phase 1 | 🔴 Critical | Initiation readiness failure not wired to ErrorManager / error catalog (T1.78 G7 false-DONE) | Review of T1.78 found the `P1-SETUP-*` error code constants added in `setup_validator.py:19-26` are never attached to any `validate_all()` result (no `error_code` field emitted in folders/files/output/env entries). The readiness gate in `phase1_server.py:580-587` raises a generic `RuntimeError(error_msg)` instead of raising through `ErrorManager` with a structured code. This contradicts the T1.78 workplan claim "Wire readiness failure to ErrorManager/error catalog + structured code" and violates AGENTS.md §19 (every business logic needs an independent error code). T1.78 G7 is therefore mislabeled ✅ DONE. | ✅ Resolved | T1.79: `P1-SETUP-*` codes attached to validation results; readiness raised through `ErrorManager.handle_system_error("P1-SETUP-READINESS")`. setup_validator.py v0.4, phase1_server.py v0.5. |
| I080 | 2026-07-09 | Phase 1 | 🟠 High | Hardcoded output / eks.yml paths in initiation violate schema-driven design | `setup_validator.py:187,195` hardcodes `project_root/eks/eks.yml` for `eks_yml_exists` — no schema field backs it (`environment_setup_def` has only python/conda/dependencies) and it redundantly duplicates `required_files` (schema-driven). `setup_validator.py:262-265` hardcodes `["output","output/checkpoints"]` for `_validate_output_paths` — not derived from `global_paths.output_dir` (`eks_base_schema.json:43`) or `registry.connection_string`; `output/checkpoints` is also dead (U118 confirms checkpoints are flat files `output/checkpoint_{id}_{A,B,C}.json`). `phase1_server.py:478,558` hardcode `PRJ_DIR/"eks"/"output"` for the G4 check and `OUTPUT_DIR` instead of `config.global_paths.output_dir`. | ✅ Resolved | T1.80: paths derived from `config.global_paths.output_dir` + `required_files` membership. setup_validator.py v0.5, phase1_server.py v0.6. |
| I081 | 2026-07-09 | Phase 1 | 🟡 Medium | Hardcoded fallback lists duplicate `eks_config.json` (SSOT violation) | `setup_validator.py:60-73` hardcodes `required_folders`, `required_engine_subfolders`, `required_files` lists that duplicate `eks_config.json → project_setup` values. `setup_validator.py:223-228` hardcodes a 15-package dependency list duplicating `eks_config.json → environment.dependencies`. These fallbacks run whenever config is absent, creating a second source of truth; if config evolves, the gate silently diverges. Violates AGENTS.md §10 (SSOT). | ✅ Resolved | T1.81: removed all 4 hardcoded fallback lists; raises `ValueError` if config absent. setup_validator.py v0.6. |
| I082 | 2026-07-09 | Phase 1 | 🟡 Medium | Schema-driven `validation_options.auto_create_folders` ignored by readiness gate | `phase1_server.py:579` calls `validator.validate_all(auto_create=True)` hardcoded, ignoring `project_setup.validation_options.auto_create_folders` (schema-driven, `validation_options_def`). The config value is loaded into `self.validation_options` but never consulted. | ✅ Resolved | T1.82: `phase1_server.py` passes `_vo.get("auto_create_folders", True)` from config. phase1_server.py v0.7. |
| I083 | 2026-07-09 | Phase 1 | 🟢 Low | Hardcoded `data_dir` / global_paths defaults in server | `phase1_server.py:447,359` default `data_dir` to literal `"eks/data"` while config `global_paths.data_dir="data"`. `_handle_config_paths` (lines 310-331) hardcodes a fallback `global_paths` dict instead of schema defaults. Inconsistent with schema SSOT. | ✅ Resolved | T1.82: defaults derived from `config.global_paths`. phase1_server.py v0.7. |
| I084 | 2026-07-09 | Phase 1 | 🟢 Low | `eks` package root hardcoded 9× in phase1_server (not schema-driven) | `phase1_server.py:322,365,567,590,591,602,604,605,606` hardcode `PRJ_DIR/"eks"/...` for config/archive/log/schema dirs. `global_paths` schema has no field for the `eks` package root, so relocation requires code edits. Observational — structural convention, lower priority. | ✅ Resolved | T1.83: `eks_root` added to `global_paths_def` schema + `eks_config.json`; all 10× `PRJ_DIR/"eks"` literals replaced. phase1_server.py v0.8. |
| I085 | 2026-07-09 | Phase 1 | 🟡 Medium | EKS `project_setup` schema diverges from DCC's universal pattern (schema-design inconsistency) | Re-audit comparison found EKS defines folders/files as **flat string arrays** (`required_folders`, `required_engine_subfolders`, `required_files`) with no per-item metadata, while DCC uses **rich object arrays** plus `discovery_rules`, location-aware `environment`, and a separate `dependencies` object. | ✅ Resolved | Phase 1.3 (T1.84–T1.89): created shared `ValidationManager` in `common/library/utility/validation/`, reshaped EKS `project_setup` schema to DCC object model (8 new defs in base v1.7.0), extracted `eks_project_setup_config.json`, refactored `setup_validator.py` as adapter. DCC migration deferred. 235/235 tests pass. |
| I086 | 2026-07-09 | Phase 1 | 🟡 Medium | Residual project_setup wrapper in eks_config.json contradicted DCC project_config.json (no wrapper) and blocked a single universal loader/validator | Post-Phase-1.3 re-audit found EKS nested setup values under a project_setup subkey while DCC keeps them top-level; the T1.86 extraction also left an orphaned eks_project_setup_config.json that nothing loaded. | ✅ Resolved | T1.90-T1.95: flattened eks_config.json to top-level (DCC pattern), dropped wrapper in eks_setup_schema.json v1.5.0, made setup_validator.py/phase1_server.py flatten-aware, deleted orphan. 236/236 tests pass. |
| I087 | 2026-07-10 | Phase 1 | 🟡 Medium | EKS config lacks `discovery_rules` (schema auto-discovery gap vs DCC) | DCC `project_config.json` defines `discovery_rules` (patterns/recursive/exclude filters) for automatic schema file discovery and registry building in `ref_resolver.py:164-230`. EKS `eks_config.json` has no equivalent — schema files are only explicitly listed in `schema_files`. A universal loader must support `discovery_rules` from both config shapes. | ✅ Resolved | T1.96: added 5 `discovery_rules` to `eks_config.json`. `discover_schema_files()` extracted from DCC into `common/library/loader/schema_discovery.py`. I087 resolved; I088–I090 tracked separately. |
| I088 | 2026-07-10 | Phase 1 | 🟡 Medium | EKS config lacks `system_parameters` (loader/validator behavior flags) | DCC `project_config.json` defines `system_parameters` (log_level, retry, timeout, schema_cache_ttl) controlling loader/validator runtime behavior. EKS has no equivalent — these are spread across hardcoded values and separate configs. A universal loader must support `system_parameters` from both config shapes. | ✅ Resolved | T1.97: added universal `common/library/config` helpers, `system_parameters_def` in `eks_base_schema.json`, `system_parameters` in `eks_config.json`, and runtime consumers in `phase1_server.py`, `error_manager.py`, `registry.py`, and `eks/server.py`. Added `test_system_parameters.py`. Full EKS suite 243/243 pass. |
| I089 | 2026-07-10 | Phase 1 | 🟡 Medium | Path model mismatch: EKS `global_paths` vs DCC discovery-relative paths | EKS defines static paths in `global_paths` (`data_dir`, `output_dir`, `log_dir`, `config_dir`, `archive_dir`). DCC uses `discovery_rules` to derive working directories relative to discovered schema roots — no hardcoded `*_dir` keys. A universal loader must normalize both models into a common path resolution layer. | ✅ Resolved | T1.98.1–T1.99.8: re-audit found DCC's path model weaker (hardcoded `base_path/"data"` literals, `default_base_path()` script traversal, `discovery_rules` used only for schema discovery). Adopted EKS `global_paths` as the **universal canonical path pattern** (L16). Added `common/library/paths/resolver.py` (`resolve_paths()`, `ResolvedPaths`) normalizing both EKS and DCC shapes. Wired `ConfigRegistry` + `phase1_server.py` to the resolver. Universal architecture doc: L16 + §3.18 + §2.2/§2.3/§2.4/§4.1/§4.2/§9/§10. `eks/knowledge.json` v2.6.0. 252/252 tests pass (243 + 9 new). |
| I090 | 2026-07-10 | Phase 1 | 🟢 Low | EKS lacks `workflow_files`/`tool_files`/`folder_creation` present in DCC project_config | DCC `project_config.json` validates `workflow_files` (pipeline YAML manifests), `tool_files` (utility scripts), and `folder_creation` (intent-based folder creation on init). EKS has none of these — folder validation is code-driven in `setup_validator.py`. Adding these would enable purely schema-driven initialization. | ✅ Resolved | T1.98.6–T1.99.7 (with I089): added `workflow_file_entry_def`/`tool_file_entry_def` to `eks_base_schema.json`, `workflow_files`/`tool_files` to `eks_setup_schema.json` + `eks_config.json` (DCC parity). `folder_creation` concern satisfied by canonical `global_paths` (resolver derives create-list) — EKS deliberately does NOT add a separate DCC-style `folder_creation` block. `setup_validator.py` validates `workflow_files`/`tool_files` existence. 252/252 tests pass. |
| I091 | 2026-07-11 | Phase 1 | 🟡 Medium | `system_parameters` not defined as universal feature in universal pipeline architecture | `common/library/config/__init__.py` exists with `normalize_system_parameters()` and `get_system_param()` but `config` is not listed as an architecture-aligned sub-package in `common/library/__init__.py`, not imported, and not documented in `common/universal_pipeline_architecture_design.md`. Both DCC and EKS implement `system_parameters` independently but the concept is not formalized as L15 in the universal pipeline architecture. | ✅ Resolved | T1.97.10–T1.99.14: registered `config/` as architecture-aligned sub-package in `common/library/__init__.py`; added L15 to inventory, §3.17 System Parameters Pattern, and updated §4.1/§9/§10 in universal architecture doc; updated EKS knowledge.json. 243/243 tests pass. |
| I092 | 2026-07-11 | Phase 1 / Phases 2–5 | 🟡 Medium | EKS pipeline entry points do not converge on a shared run_pipeline(context); per-phase sub-pipeline requirement (R60 / AGENTS.md 18.13) not yet met for Phases 2–5 | DCC exposes a CLI, a programmatic UI entry (run_engine_pipeline_with_ui), and a web server that all converge on one BootstrapManager + PipelineContext + run_engine_pipeline(context). EKS has no such convergence for Phase 1: the only working runner is the HTTP backend phase1_server._run, which re-implements phases A to C inline and never calls PipelineOrchestrator.run_full_pipeline (dead code); the three CLI files (parsers/cli.py, discovery_cli.py, health_cli.py) are stubs returning fake SUCCESS; engine_endpoints.py is an orphaned stubbed FastAPI app; there is no non-HTTP UI entry and no shared bootstrap/context helper; ConfigRegistry SSOT is bypassed at the entry level (raw config dict passed); root launcher is server.py not the mandated serve.py; no pyproject console_scripts. Separately, per R60 / AGENTS.md 18.13, EACH phase must run as an independent sub-pipeline with its own phase{N}_server.py backend; only Phase 1 has one today — Phases 2 to 5 have no dedicated backend servers. | 🟡 In Progress (Phase 1 ✅; Phases 2–5 🔷 PLANNED) | **Phase 1 resolved 2026-07-11 (v3.51 / T1.99.1–7)**: shared `bootstrap_pipeline()`/`run_pipeline(context)` added (`eks/engine/core/pipeline_runner.py`); unified `eks-pipeline` CLI + `console_scripts` (`eks/pyproject.toml`); `phase1_server._run` wired to `run_pipeline()`; orphan `engine_endpoints.py` archived (`archive/ui/backend/`); canonical `eks/serve.py` added (`server.py` now shim); `ConfigRegistry` SSOT used at entry; full suite 257/257 green. **Phases 2–5 still open**: standalone `phase{N}_server.py` + `run_phase{N}_pipeline(context)` + `serve.py` proxy wiring — tasks logged: Phase 2 (T2.25–T2.26), Phase 3 (T3.36–T3.37), Phase 4 (T4.26–T4.27), Phase 5 (T5.21–T5.22), all 🔷 PLANNED pending future workplans. |
| I093 | 2026-07-13 | Phase 1 | 🟡 Medium | CLI entry points for discovery and health-scorer engines are non-functional stubs (T1.56 overstated as ✅) | `eks/engine/core/discovery_cli.py` and `eks/engine/core/health_cli.py` `run()` methods returned a placeholder `EngineOutput(status="SUCCESS")` with `TODO: Implement actual … execution` — they did not invoke any engine. Only `eks/engine/parsers/cli.py` was a real CLI (made functional in T1.99.2 via the `eks-pipeline` console_scripts). No tests covered the two stub CLIs (AGENTS.md §21 coverage gap). Root cause: Appendix F anticipated standalone "discovery" and "health scorer" engines with their own CLIs, but those engines were never built — discovery is part of the phase-1 orchestrator (`run_phase_a`) and health scoring runs inside it (`HealthScorer.score`), so there was no independent engine for the CLIs to drive. | ✅ Resolved | **Option (1) implemented (T1.56.1–T1.56.5, v3.58)**: `discovery_cli.py` `run()` now calls `PipelineOrchestrator.run_phase_a()` via the shared `bootstrap_pipeline()` funnel (T1.56.1); `health_cli.py` `run()` now calls `HealthScorer.score()` / `score_batch()` over the DuckDB registry (T1.56.2); added `test_discovery_cli.py` + `test_health_cli.py` closing the §21 coverage gap (T1.56.3–T1.56.4); `T1.56` flipped 🔶 PARTIAL → ✅; full EKS suite 264/264 green. U146. |
| I094 | 2026-07-13 | Phase 1 | 🟢 Low | Pipeline architecture diagram (§9 Mermaid) incorrectly shows initialize_context inside Phase A subgraph | The Mermaid diagram in `phase_1_foundation_workplan.md` §9 placed step 9 (`PipelineOrchestrator.initialize_context()`) inside Phase A — File Discovery subgraph. Actual implementation (`pipeline_runner.py:180`) calls `initialize_context()` as a distinct step between `bootstrap_pipeline()` and `run_phase_a()`. | ✅ Resolved | Moved `initialize_context()` step 9 out of Phase A subgraph into a new standalone "Phase 1 Context Setup" subgraph between Bootstrap and Phase A. Updated flow arrow to `BOOT --> CTX --> PHA`. v3.56. |
| I095 | 2026-07-13 | Master | 🟢 Low | Master workplan high-level pipeline diagram (§10.1) omits bootstrap & entry-point convergence | The Mermaid diagram in `eks_system_workplan.md` §10.1 started at raw file input → `SchemaToDDL`, skipping the global bootstrap layer and three entry-point types shown in the Phase 1 detailed diagram. Phase 3 was underspecified (only "Neo4j + Ontology"). | ✅ Resolved | Added ENTRY subgraph (CLI/Web/HTTP converging on `run_pipeline`), BOOT subgraph (ConfigRegistry/SchemaLoader/DocumentRegistry/ErrorManager), expanded PH3 to ontology → asset ingestion → relationship builders → Neo4j. v1.11. |
| I096 | 2026-07-14 | Phase 1 | 🟡 Medium | Main pipeline CLI entry misnamed/located; per-phase separability unsupported; funnel not DCC-faithful | `eks/engine/parsers/cli.py` is the unified `eks-pipeline` CLI but (1) its name/location (`parsers/` subpackage) is confusing and contradicts the DCC convention where the main entry (`dcc/workflow/dcc_engine_pipeline.py`) lives at the workflow root and is named for the pipeline, not "cli"; (2) it cannot run a single Phase 1 phase (A/B/C) separately, though Appendix F §2.3.3/§2.3.5 and R60/AGENTS.md §18.13 require each phase be independently callable (modular debugging/resume); (3) the shared funnel `pipeline_runner.run_pipeline` manually re-implements the A→B→C sequence already in `PipelineOrchestrator.run_full_pipeline`, so phase coordination is duplicated rather than single-sourced like DCC's `run_engine_pipeline(context)`. Cross-check with `common/universal_pipeline_architecture_design.md` (v1.6) + `common/library/core/pipeline/` confirms the underlying component features (context, engine contract, telemetry/heartbeat, checkpoint, paths, system-parameters, messaging/logger) are already established as shared `common.library` modules — the missing piece is the integration/runner layer, which this entry supplies. | ✅ Resolved | Introduce `eks/engine/eks_engine_pipeline.py` as the single main entry, built ON TOP of the established `common.library` building blocks rather than EKS-local duplicates — `common.library.core.pipeline` (`BaseEngine`/`BasePipelineContext`/`TelemetryHeartbeat`/`EngineInput`/`EngineOutput`), `common.library.messages` (milestone/status), `common.library.logging` (tiered logger + `log_depth`), `common.library.config.get_system_param` (verbose/debug), `common.library.paths.resolve_paths` (anchor/base path), `common.library.utility.validation.ValidationManager` (bootstrap readiness gate). Deleted `parsers/cli.py`; archived `pipeline_runner.py`; repointed 7 import sites. Full EKS suite 264/264 green. Advances I078. U148. |
| I097 | 2026-07-14 | Phase 1 | 🟡 Medium | EKS pipeline path resolution is brittle — hardcoded `parent.parent.parent` instead of anchor-folder walk + schema-driven defaults | `eks/engine/eks_engine_pipeline.py` resolves `PRJ_DIR = Path(__file__).parent.parent.parent` with no anchor-folder fallback, no `__main__` guard against script-dir shadowing of stdlib `logging`. `--data-dir` is `required=True` with no schema-driven default from `global_paths.data_dir`. Schema/config paths are hardcoded. DCC uses `resolve_pipeline_base_path()` → `default_base_path("workflow")` walking `__file__.parents` for `workflow/` anchor + schema-driven folder validation via `project_config.json`. EKS should adopt the same pattern with `engine/` as anchor, make `--data-dir` optional with `global_paths.data_dir` default, and route all path defaults through resolved base path + `global_paths` schema (precedence: CLI > Schema > Native). | ✅ Resolved | Added `resolve_pipeline_base_path()` with `engine/` anchor + `Path.cwd()` fallback (DCC-faithful); replaced hardcoded `parent.parent.parent`; changed `--data-dir` to optional with schema-driven default from `global_paths.data_dir` (CLI > Schema > Native); routed all 4 hardcoded path literals (`output_dir`, `schema_dir`, `config_file`, `eks_root`) through `global_paths` lookups. Added 7 tests. Full EKS suite 271/271 green. U149. |
| I098 | 2026-07-15 | Phase 1 / Master | 🟠 High | `pipeline_dir` anchor-folder discovery design not formalized; EKS `resolve_pipeline_base_path` has residual entry-point path issues vs DCC anchor pattern | DCC implements this faithfully via `default_base_path("workflow")` which walks `__file__.parents` for the `workflow` anchor and returns its parent — the project root — from which config/data/output are discovered. EKS implemented `resolve_pipeline_base_path(anchor="engine")` but diverged: EKS nests code two levels deep (`project_root/eks/engine/...`) while related folders (config/data/output) live under `project_root/eks/`, NOT under `engine/`. Choosing anchor=`engine` forced a hardcoded `parent.parent.parent` (2 levels up) plus per-path `eks_root` re-prefixing. The natural EKS anchor is `eks`, not `engine`. Residual issues: (1) HIGH — `run()` derives the `data_dir` default WITHOUT the `eks_root` prefix, so a default CLI run scans `project_root/data` instead of `project_root/eks/data`, contradicting `bootstrap_pipeline()` and `phase1_server.py`. (2) MEDIUM — `resolve_pipeline_base_path` hardcodes `parent.parent.parent`, not DCC-faithful (`parent.parent`), relocation-fragile. | ✅ Resolved | Aligned EKS entry with universal pattern L17 + DCC's `pipeline_dir`/`pipeline_start`/`resolve_pipeline_base_path()` flow: OS detection at entry (`detect_os`, L12); renamed walk to `default_base_path("eks")` returning parent of anchor; cwd/`--base-path` resolver; `discover_project_root()` + `--base-path` CLI + `==pipeline_dir` strip; routed all sub-paths via `resolve_paths()` honoring `eks_root` (fixes default `data_dir` bug); OS-gated auto-create + `safe_posix()`; raise if anchor missing; wired common L12/L17 (advances I078). U150–U156. |
| I099 | 2026-07-15 | Phase 1 / Master | 🟡 Medium | EKS and DCC CLI arg parsers are not universal and not schema-driven | EKS `build_parser()` is hardcoded + project-local (fails universal + schema-driven); neither derives defaults from the resolved root schema at build time; precedence CLI>Schema>Native applied ad-hoc; neither returns a single structured pipeline-input value. Violates AGENTS.md §10 SSOT and the four review principles. | ✅ Resolved | Created `common/library/cli/schema_cli.py` (L18) with `build_parser_from_schema()` (schema-driven args) and `parse_cli_args()` returning `CliResult` (namespace + explicit overrides + `overrides_provided` + `pipeline_input` with resolved paths + schema params). Wired EKS via `build_schema_driven_parser()` + `parse_eks_cli()`; `run()` consumes the universal parser preserving behavior. 15 new tests, all green. Report RP-EKS-P1-CLI-001 (U155). Advances I078. |
| I100 | 2026-07-15 | Phase 1 | 🟡 Medium | EKS suite config drift — missing `project_setup` config section causes 15 pre-existing test failures | During I099 / L18 implementation, the full EKS suite showed 15 failures (262 passed / 277 total) in `test_path_resolver`, `test_phase1`, `test_phase1_server`, and `test_eks_engine_pipeline` (bootstrap_pipeline / run_pipeline / cli). All fail identically inside `ProjectSetupValidator.validate_all` (raises "requires a 'project_setup' config section") and `ConfigRegistry`, which L18 did not modify. Root cause: `eks/config/schemas/eks_config.json` lacks proper config resolution so the readiness gate cannot run. | ✅ Resolved | Root cause was a config-resolution bug: `common/library/cli/schema_cli.py::_schema_config_candidates` probed `eks/config/eks_config.json` but EKS keeps it at `eks/config/schemas/eks_config.json`. Fixes: (1) `_schema_config_candidates` now also probes `eks/config/schemas/eks_config.json` (+ `dcc/config/schemas/project_config.json`); (2) `ConfigRegistry.__new__` promotes the singleton only after a successful `load_all()`, preventing a poisoned empty singleton. Added 2 regression tests. Full EKS suite 277/277 green. U160. |
| I101 | 2026-07-15 | Phase 1 / Master | 🟡 Medium | DCC not yet wired to the universal L18 CLI parser (SSOT gap remains) | I099 delivered the universal schema-driven CLI parser (`common/library/cli`, L18) and wired EKS to it, but DCC still uses its project-local `create_parser` / `parse_cli_args` (`dcc/workflow/utility_engine/cli/cli_parser.py`) and the schema-driven `create_parser_from_registry` / `parse_cli_args_from_registry` (`cli_registry.py`). This leaves duplicated parser logic across projects, violating AGENTS.md §10 SSOT. | ⛔ Won't Implement | Per user directive: DCC-related issues within the EKS pipeline are not to be implemented. Rewire DCC `dcc_engine_pipeline.py` + `cli_parser.py` to `common.library.cli.parse_cli_args()`; remove duplicated parsers. Tracked via T1.99.30 (also marked not-to-implement). |
| I102 | 2026-07-15 | Phase 1 / Master | 🟡 Medium | `DEFAULT_PIPELINE_DIR = "engine"` is hardcoded in common.library (violates project-agnostic SSOT) | `common/library/paths/root_discovery.py` defines `DEFAULT_PIPELINE_DIR = "engine"` at module level (L17) and uses it as the default `pipeline_dir` in `resolve_pipeline_base_path()` and `discover_project_root()`. The value `"engine"` is EKS-specific; DCC uses `"workflow"`. A universal shared library must not bake in one project's folder convention. | ✅ Resolved | Removed `DEFAULT_PIPELINE_DIR` from `common/library/paths/root_discovery.py` (L17) and its re-export in `common/library/paths/__init__.py`; `schema_cli.py` (L18) no longer imports it and defaults `pipeline_dir` to `None`. Each pipeline entry defines its own `pipeline_dir` constant and passes it explicitly. Added a test asserting the common library exposes no project-specific `pipeline_dir` default. U157. |
| I103 | 2026-07-15 | Phase 1 / Master | 🟡 Medium | EKS `main()` delegates to a separate `run()` instead of containing the pipeline steps (DCC-faithful entry-point pattern) | EKS `eks/engine/eks_engine_pipeline.py` `main()` is `sys.exit(run())` — a thin wrapper that delegates all orchestration to a separate `run()` function. By contrast DCC's `dcc/workflow/dcc_engine_pipeline.py::main() -> int` contains every pipeline step inline and has no separate `run()`. The split EKS design diverges from the DCC entry-point shape and violates AGENTS.md §10 SSOT (I092/I096/I099). | ✅ Resolved | Mirror DCC: move `run()`'s body into `main(args: Optional[list] = None) -> int` (parse via `parse_eks_cli` -> resolve params/paths -> logger + `TelemetryHeartbeat` -> `run_pipeline()` -> `EngineOutput` -> `return 0/1`); delete the separate `run()`; set `if __name__ == "__main__": sys.exit(main())`. Keep `run_pipeline()` / `bootstrap_pipeline()` funnels (used by `phase1_server.py` + tests). `eks/test/test_eks_engine_pipeline.py` now calls `main()`. U158. |
| I104 | 2026-07-15 | Phase 1 / Master | 🟡 Medium | EKS main() does not declare anchor/pipeline_dir locally (DCC-faithful I/O clarity) | EKS `eks/engine/eks_engine_pipeline.py::main()` relied on module-level SSOT constants `_EKS_ANCHOR` / `_EKS_PIPELINE_DIR` (consumed inside `parse_eks_cli` -> `parse_cli_args`), so `main()` never named its folder constants and its I/O dependencies were implicit. By contrast DCC's `dcc/workflow/dcc_engine_pipeline.py::main()` declares `pipeline_dir = "workflow"` as a local at the top of `main()`, making the entry point's inputs self-documenting. This reduced readability/maintainability of the EKS entry point and diverged from the DCC-faithful shape (I092/I096/I099). | ✅ Resolved | Declare `anchor` and `pipeline_dir` as local literals at the top of `main()` (DCC-faithful) and pass them explicitly to `parse_eks_cli(anchor=, pipeline_dir=)`; widen `parse_eks_cli` / `build_schema_driven_parser` to accept `anchor` / `pipeline_dir` params (defaulting to the same literals so other callers/tests are unaffected); the module-level `_EKS_ANCHOR` / `_EKS_PIPELINE_DIR` constants were removed. Advances AGENTS.md §10 SSOT + I/O clarity. Tracked via T1.99.34. |
| I105 | 2026-07-16 | Phase 1 | 🟠 High | EKS pipeline messaging is duplicated and the entry-point uses the wrong catalog file | Two near-identical message-manager implementations exist: (1) `eks/engine/core/message_manager.py` `MessageManager` (class-based, injected `EKSLogger`, loads `eks_message_config.json`) and (2) `common/library/core/messages/message_manager.py` `BaseMessageManager(ABC)` (the intended shared SSOT, configurable `_catalog_filename`, `print()` fallback, verbosity clamp). EKS `eks_engine_pipeline.py:505` calls `BaseMessageManager(config_dir=..., logger=logger, verbosity=level)` with the **default** `_catalog_filename="message_config.json"` — but EKS's catalog is `eks_message_config.json`. The entry-point milestone `STATUS_PIPELINE_START` (line 506) therefore resolves to no message and prints nothing — a silent wrong-catalog bug. DCC meanwhile has a third, independent design: module-level functions in `dcc/workflow/utility_engine/console/console_output.py` (`status_print`/`milestone_print`/`debug_print`) bound to a process-global `DEBUG_LEVEL`/`DEBUG_OBJECT` and its own `pipeline_message_config.json`. The three designs diverge (class vs functions, per-instance vs global verbosity, icon support present only in DCC, different missing-ID fallbacks) while the underlying 3-layer schema model is already ~95% identical across EKS and DCC. This violates AGENTS.md §10 SSOT and perpetuates I078 (common-library not wired into EKS runtime). | ✅ Resolved | T1.99.35–39 implemented (U162): BaseMessageManager hardened with `_make_default_logger()` hook, `_messages_dict()` helper, icon support in `show()`; EKS MessageManager rewritten as thin subclass with `_catalog_filename="eks_message_config.json"`; fixed `eks_engine_pipeline.py:505` wrong-catalog bug (BaseMessageManager → MessageManager); added `icon` property to `eks_message_base.json`; 10 new common BaseMessageManager tests + all 7 EKS MessageManager tests green. I105 → Resolved. |
| I106 | 2026-07-16 | Phase 1 | 🟡 Medium | EKS pipeline context not threaded through `main()` | DCC `main()` uses `BootstrapManager` to run 8 initialization phases (CLI, paths, registry, defaults, fallback, env, schema, params), converts to `PipelineContext` via `manager.to_pipeline_context()`, and passes it through `run_engine_pipeline(context)`. EKS `main()` builds `EngineInput`/`EngineOutput` (L08) but never constructs or threads an `EKSPipelineContext` — it lives only inside `PipelineOrchestrator`, never seeded from `EngineInput` or surfaced to `main()`. `EKSPipelineContext` also does **not** extend `common.library.core.pipeline.BasePipelineContext` (L06) despite workplan T1.52 claiming it should (verified `issubclass(…) == False`). | ✅ Resolved | T1.99.40–44 implemented (U163): `EKSPipelineContext` now extends `BasePipelineContext` (L06) implementing universal contract; `PipelineOrchestrator.initialize_context()` accepts bootstrap data (parameters, config_registry, schema_registry, checkpoint_state); `run_pipeline()` accepts optional `context` parameter, skips bootstrap when provided, surfaces context in return dict; `main()` builds `EKSPipelineContext` from `EngineInput` + bootstrap, passes to `run_pipeline(context=ctx)`, extracts `EngineOutput` from returned context. DCC-faithful context threading complete. I106 → Resolved. |
| I107 | 2026-07-16 | Phase 1 | 🟡 Medium | EKS bootstrap incomplete vs DCC — init functions still outside `bootstrap_pipeline()` (post-I106 residual) | I106 closed the context-threading gap but left `bootstrap_pipeline()` implementing only a subset of DCC's 8 `BootstrapManager` phases (config-load, path-resolve, readiness gate, managers). The following init concerns remain in `eks_engine_pipeline.py::main()` instead of inside bootstrap, diverging from the DCC-faithful shape: (1) `detect_os()` at L498 — DCC does OS detection inside `_bootstrap_environment` (P6); (2) `_read_system_params()` + `get_system_param(log_level)` at L509–510 — a **2nd config load** just to read `log_level`, duplicating bootstrap's config load (P1/P8 in DCC); (3) `data_dir` CLI>Schema>Native precedence resolution at L521–527 — DCC resolves this inside `_bootstrap_parameters` (P8); (4) `mm = MessageManager(...)` at L535 — a **double MessageManager** (bootstrap_pipeline also creates one at L149; main()'s instance is used only for the start banner then discarded). **Most serious: split path-resolution source** — `main()` reads `schema_dir`/`output_dir` from `cli.resolved_paths` (L529–530) but `archive_dir`/`log_dir` from `boot["resolved_paths"]` (L570–572); the two are computed by different code paths (cli uses `_schema_config_candidates` first-match; bootstrap uses `ConfigRegistry(project_root/"eks"/"config")`) and can diverge if `config_dir` differs. **Variable audit (2026-07-16)**: Correctly OUTSIDE bootstrap (DCC-faithful, no change) — `project_root` (L505), `pipeline_root_dir`/`pipeline_dir` literals (L494–495), `logger` setup (L534), `TelemetryHeartbeat` (L539). Belong INSIDE bootstrap — `os_info`/`detect_os` (L498), `cli`/`parsed` parse (L503–504), `config_dir` (L506), `params`/`level` (L509–512), `resolved` (L515), `eks_root` (L518), `data_dir` (L521–527), `output_dir` (L529), `schema_dir` (L530), `config_file` (L531), `mm` (L535). Defects confirmed: (A) context `paths` assembled from two different `resolved` dicts (cli vs boot) — fragile; (B) redundant config load + double MessageManager. | ✅ Resolved | T1.99.45–48 implemented (U165): `bootstrap_pipeline()` now handles all init — OS detection (L133), CLI parse (L136), config load once (L142–152), log-level precedence (L157–163), `eks_root` (L160), single `resolve_paths()` (L166), data-dir precedence (L168–175), single MessageManager (L178). `main()` delegates everything to bootstrap (L542–547) and reads the single `resolved_paths` + `boot["mm"]` (Defect A+B fixed). Context path in `run_pipeline()` fixed to wire `em`/`mm` into orchestrator (L273–274). Non-context path in `run_pipeline()` fixed to pass `project_root` to `bootstrap_pipeline()` (L284). T1.99.49 still 🔶 PLANNED: tests + knowledge base + logs update. |
| I108 | 2026-07-17 | Master | 🟠 High | Create universal `BootstrapManager` in `common/library/bootstrap/` as L19 — current `common/library` lacks a shared bootstrap orchestrator | DCC's `BootstrapManager` (~1223 lines in `dcc/workflow/utility_engine/bootstrap/boot_pipeline.py`) is the only implementation of a stateful, 8-phase bootstrap orchestrator (P1–P8: CLI, paths, registry, defaults, fallback, env, schema, params). It provides mature patterns: per-phase `BootstrapPhaseStatus` tracking with start/end/duration/error, `preload_trace`/`postload_trace` via `ContextTraceItem`, `_validate_pre_context_gate()`, dual-mode `bootstrap_all()`/`bootstrap_for_ui()`, structured `BootstrapError` with `S-C-S-XXXX` codes, and a `bootstrap_summary` property. The `common/library` inventory (L01–L18) has individual building blocks — L05 `TelemetryHeartbeat`, L06 `BasePipelineContext`, L08 `EngineInput`/`EngineOutput`, L10 `BaseErrorManager`, L13 `ValidationManager`, L15–L17 path utilities — but **no `BootstrapManager`** orchestrator exists. This means every project (EKS, future projects) must build its own bootstrap logic from scratch, violating AGENTS.md §10 SSOT. **Option A (library-first)** was selected: create `common/library/bootstrap/` as L19 containing a universal `BootstrapManager` extracted from DCC's mature implementation, with project-specific hooks (anchor folder, config shape, error codes) passed via constructor or a strategy parameter. The universal manager must support: (a) phase tracking with `BootstrapPhaseStatus`, (b) pre/post-load context traces, (c) `to_pipeline_context()` → `BasePipelineContext` (L06), (d) dual-mode (`bootstrap_all` + `bootstrap_for_ui`), (e) structured `BootstrapError`, (f) project-agnostic phase ordering configurable via a phase registry. | ✅ Resolved | T1.99.50–55 (U167): Created `common/library/bootstrap/` as L19 — `manager.py` (~500 lines, stateful 8-phase orchestrator), `errors.py` (structured `BootstrapError` with `code`/`message`/`phase` + `to_system_error()`), `phases.py` (`BootstrapPhaseRegistry` with fluent API), 37 universal tests. `common/library/__init__.py` updated. EKS `bootstrap_pipeline()` refactored as thin wrapper delegating to `EKSBootstrapManager` (T1.99.57–58, U167); `main()` simplified via `to_pipeline_context()` chain collapsing ~30 lines of manual assembly (T1.99.59–61, U167); structured `P1-BOOT-*` codes registered in `eks_error_config.json` + `eks_error_setup_schema.json` + `eks_error_code_base.json` (T1.99.62, U168); `RuntimeError` replaced with `BootstrapError("P1-BOOT-READINESS", ...)` in `bootstrap_pipeline()`; 6 new error-path tests added (T1.99.63, U168). 37 universal + 29 EKS pipeline tests pass (66/66). I108 → Resolved. |
| I109 | 2026-07-17 | Phase 1 | 🟡 Medium | Refactor EKS `bootstrap_pipeline()` to delegate to universal `BootstrapManager` (L19) | Currently `eks/engine/eks_engine_pipeline.py::bootstrap_pipeline()` (L102–216) is a stateless function returning a `dict` with ~13 keys. It manually sequences init steps (OS detection, CLI parse, config load, path resolution, readiness gate, ErrorManager, MessageManager) with no phase tracking, no trace, no structured error wrapping, and no UI-mode path. After I108 creates the universal L19 `BootstrapManager`, EKS should: (1) replace `bootstrap_pipeline()` with a thin wrapper that instantiates the universal manager with EKS-specific hooks (`pipeline_root_dir="eks"`, `pipeline_dir="engine"`, `ConfigRegistry` adapter, `ProjectSetupValidator` readiness gate); (2) the returned dict keys should be generated from the manager's structured state rather than hand-assembled; (3) `main()` should call `manager.bootstrap_all(cli_args)` → `manager.to_pipeline_context()` → pass context to `run_pipeline(context=ctx)` — identical to DCC's `BootstrapManager(...).bootstrap_all().to_pipeline_context()` pattern. Backward compatibility: existing callers (`phase1_server.py`, tests) that consume the current `bootstrap_pipeline()` dict return shape must be updated or a compat shim provided. | ✅ Resolved | I108 resolved; EKS wiring complete (U167): `EKSBootstrapManager` subclass in `eks/engine/core/bootstrap.py` injects EKS hooks (`ConfigRegistry`, `ProjectSetupValidator`, `parse_eks_cli`, `resolve_paths`, `detect_os`, `ErrorManager`/`MessageManager` factories); `bootstrap_pipeline()` refactored as thin wrapper (T1.99.58); `main()` uses `EKSBootstrapManager().bootstrap_all(args)` chain (T1.99.59); backward-compat `to_dict()` returns same 13-key shape as old `bootstrap_pipeline()`. All existing callers (`phase1_server.py`, tests) work unchanged. I109 → Resolved. |
| I110 | 2026-07-17 | Phase 1 | 🟡 Medium | Simplify EKS `main()` — delegate context construction to `BootstrapManager.to_pipeline_context()` | After I108 + I109, `eks_engine_pipeline.py::main()` (L521–672) still manually constructs `EKSPaths`, `EKSData`, `EKSState`, `EKSTelemetry`, `ctx_params` dict, and `EKSPipelineContext` (L601–631) from individual `boot` dict keys. This is 30+ lines of manual context assembly that DCC's `BootstrapManager.to_pipeline_context()` handles in a single method call. The post-I109 flow should collapse to: `ctx = manager.bootstrap_all(...).to_pipeline_context()` — removing the manual assembly and making `main()` a thin shell (parse → bootstrap → run → report), matching DCC's entry-point shape exactly. The `EngineInput` construction (L588–595) should be derived from the context, not hand-built. | ✅ Resolved | I108+I109 resolved; `main()` simplified (U167): ~30 lines of manual context assembly (EKSPaths/EKSData/EKSState/EKSTelemetry/EKSPipelineContext) collapsed into single `ctx = mgr.to_pipeline_context()` call (T1.99.60); `EngineInput` derived from `ctx.paths` (T1.99.61); DCC-faithful `BootstrapManager(...).bootstrap_all().to_pipeline_context()` chain in place. I110 → Resolved. |
| I111 | 2026-07-17 | Phase 1 | 🟠 High | Structured `BootstrapError` + `P1-BOOT-*` error codes via shared error system — EKS bootstrap failures use generic `RuntimeError` | DCC's `BootstrapManager` defines a `BootstrapError(Exception)` class (L76–108) with `code`/`message`/`phase` attributes and `to_system_error()` returning `(code, message)` for `system_error_print()`. Error codes follow `S-C-S-XXXX` format (e.g. `S-F-S-0206`, `S-B-S-0601`). EKS `bootstrap_pipeline()` raises bare `RuntimeError(error_msg)` on readiness failure (L197–201) — no structured code, no phase attribution, no `to_system_error()` contract. The universal `BootstrapManager` (I108) must carry a universal `BootstrapError` that both EKS and DCC can subclass or use directly, wired into the shared `common.library.core.errors.BaseErrorManager` (L10). EKS-specific bootstrap error codes (`P1-BOOT-*`) should be registered in `eks_error_config.json` under `system_errors` with proper ranges, message templates, and severity. | ✅ Resolved | I108 resolved; structured errors implemented (U168): (1) universal `BootstrapError` in `common/library/bootstrap/errors.py` with `code`/`message`/`phase` + `to_system_error()` → `(code, message)` + `to_dict()`/`from_system_error()` (T1.99.54); (2) 5 `P1-BOOT-*` codes registered in `eks_error_config.json` under `system_errors` + `bootstrap_p1` range added to `system_error_ranges` + pattern updated in `eks_error_code_base.json` + property added to `eks_error_setup_schema.json` (T1.99.62); (3) `RuntimeError` in `bootstrap_pipeline()` replaced with `raise BootstrapError("P1-BOOT-READINESS", ...)` (T1.99.63); (4) 6 new error-path tests added: `test_bootstrap_readiness_failure_raises_bootstrap_error`, `test_bootstrap_error_has_code_and_phase`, `test_bootstrap_error_to_system_error`, `test_bootstrap_error_to_dict`, `test_bootstrap_error_from_system_error`, `test_bootstrap_error_registered_in_catalog`. 29/29 EKS pipeline tests pass. I111 → Resolved. |
| I112 | 2026-07-17 | Phase 1 | 🟠 High | Bootstrap error codes misaligned with Appendix D pipeline message/error design | Cross-reference audit of 5 sources (Appendix D, `eks_error_config.json`, `common/library/bootstrap/errors.py`, `common/library/bootstrap/manager.py`, `eks/engine/core/bootstrap.py`) found 6 misalignments: **(1) 🔴 `P1-BOOT-*` format doesn't match Appendix D** — Appendix D D2 defines only two formats: `S-{cat}-S-{id}` (system) and `P{phase}-{module}-{function}-{id}` (data). `P1-BOOT-READINESS` etc. use `BOOT` as a pseudo-module with human-readable suffixes instead of 4-digit IDs — this is a third, undocumented format. **(2) 🔴 `S-B-S-06xx` category `B` not in Appendix D D3** — `eks_error_config.json` registers `S-B-S-0601`/`S-B-S-0602` but Appendix D's system error category table only lists E/F/C/R/D (ending at 0599). Bootstrap (`B`) category and `06xx` range are not documented. **(3) 🔴 Universal `B-*` codes (14 codes) unregistered in EKS catalog** — `BootstrapManager` uses `B-CLI-001`, `B-PATH-001/002`, `B-REG-001`, `B-DEF-001`, `B-FALL-001`, `B-ENV-001`, `B-SCH-001`, `B-PAR-001/002`, `B-BOOT-0601`, `B-CTX-001`, `B-UNK-001/002`, `B-{phase_id}-ERR`. EKS overrides only P6 and P8 to use `P1-BOOT-*`; phases P1–P5, P7 can still fire `B-*` codes not in `eks_error_config.json` — `ErrorManager` lookup fails silently. **(4) 🟡 No bootstrap messages in `eks_message_config.json`** — 33 messages exist but zero bootstrap-related ones (no `MILESTONE_BOOTSTRAP_START`, `STATUS_CONFIG_LOADED`, etc.). Appendix D D6 also lacks bootstrap message entries. **(5) 🟡 Appendix D missing entire bootstrap error section** — No `bootstrap` category in system errors, no `P1-BOOT-*` documentation, no `bootstrap_p1` range. **(6) 🟢 Field schema is consistent** — Both `S-B-S-06xx` and `P1-BOOT-*` use same JSON fields (`code`/`name`/`message`/`severity`/`category`/`stops_pipeline`/`promote_detail`); `BootstrapError.to_system_error()` returns `(code, message)` compatible with `BaseErrorManager`; `_run_phase()` wrapper properly propagates `BootstrapError` (re-raises) vs generic exceptions (wraps). | 🔴 Open | **Recommendations**: (1) Add Bootstrap (`B`) category to Appendix D D3 with range `S-B-S-0600–0699`; (2) Register all 14 `B-*` universal codes in `eks_error_config.json` under a new `bootstrap_universal` range; (3) Add bootstrap milestone/status messages to `eks_message_config.json` and Appendix D D6; (4) Decide: either change `P1-BOOT-*` to `S-B-S-0603`–`S-B-S-0607` (system error format) OR update Appendix D to document `P1-BOOT-*` as a setup/bootstrap hybrid format. See full analysis in conversation history (2026-07-17). |
| I113 | 2026-07-17 | Phase 1 | 🟠 High | EKS `main()` creates logger + heartbeat **after** bootstrap; DCC calls `setup_logger()` + `set_debug_level()` **before** bootstrap | Comparison of `eks/engine/eks_engine_pipeline.py::main()` vs `dcc/workflow/dcc_engine_pipeline.py::main()` found a sequencing gap: **(1) 🟠 Pre-bootstrap logger missing** — DCC `main()` calls `setup_logger()` (L495) then `set_debug_level(verbose_level)` (L496–497) to configure Python `logging` **before** `BootstrapManager.bootstrap_all()` (L505). EKS `main()` creates `UniversalLogger("eks-pipeline", level=level)` at L529 **after** `EKSBootstrapManager.bootstrap_all(args)` returns — bootstrap phases P1–P8 run with no logger at all. **(2) 🟠 Verbosity level unknown before bootstrap** — DCC derives `verbose_level` from `parse_cli_args()` (L490–496) which runs before bootstrap. EKS CLI parsing happens **inside** `bootstrap_all()` (via `_eks_cli_parser` hook at Phase P2), so `level` cannot be known until bootstrap completes. **(3) 🟡 TelemetryHeartbeat also post-bootstrap** — `TelemetryHeartbeat` created at L534 after bootstrap; bootstrap phases are not covered by telemetry. **(4) 🟢 EKSBootstrapManager already accepts `logger` parameter** — The constructor (L58) accepts `logger: Any = None` but `main()` never passes one. Fix: parse CLI early for verbosity flags, create `UniversalLogger` pre-bootstrap, pass to `EKSBootstrapManager(logger=logger)`, mirroring DCC's two-phase setup. | 🟢 Resolved | **Resolution (T1.99.70–T1.99.74)**: **(1)** Added `_parse_early_verbosity(args)` — lightweight argparse for `--level`/`--debug` only, no schema/config dependency, runs before bootstrap (mirrors DCC `parse_cli_args()` before `setup_logger()`). **(2)** `UniversalLogger("eks-pipeline", level=early_level)` created pre-bootstrap and passed into `EKSBootstrapManager(logger=logger)`. **(3)** `TelemetryHeartbeat` created pre-bootstrap, started before `discover_project_root()` — covers all 8 bootstrap phases. **(4)** Logger reconciliation: if bootstrap resolves a different `level`, heartbeat is recreated. **(5)** Hooks verified: `_eks_error_factory` and `_eks_message_factory` already pass `self.logger` through; `BootstrapManager._log()` uses `self.logger` for all phase logging. See `eks/engine/eks_engine_pipeline.py` L470–594. |
| I114 | 2026-07-17 | Phase 1 | 🟠 High | No environment/dependency check in EKS bootstrap — pipeline can crash mid-run with bare `ImportError`; top-level imports in `main()` bypass any env check | **Two-pronged gap**: **(A)** DCC `BootstrapManager._bootstrap_environment()` (Phase 6) calls `test_environment()` which reads `project_setup.json → dependencies` (required/optional/engines) and runs `importlib.import_module()` on each, returning `{ready: True/False, errors: [...]}`. EKS `EKSBootstrapManager._bootstrap_env()` (Phase 6) **only does OS detection** — zero dependency verification. **(B) More critical: top-level module imports in `main()` bypass `test_environment()` entirely.** EKS `eks_engine_pipeline.py` L47-63 imports ~15 heavy modules at **module level** — these execute at Python import time, before `main()` is even called. If any transitive dependency (pandas, duckdb, jsonschema, etc.) is missing, Python throws a bare `ModuleNotFoundError` with no user-friendly message — the `test_environment()` in P6 never runs. **Resolution (T1.99.75–80)**: (a) Created `common/library/core/system/` with universal `test_environment(dependencies: dict)` — stdlib-only, one-by-one `importlib.import_module()`, returns `{ready, errors, ...}` (T1.99.75). (b) Added `env_tester` strategy hook to universal `BootstrapManager` (T1.99.76). (c) Wired `EKSBootstrapManager._bootstrap_env()` to call `test_environment()` with deps from `eks_config.json`; `ready=False` → `BootstrapError("P1-BOOT-ENV", ...)` with "Run: conda activate eks" (T1.99.77). (d) Registered `P1-BOOT-ENV` in `eks_error_config.json` (T1.99.78). (e) Lazy-import refactor in `main()` — only stdlib + `test_environment` at module level; all heavy imports deferred inside functions; `_PRJ_DIR` discovery wrapped in try/except. `test_environment()` (P6) runs first, reports ALL missing deps, no bare `ModuleNotFoundError` reaches the user (T1.99.80). | ✅ Resolved | T1.99.75–80 implemented. See §30.5.2 v3.84. |
| I115 | 2026-07-17 | Phase 1 / Master | 🟡 Medium | `universal_pipeline_architecture_design.md` v1.9 gap-closure audit not tracked in issue log | On 2026-07-17, a comprehensive gap-closure audit was performed comparing `common/universal_pipeline_architecture_design.md` (then v1.8) against the actual `common/library/` implementation. 5 major gaps were identified and resolved: (1) L18-L21 missing from §2.2 inventory table; (2) stale counts (14→21 modules, 15→21 patterns); (3) missing §3.21 BootstrapManager Pattern and §3.22 Environment/Dependency Probe Pattern; (4) §2.3 layout outdated (flat proposal vs actual two-layer façade architecture); (5) §8.2 EKS status stale. 14 targeted edits were applied, bumping the doc to v1.9. However, this significant documentation update was never logged as an issue in `issue_log.md` — only the individual code changes (I108–I114) were tracked. The doc audit itself constitutes a standalone deliverable (L18-L21 formalization, architecture alignment). | 🟢 Resolved | Retroactively logged as I115. The 14 edits are documented in the doc's revision history (v1.9 entry). No code changes needed — documentation-only gap closure. |
| I116 | 2026-07-17 | Phase 1 / Master | 🟢 Low | `universal_pipeline_architecture_design.md` Document Index has incorrect § numbering sequence | The Document Index listed sub-patterns §3.17–§3.22 as top-level entries (using §17–§22 instead of §3.17–§3.22), breaking the hierarchical numbering. Additionally, §3.1–§3.16 were entirely missing from the index. This made the index inconsistent — section numbers jumped from §3 directly to §17 then to §4. Fixed by: (1) renumbering §17–§22 → §3.17–§3.22; (2) adding missing §3.1–§3.16 entries with correct anchor links matching actual `### 3.x` headings. No content changes — index-only fix. | ✅ Resolved | Index corrected. All 22 sub-patterns now listed as §3.1–§3.22 under the parent §3 entry; anchor links verified against actual heading text. |
| I117 | 2026-07-17 | Phase 1 | 🟠 High | Pre-bootstrap `common.library` imports in `main()` are not error-guarded — bare `ImportError` reaches user if `common.library` is not installed | **Root cause (chicken-and-egg)**: `eks_engine_pipeline.py::main()` L558–L561 imports 4 `common.library` modules (`safe_posix`, `should_auto_create_folders`, `discover_project_root`, `UniversalLogger`, `TelemetryHeartbeat`) **before** `EKSBootstrapManager.bootstrap_all()` runs. The `test_environment()` dependency check lives inside bootstrap Phase 6 — if `common.library` itself is not importable (e.g. `pip install` not run, `PYTHONPATH` wrong), the bare `ImportError` / `ModuleNotFoundError` hits the user **before** any error detection or message infrastructure exists. These imports cannot be deferred to after bootstrap because bootstrap itself needs them (logger, heartbeat, project-root discovery). **Resolution (T1.99.81)**: Created `_preload_infrastructure()` — a module-level pure-stdlib function in `eks_engine_pipeline.py` that individually try/except-guards each `common.library` import, collects ALL errors into a single dict, and returns `{ready, errors, logger, heartbeat, project_root, ...}`. `main()` now calls `infra = _preload_infrastructure(...)` and gates on `infra["ready"]` — prints all errors with `FATAL:` prefix and exits code 1. **This function CANNOT live in `common.library`** (circular: cannot guard the import of a package from inside that package). It is a **universal preload pattern** (§3.23) — each project replicates the same structure with its own `pipeline_root_dir`/`pipeline_dir`/`logger_name` literals. DCC does not have this issue (all imports are local relative paths, no external common-library dependency). **v2 refinements (I118)**: (a) every error now `print(..., file=sys.stderr)` immediately at the failure point, not just collected; (b) all variables pre-bound with safe defaults before any step runs — no `NameError` if early step fails; (c) `logger`/`heartbeat` instantiation moved OUT of `_preload_infrastructure()` into `main()` — the preload function now returns imported **classes** (`_UniversalLogger`, `_TelemetryHeartbeat`), not instantiated objects, making it truly pure-stdlib. | ✅ Resolved | T1.99.81 implemented + v2 refinements (I118). `_preload_infrastructure()` at module level in `eks_engine_pipeline.py`; `main()` simplified to single `infra["ready"]` gate + local logger/heartbeat instantiation. All 4 `common.library` imports individually guarded; errors collected AND immediately printed to stderr. |
| I118 | 2026-07-17 | Phase 1 | 🟠 High | `_preload_infrastructure()` v1 had two critical defects: (a) errors not printed to stderr at failure point, (b) logger/heartbeat instantiated inside preload defeating pure-stdlib guarantee | **Defect (a) — Silent failures**: `_preload_infrastructure()` v1 collected import errors into `errors: list` but only printed them in `main()`'s `if not infra["ready"]:` block. If a different caller used the function without printing, errors were silently lost. If `_parse_early_verbosity` threw, `early_verbosity` was never assigned but `debug_mode = early_verbosity.get("debug", False)` at L613 ran unconditionally — `NameError` crashed the function before errors could be returned. **Defect (b) — Not pure-stdlib**: `_preload_infrastructure()` v1 called `_UniversalLogger(logger_name, level=early_level)` and `_TelemetryHeartbeat(enabled=...)` + `hb.start()` inside the function body. This meant the function itself depended on `common.library` at runtime — if `common.library` failed to import, the function still crashed trying to instantiate classes it had just guarded against. **Resolution**: **(a)** Every error path now immediately `print(f"FATAL: {msg}", file=sys.stderr)` at the failure point — no error is ever silently collected. All variables (`early_level`, `debug_mode`, `_safe_posix`, `_should_auto_create`, `_discover_root`, `_UniversalLogger`, `_TelemetryHeartbeat`) pre-bound to safe defaults BEFORE any step runs — a failure in step N can never cause `NameError` in step N+1. **(b)** `_preload_infrastructure()` no longer instantiates logger or heartbeat — it returns the imported **classes** as `_UniversalLogger` / `_TelemetryHeartbeat` keys. `main()` instantiates them locally after the preload gate passes: `logger = _UniversalLogger("eks-pipeline", level=early_level)` / `hb = _TelemetryHeartbeat(enabled=early_level >= 2)` / `hb.start()`. Removed `logger_name` parameter from `_preload_infrastructure()`. | ✅ Resolved | T1.99.84: `_preload_infrastructure()` v2 — all 6 failure points print to stderr immediately; all variables pre-bound; logger/heartbeat instantiation moved to `main()`. Updated I117 entry with v2 refinements. |

- 🔴 Critical — blocks phase completion
- 🟠 High — significant impact, workaround needed
- 🟡 Medium — moderate impact, can proceed
- 🟢 Low — minor, cosmetic, or deferred
