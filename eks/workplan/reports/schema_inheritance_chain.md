# Schema Inheritance Chain — Base → Setup → Config → Actual Value

**Revision**: 1.10  
**Date**: 2026-06-24  
**Author**: AI Assistant  
**Summary**: Complete trace of every definition, property, and value across all 23 schema files. Keys listed first, then columns trace each through the 3-layer inheritance pattern per AGENTS.md §9. Each column header indicates the source file. v1.5: document_relationship_trigger_map stripped to shape-only per U086. v1.6: revision_id moved to doc schema set, revision_validation added per U087. v1.7: Asset schema set §2 consolidated from split 2a+2b into single unified table matching core schema set pattern. v1.8: Added **Purpose** and **Validation** columns to §1 (Core) and §2 (Asset) tables. v1.9: `item_core` made shape-only (required constraints moved to `eks_project_rules_config.json` `fragment_required_fields`). `project_rules_def` extended with `fragment_required_fields` property. Added §8 Business Logic vs Schema Layers. v1.10: §2 Asset table Base column reformatted to `{prop: type}` JSON Schema notation matching §1 Core pattern throughout all columns.

---
## 1. Core Schema Set

| Key | Type | Base (`eks_base_schema.json`) | Setup (`eks_setup_schema.json` v1.2.2) | Config | Actual Values | Purpose | Validation |
|---|---|---|---|---|---|---|---|
| **discipline_entry_def** | Domain | `{code: string, description: string}` — required both, no additional | `discipline_registry` — `{$ref: string}` (SSOT) | `$ref: eks_discipline_schema.json` | Fragment: 20 entries — PI→Piping, EL→Electrical, ..., NA→Not Applicable | Define valid engineering disciplines for project scoping | `code` required, unique; fragment schema validates against 20-code enum |
| **project_rules_def** | Domain | `{allowed_disciplines: string[], fragment_required_fields: {string → string[]}}` — required allowed_disciplines, no additional. `fragment_required_fields` is optional — SSOT for per-project fragment required overrides. | `project_rules_registry` — `{$ref: string}` (SSOT) | `$ref: eks_project_rules_config.json` | Fragment: 2 entries — 131101(9 disciplines, item_core→keytag+tag_type+tag_no+description, manufacturer→brand+model_number), 131242(13 disciplines, item_core→keytag+tag_type+tag_no) | Bind allowed disciplines + fragment required field overrides per project | `allowed_disciplines` cross-checked vs discipline_registry; `fragment_required_fields` fragment names validated against asset base defs; field paths validated against fragment properties |
| **project_entry_def** | Domain | `{code: string, description: string}` — required both, no additional | `project_registry` — `{$ref: string}`, required | `$ref: eks_project_code_schema.json` | Fragment: 3 entries — 131101, 131242, 999999 | Register valid project codes for pipeline scoping | `code` required, unique; fragment schema validates 3-code enum |
| **department_entry_def** | Domain | `{code: string, description: string}` — required both, no additional | `department_registry` — `{$ref: string}`, required | `$ref: eks_department_schema.json` | Fragment: 11 entries — PRJ→Project Management, ..., NA→Not Applicable | Define valid department codes for asset accountability | `code` required, unique; fragment schema validates 11-code enum |
| **facility_entry_def** | Domain | `{prefix: string, description: string}` — required both, no additional | `facility_registry` — `{$ref: string}`, required | `$ref: eks_facility_schema.json` | Fragment: 12 entries — WSD11, WSW41, WST02, ..., WST01 | Register facility prefixes for area-based asset identification | `prefix` required, unique; fragment schema validates 12-prefix enum |
| **global_paths_def** | Pipeline | `{data_dir, output_dir, archive_dir, config_dir: string}` — required all 4, no additional | `global_paths` — $ref → def, required | given in `eks_config.json` | `"data"`, `"output"`, `"archive"`, `"config"` | Configure pipeline filesystem directory structure | Path existence validated at pipeline bootstrap; fallback to defaults |
| **registry_def** | Pipeline | `{type: enum[duckdb,postgresql], connection_string: string, timeout: int(default:30)}` — required type+conn, no additional | `registry` — $ref → def, required | given in `eks_config.json` | duckdb, `output/eks_registry.db`, timeout 30 | Configure database backend for asset/document registry | `type` enum constrained to 2 engines; connection tested on registry init |
| **parsers_def** | Pipeline | `additionalProperties: string` | `parsers` — $ref → def, required | given in `eks_config.json` | .pdf→PDFParser, .docx→DOCXParser, .xlsx→XLSXParser, .dgn→DGNParserStub, .dwg→DWGParserStub | Map file extensions to parser classes for extraction pipeline | Extension uniqueness enforced; parser class importable at runtime |
| **embedding_def** | Pipeline | `{active_provider: enum[openai,ollama], model_name: string, dimensions: int}` — required all 3, no additional | `embedding` — $ref → def, required | given in `eks_config.json` | openai, text-embedding-3-small, 1536 | Configure vector embedding provider for semantic search | `active_provider` enum constrained; model checked against provider's available models |
| **vector_store_def** | Pipeline | `{url: string, collection_name: string, distance_metric: enum[Cosine,Euclidean,Dot]}` — required all 3, no additional | `vector_store` — $ref → def, required | given in `eks_config.json` | localhost:6333, eks_chunks, Cosine | Configure vector database for chunk storage and retrieval | `distance_metric` enum constrained; URL reachability tested at pipeline init |
| **logging_def** | Pipeline | `{default_level: $ref verbosity_level(default:1), debug_file_path: string}` — required default_level, no additional | `logging` — $ref → def, required | given in `eks_config.json` | level 1, `output/debug_log.json` | Configure logging verbosity and debug output path | `default_level` validated against verbosity_level enum; path writable check |
| **verbosity_level** | Pipeline | `integer enum [0,1,2,3]` — (0=quiet, 1=normal, 2=debug, 3=trace) | ref'd by logging_def + `eks_message_base.json` | — | — | Define shared log verbosity scale (SSOT for all message/error levels) | Enum range [0,1,2,3] enforced by schema; cross-referenced by logging + message |
| **document_relationship_trigger_map** | Pipeline | `{additionalProperties: string}` — shape only (v1.4.0). Actual entries removed from base, now SSOT in config files. | ref'd by `eks_asset_setup_schema.json` (as `document_triggers`) + `eks_doc_setup_schema.json` (as `ontology_triggers`) | — | Shape only — no values in base. See Section 2/3 for config values. | Define shape for trigger mapping tables (SSOT enforced — values live in config files, not base) | `additionalProperties: string` only — no enum constraints at base level; content validated per-setup |
| — | — | *(moved to doc schema set v1.2.0 — see Section 3)* | — | — | — | — | — |

## 2. Asset Schema Set

| Key | Type | Base (`eks_asset_base_schema.json`) | Setup (`eks_asset_setup_schema.json`) | Config (`eks_asset_config.json`) | Actual Values | Purpose | Validation |
|---|---|---|---|---|---|---|---|
| **item_core** | Domain | `{keytag: string, tag_type: string, tag_no: string, project_prefix: string, unit: string, service: string, tag_loop_number: string, tag_suffix: string, description: string, short_description: string, hazardous_zone: string, contract_info: string, device_type_code: string, p_and_id_file: string\|string[], design_specification: string}` — no required (shape-only), no additional | `asset_type_registry[].fragments[]` — referenced by name via `fragment_name` enum | given in `eks_asset_config.json` (asset_type_registry) | present in 14/14 types — universal identity | Provide universal asset identity (tag, type, location, classification) | Required constraints delegated to `eks_project_rules_config.json` (`fragment_required_fields`) |
| **process_conditions** | Domain | `{design_pressure: number, pressure_rating: string, operating_pressure_normal/min/max: number, operating_temperature_normal: number, flow_rate_nominal/min/max: number, test_pressure: number}` — no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 8/14 types — design & operating conditions | Capture asset design limits and normal operating ranges | Numeric type checks; `test_pressure` > `design_pressure` sanity rule |
| **manufacturer** | Domain | `{brand: string, model_number: string, serial_number: string, manufacturer_name/website/phone/fax/email/location: string, lot_number: string, manufacture_date: string(format:date), model_2d_file: string, model_3d_file: string}` — no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 10/14 types — manufacturer & product details | Track supplier, model, and product identification | `serial_number` uniqueness advisory; URL format check on website |
| **asset_lifecycle** | Domain | `{ace_category: string, generic_equipment_type: string, ace_asset_class: string, life_span: string, supplier: string, cost_center: string, replacement_cost: number, wbs_element: string, est_replacement_date: string(format:date), date_of_commission: string(format:date), warranty_terms/start_date/expiry_date: string, product_certification: string, ace_asset_number/sub_number: string}` — no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 8/14 types — ACE fields, lifecycle, financial | Manage ACE classification, lifecycle stage, warranty, and financial tracking | `ace_category` enum validated; date formats for lifecycle events |
| **control_system** | Domain | `{lcs_type: string, plc_panel: string, plc_panel_location: string, rio_panel: string, rio_panel_location: string}` — no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 4/14 types (AT_INST_*, AT_MOTOR) — control & I&C interface | Map asset to DCS/PLC control system and panel locations | `plc_panel`/`rio_panel` cross-referenced for consistency |
| **piping_connection** | Domain | `{pipe_size_nominal_mm: number, pipeline_tag_number: string, lining_material: string, end_condition: string}` — no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 4/14 types (AT_PROCESS, AT_CVALVE, AT_PSV, AT_HVALVE) — piping interface | Define pipe-to-component connection interface | `pipe_size_nominal_mm` numeric; `pipeline_tag_number` links to pipeline_route |
| **valve_internals** | Domain | `{valve_duty: string, valve_internal_type: string, body_material: string, stem_material: string, closure_element: string, seat_material: string, locked_position: string}` — no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 3/14 types (AT_CVALVE, AT_PSV, AT_HVALVE) — valve trim & body | Specify valve internal trim, body materials, and locking | Materials referenced against known standards; `locked_position` boolean |
| **actuator** | Domain | `{actuator_tag_number: string, actuator_display_label/internal_type/fail_mode/rpm/motor_rpm/torque_range/locked_rotor_torque/rated_voltage/frequency/current/rated_torque_current/motor_rating_kw/stem_direction: string\|number, actuator_brand/model/serial/manufacturer_*: string, actuator_model_3d_file/lot_number/manufacture_date: string, actuator_ace_*: string, actuator_*_date: string(format:date)}` — 43 props total, no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 2/14 types (AT_CVALVE, AT_HVALVE) — full actuator + ACE lifecycle | Provide complete actuator specs plus ACE lifecycle tracking | `actuator_tag_number` uniqueness; torque/power numeric ranges; ACE fields cross-validated |
| **rotating_equipment** | Domain | `{rpm/efficiency: number, impeller_type/material/wear_ring: string, rotor/stator/casing/gearbox/conveyor_hub_material: string, seal_type: string, npsh_min/submergence_min/head_loss: number, suction/discharge_nozzle_size: number, design_capacity: number, design_capacity_unit: string, duty_standby: string, insulation_class: string, voltage/frequency: number, phase: string, motor_rating: number, motor_speed_class/rated_current/torque_range/locked_rotor_torque/rated_torque_current: string\|number}` — 28 props total, no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 6/14 types (AT_EQPMP, AT_EQTNK, AT_EQVES, AT_EQEXC, AT_INST_CS, AT_CVALVE) — pump, motor, compressor | Capture rotating machinery specs — pump hydraulics, motor electrical, compressor performance | `efficiency` [0,1]; `npsh_min` < `submergence_min`; `voltage`/`frequency` enum validated |
| **instrumentation** | Domain | `{isa_instrument_id: string, instrument_function/measurement_principle/sample_point: string, operating_range: string, sensor_model/material/ip_rating/range/wetted_parts: string, thermowell/housing_material/ingress_protection/lightning_arrestor: string, output_signal/output_signal_2: string, accuracy/calibration_range/supply_voltage: string, alarm_limit_hh/h/l/ll/set_point: number, tube_material/size: string, ams_category: string}` — 27 props total, no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 4/14 types (AT_INST_*, AT_MOTOR) — sensor, transmitter, alarms | Specify sensor type, measurement range, signal output, and alarm thresholds | `isa_instrument_id` pattern; alarm limits sanity (hh>h>l>ll); `accuracy` percentage |
| **pipeline_route** | Domain | `{pipe_material: string, outside_diameter_mm/wall_thickness_mm: number, design_specification: string, insulation_material: string, insulation_thickness: number, from_component: string\|null, to_component: string\|null}` — no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 1/14 types (AT_PROCESS) — piping geometry | Define pipeline segment geometry, material, and connectivity | `outside_diameter_mm` > `wall_thickness_mm`; `from_component`/`to_component` refs validated |
| **specialist_equipment** | Domain | `{aperture_size: string, diaphragm_material: string, lineshaft_type: string, uv_lamp_type: string, removal_dosage: number, flux: number}` — no required, no additional | `conditional_fragment_rule` when `device_type_code in [UV,FILT,CONV,SCR,DOSING]` | given in `eks_asset_config.json` (conditional_fragments) | present in 1/14 types (AT_EQUIP, conditional) — specialty (UV, filtration) | Capture specialty equipment parameters for UV, filtration, dosing | Conditionally required based on `device_type_code`; numeric range checks |
| **motor_control** | Domain | `{starter_type: string, mcc_fed_from: string, equipment_number: string}` — no required, no additional | `asset_type_registry[].fragments[]` — referenced by name | given in `eks_asset_config.json` (asset_type_registry) | present in 1/14 types (AT_MOTOR) — motor electrical | Define motor starter and MCC feed configuration | `equipment_number` cross-referenced; `mcc_fed_from` panel reference validated |
| **fragment_name** | Domain | — | `definitions.fragment_name` — `string enum [item_core, process_conditions, ..., motor_control]` | — | Used as `$ref` target in `asset_type_registry` | Register 13 valid fragment names for asset type composition | Enum constrained to 13 strings; every fragment_name must appear in at least 1 asset type |
| **conditional_fragment_rule** | Domain | — | `definitions.conditional_fragment_rule` — `{fragment: $ref fragment_name, when: string, in: string[]}` — required all 3, minItems:1 on in[], no additional | — | 1 rule: specialist_equipment when device_type_code in [UV,FILT,CONV,SCR,DOSING] for AT_EQUIP | Define rules for conditionally applying fragments based on property values | `fragment` must be a valid `fragment_name`; `in[]` minItems:1 |
| **relationship_triggers** | Domain | — | `properties.relationship_triggers` — `additionalProperties: string` | given in `eks_asset_config.json` | 21 mappings — p_and_id_file→REFERENCED_BY_DWG, ..., serial_number→INSTALLED_AT | Map asset property paths to 21 ontology relationships for graph construction | Property paths must match actual fragment props; relationship names validated against ontology set |
| **document_triggers** | Domain | `$ref → eks_base_schema.json#/definitions/document_relationship_trigger_map` (shape-only) | `properties.document_triggers` — `$ref` → base trigger map | given in `eks_asset_config.json` | 3 mappings — document_number→SUPERSEDES, asset_tags→REFERENCES_ASSET, originator_company→PRODUCED_BY | Map document properties to 3 ontology relationships for doc graph linking | `$ref` resolves to base shape; value side validated in doc config |
| **asset_type_registry** | Domain | — | `properties.asset_type_registry` — `additionalProperties: {label: string, fragments[](minItems:2): $ref fragment_name, conditional_fragments[]: $ref conditional_fragment_rule}` — required [label, fragments] | given in `eks_asset_config.json` | 14 types — AT_EQUIP(6f+cond), AT_EQPMP(6f), ..., AT_HVALVE(6f) | Define 14 asset types by composing reusable fragment definitions | `fragments[]` minItems:2; each fragment name in enum; `label` required |
| **ontology_class_map** | Domain | — | `properties.ontology_class_map` — `additionalProperties: string` | given in `eks_asset_config.json` | 14 mappings — AT_EQUIP→TaggedEquipment, ..., AT_HVALVE→ManualValveTag | Map each asset type to exactly one ontology class for semantic reasoning | 14 bijective mappings; every AT key maps to a valid ontology class name |
| **fragment_category_registry** | Domain | — | `properties.fragment_category_registry` — `additionalProperties: enum[functional,physical]` | given in `eks_asset_config.json` | 13 entries — functional(11), physical(2) | Categorize each fragment as functional or physical for schema grouping | Enum constrained to `[functional,physical]`; every fragment has exactly 1 category |
| **column_normalization** | Domain | — | `properties.column_normalization` — `additionalProperties: {additionalProperties: string}` | given in `eks_asset_config.json` | 7 sheet maps, 233 total columns | Map source spreadsheet column names to normalized asset fragment fields for import | Source column names unique per sheet; target fields must match actual fragment property paths |

## 3. Document Schema Set

| Key | Type | Base (`eks_doc_base_schema.json`) | Setup (`eks_doc_setup_schema.json`) | Config (`eks_doc_config.json`) | Actual Values |
|---|---|---|---|---|---|
| **doc_id_format** | Domain | `string pattern: ^[A-Za-z0-9_-]+-[A-Za-z0-9]+$` | — | — | `{doc_number}-{revision}` |
| **document_type_code** | Domain | `enum [DWG, PI-PID, SPC, DS, MAN, OM, RPT]` | `document_type_registry` — array of `{code, label, ontology_class, description, expected_file_types[]}`, minItems:1 | `document_type_registry` — 7 entries | DWG→Drawing(pdf), PI-PID→PID_Drawing(pdf,dgn), SPC→Specification(pdf,docx), DS→Specification(pdf,xlsx), MAN→Manual(pdf), OM→Manual(pdf,docx), RPT→Report(pdf,docx) |
| **file_type_code** | Domain | `enum [pdf, dgn, docx, xlsx, dwg]` | `file_type_registry` — array of `{extension, display_name, parser_class, description, mime_type}`, minItems:1 | `file_type_registry` — 5 entries | pdf→PDFParser(application/pdf), dgn→DGNParserStub(image/vnd.dgn), docx→DOCXParser(...), xlsx→XLSXParser(...), dwg→DWGParserStub(image/vnd.dwg) |
| **element_type_code** | Domain | `enum [cover_page, revision_table, section, table, image, link, legend, note]` | `element_type_registry` — array of `{element_type, description, source_method[regex,table,heuristic], phase_2_use, phase_3_use, expected_by_cover_types[]}`, minItems:1 | `element_type_registry` — 8 entries | cover_page(regex)→Section→Document node[A,B,D,E]; revision_table(table)→Change→Revision[A,B]; section(regex)→Chunk→Section[A,B,D,E]; table(heuristic)→Context→Table[E]; image(heuristic)→Skip→Figure[A,B]; link(regex)→Skip→Reference[A,B,C,D,E]; legend(heuristic)→Skip→Legend[A,B]; note(heuristic)→Skip→Annotation[A,B] |
| **project_metadata_def** | Pipeline | `{project_title, project_number, area, discipline($ref disc.code), department}` — req title+number | — | — | Used in extraction pipeline |
| **document_metadata_def** | Pipeline | 19 props — source_type(enum[ingested,referenced,stub]), document_type, document_number, revision, status, is_latest(true), file_path, file_type, ingested_at, created_by/checked_by/approved_by, originator_company, security_class, asset_tags[](default:[]), page_count, extract_status(enum[pending,success,partial,failed]), extraction_confidence([0,1]), extraction_notes, verified_by — req source_type+document_type+document_number+revision | — | — | Used in ingestion pipeline |
| **document_element_def** | Pipeline | 7 props — doc_id($ref doc_id), element_type($ref ETC), element_id, title, content, confidence([0,1]), source(enum[regex,ocr,heuristic,manual]) — req doc_id+element_type+source | — | — | Used in extraction pipeline |
| **ontology_triggers** | Domain | (ref'd via document_relationship_trigger_map in `eks_base_schema.json`) | `ontology_triggers` — $ref → document_relationship_trigger_map | `ontology_triggers` — 5 mappings | document_type→IS_A, document_number→SUPERSEDES, asset_tags→REFERENCES_ASSET, originator_company→PRODUCED_BY, file_type→HAS_FORMAT |
| **revision_id** | Pipeline | `{type: string}` — doc base v1.2.0. Moved from `eks_base_schema.json`. | `revision_validation` — `{project_code → {pattern: string}}`, required | `revision_validation` — 2 entries | 131101→`^[A-Z0-9]{1,2}$`, 131242→`^[0-9]{3}$` |
| **health_scoring** | Pipeline | — | `health_scoring` — `{dimensions[], score_tiers[], weight_tiers{}}` — req dimensions+score_tiers | `health_scoring` | **6 dims**: completeness(0.20), extraction_confidence(0.20), structural_completeness(0.20), source_quality(0.15), xref_quality(0.15), consistency(0.10); **5 tiers**: 0.90→auto_register ... 0.00→manual_entry; **3 weight tiers**: critical(2.0), standard(1.0), optional(0.5) |
| **element_expectations** | Domain | — | `element_expectations` — additionalProperties: `{expected_elements[], threshold, cover_type}` + propertyNames `^(DWG\|PI-PID\|SPC\|DS\|MAN\|OM\|RPT)$` | `element_expectations` — 7 entries | DWG→[cover_page,revision_table,section,image,link] thr:4 A; PI-PID→same thr:4 B; SPC→[] thr:0 C; DS→[cover_page,section,table] thr:2 E; MAN→[cover_page,section] thr:2 D; OM→same D; RPT→[cover_page,section,table] thr:2 E |

## 4. Ontology Schema Set

| Key | Type | Base (`eks_ontology_base_schema.json`) | Setup (`eks_ontology_setup_schema.json`) | Config (`eks_ontology_config.json`) | Actual Values |
|---|---|---|---|---|---|
| **ontology_class** | Domain | 8 props — name, label, description, subClassOf, tag_type_mapping, document_type_mapping, tag_type_aliases[](uniqueItems), fragments[] — req name+label, no additional | `classes` — array of $ref → ontology_class, minItems:1 | `classes` — 35 classes | Root: ISO15926_Entity → **L2**: FunctionalObject, PhysicalObject, Document, InfrastructureObject, GovernanceObject → **L3**: TaggedEquipment(→TaggedRotating/Static), TaggedPiping, TaggedInstrument, PumpUnit, ValveUnit, MotorUnit, InstrumentUnit, EquipmentUnit, ActuatorUnit, ElectricalPanel, ControlPanel, EngineeringStandard, Originator, Drawing(→PID_Drawing), Specification, Manual, Report → **L4**: PumpTag(AT_EQPMP), MotorTag(AT_MOTOR), TankTag(AT_EQTNK), VesselTag(AT_EQVES), HeatExchangerTag(AT_EQEXC), PipelineTag(AT_PROCESS), InlineComponentTag(AT_INCOMP), ValveTag→ControlValveTag(AT_CVALVE)/SafetyValveTag(AT_PSV)/ManualValveTag(AT_HVALVE), ControlSystemInstrumentTag(AT_INST_CS), FlowInstrumentTag(AT_INST_FLO), DataSheet(DS), OpsManual(OM) |
| **relationship** | Domain | 5 props — name, inverse, transitive, symmetric, description — req name, no additional | `relationships` — array of $ref → relationship, minItems:1 | `relationships` — 14 relationships | SUBCLASS_OF↔SUPERCLASS_OF(t), IS_A, CONNECTS_TO(sym), FLOWS_TO↔FLOWS_FROM(t), INSTALLED_AT↔HAS_PHYSICAL, REFERENCED_BY_DWG↔REFERENCES_ASSET, CONTROLS↔CONTROLLED_BY(t), ENERGIZED_BY↔POWERS(t), GOVERNED_BY↔VALIDATES_ASSET(t), SET_POINT_IN↔SPECIFIES_SET_POINT, SUPERSEDES↔SUPERSEDED_BY(t), SUPPLEMENTS↔SUPPLEMENTED_BY(t), REFERENCES_DOC↔REFERENCED_BY_DOC(t), PRODUCED_BY↔ORIGINATED, FEEDS_FROM |

## 5. Error Schema Set

| Key | Type | Base (`eks_error_code_base.json`) | Setup (`eks_error_setup_schema.json`) | Config (`eks_error_config.json`) | Actual Values |
|---|---|---|---|---|---|
| **layer_code** | Pipeline | `string pattern: ^P[0-9]$` — P1=Discovery, P2=Parsing, P3=Extraction, P4=Ingestion | — | — | Used in data_error_entry.layer |
| **module_code** | Pipeline | `enum [D, P, E, X, R, G, S, C, F, V]` — 10 module codes | — | — | Used in data_error_entry.module |
| **function_code** | Pipeline | `enum [P, V, C, F, S, X, G, E]` — 8 function codes | — | — | Used in data_error_entry.function |
| **unique_id** | Pipeline | `string pattern: ^[0-9]{4}$` | — | — | Used in code formats |
| **error_severity** | Pipeline | `enum [FATAL, CRITICAL, HIGH, MEDIUM, WARNING, INFO]` | — | — | 6 severity levels |
| **system_category** | Pipeline | `enum [Environment, File, Config, Runtime, AI, Bootstrap]` | — | — | 6 system categories |
| **error_code_format** | Pipeline | `string pattern: ^P[0-9]-[A-Z]-[A-Z]-[0-9]{4}$` | — | — | P{phase}-{module}-{function}-{id} |
| **system_error_code_format** | Pipeline | `string pattern: ^S-[A-Z]-S-[0-9]{4}$` | — | — | S-{category}-S-{id} |
| **data_error_entry** | Pipeline | 12 props — code($ref error_code_format), name, message, severity($ref severity), layer(pattern P[0-9]), module(enum 10), function($ref function_code), column, source, health_score_impact(int), remediation, remediation_type(enum[MANUAL_FIX,REVIEW,CALCULATION,INFO_ONLY]) — req code+name+severity+layer+module+function, no additional | `data_logic_errors` — `additionalProperties` `$ref` → `data_error_entry` | `data_logic_errors` — 35 entries | **P1(2)**: FILE_DISCOVERY_FAILED(CRITICAL,-5), DIRECTORY_NOT_FOUND(CRITICAL,-5); **P2(8)**: PDF_PARSE_OPEN_FAIL(HIGH,-3), PDF_PAGE_EXTRACT_FAIL(HIGH,-3), PDF_NO_TEXT_LAYER(WARNING,-3), PDF_ENCRYPTED(HIGH,-3), PDF_IMAGE_EXTRACT_FAIL(WARNING,-2), PDF_TABLE_EXTRACT_FAIL(WARNING,-2), DOCX_PARSE_FAIL(HIGH,-3), DGN_UNSUPPORTED(HIGH,-3); **P3 extract(17)** — see full list below; **P3 xref(5)** — DATADROP_LOAD_FAIL(CRITICAL,-5), etc.; **P3 graph(3)** — NODE_CREATION_FAILED(WARNING,-2), EDGE_CREATION_FAILED(WARNING,-2), GRAPH_QUERY_FAILED(WARNING,-2) |
| **system_error_entry** | Pipeline | 9 props — code($ref sys_code), name, message, severity($ref severity), category($ref sys_cat), stops_pipeline(bool), promote_detail(bool), promotion_text(string) — req code+name+severity+category+stops_pipeline, no additional | `system_errors` — `additionalProperties` `$ref` → `system_error_entry` | `system_errors` — 30 entries | **Env(5)**: MISSING_PACKAGE(FATAL↑), WRONG_PYTHON_VERSION(FATAL↑), IMPORT_ERROR(FATAL↑), ENVIRONMENT_NOT_READY(FATAL↑), DUCKDB_UNAVAILABLE(FATAL↑); **File(6)**: INPUT_FILE_NOT_FOUND(FATAL↑), FILE_UNREADABLE(FATAL↑), OUTPUT_DIR_NOT_WRITABLE(FATAL↑), SCHEMA_FILE_NOT_FOUND(FATAL↑), CONFIG_FILE_NOT_FOUND(FATAL↑), OUTPUT_DIR_CREATION_FAILED(FATAL↑); **Config(8)**: INVALID_PARAMETER(FATAL↑), SCHEMA_PARSE_ERROR(FATAL↑), SCHEMA_VALIDATION_FAILED(FATAL↑), MISSING_REQUIRED_CONFIG(FATAL↑), ERROR_CATALOG_LOAD_FAILED(WARNING), MESSAGE_CATALOG_LOAD_FAILED(WARNING), REGISTRY_CONNECTION_FAILED(FATAL↑), SCHEMA_RESOLUTION_ERROR(FATAL↑); **Runtime(6)**: FAIL_FAST_TRIGGERED(FATAL↑), PIPELINE_ABORTED(FATAL↑), MEMORY_ERROR(FATAL↑), BATCH_PROCESSING_FAILED(FATAL↑), GRAPH_ENGINE_FAILED(HIGH), PRE_PIPELINE_VALIDATION_FAILED(FATAL↑); **AI(3)**: AI_OPS_FAILED(WARNING), EMBEDDING_SERVICE_FAILED(WARNING), OLLAMA_UNAVAILABLE(WARNING); **Bootstrap(2)**: BOOTSTRAP_NOT_COMPLETE(FATAL↑), PHASE_DEPENDENCY_FAILED(FATAL↑) |
| **error_category_range** | Pipeline | 6 props — prefix, description, count, start_id, end_id — req all, no additional | `system_error_ranges` — 6 keys → error_category_range; `data_error_ranges` — 5 keys → error_category_range | `system_error_ranges` — 6 ranges; `data_error_ranges` — 5 ranges | System: S-E-S-01xx(5), S-F-S-02xx(6), S-C-S-03xx(8), S-R-S-04xx(6), S-A-S-05xx(3), S-B-S-06xx(2); Data: P1-D-P-01xx(2), P2-P-P-02xx(8), P3-E-E-03xx(17), P3-X-X-04xx(5), P3-G-G-05xx(3) |
| **error_catalog_metadata** | Pipeline | 7 props — version, last_updated, total_codes, system_codes, data_logic_codes, data_logic_format(enum single), system_format(enum single) — req version+last_updated+total_codes, no additional | `metadata` — $ref → error_catalog_metadata | `metadata` | v1.0.0, 2026-06-19, 65 total(30 sys + 35 data), format P{phase}-{module}-{function}-{id}, S-{category}-S-{id} |
| **migration_log_entry** | Pipeline | 5 props — date, description, changes[](string), references{additionalProperties:string} — req date+description+changes, no additional | `migration_log` — $ref → migration_log_entry | `migration_log` | 2026-06-19, "Initial EKS error catalog...", 4 changes |

> **P3 extract full list (17 entries)**: COVERSHEET_UNRECOGNIZED(WARNING,-2), DOC_NUMBER_EXTRACT_FAIL(WARNING,-2), REVISION_EXTRACT_FAIL(WARNING,-2), DISCIPLINE_EXTRACT_FAIL(WARNING,-1), STATUS_EXTRACT_FAIL(WARNING,-1), CREATED_BY_EXTRACT_FAIL(INFO,0), ORIGINATOR_EXTRACT_FAIL(INFO,0), METADATA_INCOMPLETE(INFO,0), CONFIDENCE_LOW(WARNING,-2), COVER_PAGE_MISSING(WARNING,-3), REVISION_TABLE_MISSING(WARNING,-2), SECTIONS_MISSING(INFO,0), TABLES_EMPTY(INFO,0), IMAGES_DETECTED(INFO,0), SCANNED_PAGES_FOUND(WARNING,-2), ELEMENT_STORAGE_FAIL(WARNING,-1), STRUCTURE_LOW_SCORE(WARNING,-2)

## 6. Message Schema Set

| Key | Type | Base (`eks_message_base.json`) | Setup (`eks_message_setup_schema.json`) | Config (`eks_message_config.json`) | Actual Values |
|---|---|---|---|---|---|
| **message_id** | Pipeline | `string pattern: ^[A-Z_]+$` | — | — | MILESTONE_PIPELINE_START, STATUS_PARSING_FILE, etc. |
| **verbosity_level** | Pipeline | `$ref → eks_base_schema.json#/definitions/verbosity_level` (integer enum 0-3) | — | — | Referenced via message_entry.level |
| **template** | Pipeline | `string` — Python-style `{placeholders}` | — | — | "Starting EKS pipeline for {target}", etc. |
| **message_category** | Pipeline | `enum [milestone, status, progress, warning, error]` | — | — | 5 categories |
| **message_metadata** | Pipeline | 3 props — version, last_updated, total_messages — req version+last_updated, no additional | `metadata` — $ref → message_metadata | `metadata` | v1.0.0, 2026-06-19, 33 total |
| **message_entry** | Pipeline | 3 props — template($ref template), level($ref verbosity), category($ref msg_cat) — req template+level, no additional | `messages` — `additionalProperties` `$ref` → `message_entry` | `messages` — 33 entries | **Milestone(8)**: PIPELINE_START(1), FILE_DISCOVERY(1), PARSE_COMPLETE(1), EXTRACTION_COMPLETE(1), REGISTRATION_COMPLETE(1), PHASE_COMPLETE(1), HEALTH_SCORED(1), PIPELINE_DONE(1); **Status(8)**: PARSING_FILE(1), EXTRACTING(1), REGISTERING(1), DETECTING_STRUCTURE(2), STORING_ELEMENTS(2), HEALTH_SCORE(2), XREF_CHECK(2), BATCH_PROGRESS(1); **Progress(4)**: all level 1; **Warning(8)**: SCANNED_PDF(1), LOW_CONFIDENCE(1), NO_MATCH(2), AMBIGUOUS_MATCH(2), SKIPPED_FILE(1), NO_COVER_PAGE(1), NO_REVISION_TABLE(1), STRUCTURE_LOW(1); **Error(4)**: all level 0 |

## 7. Fragment/Config Schemas (Direct — `allOf` → base_entry_def)

| Key | Type | File | $ref Target (from `eks_base_schema.json`) | Properties | Actual Values |
|---|---|---|---|---|---|
| **project_rules** | Domain | `eks_project_rules_config.json` v1.1.0 | `#/definitions/project_rules_def` | `project_rules{}: {code → {allowed_disciplines[]}}` | 2: 131101(9 disciplines), 131242(13 disciplines) |
| **projects** | Domain | `eks_project_code_schema.json` v1.0.0 | `#/definitions/project_entry_def` | `projects[]: {code, description}` | 3: 131101(WSD11 Project Specs), 131242(WSD11 TWRP Design), 999999(Unknown) |
| **disciplines** | Domain | `eks_discipline_schema.json` v1.0.0 | `#/definitions/discipline_entry_def` | `disciplines[]: {code, description}` | 20: PI, EL, IN, CI, AR, ME, CL, BQ, QA, VI, M3, DR, DS, SP, RT, CD, CH, PP, IM, SG, NA |
| **departments** | Domain | `eks_department_schema.json` v1.0.0 | `#/definitions/department_entry_def` | `departments[]: {code, description}` | 11: PRJ, QAQC, CNT, PRC, PIP, CSA, ELE, ICA, MEC, BIM, NA |
| **facilities** | Domain | `eks_facility_schema.json` v1.0.0 | `#/definitions/facility_entry_def` | `facilities[]: {prefix, description}` | 12: WSD11, WSW41, WST02, WIL00, WIL11-13, WIL22-23, WSB01, WSE00, WST01 |

## 8. Business Logic vs Schema Layers

Starting with v1.9, a clean separation is enforced:

| Concern | Layer | File | Scope |
|---|---|---|---|
| **Shape** (what properties exist, their types) | Base | `*_base_schema.json` | Universal |
| **Composition rules** (which fragments compose which types) | Setup | `*_setup_schema.json` | Universal |
| **Standard values** (actual asset types, column maps, triggers) | Config | `*_config.json` | Universal |
| **Business logic** (per-project required field overrides, allowed disciplines) | Project Rules | `eks_project_rules_config.json` | Per-project |

**Rule:** Fragment definitions in `eks_asset_base_schema.json` are **shape-only** — they define properties and types but never carry `required` constraints. Per-project mandatory field rules are defined in `eks_project_rules_config.json` under `fragment_required_fields`.

**Validation flow:**
1. `SchemaLoader.load_all()` validates all schema files against their setup schemas
2. `_validate_project_rules()` cross-checks `fragment_required_fields` against actual fragment definitions (fragment names must exist, field paths must be valid properties)
3. At asset ingest time, `ConfigRegistry.resolve_required_fields(project_id, fragment_name)` returns the project's required field list for that fragment
4. Asset instance validation merges base shape + project-specific required constraints

## 9. Aggregate Summary

| Schema Set | Source Files | Keys | Config Values | Base Defs → Setup Props → Config |
|---|---|---|---|---|
| Core | `eks_base_schema.json` + `eks_setup_schema.json` + `eks_config.json` + 5 fragment/config files | 13 | 11 config groups + 47 fragment entries | 13 defs → 11 props(11 req) → 11 config values |
 | Asset | `eks_asset_base_schema.json` + `eks_asset_setup_schema.json` + `eks_asset_config.json` | 21 (13 fragments + 8 setup) | 7 groups (233 col maps, 14 asset types, 21 triggers, 14 class maps, 13 categories) | 13 base defs + 2 setup defs → 7 props(2 req) → 7 config groups |
| Document | `eks_doc_base_schema.json` + `eks_doc_setup_schema.json` + `eks_doc_config.json` | 10 | 7 groups (7 doc types, 5 file types, 8 element types, 7 expectations, 5 triggers, 2 revision validation, 1 health config) | 7 defs → 7 props(3 req) → 7 config groups |
| Ontology | `eks_ontology_base_schema.json` + `eks_ontology_setup_schema.json` + `eks_ontology_config.json` | 2 | 2 groups (35 classes, 14 relationships) | 2 defs → 4 props(2 req) → 2 config groups |
| Error | `eks_error_code_base.json` + `eks_error_setup_schema.json` + `eks_error_config.json` | 11 | 6 groups (30 sys + 35 data errors, 11 ranges, metadata, migration) | 11 defs → 5 props(0 req) → 5 config groups |
| Message | `eks_message_base.json` + `eks_message_setup_schema.json` + `eks_message_config.json` | 5 | 2 groups (33 messages) | 5 defs → 2 props(0 req) → 2 config groups |
| Fragments | `eks_project_rules_config.json`, `eks_project_code_schema.json`, `eks_discipline_schema.json`, `eks_department_schema.json`, `eks_facility_schema.json` | 5 | 47 entries | Direct allOf → base_entry_def from `eks_base_schema.json` |
| **Total** | **23 files** | **63+** | **78+ config sections** | **52 base defs → 35 setup props → 33 config groups** |
