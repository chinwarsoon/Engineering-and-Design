# Phase 1 Update Log

**Project**: Engineering Knowledge System (EKS)  
**Location**: `eks/log/phase1/p1_update_log.md`  
**Current Update**: U305 - 2026-08-18 - I318 Phase 1.2 de-risking spike / T1.309–T1.312 ✅ COMPLETE — minimal BaseAssetLoader delivered (datadrop Excel → column_normalization → conditional_fragments canonical records; pre-materialised nested objects; null/blank tolerance + per-asset HealthScorer I021; column_coverage 7-sheet pre-check I020, report rp_p1.2_asset_column_coverage.md); 18 loader tests (test_asset_loader.py); full suite **805 PASS / 4 FAIL** (3 pre-existing I288 real-PDF fixture absences + 1 flaky phase1_server DuckDB lock race — zero new failures); I318 ✅ Resolved (issue log v125); I228 Phase 3 risk de-risked; knowledge.json v2.13.0→v2.14.0 - prior: **Current Update**: U304 - 2026-08-14 - I313 Phase 4 / T1.308 ✅ COMPLETE — full-suite closure (788 PASS / 3 FAIL I288-only); I313 ✅ Resolved + I306 📐 Aligned; consolidated report; I317 fixture gap logged - prior: **Current Update**: U303 - 2026-08-14 - I313 Phase 3 / T1.307 ✅ COMPLETE — output-name literal removal BLOCK-1–4 (config-driven view iteration + view_id artifact_type, catalog-order phase map, `_require_system_param` fail-fast S-C-S-0304, view_id artifact tracking; extended literal guard; full suite 788 PASS / 3 FAIL I288-only) - prior: **Current Update**: U302 - 2026-08-14 - I313 Phase 2 / T1.306 ✅ COMPLETE — §24 cross-source audit + D1–D6/O1 drift fixes (review_flags csv+xlsx, extraction_results 50 cols, view-config v1.2.0 refs, FK description/source-column alignment, fk_doc_type_composite downgrade, db_manifest documented)cy 5/5, GAP-016 re-verified RESOLVED); findings I315 + I316 logged; issue log v119→v120 (Open 11→13, Total 294→296), task log T1.305 ⏳→✅ (Complete 451→452, Planned 44→43), test log TL059; verification-only (zero engine/config code changed) - prior: **Current Update**: U300 - 2026-08-14 - I312 IMPLEMENTED (T1.301–T1.304 ✅ COMPLETE: schema-driven db_manifest provenance table replaces _eks_schema_meta; manifest.py writer + db_manifest_keys SSOT + I196 NOT NULL check ported to migration gate; legacy _migrate_schema/_ensure_schema_version retired; tests added/repointed; issue log v116→v117, task log T1.301–T1.304 ✅) - prior: **Current Update**: U298 - 2026-08-13 - I314 FOLLOW-UP Gap-2 sub-item B RESOLVED (schema-driven HTTP download file names): eks_base_schema.json v1.21.0→v1.22.0 + eks_config.json v1.13.0→v1.14.0 add system_parameters.export_download_file_name_template ('eks_export_{phase}.{ext}'); phase1_server _handle_export renders the template at runtime (single-phase download), reuses export_workbook_file_name for all-phases workbook download, template with phase='all' for all-phases CSV; hardcoded 'eks_export_phase_' literal REMOVED; test_i309_exports.py +1 test (test_i314_phase1_server_schema_driven_download_file_name) + version pins updated; tests 26+61 passed (1 known flaky DuckDB lock race, passes standalone); issue log v112→v113 (I314 Description appended — Gap-2 sub-item A: run_pipeline column_override pass-through DECISION deferred to implementation, download-only vs runtime; Status Summary unchanged 294 total / 16 deferred). - prior: **Current Update**: U297 - 2026-08-13 - I309 IMPLEMENTED + VERIFIED (T1.287-T1.291 ✅ COMPLETE): Excel single-workbook export + export backend delivered - openpyxl REQUIRED verified (T1.287); `export_to_workbook(sheets, path, columns=None)` + `export_multi_sheet` delegate (T1.288); pipeline ONE-workbook export all 3 views incl. review_flags xlsx + schema-driven `export_workbook_file_name` + S-C-S-0313 column override validation (T1.289); phase1_server backend `GET /api/v1/export_views` + run POST `export_columns` override + `_mk_rows_fn` (T1.290 backend); test_i309_exports.py 18/18 (TL057); full suite 778 passed / 4 failed (3 pre-existing I288 + 1 flaky phase1_server lock race, no I309 regressions); frontend export column multi-select/re-order UI deferred to I314; I309 ✅ Resolved, I314 ⏸️ Deferred (issue log v112). - prior: **Current Update**: U296 — 2026-08-13 — I309 APPROVED (review Q1–Q10): Excel single-workbook export + UI dynamic column selection — openpyxl REQUIRED (Q1); `export_to_workbook(sheets, path, columns=None)` + `export_multi_sheet` delegates (Q2); all 3 views xlsx incl. review_flags (Q3); sheet names + workbook file name schema-driven, no hardcode (Q4/Q6, `export_workbook_file_name` in eks_config.json); column sequence SSOT = view config `columns[]` order (Q5); UI default columns = CLI schema-driven, user dynamic select/re-order → `column_override` validated vs view config + table schema, new error **S-C-S-0313** (Q7/Q5); tests agreed test_i309_exports.py (Q8); line refs corrected 268–287→313–331 (Q9); closure checklist agreed (Q10); I309 🔴 Open→🟢 Approved; T1.287–T1.291 ⏳→🟢 Approved; phase_1.2 workplan T1.290 registered (Phase 1.2.22); issue log v110→v111. schema-driven default export views (eks_export_view_config.json v1.1.0 + filter, exporter resolve_export_columns/resolve_export_views read config SSOT, hardcoded lists + _fallback deleted, S-C-S-0312 fail-fast, generate_view_ddl → persistent v_* views created after the I311 migration gate, documents.flag_reason materialized at ingest via core/flag_utils, schema-driven file/sheet names + view_id artifact_type); full suite **759 passed / 5 failed** (3 pre-existing I288 real-PDF fixture absences + 2 fixed lazy-import path errors `..core.flag_utils`); T1.282–T1.286 ✅ Complete; I308 ✅ Resolved.
**Last Updated**: 2026-08-18 (U305 - I318 spike / T1.309–T1.312 complete; 18 loader tests + full suite 805 PASS / 4 FAIL I288-only + flaky lock race; I318 ✅ Resolved) - prior: **Last Updated**: 2026-08-14 (U304 - I313 Phase 4 / T1.308 full-suite closure; 788 PASS / 3 FAIL I288-only; I313 ✅ + I306 📐) - prior: **Last Updated**: 2026-08-14 (U303 - I313 Phase 3 / T1.307 output-name literal removal BLOCK-1–4; full suite 788 PASS / 3 FAIL I288-only) - prior: **Last Updated**: 2026-08-14 (U302 - I313 Phase 2 / T1.306 §24 audit + D1–D6/O1 drift fixes; 139 version-pin + 8 consumer tests PASS) - prior: **Last Updated**: 2026-08-14 (U301 - I313 Phase 1 / T1.305 53-table matrix verified; findings I315/I316 logged; verification-only)1–T1.304 ✅ COMPLETE: db_manifest replaces _eks_schema_meta; manifest.py + db_manifest_keys SSOT + I196 port to gate; legacy methods retired; tests green) - prior: **Last Updated**: 2026-08-13 (U298 - I314 FOLLOW-UP Gap-2 sub-item B RESOLVED — schema-driven HTTP download file names (2026-08-13): **(1) Schema** — `system_parameters.export_download_file_name_template` ('eks_export_{phase}.{ext}') added to `eks_base_schema.json` v1.21.0→v1.22.0 (system_parameters_def property + required) and `eks_config.json` v1.13.0→v1.14.0 (value). **(2) phase1_server `_handle_export`** — download file names now schema-driven: single-phase → template rendered with {phase}/{ext}; all-phases xlsx → reuses `export_workbook_file_name` (eks_export.xlsx, matches pipeline output); all-phases csv → template with phase='all'; hardcoded `eks_export_phase_{phase}.{fmt}` literal REMOVED. **(3) Tests** — test_i309_exports.py +1 (`test_i314_phase1_server_schema_driven_download_file_name`: template present in server + both schema layers, hardcoded literal absent) + version pins 1.21.0→1.22.0 / 1.13.0→1.14.0; i309+system_parameters 26 passed, phase1_server 61 passed (1 known flaky DuckDB lock race passes standalone). **(4) Issue log** v112→v113 — I314 Description appended (Gap-2 sub-item A): run POST export_columns override currently affects ONLY the download endpoint; `run_pipeline()` has NO column_override param → pipeline-generated eks_export.xlsx always schema-default columns; DECIDE at implementation (pass-through vs download-only); Status Summary UNCHANGED (294 total / 16 deferred).) - prior: **Last Updated**: 2026-08-13 (U297 - I309 IMPLEMENTED + VERIFIED (T1.287-T1.291 ✅ COMPLETE): Excel single-workbook export + export backend delivered - **(1) openpyxl REQUIRED** (T1.287 verified - openpyxl==3.1.5 in eks.yml + eks_config.json dependencies.required). **(2) `exporter.export_to_workbook(sheets, path, columns=None)`** (T1.288) in common/library/export/exporter.py + `__init__.py` re-export; `export_multi_sheet()` delegates; sheet names view-config `sheet_name`; missing openpyxl non-fatal for CSV. **(3) Pipeline export rework** (T1.289) - ONE workbook all 3 views incl. review_flags xlsx; workbook file name `export_workbook_file_name` schema-driven; per-view CSVs + workbook; `column_override` validated subset of view config + table schema → fail-fast **S-C-S-0313 EXPORT_COLUMN_NOT_ALLOWED**; no hardcoded names. **(4) Backend** (T1.290 ✅) - phase1_server `GET /api/v1/export_views` (views + ordered columns from `resolve_export_columns()`, schema-driven matches CLI), run POST `export_columns` override validated S-C-S-0313, `_mk_rows_fn` + `_handle_export`. **Frontend ⏸️ deferred to I314** - eks/ui/eks.js + phase1_ingestion.html export column multi-select/re-order panel NOT implemented; Phase 1.2.22 T1.2.22.3/T1.2.22.4. **(5) Tests** (T1.291, TL057) - test_i309_exports.py 18/18; full suite **778 passed / 4 failed** (3 pre-existing I288 real-PDF fixture absences + 1 known flaky phase1_server DuckDB lock race passing on rerun - no I309 regressions). Issue log v111→v112 (I309 ✅ Resolved, Resolved 163→164 / Approved 1→0 / Deferred 15→16 / Total 293→294; I314 ⏸️ Deferred added; priority row 6 → I312→I313 count 2); task log T1.287-T1.291 → ✅ (Complete 442→447, Planned 53→48); test log TL057; workplan Phase 1.2.22 backend ✅ / frontend ⏸️.) - prior: 2026-08-13 (U296 — I309 APPROVED (design review Q1–Q10, 2026-08-13): Excel single-workbook export + UI dynamic column selection approved — **(1) openpyxl REQUIRED** (Q1: Excel output is a Phase 1 deliverable; T1.287 largely verified — openpyxl==3.1.5 already in eks.yml + eks_config.json dependencies.required; `_PIP_TO_IMPORT` needs no entry, pip name == import name). **(2) `export_to_workbook(sheets, path, columns=None)`** (Q2) — per-sheet column control `Dict[view_id, List[str]]`; `export_multi_sheet()` delegates to it (backward compatible). **(3) All 3 views xlsx** (Q3) — review_flags gains xlsx in `eks_export_view_config.json`. **(4) Sheet names schema-driven** from view config `sheet_name` (Q4), workbook file name schema-driven `export_workbook_file_name` in eks_config.json (Q6) — no hardcoded names. **(5) Column sequence SSOT** = view config `columns[]` order (Q5); optional `column_override` (per-run) validated as subset of view config columns + table schema → fail-fast new error **S-C-S-0313 EXPORT_COLUMN_NOT_ALLOWED** registered in eks_error_config.json. **(6) UI (Q7)** — phase1_server.py L834-855 hardcoded column lists removed; default columns = CLI schema-driven `resolve_export_columns()`; user dynamically selects/re-orders per view on UI; T1.290 registered in phase_1.2 workplan Phase 1.2.22. **(7) Tests (Q8)** — test_i309_exports.py planned. **(8) Docs (Q9)** — I309 issue row line ref corrected 268–287→313–331. **(9) Closure (Q10)** — task rows, update log, issue status, cross-source audit, knowledge.json at implementation close. Issue log v110→v111 (I309 🔴 Open→🟢 Approved, Open 13→12, Approved 0→1, Total 293; priority row 6 ① I309 marked 🟢 approved; outstanding 28 unchanged); task log T1.287–T1.291 ⏳→🟢 Approved (Status Summary unchanged — Approved is a sub-state of Planned, T1.254 precedent).) — prior: 2026-08-13 (U295 — I308 IMPLEMENTED + VERIFIED — **schema-driven default export views (T1.282–T1.286 ✅ COMPLETE)**: **(1) View config SSOT** — `eks_export_view_config.json` v1.0.0→v1.1.0 adds schema-driven `filter` (`{"column":"is_latest","value":true}`) to all 3 views (discovery_inventory/extraction_results/review_flags); `eks_base_schema.json` v1.19.0→v1.20.0 adds `export_view_def.filter`; **exporter.py** `resolve_export_columns()`/`resolve_export_views()` read the config (hardcoded `_project_meta_order`/`_doc_meta_order`/`extraction_specific`/`review` lists + `_fallback` dicts deleted) — missing/unreadable/empty config raises fail-fast **S-C-S-0312** (EXPORT_VIEW_CONFIG_MISSING, stops_pipeline, registered eks_error_config.json, total_codes 138→139). **(2) Persistent views** — `schema_to_ddl.generate_view_ddl()` renders `CREATE OR REPLACE VIEW v_<view_id> AS SELECT <cols> FROM <table> WHERE <fcol> = <fval>` (bool → TRUE/FALSE, str quoted); registry creates them via new `_create_export_views()` placed **AFTER** `_run_migration_gate()` so additive migrations (e.g. `flag_reason` on a stale registry) are applied before the views referencing them are created (root-caused Binder Error on stale test DBs). **(3) flag_reason materialized at ingest** — new shared `eks/engine/core/flag_utils.py` (`compute_flag_reason`, clean doc → None/NULL) written into `documents.flag_reason` (eks_db_config.json v1.0.0→v1.1.0 + eks_doc_base_schema flag_reason) so `v_review_flags` is a pure projection. **(4) Export loop** — schema-driven file base names/sheet names + view_id as artifact_type (no hardcoded literal). **(5) Tests** — test_i308_default_views.py 9 tests (TL056); lazy-import path bug fixed (`...core.flag_utils` → `..core.flag_utils`); full suite **759 passed / 5 failed** (3 pre-existing I288 real-PDF fixture absences + 2 fixed import-path errors). I308 🔴→✅ Resolved, Resolved 162→163 / Open 14→13, outstanding 29→28; priority row 6 ① I308 marked resolved (count 4→3); task log T1.282–T1.286 ⏳→✅; test pins updated (eks_base_schema 1.19.0→1.20.0, eks_db_config 1.1.0).) — prior: 2026-08-12 (U294 — I311 IMPLEMENTED + VERIFIED: **_eks_table_relations_ runtime follows config `id` PK** — SchemaToDDL `generate_relations_manifest_ddl()` renders config-shaped DDL (`id VARCHAR PRIMARY KEY, relation_name VARCHAR UNIQUE`, uuid5 id mirroring definition_loader) with `INSERT ... ON CONFLICT (id) DO UPDATE SET ... EXCLUDED` (both constraints unique → DuckDB rejects `INSERT OR REPLACE`); registry `_init_db()` un-excludes `_eks_table_relations` from config pre-creation (`exclude_tables={documents, document_elements}`); error-code count test_t132 total_codes 135→138 (S-C-S-0311/P1-R-P-0004/0005 registered in eks_error_config.json v1.9.0); gate wired through eks_engine_pipeline/runner/discovery_cli/health_cli. Root cause: registry.py:275-283 deliberately kept legacy manifest shape (relation_name PK) while config declared `id` PK — gate correctly flagged drift, blocking every fresh test DB. Full suite **751 passed / 4 failed** (3 pre-existing I288 real-PDF fixture absences; 1 flaky phase1_server Windows DuckDB file-lock race between background pipeline thread + HTTP handler — confirmed 2x repeat pass standalone); issue log v108→v109, I311 🔴→✅ Resolved, Resolved 161→162 / Open 15→14; priority row 6 re-sequenced (I308→I309→I312→I313, count 5→4); task log T1.297–T1.300 ⏳→✅ Complete (Complete 433→437, Planned 62→58); TL055). — prior: 2026-08-12 (U293 — I311 point (5) SCOPING CORRECTED: `_eks_schema_meta` + `_ensure_schema_version()` retirement and drift-tracking repoint to `db_manifest` executed in I312 (T1.301–T1.303), replacement-first, no tracking gap; gate is PRAGMA-based, needs zero interaction with `_eks_schema_meta`; retiring it in I311 would create a no-drift-tracking window and make I312/T1.303 redundant; T1.300 absence-assert moved to I312/T1.304; issue log v107→v108) — prior: 2026-08-12 (U292 — I311 DESIGN CONFIRMED (Q1–Q6): default migration_policy = non-destructive schema-driven; --db-check/--db-apply/--db-force with --db-force = total override + mandatory backup; non-TTY aborts destructive with log detail; UUID migration = additive backfill (no type change); structural=HARD / row-count=SOFT; gate error codes P1-R-P-0004/0005 + S-C-S-0311 to register; issue log v106→v107, T1.297–T1.300 updated, all ⏳ Planned) — prior: 2026-08-11 (U287 — I307 RESOLVED / T1.275–T1.281 ✅ COMPLETE: schema-driven DB-layer schema set (eks_base_schema v1.18.0, eks_setup_schema v1.12.0, eks_db_config.json 53 tables, eks_export_view_config.json 3 views, SchemaToDDL rev 1.2 runtime DDL re-homed, eks_doc_base_schema v1.21.0 physical-FK intent, SchemaLoader rev 1.6.0 startup validation); test_i307_db_schema_set.py 35 tests (TL053); full suite 730 passed / 4 pre-existing I288 fixture failures; task log Complete 426→433, Planned 69→62; issue log v101→v102, I307 ✅ resolved, outstanding 32→31, high 7→6; test_t132_modules version pins updated) — prior: 2026-08-11 (U286 — T1.269 ✅ COMPLETE: 53-table DB-layer inventory delivered `eks/workplan/t1.269_db_layer_inventory.md` v1.0 (T1.269-INV) fed to I307; 22 config files verified, 24 count-drifts recorded, materialization gap 11/53 confirmed; task log Complete 425→426, Planned 70→69; issue log v100→v101, Priority Resolution Sequence row 6 re-sequenced to execution order I307→I310→I311→I308→I309→I312→I313) — prior: 2026-08-10 (U285 — I292/I296/I297 RESOLVED + T1.255/T1.259/T1.260 ✅ COMPLETE: GAP-003 element_by_cover_type row count 33→30 corrected; GAP-007 page_count SSOT declared (1 actual copy confirmed — health_score only Tier-2 checks, total_sheets is derived column); GAP-008 load order 45→48 steps (documents@43, document_elements@44, document_reference@45, batch_run@46, health_score@47, health_batch@48); issue log v96→v97 (I292/I296/I297 📐→✅, Resolved 149→152, Aligned 106→103, priority row 5 removed, outstanding 34→31, medium 10→7); task log T1.255/T1.259/T1.260 🔷→✅, Complete 418→421, Planned 37→34; appendix_b.2 v1.7, appendix_b.1 v1.5, appendix_b_document_registry §B4 SSOT annotation) — prior: U284 — I293/I294/I295/I300 IMPLEMENTED / T1.256/T1.257/T1.258/T1.263 ✅ COMPLETE: four GROUP 11 runtime tables created (batch_run, health_score+health_batch, document_reference) + CRUD + orchestrator phase-boundary wiring; HealthScorer.score_batch() rev 0.4 returns doc_scores with documents.id UUID; _LogCapture.run_id added (phase1_server.py); eks_doc_config.json v1.14.0 (ontology_triggers 6→7) + eks_ontology_config.json v1.8.0 (relationships 15→16); appendix_b.2 v1.5→v1.6 + appendix_b.1 v1.3→v1.4; 4 new test files (24 tests, TL052); full suite 679 passed/0 failed; issue log v93→v94 (I293/I294/I295/I300 → 📐 Aligned)) — prior: U282 — I291 revision-control + test-log completeness fixes (T1.254): TL051 entry added to p1_test_log.md (7-test detail, full suite 655/0); registry.py revision 0.9→1.0 (I291 store_elements UUID injection + element_type/doc_id validation + _element_type_codes); schema_to_ddl.py revision header added 1.0 (I291 elements DDL + migration path); issue log v92 unchanged, I291 remains 📐 Aligned) — prior: U281 — I291 IMPLEMENTED / T1.254 ✅ COMPLETE: `document_elements` shape enrichment done — eks_doc_base_schema.json v1.19.0→v1.20.0 (document_element_def id UUID PK + created_at + element_seq; fk_element_doc/fk_element_type declared_only relations — NO physical FK DDL per I290 precedent); SchemaToDDL elements DDL emits id PK + created_at NOT NULL DEFAULT now() (+ element_seq, migration skips PK); store_elements injects UUID id + validates element_type 11-code enum + doc_id existence; GROUP 11 "RUNTIME TABLES" appendix_b.2 v1.3→v1.4; D7.10 enriched appendix_d v2.2→v2.3; B.1 GAP-002 IMPLEMENTED v1.1→v1.2; test/test_i291.py (7 tests, TL051); full suite 655 passed/0 failed; issue log v91→v92 I291 → 📐 Aligned) — prior: U280 — update-log pipe cleanup (§17.d)8/U182/U140 to restore the strict 7-column parse, no content change) — prior: 2026-08-09 (U278 — I291 DESIGN REVIEW APPROVED (2026-08-09): `document_elements` re-scoped from "missing runtime table" to shape enrichment + declared_only FK relations + element_type validation + GROUP 11 "RUNTIME TABLES" doc + integration test; severity 🔴 Critical→🟡 Medium; canonical plural `document_elements`; FK SSOT `documents.id`; GROUP 11 = RUNTIME TABLES family ruling resolves 5-way I290/I291/I298/I299/I301 collision; load-order renumber deferred to I297; T1.254 🟢 APPROVED; D7.10 + Appendix B.1 stale refs corrected) — prior: U277 — I290 RESOLVED, T1.253 ✅ COMPLETE: schema-driven `registry_relations` (6 declared FK rels incl. composite fk_doc_type_composite and self-FKs) + nullable `project_code` on project_metadata_def in eks_doc_base_schema.json v1.18.0→v1.19.0; SchemaToDDL.registry_relations()/generate_relations_manifest_ddl() materialize manifest into DuckDB `_eks_table_relations`; DocumentRegistry._init_db executes it; physical FK DDL deliberately NOT emitted (definition-layer targets not in runtime DB; DuckDB blocks PK UPDATE under self-FK) — declared_only, validation-layer enforcement; canonical runtime table `documents`; TL050; full suite 648 passed/0 failed; issue log v89, task log T1.253 COMPLETE) — prior: **Last Updated**: 2026-08-09 (U276 — I290 DESIGN CONFIRMED (Q1–Q7): `document_registry` wide instance/transaction table; composite FK `(project_code,local_code)` → project_doc_type; schema-driven DDL via SchemaToDDL (SSOT); junction split for multi-valued (I295); UUID v4 PK at ingest + `document_number` natural key; full-scope ingest; T1.253 re-scoped, T1.254/T1.257/T1.258/T1.260 aligned; tasks stay 🔷 PLANNED awaiting review; issue log v88; 2026-08-08 (U275 — I289 COMPLETE: canonical-only `X-X-X-XXXX` error-code format migration (T1.248–T1.252) — base schema regex/enum restricted to `S-{cat}-S-{id4}`/`B-{cat}-S-{id4}` (v1.4.0); 15 legacy `B-{module}-{id}` raises in `common/library/bootstrap/manager.py` → registered `B-{cat}-S-{4d}` family, dynamic `B-{phase_id}-ERR` eliminated; `eks/engine/core/bootstrap.py` override hooks + `errors.py` synced; 3 pre-existing test failures repointed to `S-B-S-*`/`S-F-S-*`/`S-E-S-*`; AGENTS.md §19.1 + appendix D (v2.2) + appendix H (v0.4) + `errors.py` swept; orphan `validator.py` archived; full suite 646 passed/0 failed; §24 audit green; issue log v86, TL049); U274 — I288 IMPLEMENTED: `processing_config` via `to_pipeline_context()` ONLY (context = single SSOT, §24) + `bootstrap_pipeline()` exposes `context` + both `run_pipeline()` branches and `discovery_cli.py` forward `processing_config` → ParserRouter registers pdf/docx/dwg/dgn/xlsx; discovery_cli missing `project_root` fixed; `_LogCapture` gains debug/trace/set_level; real-PDF e2e + 8 new tests (TL048); full suite 643 passed / 3 pre-existing; issue log v85); U273 — I286 IMPLEMENTED: schema-driven `correct_metadata` — `allowed_fields` derived from `column_processing` (no hardcoded set/fallback, §16), `manual_review` marker on `column_processing_entry_def` (doc base v1.18.0) + config values (doc config v1.13.0, 46 column entries incl. 4 new legacy fields), JSON/date/enum/unknown-column validation, phase1_server wired via cached SchemaLoader, 9 new tests (TL047), full suite 635 passed / 3 pre-existing; issue log v82); U272 — I286 RE-SCOPED for review: schema-driven `allowed_fields` + `manual_review` marker on `column_processing_entry_def` (doc base v1.18.0); filename/property-derivable columns NOT manual but review-allowed; JSON serialization for list columns; enum/date/json validation; T1.237 re-scoped 🔷 PLANNED; issue log v81); U271 — I283 IMPLEMENTED: four-level Class→Type→Template→Element detection — element_type_code 8→11 (title_block/grid/signature_block) + element_type_registry 3 entries (doc base v1.17.0, doc config v1.12.0), carrier v2.3.0 twrp_drawing/twrp_pandid expected_elements → 8 (threshold 5); StructureDetector.detect() gated by template expected_elements (all 11 detectors incl. link/note placeholders) + schema-first cover_type param, classify_cover_type() retired; EKSColumnProcessor.resolve_cover_type() → Optional (None = schema unavailable → detection fallback) + resolve_expected_element_types(); orchestrator wires both into detect() + HealthInput.cover_type; valid_element_types derived from base enum; tasks T1.230–T1.231 ✅ COMPLETE; 33 new tests TL046, full suite 617 passed / 3 pre-existing; issue log v78; U270 — I283 RESUMMARIZED: four-level Class→Type→Template→Element detection model — element-set SSOT = template `expected_elements`, add `title_block`/`grid`/`signature_block` element codes + detectors (8→11), `classify_cover_type()` retired (cover type schema-first, detection fallback), Phase 1 gates all detectors, `link`/`note` placeholders, detection feeds health score (metadata-only); issue log v77; U269 — I288 LOGGED: Phase B parse failures — processing_config dropped at runner/bootstrap → zero parsers registered + _LogCapture missing debug/log/set_level; tasks T1.244–T1.247 🔷 PLANNED; issue log v76; U268 — I280 IMPLEMENTED: B3.2 sub-objects added — `structural_profile_def` + `extraction_profile_ref`/`retrieval_profile_ref`/`validation_profile_ref` + Phase-3 stubs as optional fields on `document_type_def`/`document_class_def` (doc base v1.16.0), carrier v2.2.0 populated `structural_profile` on 8 classes/28 types + `extraction_profile_ref` on 8 classes, SchemaLoader `structural_profile_for()` helper + projection wiring, 19 new tests TL045, Appendix B v2.1.3 + Appendix C v1.10, full suite 585 passed / 3 pre-existing; issue log v75; U267 — I280 RE-SCOPED: B3.2 sub-object defs re-targeted to `document_type_def`/`document_class_def` (concept layer removed by I282), Q1.2 resolved, carrier population 6 concepts → 8 classes/28 types, `extraction_profile_ref` resolves to I281 `extraction_profiles`, refs §B3.2→§B2.1; issue log v74; U266 — I287 IMPLEMENTED: Phase 1 file-processing consolidation — profile chain `filename_profiles`/`file_property_profiles`/top-level `os_properties` in eks_processing_config.json v2.0.0, parser-class single source in extraction_profiles (3 legacy sources removed), core base v1.17.0 / setup v1.11.0 / doc config v1.11.0 / doc setup v1.11.0 / eks_config v1.11.0, full consumer repoint, 13 new tests TL044, suite 567 passed / 3 pre-existing; issue log v73)

---

## Legend

### Status

| Marker | Status | Meaning |
|:------:|:-------|:--------|
| ✅ | Done | Task or issue implementation fully complete and verified |
| ✅ | Complete | Task fully implemented (variant of Done) |
| ✅ | Resolved | Issue resolved and closed |
| 🔷 | Planned | Task defined but not yet implemented |
| 🔴 | Open | Not yet addressed; active in queue |
| ⚠️ | Partially reverted | Change partially reverted due to side effects |

---

### Status Summary

| Status | Marker | Count |
| :----- | :----: | ----: |
| Done | ✅ | 250 |
| Complete | ✅ | 1 |
| Resolved | ✅ | 2 |
| Planned | 🔷 | 18 |
| Open | 🔴 | 4 |
| Partially reverted | ⚠️ | 1 |
| **Total** | | **276** |

---

## Update History

| # | Date | Phase | Task | Description | Author | Status |
| :- | :--- | :---- | :--- | :---------- | :----- | :----: |
| U305 | 2026-08-18 | Phase 1 | I318, T1.309–T1.312, TL063 | **I318 Phase 1.2 asset-loader de-risking spike ✅ RESOLVED (2026-08-18)** — **(1) T1.309** — new `eks/engine/extractors/base_asset_loader.py`: reads datadrop Equipment/Motor sheets by tag_type (AT_EQPMP/AT_MOTOR), applies `column_normalization` (dotted routing `asset_context.system_hierarchy.area`, config-wins + native item_core fallback), composes `conditional_fragments`, pre-materialises nested objects (asset_context/location_hierarchy/system_hierarchy/lifecycle_context — stable canonical key sets); null/blank tolerance — blank fields never written to canonical records, surfaced via asset_health (I021). **(2) T1.310** — new `eks/test/test_asset_loader.py` (18 tests): sheet read by tag_type, column mapping, native fallback, fragment composition, dotted routing, conditional-fragment trigger, canonical shape, null tolerance, unknown-tag skip, coverage report format — 18/18 PASS. **(3) T1.311** — 7-sheet column-coverage pre-check `eks/workplan/reports/rp_p1.2_asset_column_coverage.md` (I020): unmapped columns surfaced per sheet, no silent column drops. **(4) T1.312** — per-asset health score via HealthScorer + null/blank tolerance (I021). **(5) Validation** — full suite **805 PASS / 4 FAIL** (3 pre-existing I288 real-PDF fixture absences + 1 flaky phase1_server DuckDB lock race — zero new failures). **(6) Logs** — issue log v124→v125 (I318 → ✅ Resolved, Resolved 166→167, Deferred 16→15, outstanding 16→15, priority row 1 → I228 only); task log T1.309–T1.312 → ✅ COMPLETE (Complete 460→464, Planned 35→31); test log TL063 (rev 1.33); knowledge.json v2.13.0→v2.14.0 (I318 resolved, extractors architecture). | opencode | ✅ Done |
| U304 | 2026-08-14 | Phase 1 | I313, T1.308, TL062; I306, T1.273/T1.274 | **I313 Phase 4 (T1.308) ✅ COMPLETE + I306 📐 ALIGNED — full-suite closure (2026-08-14)**: **(1)** Full suite `conda run -n eks python -m pytest eks/test/ -q` → **788 PASS / 3 FAIL** in 248.32s — pre-existing I288 real-PDF fixture absences only (identical to pre-I313 baseline; zero new failures across I306/I307–I313). **(2)** Consolidated report `eks/workplan/reports/I306-I313_impl_test_report.md` (v1.0, §16). **(3)** I313 → **✅ Resolved** (T1.305–T1.308 all complete, TL059–TL062, U301–U304). **(4)** I306 → **📐 Aligned** (§26.8 — workplan referenced; I307–I313 sub-issues all resolved). **(5)** I317 (real-PDF fixture gap) 🔴 Open logged as separate issue. **(6)** knowledge.json v2.10.0→**v2.12.0** (T1.273 — DB-layer architecture: 53 materialized tables, views, db_manifest, migration gate, config-driven exports; 18 revision entries). **(7)** Task log T1.308 + T1.273/T1.274 → ✅ Complete (Complete 454→455, Planned 41→39); test log TL062 (rev 1.32). **(8)** Issue log v122 — Status Summary: Resolved 165→166, Aligned 97→98, Open 13→12 (I313/I306 closed) then →13 (+I317), Total 296→297; Priority row 5 (I306) removed, row 6 re-sequenced to I315/I316 (count 2). §17.7 integrity green. | opencode | ✅ Done |
| U303 | 2026-08-14 | Phase 1 | I313, T1.307, TL061 | **I313 Phase 3 (T1.307) ✅ COMPLETE — output-name literal removal BLOCK-1–4 (2026-08-14)**: **(1)** **BLOCK-1** — `eks_engine_pipeline.py` rev 2.3→2.4: export_views built by iterating `resolve_export_views()` keys — `artifact_type` derived from each view's `view_id`; literal `export_config["..."]` column indices and literal `"artifact_type"` values removed; row-builder dispatched off the config column set (`flag_reason` in columns ⇒ `_build_flagged_rows`), not a literal view_id. **(2)** **BLOCK-2** — `phase1_server.py` rev 0.13→0.14: phase→view map derived from the export view catalog order (`chr(ord("a")+i)`, no literal view_id values — verified identical mapping a→discovery_inventory/b→extraction_results/c→review_flags); `_mk_rows_fn` dispatch keyed off `flag_reason` column presence instead of `view_id == "review_flags"`. **(3)** **BLOCK-3** — new module-level `_require_system_param()` raises `FAIL_FAST [S-C-S-0304]` when `export_download_file_name_template`/`export_workbook_file_name` are missing; the `eks_export_{phase}.{ext}`/`eks_export.xlsx` fallback literals removed (AGENTS §16 — no silent fallback). **(4)** **BLOCK-4** — `insert_artifact` records the exported **view_id(s)** as `artifact_type` (I306 model) — never the phase letter `a/b/c/all`. **(5)** **Tests** — `test_i308_default_views.py` rev 0.1→0.2 extended no-hardcoded-literal guard (both files, BLOCK-1–4 patterns); `test_i307_db_schema_set.py` §24 `test_view_ids_consistent_across_sources` updated to assert the config-driven pipeline contract + reject literal artifact_type values/column indices. **(6)** **Validation** — focused 28 PASS (test_i308/test_i309) + 169 PASS (test_eks_engine_pipeline/test_i298_i305/test_phase1) + 48 PASS (test_i307/test_i291/test_i310); full suite **788 PASS / 3 FAIL** (pre-existing I288 real-PDF fixture absences only — zero new failures); grep audit zero runtime literals in `eks/engine`+`eks/ui` export paths. **(7)** Report `eks/workplan/reports/rp_eks_p1_i313_output_names.md` (v1.0); task log T1.307 ⏳→✅ (Complete 453→454, Planned 42→41); test log TL061 (rev 1.31). D-3 (phase-"a" pending filter alignment) deferred — noted in issue log. | opencode | ✅ Done |
| U302 | 2026-08-14 | Phase 1 | I313, T1.306, TL060 | **I313 Phase 2 (T1.306) ✅ COMPLETE — §24 cross-source audit + D1–D6/O1 drift fixes (2026-08-14)**: **(1)** **D1** — `review_flags` formats csv→csv+xlsx (appendix_b_document_registry:1023/1066; matches view config `formats` = 2). **(2)** **D2** — `extraction_results` columns 49→50 (appendix_b_document_registry:1021, appendix_b.2:1333; matches view config = 50). **(3)** **D3** — 5 current-state `eks_export_view_config.json v1.1.0`→**v1.2.0** refs (appendix_b.1:1021, appendix_b.2:1311, appendix_b_document_registry:16/1021/1066); timestamped historical revision records preserved (AGENTS §13). **(4)** **D4** — 6 `registry_relations` descriptions → actual config FK names (fk_doc_pdt→fk_doc_type_composite, fk_doc_supersedes→fk_supersedes, fk_doc_superseded_by→fk_superseded_by, fk_doc_project_def→fk_project_code, fk_doc_discipline→fk_discipline, fk_doc_filetype→fk_file_type). **(5)** **D5** — `fk_project_code` `source_columns` project_code→**project_definition** (matches config FK spec; `documents.project_definition` is a live column). **(6)** **D6** — `fk_doc_type_composite` downgraded composite→simple per decision **D-2 option (b)** — project_doc_type has no UNIQUE (project_code, local_code) constraint (local_code non-UNIQUE, I315), so the composite shape is a logical lookup only. **(7)** **O1** — `db_manifest` documented in appendix_b.2 (GROUP 12 table lists + TABLE SUMMARY row 52; replaces `_eks_schema_meta`). **(8)** **Validation** — version-pin tests **139 PASS** (test_i307/test_t132/test_i308/test_i309) + registry_relations consumers **8 PASS** (test_i291/test_phase1 manifest/test_i310); `eks_doc_base_schema.json` valid JSON + SchemaLoader load OK; post-fix grep clean (zero current-state stale `v1.1.0`/`49 columns`/legacy `fk_doc_*` refs). **(9)** Report `eks/workplan/reports/rp_eks_p1_i313_audit_24.md` (v1.0); task log T1.306 ⏳→✅ (Complete 452→453, Planned 43→42); test log TL060 (rev 1.30). Doc/schema-only — zero engine code changed. Phase 3 (T1.307 output-name literal removal) next. | opencode | ✅ |
| U301 | 2026-08-14 | Phase 1 | I313, T1.305, TL059 | **I313 Phase 1 (T1.305) ✅ COMPLETE — 53-table live materialization matrix verification (2026-08-14)**: **(1)** Fresh `DocumentRegistry()` build of `eks/output/eks_registry.db` + PRAGMA matrix probe via new `eks/test_output/i313_phase1_verify.py` (rev 0.2) — **PASS**: 53/53 tables present (0 missing / 0 extra), 3/3 `v_*` views, `id` PK on all 53, 42/42 definition sources resolve (0 empty, 0 non-UUID), `db_manifest` 53 `table:*` keys all match live counts (0 mismatch), physical FK policy 5/5 (document_reference×2, health_score×2, health_batch×1). **(2) GAP-016** re-verified still RESOLVED (appendix_b.1:1021). **(3) FINDING I315** — 14 definition tables lack natural-key UNIQUE (13 composite-key junctions + `project_revision_pattern`); `SchemaToDDL` emits per-column UNIQUE only — no composite mechanism. **(4) FINDING I316** — 5 FK pairs / 336 orphan rows (`project_doc_type` 15, `project_definition.discipline` 2, `asset_column_normalization` 297, `asset_trigger` 18 + 4); `validate_relationships()` flags them but the warning is suppressed at default log level 1. **(5)** Evidence `eks/test_output/i313_phase1_matrix_results.json`; verification-only (zero engine/config code changed). **(6) Logs** — issue log v119→v120 (I315/I316 🔴 Open added, Open 11→13, Total 294→296, priority row 6 → I313/I315/I316); task log T1.305 ⏳→✅ (Complete 451→452, Planned 44→43); test log TL059 (rev 1.29); report `eks/workplan/reports/rp_eks_p1_i313_matrix.md`. Phase 2 (T1.306 §24 audit) awaits D-1–D-4 sign-off. | opencode | ✅ |
| U300 | 2026-08-14 | Phase 1 | I312, T1.301–T1.304, TL058 | **I312 IMPLEMENTED — schema-driven db_manifest provenance table (T1.301–T1.304 ✅ COMPLETE)**: (1) Config SSOT — eks_db_config.json v1.1.0→v1.1.1 reworks db_manifest to key-value shape (id VARCHAR PK, key VARCHAR UNIQUE, value JSON, updated_at TIMESTAMP DEFAULT now()) and adds top-level db_manifest_keys (declared key taxonomy: global keys schema_version/engine_version/schema_hash; config:&lt;source&gt; for 8 source configs; table:&lt;name&gt; per table; validation:not_null); eks_setup_schema.json v1.12.0→v1.13.0 adds db_manifest_keys property (additionalProperties:false). (2) manifest.py — new DBManifestWriter generic over db_manifest_keys (no code-literal key lists); transactional retried UPSERT on key; cheap keys every open, expensive per-table counts only when config-SSOT hash changes (replacement-first); legacy _eks_schema_meta copied then DROPped. (3) schema_to_ddl.py — adds ALWAYS_NULLABLE_COLUMNS SSOT frozenset (project_title/project_number/area/discipline/department/project_code), reused by gate + DDL. (4) migration_gate.py — ports I196 NOT NULL advisory check (SOFT warn) onto always-nullable project-metadata columns; db_manifest excluded from structural validation via METADATA_TABLES; plan returns not_null_warnings. (5) registry.py — _ensure_schema_version() + _migrate_schema() retired; _refresh_manifest() writes db_manifest on every open (records gate I196 outcome); revision 1.6→1.7. (6) Tests — test_i312_manifest.py (8 tests, TL058) covers key taxonomy, SSOT versions, idempotent UPSERT, legacy migration, per-table counts, gate I196 + db_manifest exclusion, registry integration; test_phase1.py 4 _eks_schema_meta assertions repointed to db_manifest; test_i307_db_schema_set.py version pins updated; 2 pre-existing base-schema version-drift assertions fixed (1.21.0→1.22.0, unrelated to I312). Full suite 787 passed / 4 failed (3 pre-existing real-PDF pipeline tests + 1 pre-existing base-schema drift now fixed) — no I312 regressions. Issue log v116→v117 (I312 🟢 Approved→✅ Resolved); task log T1.301–T1.304 🟢→✅. | opencode | ✅ Done |
| U299 | 2026-08-14 | Phase 1 | I312, T1.301–T1.304 | **I312 REFINED (design review 2026-08-14)** — best-practice SSOT/schema-driven design embedded into the I312 issue row + T1.301–T1.304 task rows: (1) db_manifest is a regenerated projection of config SSOT (never hand-edited, never a competing source of truth); (2) key taxonomy declared in db_manifest_keys (manifest.py generic, no code literals); (3) validation lives in the migration gate, manifest only records results; (4) refresh transactional + retried UPSERT (DuckDB cross-process safety), cheap keys every open / expensive counts on config-SSOT hash change; (5) legacy _eks_schema_meta migrated replacement-first (no tracking gap); (6) I196 NOT NULL check ported into the gate as SOFT warn. Issue log v115→v116; task log header v116; status remained 🟢 Approved. | opencode | ✅ Done |
| U298 | 2026-08-13 | Phase 1 | I314 (Gap-2 sub-item B), T1.291 | **I314 FOLLOW-UP — Gap-2 sub-item B RESOLVED (schema-driven HTTP download file names, I309 re-evaluation 2026-08-13)**: **(1) Schema** — `system_parameters.export_download_file_name_template` ('eks_export_{phase}.{ext}') added to eks_base_schema.json v1.21.0→v1.22.0 (system_parameters_def property + required) + eks_config.json v1.13.0→v1.14.0 (value). **(2) phase1_server `_handle_export`** — download file names now schema-driven: single-phase → template rendered with {phase}/{ext}; all-phases xlsx → reuses `export_workbook_file_name`; all-phases csv → template with phase='all'; hardcoded 'eks_export_phase_' literal REMOVED. **(3) Tests** — test_i309_exports.py +1 (`test_i314_phase1_server_schema_driven_download_file_name`) + version pins updated; i309+system_parameters 26 passed, phase1_server 61 passed (1 known flaky DuckDB lock race passes standalone). **(4) Issue log v112→v113** — I314 Description appended (Gap-2 sub-item A): run POST export_columns override affects ONLY the download endpoint; `run_pipeline()` has NO column_override param → runtime eks_export.xlsx always schema-default columns; DECIDE at implementation (pass-through vs download-only); Status Summary UNCHANGED (294 total / 16 deferred). | opencode | ✅ Done |
| U297 | 2026-08-13 | Phase 1 | I309, T1.287–T1.291, TL057 | **I309 IMPLEMENTED + VERIFIED — Excel single-workbook export + export backend (T1.287–T1.291 ✅ COMPLETE)**: **(1) openpyxl REQUIRED** (T1.287 verified — openpyxl==3.1.5 in eks.yml + eks_config.json dependencies.required; `_PIP_TO_IMPORT` no entry needed). **(2) `exporter.export_to_workbook(sheets, path, columns=None)`** (T1.288) in `common/library/export/exporter.py` + re-exported via `__init__.py`; `export_multi_sheet()` delegates to it; sheet names from view config `sheet_name`; missing openpyxl → clear message, non-fatal for CSV mode. **(3) Pipeline export rework** (T1.289) in eks_engine_pipeline.py — ONE workbook all 3 views incl. review_flags (xlsx); workbook file name `export_workbook_file_name` schema-driven (eks_config.json); per-view CSV + workbook; `column_override` validated as subset of view config + table schema → fail-fast **S-C-S-0313 EXPORT_COLUMN_NOT_ALLOWED**; no hardcoded file/sheet/column names. **(4) Backend** (T1.290 backend ✅) — phase1_server `GET /api/v1/export_views` returns views + ordered columns from `resolve_export_columns()` (schema-driven, matches CLI); run POST `export_columns` override validated S-C-S-0313; `_mk_rows_fn` + `_handle_export` workbook export. **Frontend ⏸️ deferred** — `eks/ui/eks.js`/`phase1_ingestion.html` column multi-select/re-order panel NOT implemented; tracked as **I314** (issue log) + Phase 1.2.22 T1.2.22.3/T1.2.22.4. **(5) Tests** — test_i309_exports.py 18 tests (TL057): 18/18 passed; full suite **778 passed / 4 failed** (3 pre-existing I288 real-PDF fixture absences + 1 known flaky phase1_server DuckDB lock race passing on rerun — no I309 regressions). Issue log v111→v112 (I309 ✅ Resolved, Resolved 163→164 / Approved 1→0, Deferred 15→16, Total 293→294; I314 added ⏸️ Deferred; priority row 6 → I312→I313, 2); task log T1.287–T1.291 ⏳/🟢 → ✅ (Complete 442→447, Planned 53→48); workplan Phase 1.2.22 backend ✅ / frontend ⏸️. | opencode | ✅ Done |
| U296 | 2026-08-13 | Phase 1 | I309, T1.287–T1.291 | **I309 APPROVED (design review Q1–Q10, 2026-08-13) — Excel single-workbook export + UI dynamic column selection**: openpyxl **REQUIRED** (Q1, Phase 1 deliverable; T1.287 largely verified — eks.yml + eks_config.json dependencies.required, `_PIP_TO_IMPORT` no entry needed); `export_to_workbook(sheets, path, columns=None)` with per-sheet `columns` control + `export_multi_sheet()` delegates (Q2); **all 3 views xlsx** incl. review_flags (Q3, view config formats updated); sheet names from view config `sheet_name` + workbook file name `export_workbook_file_name` in eks_config.json — schema-driven, no hardcode (Q4/Q6); column sequence SSOT = view config `columns[]` order; optional `column_override` validated as subset of view config + table schema → fail-fast **S-C-S-0313 EXPORT_COLUMN_NOT_ALLOWED** registered (Q5); UI default columns = CLI schema-driven `resolve_export_columns()`, user dynamic select/re-order per view, phase1_server.py hardcoded lists (L834-855) removed, T1.290 registered in phase_1.2 workplan Phase 1.2.22 (Q7); test_i309_exports.py planned (Q8); I309 row line ref corrected 268–287→313–331 (Q9); closure checklist agreed (Q10). Issue log v110→v111 (I309 🔴→🟢 Approved, Open 13→12, Approved 0→1); task log T1.287–T1.291 ⏳→🟢 Approved. | opencode | ✅ Done |
| U295 | 2026-08-13 | Phase 1 | I308, T1.282–T1.286, TL056 | **I308 IMPLEMENTED + VERIFIED — schema-driven default export views (T1.282–T1.286 ✅ COMPLETE)**: **(1) View config SSOT** — `eks_export_view_config.json` v1.0.0→v1.1.0 adds schema-driven `filter` (`is_latest = TRUE`) to all 3 default views; `eks_base_schema.json` v1.19.0→v1.20.0 adds `export_view_def.filter`; exporter `resolve_export_columns()`/`resolve_export_views()` read the config (hardcoded column lists + `_fallback` dicts deleted; missing config → fail-fast **S-C-S-0312** EXPORT_VIEW_CONFIG_MISSING, total_codes 138→139). **(2) Persistent views** — `schema_to_ddl.generate_view_ddl()` renders `CREATE OR REPLACE VIEW v_<view_id>`; registry `_create_export_views()` runs **AFTER** the I311 migration gate (additive `flag_reason` column lands before views reference it — fixes Binder Error on stale registries). **(3) `documents.flag_reason`** materialized at ingest via shared `core/flag_utils.compute_flag_reason` (clean → NULL; eks_db_config.json v1.0.0→v1.1.0 + eks_doc_base_schema). **(4) Export loop** — schema-driven file/sheet names + view_id as artifact_type. **(5) Tests** — test_i308_default_views.py 9 tests (TL056); lazy-import path fix `..core.flag_utils`; full suite **759 passed / 5 failed** (3 pre-existing I288 real-PDF fixture absences + 2 fixed import-path errors). I308 ✅ Resolved; task log T1.282–T1.286 ⏳→✅; issue log v109→v110. | opencode | ✅ Done |
| U294 | 2026-08-12 | Phase 1 | I311, T1.297–T1.300, TL055 | **I311 IMPLEMENTED + VERIFIED — DB migration/validation gate + `_eks_table_relations` config-shape alignment (T1.297–T1.300 ✅ COMPLETE)**: **(1) Root cause** — the migration gate correctly exposed a cross-source misalignment: `eks_db_config.json` declares `_eks_table_relations.id` as PK (`id_strategy` uuid5 namespace `eks:_eks_table_relations`), but `registry.py:275-283` + hardcoded `generate_relations_manifest_ddl()` deliberately kept the legacy manifest shape (`relation_name` PK) — systemic PK drift → gate blocked (P1-R-P-0004) on every fresh test DB. Per design review decision (runtime follows config `id` PK; config stays SSOT — I307 invariant `test_i307_db_schema_set.py:153` untouched). **(2) `schema_to_ddl.py`** — `generate_relations_manifest_ddl()` rewritten to render `_render_table_from_config("_eks_table_relations", include_fk=False)` → `id VARCHAR PRIMARY KEY, relation_name VARCHAR UNIQUE, ...`; `id` = uuid5(uuid5(NAMESPACE_URL, "eks:_eks_table_relations"), relation_name) mirroring definition_loader.py; inserts switched `INSERT OR REPLACE` → `INSERT ... ON CONFLICT (id) DO UPDATE SET ... EXCLUDED` (config-shaped table has TWO unique constraints → `OR REPLACE` ambiguous in DuckDB). `import uuid` added. **(3) `registry.py`** — `_init_db()` un-excludes `_eks_table_relations` from config pre-creation (`exclude_tables={"documents","document_elements"}`); comment updated. **(4) Error codes** — S-C-S-0311 (invalid policy), P1-R-P-0004 (destructive-blocked), P1-R-P-0005 (backup-failed) registered in `eks_error_config.json` v1.9.0 / `test_t132_modules.py` `total_codes` 135→138. **(5) CLI/entry-point wiring** (from earlier gate work) — `migration_policy` in eks_config.json + base schema; `--db-check`/`--db-apply`/`--db-force` in cli.py, eks_engine_pipeline.py main(), discovery_cli.py, health_cli.py; `run_pipeline(migration_mode=...)` bootstrap branch → `DocumentRegistry.__init__` → `_run_migration_gate()`. **(6) Tests** — TL055: `test_migration_gate.py` (T1.300) + focused 139 passed across test_migration_gate/test_phase1/test_i291/test_i307/test_t132/test_i310; full suite **751 passed / 4 failed** — 3 pre-existing I288 real-PDF fixture absences (FileNotFoundError under eks/data/twrp/) + 1 flaky phase1_server Windows DuckDB file-lock race (background pipeline thread + HTTP handler; confirmed 2x repeat pass standalone; same class as I053). Excerpt: `_probe` verified config-shaped DDL + 8 manifest rows. **Issue log** v108→v109 (I311 🔴→✅ Resolved, Resolved 161→162, Open 15→14, priority row 6 re-sequenced I308→I309→I312→I313 count 5→4, outstanding 30→29). **Task log** T1.297–T1.300 ⏳→✅ COMPLETE (Complete 433→437, Planned 62→58). 45 gate-generated backup `.db` files in eks/archive/ cleaned. | opencode | ✅ Done |
| U293 | 2026-08-12 | Phase 1 | I311, T1.300 | **I311 point (5) SCOPING CORRECTED** — `_eks_schema_meta` retirement (table drop + `_ensure_schema_version()` removal + drift-tracking repoint to `db_manifest`) is **deferred to I312 (T1.301–T1.303)**, executed replacement-first so there is no drift-tracking gap. The I311 gate is PRAGMA-based (diffs live tables vs `eks_db_config.json`) and needs **zero** interaction with `_eks_schema_meta`; `_migrate_schema()` (registry.py:291) is replaced by the gate while `_ensure_schema_version()` (registry.py:292) stays until I312 removes it. Rationale: retiring in I311 would create a window with no drift tracking (db_manifest writer not yet built, I312/T1.302) and would make I312's T1.303 redundant. Updates: p1_issue_log v107→v108 (I311 row point (5) + Last Updated), T1.300 test description updated (removed `_eks_schema_meta`-absence assert, deferred to I312/T1.304); I311 remains 🔴 Open awaiting implementation approval. | opencode | ✅ Done |
| U292 | 2026-08-12 | Phase 1 | I311, T1.297–T1.300 | **I311 DESIGN CONFIRMED (review Q1–Q6)** — issue row + task rows updated in the logs, awaiting implementation approval. Confirmed design: (1) **Default `migration_policy` = non-destructive**, schema-driven via `system_parameters.migration_policy` (eks_config.json + base schema); main pipeline CLI parses it and transfers into all standalone `DocumentRegistry` entry points (1.3 runner / eks_engine_pipeline / discovery_cli / health_cli). CLI flags: `--db-check` (plan only, exit 0) / `--db-apply` (= `--yes`) / `--db-force` (**TOTAL override** incl. documents/document_elements + mandatory timestamped backup to `output/archive/` before destructive). (2) Non-TTY: auto-apply additive; abort destructive with warning + log detail in pipeline_event_log (non-zero exit, no hang). (3) UUID migration = additive backfill, NO data-type change (id stays VARCHAR), no prompt. (4) Gate is independent of `_eks_schema_meta` (Q5); retirement repoint to `db_manifest` deferred to I312 (see U293). (5) Structural mismatches = HARD (blocking); definition-table row-count drift = SOFT (warning only, config=SSOT per T1.293). (6) Gate-specific error codes to register across the 5-source chain (P1-R-P-0004 destructive-blocked, P1-R-P-0005 backup-failed, S-C-S-0311 invalid policy). Replaces the narrow silent `_migrate_schema()` (registry.py:290). Issue log v106→v107; task log T1.297–T1.300 updated (all remain ⏳ Planned). | opencode | ✅ Done |
| U291 | 2026-08-12 | Phase 1 | I310, T1.292–T1.296, TL054 | **I310 FULL REGRESSION VERIFIED**: vectorized multi-row literal inserts (17x faster than per-row parameter binding) reduced registry init to ~2.3s; `_eks_table_relations` keeps legacy manifest shape; documents/document_elements keep schema-def DDL; health CRUD preserves runtime batch references. Full suite `python -m pytest eks/test/` → **737 passed / 3 failed in 270.64s** (was 16 min). All 3 failures are pre-existing I288 real-PDF fixture absences (no PDF under eks/data/twrp/); zero I310 regressions. I310 ✅ RESOLVED. | opencode | ✅ Done |
| U290 | 2026-08-12 | Phase 1 | I310, T1.292–T1.296, TL054 | **I310 IMPLEMENTED + FOCUSED-VALIDATED**: SchemaToDDL renders all 53 configured tables (reserved-identifier quoting, eligible runtime-to-runtime physical FKs, schema-def DDL preserved for documents/document_elements, self-FKs validation-only); DefinitionLoader extracts all 42 definition sources with UUIDv5 IDs, JSON serialization, dependency order, idempotent per-table inserts, and post-load relationship validation; registry initialization materializes the configured DB layer while leaving persistent views to I308. Dedicated materialization error codes registered in eks_error_config.json v1.8.0 (S-C-S-0309/0310, S-R-S-0411/0412, P1-R-P-0001–0003) + setup-schema range; DefinitionLoadError carries codes. I310 tests 6/6; bounded I307/I293–I295/I300 regression 65 passed; error-catalog format suite 82 passed. Full-suite regression result recorded separately. | opencode | ✅ Done |
| U289 | 2026-08-11 | Phase 1 | I310, T1.292–T1.296 | **I310 documentation updates completed**: approved `DefinitionLoader`, UUIDv5 IDs in configured `VARCHAR` columns, I308 ownership of persistent views, derived `_eks_table_relations` FK audit reuse, load-then-validate self-references, runtime row-count growth, and dedicated materialization error-code coverage. I307 task records remain unchanged. | opencode | ✅ Done |
| U288 | 2026-08-11 | Phase 1 | I310, T1.292–T1.296 | **I310 scope clarified from post-I307 decisions**: retain I307 as completed history; use a new DefinitionLoader while keeping SchemaLoader for schema discovery/validation; use deterministic UUIDv5 values in configured VARCHAR IDs; leave persistent views to I308; reuse `_eks_table_relations` as a derived physical-FK audit/compatibility table rather than an FK source of truth; load self-referential data before relationship validation; measure actual runtime row counts and growth; add dedicated materialization/config/data error-code coverage. | opencode | 🔷 Planned |
| U287 | 2026-08-11 | Phase 1 | I307, T1.275–T1.281, TL053 | **I307 RESOLVED — schema-driven DB-layer schema set (T1.275–T1.281 ✅ COMPLETE)**: (1) **T1.275** — `eks_base_schema.json` v1.17.0→v1.18.0: 6 shared db-layer defs added (table_spec_def composing columns/foreign_keys/id_strategy/transform, column_spec_def incl. DuckDB JSON marker + default, fk_spec_def with on_delete restricted to NO ACTION/RESTRICT per DuckDB FK limitation verified 2026-08-11, id_strategy_def uuid5/uuid4, transform_def 5-value enum, export_view_def). (2) **T1.276** — `eks_setup_schema.json` v1.11.0→v1.12.0: `db_tables` + `export_views` properties added (items $ref base defs, optional so core config still validates). (3) **T1.277** — `eks_db_config.json` created: **53 table specs** (42 definition + 9 runtime + 2 metadata), every table id PK + id_strategy, FK targets cross-checked against declared tables, runtime tables batch_run/health_batch keep run_id PK (matches current runtime DDL), created_at DEFAULT now()/phase counters DEFAULT 0 preserved. **CONSISTENCY FOLLOW-UP**: added standard config metadata header ($schema/$id/version/title/description) to both new configs to match every other `*_config.json`. (4) **T1.278** — `eks_export_view_config.json`: 3 default views (discovery_inventory 46 cols / extraction_results 50 cols / review_flags 12 cols), columns cross-checked == exporter.resolve_export_columns(). (5) **T1.279** — `SchemaToDDL` rev 1.1→1.2: 7 runtime generators re-homed to config via `_render_table_from_config()` + lazy `load_db_config()`; DDL output byte-equivalent to prior hardcoded strings (verified by existing DDL-shape tests). (6) **T1.280** — `eks_doc_base_schema.json` v1.20.0→v1.21.0: registry_relations moved to physical-FK intent (I306 Q1), FK names aligned to config fk_spec_defs, export_artifact_def FK note added; `_eks_table_relations` retirement deferred to I310/T1.295. (7) **SchemaLoader** rev 1.5.0→1.6.0: discovers + validates both configs at startup (`_validate_db_config`/`_validate_export_view_config`, FK-target cross-check). (8) **T1.281** — `eks/test/test_i307_db_schema_set.py` (35 tests, TL053): §9 checklist + §24 audit + no-hardcoded-DDL guard. **Full suite**: 730 passed / 4 failed (all 4 = pre-existing I288 real-PDF fixture absence under eks/data/twrp, unrelated to I307). **test_t132_modules.py** version pins updated (base 1.17.0→1.18.0, doc_base 1.20.0→1.21.0). **Task log**: T1.275–T1.281 🔷→✅ COMPLETE (Complete 426→433, Planned 69→62). **Issue log** v101→v102 (I307 🔴→✅, Resolved 159→160, Open 17→16, Priority row 6 ① marked resolved, outstanding 32→31, high 7→6). | opencode | ✅ Done |
| U286 | 2026-08-11 | Phase 1 | I306, I307, T1.269 | **T1.269 ✅ COMPLETE — 53-table DB-layer inventory (I306 audit phase)**: Delivered `eks/workplan/t1.269_db_layer_inventory.md` v1.0 (T1.269-INV). Per-table spec: group, config source file → JSON path → transform (1:1-unpack / array-of-objects / junction-from-array / object-iteration), id strategy (natural key → uuid5 namespace `eks:<table>`), FK targets, JSON columns, topological load-order position. **22 live config files verified 2026-08-11** with exact row counts (44 delegated tables config-backed: doc_config (11/5/46/6/5/3/7, 30+27 junctions), document_type_schema v2.3.1 (8/28/4/6 templates, 15 project_doc_type rows), ontology_config (45/18, 11 fragments), processing_config v2.0.0 (2/5/5, 23 mappings), asset_config v1.4.0 (14/95/297/15/3), project_definition_config v1.5.0 (2/8/22/2), error_config (75+53=128), message_config 52, discipline 21 / facility 12 / department 11 / project_code 3). **24 count-drifts recorded** (design vs actual — config = SSOT): discipline 8→21, facility 6→12, department ~12→11, ontology_class 35→45, data_column 42→46, column_class 336→351, fp_property_mapping 30→23, onto_class_fragment 12→11, asset_type_fragment 112→95, asset_fragment_field 210→191, asset_column_normalization 80→297, project_engineering_standard 10→8, project_allowed_discipline 16→22. **Materialization gap confirmed**: 11/53 currently materialized (9 runtime data + 2 metadata); 42 definition-layer tables have zero `CREATE TABLE`; no table lacks both runtime path and config backing. **Task log** T1.269 🔷→✅ COMPLETE (Complete 425→426, Planned 70→69). **Issue log** v100→v101 (I306/I307–I313 unchanged 🔴 Open, counts unchanged; Priority Resolution Sequence row 6 re-sequenced to execution order I307→I310→I311→I308→I309→I312→I313 with task ranges). Consumed by I307/T1.277 (`eks_db_config.json`). | opencode | ✅ Done |
| U285 | 2026-08-10 | Phase 1 | I292, I296, I297, T1.255, T1.259, T1.260 | **I292/I296/I297 RESOLVED — three documentation-only fixes (T1.255/T1.259/T1.260 ✅ COMPLETE)**: (1) **I292/T1.255 (GAP-003)** — verified `element_by_cover_type` row count by cross-referencing `eks_doc_config.json` SSOT. Actual = 30 pairs (not 33). §B3.4 table confirmed correct (30, F not in any element's `expected_by_cover_types`). Corrected `appendix_b.2_db_table_design.md` J02 row count 33→30. Updated GAP-003 in `appendix_b.1_cross_relationship_chart.md` as RESOLVED. (2) **I296/T1.259 (GAP-007)** — investigated all 3 alleged page_count storage sites: (a) `documents.page_count` actual storage from parser metadata; (b) `health_score` only Tier-2 checks via `_FALLBACK_TIER_2` set — never copies or stores; (c) `total_sheets` derived column using page_count as priority-chain fallback. Only 1 actual copy. Added **[SSOT]** mark to `appendix_b_document_registry.md` §B4 `page_count` row + **Derived column** note on `total_sheets`. GAP-007 → RESOLVED. (3) **I297/T1.260 (GAP-008)** — load order expanded 45→48 steps in `appendix_b.2_db_table_design.md`: documents@43, document_elements@44, document_reference@45, batch_run@46 (was 43), health_score@47 (was 44), health_batch@48 (was 45). Footnote updated. GAP-008 → RESOLVED. Code execution order in `registry.py _init_db()` already matches — documentation-only alignment. **Issue log** v96→v97 (I292/I296/I297 📐→✅, Resolved 149→152, Aligned 106→103, priority row 5 REMOVED, outstanding 34→31, medium 10→7). **Task log** T1.255/T1.259/T1.260 🔷→✅ COMPLETE (Complete 418→421, Planned 37→34). **Appendix B.1** v1.4→v1.5 (GAP-003/007/008 RESOLVED, P0 1→0, P1 2→0, active 9→6). Status Summary Done 239→240, Planned 19→18. | opencode | ✅ Done |
| U284 | 2026-08-10 | Phase 1 | I305, T1.268, I284, T1.232–T1.234 | **I305 follow-up logged + task-log recount + T1.232–T1.234 status correction**: (1) **I305 LOGGED** (2026-08-10) — §24 cross-source follow-up from I300/T1.263: `eks_doc_config.json` §ontology_triggers maps `asset_tags→REFERENCES_ASSET` and `file_type→HAS_FORMAT`, but `eks_ontology_config.json` §relationships (16 rows) contains neither as a first-class `relation_name` — `REFERENCES_ASSET` is only the inverse of `REFERENCED_BY_DWG`, `HAS_FORMAT` absent entirely. Severity 🟢 Low, status 🔴 Open. Task T1.268 (🔷 PLANNED) added to task log. (2) **T1.232/T1.233/T1.234 statuses corrected** — these I284 tasks were implemented 2026-08-07 (I284 📐 Aligned, test_health_scorer_typed.py, 9 tests) but their task-log Status cells were left blank/`—`; now marked ✅ COMPLETE with Updated=2026-08-07 + test reference. (3) **Task-log Status Summary recounted** — prior 414/26/3/1 = 444 was stale (task rows had grown without a recount); recount via whole-cell status matching across all 3 table layouts, deduplicated the duplicate T1.99.30 row (2 identical rows), excluded 6 rollup group rows → **Complete 418, In Progress 0, Planned 37, Won't Implement 2, Open 1, Total 458**. (4) Issue log v94→v95 (I305 added, Open 9→10, Total 284→285, priority row 8 count 6→7); update log Done 238→239, Total 265→266. | opencode | ✅ Done |
| U283 | 2026-08-10 | Phase 1 | I293, I294, I295, I300, T1.256, T1.257, T1.258, T1.263, TL052 | **I293/I294/I295/I300 IMPLEMENTED — four GROUP 11 runtime tables + config alignment (T1.256/T1.257/T1.258/T1.263 ✅ COMPLETE)**: (1) **batch_run (I293/T1.256)** — `SchemaToDDL.generate_batch_run_ddl()` + `insert_batch()`/`update_batch()`/`get_batch()` CRUD (registry.py rev 1.1); `PipelineOrchestrator` (rev 1.1) `_sync_batch_run()` writes phase stats at A/B/C boundaries (run_id resolved from `self.logger.run_id`), Phase C finalizes `status=success` + `finished_at`; `_LogCapture.run_id` class attribute added to phase1_server.py so server job UUID reaches the orchestrator logger (wire fix). (2) **health_score + health_batch (I294/T1.257)** — `store_health_score()`/`store_health_batch()` CRUD; `document_id` UUID declared_only FK→documents.id (**NO physical FK DDL** per I290 precedent); `HealthScorer.score_batch()` (rev 0.4) returns doc_scores each carrying documents.id UUID + flattened dims; `persist_batch_health()` writes per-doc rows + batch aggregate at Phase B end. (3) **document_reference (I295/T1.258)** — `store_document_reference()`/`list_document_references()`/`delete_document_references()` CRUD with 10-enum relation_type validation + source/target existence validation; declared_only relations `fk_ref_source`/`fk_ref_target` in registry_relations; `persist_document_references()` populates `references` edges from Phase B references_documents JSON (UUID closure). (4) **config alignment (I300/T1.263)** — `eks_doc_config.json` v1.13.0→v1.14.0 ontology_triggers 6→7 (lifecycle_stage→HAS_STAGE); `eks_ontology_config.json` v1.7.0→v1.8.0 relationships 15→16 (HAS_STAGE, inverse STAGE_OF). Docs: appendix_b.2 v1.5→v1.6 (purpose 16 relations/7 triggers; TABLE SUMMARY T17 15→16, T18 6→7; load-order lines), appendix_b.1 v1.3→v1.4 (GAP-004/005/006/011 IMPLEMENTED); test_t132_modules.py version pin 1.13.0→1.14.0. Tests **TL052** — test_i293.py (6) + test_i294.py (6) + test_i295.py (7) + test_i300.py (5) = 24 new. Full suite **679 passed / 0 failed** (was 655; +24). Issue log v93→v94 (I293/I294/I295/I300 → 📐 Aligned, Status Summary Aligned 103→106 / Open 12→9, priority row 6 COMPLETED); task log T1.256/T1.257/T1.258/T1.263 🔷 PLANNED→✅ COMPLETE (Complete 410→414, Planned 30→26). | opencode | ✅ Done |
| U282 | 2026-08-10 | Phase 1 | I291, T1.254, TL051 | **I291 revision-control + test-log completeness fixes (T1.254 follow-up)**: U281 marked the I291 work COMPLETE but two log/revision gaps remained. (1) **TL051 test-log entry ADDED** — `eks/log/phase1/p1_test_log.md` revision history 1.20→1.21 + Test Execution Table row TL051 (2026-08-10, 7-test detail for `eks/test/test_i291.py`: elements DDL shape; fk_element_doc/fk_element_type in `_eks_table_relations`; register→detect→store→get_elements_by_type round-trip; unknown element_type rejected; unknown doc_id rejected; cover-C/empty expected_elements → 0 rows but registry row inserted; delete_elements semantics; full suite 655 passed / 0 failed). (2) **registry.py revision 0.9→1.0** — header `Summary` updated for I291 (store_elements injects surrogate UUID `id`, validates element_type 11-code enum + doc_id existence via new cached `_element_type_codes()`). (3) **schema_to_ddl.py revision header ADDED (1.0)** — module docstring now carries Revision/Date/Author/Summary metadata for the I291 `generate_document_elements_ddl()` + migration-path changes (§13). Issue log v92 unchanged — I291 remains 📐 Aligned (Tasks T1.254, Resolution already referenced U281/TL051). | opencode | ✅ Done |
| U281 | 2026-08-10 | Phase 1 | I291, T1.254, TL051 | **I291 IMPLEMENTED — `document_elements` shape enrichment + declared-only FK relations + element_type validation (T1.254 ✅ COMPLETE)**: (1) `eks_doc_base_schema.json` v1.19.0→v1.20.0 — `document_element_def` extended: `id` (format uuid, PK), `doc_id` (target `documents.id` — FK SSOT), `created_at` (format date-time), optional `element_seq` (integer); 2 `declared_only` relations added to `registry_relations`: `fk_element_doc` (document_elements.doc_id→documents.id) + `fk_element_type` (document_elements.element_type→element_type.element_type). **NO physical FK DDL** (I290 precedent — doc_id physical FK would lock PK UPDATE during `_migrate_ids_to_uuid`; element_type target unmaterialized definition-layer); enforced at the validation layer. (2) `schema_to_ddl.py` — `generate_document_elements_ddl()` emits `id VARCHAR PRIMARY KEY` + `created_at TIMESTAMP NOT NULL DEFAULT now()` (+ optional `element_seq`); `generate_migration_ddl()` skips the PK `id` (DuckDB cannot ALTER-add a PK) but adds `created_at TIMESTAMP NOT NULL DEFAULT now()`. (3) `registry.py` — `store_elements()` injects surrogate UUID `id`, validates `element_type` against the 11-code enum (ValueError otherwise) via new cached `_element_type_codes()`, and validates `doc_id` existence (declared_only `fk_element_doc` enforcement). (4) GROUP 11 "RUNTIME TABLES" family added to `appendix_b.2_db_table_design.md` v1.3→v1.4 (documents, document_elements, batch_run, health_score, health_batch + I293/I294/I297/I298/I299/I301 additions); load-order position 43/44 documented (renumber deferred I297/T1.260). (5) D7.10 `document_elements` row enriched (`id`, `created_at`, `element_seq`, declared_only FK notes, 3 element types title_block/grid/signature_block) — `appendix_d_pipeline_messages_errors.md` v2.2→v2.3. (6) `appendix_b.1_cross_relationship_chart.md` v1.1→v1.2 — GAP-002 marked IMPLEMENTED + summary P1 6→5, active 14→13. (7) Tests **TL051** — `eks/test/test_i291.py` (7 tests): elements DDL shape; relations manifest contains fk_element_doc/fk_element_type; register→detect→store→get_elements_by_type round-trip (id/created_at populated); unknown element_type rejected with nothing persisted; unknown doc_id rejected; cover-C/empty expected_elements → 0 element rows but registry row inserted (I290 Q6); delete_elements semantics. Full suite **655 passed / 0 failed** (was 648; +7 new). Docs: `eks/test/test_t132_modules.py` version assertion updated 1.19.0→1.20.0. Issue log v91→v92 (I291 → 📐 Aligned, priority row 5 marked COMPLETED); task log T1.254 status 🟢 APPROVED→✅ COMPLETE. | opencode | ✅ Done |
| U280 | 2026-08-10 | Phase 1 | §17.d | **Update-log pipe cleanup** — escaped unescaped pipe chars breaking the 7-column parse in 5 pre-existing rows: U277 (`relation_type simple\|composite\|self`), U274 (stray `\| opencode \| ✅ Done \|` segment inside the T1.247 summary), U268 (`required\|optional\|absent` enum), U182 (3 legacy error-code format tokens in backticks), U140 (✅ PASS/COMPLETE \| 🔶 PARTIAL \| ❌ FAIL \| 🔷 PLANNED list). All escaped as `\|`; no content change. Escape-aware 7-column check now passes for every U235+ row (legacy 5-column U230–U238 block is an intentional separate format, left untouched). Issue log v91 cleanup (I290) already logged as U279. | opencode | ✅ Done |
| U279 | 2026-08-09 | Phase 1 | I290 | **I290 row table-format cleanup** — escaped 2 unescaped pipe characters inside the I290 description (the relation_type simple/composite/self enum list), the only issue row failing the strict 9-column parse (§17.d, found during I291 review). No content/status change. Page-wide escape-aware 9-column check now passes for all 47 issue rows. Issue log v90→v91. | opencode | ✅ Done |
| U278 | 2026-08-09 | Phase 1 | I291, T1.254 | **I291 DESIGN REVIEW APPROVED — `document_elements` re-scoped (T1.254 🟢 APPROVED, 2026-08-09)**: (1) Re-scope from "missing core runtime table" to "shape enrichment + declared-only FK relations + write validation + GROUP 11 doc + integration test" — table/CRUD/wiring already exist (schema_to_ddl.py:82, registry.py:501–563, pipeline_orchestrator.py:1026, from I196/T1.99.161); severity 🔴 Critical→🟡 Medium. (2) Declared-only FK relations per I290 precedent: add `fk_element_doc` (doc_id→documents.id) + `fk_element_type` (element_type→element_type.element_type) to `registry_relations` → `_eks_table_relations`; physical FKs rejected (breaks `_migrate_ids_to_uuid`; element_type target unmaterialized). (3) Shape: `id` UUID PK + `created_at TIMESTAMP DEFAULT now()` (+ optional `element_seq`); `store_elements` gains element_type 11-code enum validation. (4) GROUP 11 = "RUNTIME TABLES" family ruling resolves 5-way collision I290/I291/I298/I299/I301. (5) Load-order renumber deferred to I297 (T1.260). (6) Canonical plural `document_elements`; FK SSOT `documents.id`; singular `document_registry.doc_id` refs in D7.10 + Appendix B.1 corrected. Test plan: `test/test_i291.py` (detect→store→query_by_type round-trip + negative + empty/cover-C set + delete). Issue log v89→v90 (I291 row re-scoped, severity re-graded, priority row 5 + total note updated); task log T1.254 → 🟢 APPROVED. | opencode | ✅ Done |
| U277 | 2026-08-09 | Phase 1 | I290, T1.253, TL050 | **I290 RESOLVED / T1.253 COMPLETE — schema-driven FK relations manifest (T1.253 ✅)**: (1) `eks_doc_base_schema.json` v1.18.0→v1.19.0 — new `registry_relations` array + `registry_relation_def` declaring 6 relationships (source/target tables+columns, relation_type simple\|composite\|self, declared_only): `fk_doc_type_composite` composite (project_code, document_type)→project_doc_type(project_code, local_code); `fk_supersedes`/`fk_superseded_by` self-FKs; `fk_project_code`→project_definition; `fk_discipline`→discipline; `fk_file_type`→file_type; nullable `project_code` added to `project_metadata_def` (composite-FK carrier). (2) `schema_to_ddl.py` — `registry_relations()` + `generate_relations_manifest_ddl()` emit CREATE TABLE + INSERT OR REPLACE for `_eks_table_relations`. (3) `registry.py` — `_init_db()` calls `_create_relations_manifest()`. (4) Physical FK DDL deliberately NOT emitted — definition-layer targets are not materialized in the runtime `eks_registry.db` and DuckDB blocks PK UPDATE of self-FK-referenced rows (would break UUID id migration); relations enforced in the validation layer, manifest is the queryable SSOT. (5) Canonical runtime table name is `documents` (real DB in output/ per naming ruling). Tests TL050 (relations manifest round-trip + project_code presence + pre-generated-DDL path); full suite 648 passed / 0 failed. Docs: Appendix B.1/B.2 naming sync tracked via T1.255/T1.260. Issue log v88→v89 (I290 ✅ Resolved, priority row 5 adjusted, Open 14→13); task log T1.253 → ✅ COMPLETE (Complete 409→410, Planned 31→30). | opencode | ✅ Done |
| U276 | 2026-08-09 | Phase 1 | I290, T1.253, T1.254, T1.257, T1.258, T1.260 | **I290 DESIGN CONFIRMED (Q1–Q7 — DB design/schema-driven/SSOT review, 2026-08-09; T1.253 re-scoped, still 🔷 PLANNED awaiting review)**: Gap-001 review resolved 8 open design questions against principles. **Q1 FK target** — `document_registry.document_type` FK → composite key `(project_code, local_code)` → `project_doc_type` (surrogate `id` rejected; bare `local_code` non-unique → violates FK candidate-key rule; composite matches the schema carrier project-bound codes). **Q2 shape** — registry is a wide instance/transaction table (1 row per discovered+validated document), columns schema-driven from `eks_doc_base_schema.json` + `column_processing` generated by SchemaToDDL — **not** a schema mirror, no hardcoded 54-col list (§16). **Q3 version source** — DDL pinned to current schema `$id`; no stale `v1.16.0` literal; `_ensure_schema_version()` gate. **Q4 SchemaToDDL** — generator is the SSOT; FK relations declared in schema/relations metadata consumed by SchemaToDDL, not hardcoded in code; self-FKs (`supersedes`/`superseded_by`) legal and largely ordered after base definitions. **Q5 shape-wide-accept** — wide 54-col accepted for 1:1 per-doc attrs; multi-valued (`references_documents`, `asset_tags`) routed to junction tables (I295 `document_reference` + asset junction) — no opaque JSON retention inline. **Q6 scope** — every discovered+validated doc persisted incl. zero-element docs. **Q7 UUID migration** — `id` UUID v4 PK assigned at ingest; `document_number` TEXT NOT NULL natural key; no data migration (new table, no legacy rows). **Log aligns**: T1.254 (UUID FK target), T1.257 (UUID-containing, group8 FK), T1.258 (junction split), T1.260 (composite FK topological loading in load order), Appendix B.1 §V GAP-001. Issue log v87→v88 (I290 row + header); task log T1.253/T1.254/T1.257/T1.258/T1.260 rows + header. | opencode | 🔷 PLANNED |
| U275 | 2026-08-08 | Phase 1 | I289, T1.248–T1.252, TL049 | **I289 COMPLETE — canonical-only `X-X-X-XXXX` error-code format migration (T1.248–T1.252 ✅ COMPLETE)**: **(1) T1.248** — `eks_error_code_base.json` v1.3.0→v1.4.0: `system_error_code_format.pattern` → `^(S-[A-Z]-S-[0-9]{4}\|B-[A-Z]-S-[0-9]{4})$`; `system_format` enum → `["S-{cat}-S-{id4}","B-{cat}-S-{id4}"]`; legacy `P1-BOOT-{reason}`/`P1-SETUP-{type}{id}`/`B-{module}-{id}`/`S-{cat}-S-{id}` alternatives DROPPED. **(2) T1.249** — `common/library/bootstrap/manager.py`: all 15 unregistered legacy `B-{module}-{id}` raises migrated to catalog-registered `B-{cat}-S-{4d}` (`B-UNK-001`→`B-U-S-0001`, `B-UNK-002`→`B-U-S-0002`, `B-CLI-001`→`B-C-S-0001`, `B-PATH-001/002`→`B-H-S-0001/2`, `B-REG-001`→`B-R-S-0001`, `B-DEF-001`→`B-D-S-0001`, `B-FALL-001`→`B-A-S-0001`, `B-ENV-001/002`→`B-E-S-0001/2`, `B-PAR-001/002`→`B-M-S-0001/2`, `B-BOOT-0601`→`B-B-S-0001`, `B-CTX-001`→`B-X-S-0001`); generic `_run_phase` catch wraps in `B-U-S-0001` (dynamic `B-{phase_id}-ERR` eliminated); `eks/engine/core/bootstrap.py` override hooks + docstrings synced; `errors.py` docstring updated; common bootstrap tests repointed (37/37 green). **(3) T1.250** — test repoints clearing 3 pre-existing failures: `test_eks_engine_pipeline.py` L-305–364 `P1-BOOT-READINESS/CONFIG/PATHS/OS/CTX` → `S-B-S-0603`–`0607` (catalog registration test included); `test_setup_validator.py` `P1-SETUP-F001/F002/F003/D001/O001` → `S-F-S-0207/0208/0209/S-E-S-0106/S-F-S-0210` (matches `_SETUP_ERR` SSOT). **(4) T1.251** — AGENTS.md §19.1 convention list canonical-only; `appendix_d_pipeline_messages_errors.md` v2.1→v2.2 (System/Bootstrap format blocks canonical-only, revision row added); `appendix_h_bootstrap_module_design.md` v0.3→v0.4 (all code tables/flow diagrams/H.19 error table canonical); orphan `eks/engine/core/validator.py` (no importers) **archived** to `eks/archive/engine/core/validator.py` per §5.4. **(5) T1.252** — full suite `conda run -n eks python -m pytest eks/test/ -q` → **646 passed / 0 failed** (3 pre-existing cleared); §24 cross-source audit green (every registered system code matches `^(S\|B)-[A-Z]-S-[0-9]{4}$`; all data_logic_errors match `P[0-9]-[A-Z]-[A-Z]-[0-9]{4}` incl. P5-F-PROP-0001..0005; regex rejects `P1-BOOT-READINESS`/`P1-SETUP-F001`/`B-CLI-001`/`B-{phase_id}-ERR`). Issue log v85→v86 (I289 Resolved, Seq 14 struck, low 11→10); task log (T1.248–T1.252 Complete, Planned 36→31, Total 444). DCC legacy raises remain OUT of scope — documented drift, tracked future. | opencode | ✅ Done |
| U274 | 2026-08-08 | Phase 1 | I288, T1.244–T1.247, TL048 | **I288 IMPLEMENTED — Phase B parser registration fix (T1.244–T1.247 ✅ COMPLETE)**: **(1) T1.244** — `bootstrap.py` v0.6→v0.7: `to_pipeline_context()` adds `ctx_params["processing_config"] = getattr(self, "processing_config", {}) or {}` — `EKSPipelineContext` is the single SSOT (§24); `to_dict()` deliberately does NOT carry it (no second routing channel). **(2) T1.245** — `runner.py` v1.0→v1.1: `bootstrap_pipeline()` returns `out["context"] = mgr.to_pipeline_context()` (try/except → None) so non-context callers can read the context; context branch passes `processing_config=context.parameters.get("processing_config", {})` to `PipelineOrchestrator`; non-context branch derives the same from `boot["context"].parameters`; `discovery_cli.py` v0.1→v0.2 applies the same context-derived fix AND fixes a pre-existing bug (missing `project_root` positional in the `bootstrap_pipeline()` call → TypeError had blocked the whole CLI). Result: `ParserRouter` registration loop (parser_router.py:71-75) registers `pdf`/`docx`/`dwg`/`dgn`/`xlsx` → real-PDF Phase B success. **(3) T1.246** — `phase1_server.py` v0.11→v0.12: `_LogCapture` gains `debug(msg, context="")` + `trace(msg, context="")` (capture + forward) and `set_level(level)`, mirroring the standardized `UniversalLogger`/`EKSLogger` interface — clears the `AttributeError: '_LogCapture' object has no attribute 'debug'` on all 14 engine call sites. **(4) T1.247** — 8 new tests in `eks/test/test_pipeline_processing_config.py` (TL048): context SSOT (processing_config present + non-empty via `to_pipeline_context()`, absent on `to_dict()`); `ParserRouter` registers `['pdf','docx','dwg','dgn','xlsx']`; real-PDF end-to-end through `run_pipeline()` (both context and non-context branches — phase_b success > 0 AND `document_elements` rows persisted, proving the `pymupdf` parser registered from `extraction_profiles`); `_LogCapture` passthrough via real `EKSLogger` (no AttributeError, entries captured); source-grep guard. **TL048**: full suite 643 passed / 3 pre-existing failures (same drift set — 2 error-code catalog in test_eks_engine_pipeline + 1 setup_validator folder code; baseline 635→643, +8 tests). Revised code-revision metadata updated in all 4 files (§13). Issue log v84→v85 (I288 row → ✅ Resolved, priority Seq 13 struck, Status Summary Resolved 146→147 / Open 11→10); task log T1.244–T1.247 → ✅ COMPLETE (Complete 400→404, Planned 40→36). \| opencode \| ✅ Done \| (2) `eks_doc_config.json` v1.12.0→v1.13.0: `manual_review: true` on the 9 Manual-source fields (`lifecycle_stage`, `revision_date`, `revision_description`, `project_phase`, `contract_package`, `issued_date`, `responsible_engineer`, `vendor_name`, `references_documents`); 4 new `column_processing` entries added to keep the review allowlist complete (`department` manual_review:false; `status`/`security_class`/`verified_by` manual_review:true) — 42→46 entries; all non-calculated, phase B, scoring_tier `excluded`. (3) `review_manager.py` v0.2: `correct_metadata()` allowed_fields derived from `column_processing` keys (no hardcoded set/fallback, §16); `_column_processing()` raises descriptive ValueError when config absent; value validation — `json_column` must be list (serialized via `json.dumps`), `date_column` must parse ISO (`_is_iso_date`), `code_column` with enum_reference validated against base-schema enum (`lifecycle_stage_code` via `_enum_values`/`_is_in_enum`); unknown/control columns rejected. (4) `phase1_server.py`: `get_doc_schema_loader()` cached SchemaLoader; `_handle_update_document` constructs `ManualReviewManager(..., doc_config=loader.doc_config, base_schema=loader.doc_base_schema)`. (5) Tests (TL047): new `eks/test/test_manual_schema_driven.py` (9 tests — allowlist derivation, manual_review markers, status/date/enum/json/text accept+reject paths, missing-config error); updated `test_phase1.py::test_review_manager_correct_metadata` (constructs with doc_config/base_schema); `test_t132_modules.py` version pins 1.13.0/1.18.0. **TL047**: full suite 635 passed / 3 pre-existing (the 3 failures are pre-existing error-code catalog drift in test_eks_engine_pipeline.py + test_setup_validator.py, unrelated to I286). Issue log v81→v82 (I286 ✅ Resolved + priority row 11 struck); task log T1.237 → ✅ COMPLETE + Last Updated. | opencode | ✅ Done |
| U272 | 2026-08-08 | Phase 1 | I286, T1.237 | **I286 RE-SCOPED (review confirmations a/b — 2026-08-08, tasks stay 🔷 PLANNED, awaiting review/approval)**: Original "single-line fix + no schema change" plan superseded. **Confirmation a** — data derivation and review workflow are SEPARATE concerns: columns derivable from filename/file properties (`project_number`, `area`, `discipline`, `document_type`, `created_by`, `asset_tags`, etc.) are NOT classed `manual` in schema, but manual review/approval is still allowed for them — the manual flag is a data-source classification, not a review gate. **Confirmation b** — marker shape: `manual_review` (boolean) on the data column. **Re-scoped T1.237**: (1) `column_processing_entry_def` + optional `manual_review` in `eks_doc_base_schema.json` v1.17.0→v1.18.0 (shape-only, §9); (2) `eks_doc_config.json` `column_processing` — `manual_review: true` on the 9 fields (`lifecycle_stage`, `revision_date`, `revision_description`, `project_phase`, `contract_package`, `issued_date`, `responsible_engineer`, `vendor_name`, `references_documents`), derivable columns NOT manual; (3) `review_manager.py` — replace hardcoded `allowed_fields` with a schema-derived set from `column_processing` (no hardcoded list/fallback, §16); generalize JSON serialization for list columns (asset_tags/references_documents, both json_column); value validation rejects bad values (lifecycle_stage enum, non-ISO dates, non-list json columns, unknown/control columns); (4) tests per field group + reject-paths + §24 cross-source audit. Issue log v80→v81 (I286 row + priority row 11); task log T1.237 row + Last Updated. | opencode | 🔷 PLANNED |
| U271 | 2026-08-07 | Phase 1 | I283, T1.230–T1.231, TL046 | **I283 IMPLEMENTED — four-level Class→Type→Template→Element detection (T1.230–T1.231 ✅)**: **T1.230** — (1) `eks_doc_base_schema.json` v1.16.0→v1.17.0: `element_type_code` enum 8→11 (`title_block`/`grid`/`signature_block`). (2) `eks_doc_config.json` v1.11.0→v1.12.0: 3 new `element_type_registry` entries (title_block/grid/signature_block, source_method `regex`; `expected_by_cover_types` title_block [A,B,D,E], grid [A,B], signature_block [A,B]). (3) `eks_document_type_schema.json` v2.2.0→v2.3.0 carrier: `twrp_drawing` (cover_type A) + `twrp_pandid` (cover_type B) `expected_elements` extended to 8 (`cover_page`,`revision_table`,`section`,`image`,`link`,`title_block`,`grid`,`signature_block`), `threshold` 4→5; `twrp_spec_c` keeps empty set (detector gates to zero elements — intended). (4) `structure_detector.py`: `detect()` (L36) gains `expected_element_types` + `cover_type` params; new `TITLE_BLOCK_PATTERN`/`GRID_PATTERN`/`SIGNATURE_PATTERN` + gated `_detect_title_block`/`_detect_grid`/`_detect_signature_block`; every sub-detector (incl. link/note placeholders) gated via `_is_allowed()`; `_detect_cover_page` annotated with schema `cover_type`; `classify_cover_type()` RETIRED (keyword classification removed). (5) `column_processor.py`: `resolve_cover_type()` → Optional[str] (None = schema value unavailable → detection fallback; `"C"` = deliberate no-cover, never flags), `resolve_expected_element_types()` added, `_resolve_admitted_methods` discards `cover_page_element` only when cover_type resolves to exactly `"C"`. (6) `pipeline_orchestrator.py` L968+: resolves cover_type + expected_element_types via `_column_processor`, passes both (plus `skip_cover_page=(cover_type=="C")`) into `detect()`; `cover_type` wired into `HealthInput`. (7) `io_contracts.py` `HealthInput.cover_type: Optional[str]=None`; `health_scorer.py` `score_from_input()` forwards `getattr(cover_type, None)` (I284 base). (8) `schema_loader.py` `valid_element_types` derived from `element_type_code` enum (11; fallback set updated 8→11). Zero hardcoded element lists — template `expected_elements` is the element-set SSOT. **T1.231** — 33 new tests (TL046): new `eks/test/test_i283.py` (TestI283Schema/ColumnProcessor/DetectionIntegration/HealthScoring) + updated `test_t132_modules.py` (9 new TestStructureDetector tests), `test_phase1.py` (len 8→11), `test_document_type_ssot.py`, `test_column_processing.py` (resolve_cover_type None semantics), `test_i280_b32_subobjects.py` (carrier v2.3.0). **TL046**: full suite 617 passed / 3 pre-existing (baseline 532; +33 new; the 3 failures are pre-existing error-code catalog drift unrelated to I283). Issue log v77→v78 (I283 ✅ Resolved + priority row 9 struck + total 28→27). | Franklin | ✅ Done |
| U270 | 2026-08-07 | Phase 1 | I283, T1.230–T1.231 | **I283 RESUMMARIZED — four-level Class→Type→Template→Element detection model (2026-08-07 project review)**: Q4.1/Q4.2 RESOLVED. (1) Element-set SSOT = template `expected_elements` (structural_profile = capability metadata only — NO `structural_profile.expected_elements` field; T1.230 wording amended). (2) Add missing element codes + wire detectors NOW: `title_block`/`grid`/`signature_block` → `element_type_code` 8→11, so Phase 1 establishes a complete document profile. (3) `classify_cover_type()` keyword classification RETIRED — cover type resolved schema-first from carrier `document_templates[template_id].cover_type` (I279 SSOT via `EKSColumnProcessor.resolve_cover_type()`), falling back to detection only when the schema value is unavailable; no keyword heuristic, no OCR fallback flag. (4) Phase 1 gates ALL detectors by template `expected_elements`; actual content-based detection logic deferred for later discussion. (5) `link`/`note` added as placeholder detectors (gated by `expected_elements`). (6) Detection output wired as the BASE for health scoring (I284) — limited to metadata sources (document class, type, family, element, file name, file properties), NOT file content extraction. Integration: reconcile I278 `skip_cover_page` no-cover path with the schema-first cover type in the same edit cycle; template `threshold` consumed by health scoring (I284). Issue stays 🔴 Open; tasks T1.230–T1.231 re-scoped, remain 🔷 PLANNED. Logs updated: issue log v76→v77 (I283 row + priority row 9 + total line), task log (T1.230/T1.231 rows + Last Updated). | opencode | 🔷 Planned |
| U269 | 2026-08-07 | Phase 1 | I288, T1.244–T1.247 | **I288 LOGGED — Phase B parse failures (T1.244–T1.247 🔷 PLANNED, awaiting review/approval)**: RCA confirmed `processing_config` never reaches `PipelineOrchestrator` — `runner.py` omits `processing_config=` in both branches (runner.py:175 / runner.py:219) and `bootstrap.to_dict()` / `to_pipeline_context()` (bootstrap.py:648-663 / 703-709) do not expose it → `ParserRouter` extraction_profiles empty → `ParserFactory.register_parser()` loop (parser_router.py:71-75) registers zero parsers → every Phase B file fails `"No parser registered for file type: {ext}"` (parser_router.py:244) with elements=[] → `document_elements` stays empty. Engine path verified via CLI run (rc=0 but phase_b 0/2 success); direct probe with `processing_config` wired confirms all 5 parsers register. UI path (`phase1_server.py` `_LogCapture` L592-609) additionally crashes earlier on `AttributeError: '_LogCapture' object has no attribute 'debug'` (engine calls logger.debug across parser_router/pipeline_orchestrator/registry/revision/structure_detector/file_scanner/file_property_parser/project_definition/eks_engine_pipeline). Tests miss it — `test_cli_runs_end_to_end` asserts only rc==0. | opencode | 🔷 Planned |
| U268 | 2026-08-06 | Phase 1 | I280, T1.218–T1.222, TL045 | **I280 IMPLEMENTED — B3.2 sub-objects (T1.218–T1.222 ✅)**: **T1.218** — `eks_doc_base_schema.json` v1.15.0→v1.16.0: `structural_profile_def` (11 fields: cover_page/revision_table/title_block/legend/grid/signature_block presence enum `required\|optional\|absent`; multi_sheet/drawing_based/section_based/contains_callouts/contains_symbols boolean), `extraction_profile_ref`/`retrieval_profile_ref`/`validation_profile_ref` (string defs), Phase-3 stubs `document_semantics_def`/`ai_profile_def`/`knowledge_relationships_def` — ALL added as OPTIONAL fields on `document_type_def`/`document_class_def` (NOT `document_type_concept_def` — removed by I282), `additionalProperties: false` preserved; `document_type_entry_def` gains `structural_profile` so the doc-setup registry projection validates. **T1.219** — `eks_document_type_schema.json` v2.1.0→v2.2.0 carrier: `structural_profile` populated on all 8 `document_classes` (class-level defaults: Drawing=drawing_based/multi_sheet/symbols, Specification/Manual/Report/Procedure=section_based, Datasheet/Register=embedded-table oriented non-drawing) + all 28 `document_types` (explicit type-level profiles; e.g. PID_DRAWING legend=required+symbols, ISOMETRIC single-sheet override) + `extraction_profile_ref` on all 8 classes resolving to I281 `extraction_profiles` ids (technip_pdf/docx/xlsx). **T1.220** — `schema_loader.py`: `structural_profile_for(type_id, class_id)` helper (type-level override wins, else class-level fallback, else `{}`; either arg empty supported); `_derive_doc_type_projection()` attaches `structural_profile` to flat `document_type_registry` entries (validated via `document_type_entry_def`). **T1.222** — new `eks/test/test_i280_b32_subobjects.py` — 19 tests across TestI280B32Schema (7 defs present, 11-field profile def + enums, string refs, class/type optional wiring, registry projection acceptance), TestI280B32Carrier (8 classes + 28 types populated, extraction refs resolve, drawing-vs-text class profiles, type overrides, version 2.2.0), TestI280B32Helper (type-overrides-class, class fallback, class-only lookup, unknown→{}, all types resolve, projection carries profile, §24 cross-source audit). Updated `test_t132_modules.py` `test_config_version_bumped` doc-base pin 1.15.0→1.16.0. Full suite 585 passed / 3 pre-existing (baseline-verified). **T1.221** — `appendix_b_document_registry.md` v2.1.1→v2.1.3 §B2.1 §2 Structural Characteristics rewritten (SSOT hierarchy, carrier population table, profile refs, Phase-3 stubs); `appendix_c_ontology.md` v1.9→v1.10 cross-reference sync (carrier v2.2.0). Issue log v74→v75 (I280 row → ✅ Resolved + priority row 8 struck). | opencode | ✅ Done |
| U267 | 2026-08-06 | Phase 1 | I280, T1.218–T1.222 | **I280 RE-SCOPED (T1.218–T1.222 🔷 PLANNED — 2026-08-06)**: B3.2 sub-object implementation re-targeted after I282 (concept layer FULLY REMOVED). **Q1.2 RESOLVED**: sub-objects attach to shape-only `document_type_def` (type_id/label/class_id/family_id) and `document_class_def` (class_id/label/ontology_class/common_rules) in `eks_doc_base_schema.json` — NOT `document_type_concept_def` (removed) — per-project overrides remain in `project_document_type_def`. **T1.218** re-scoped: `structural_profile_def` + `extraction_profile_ref`/`retrieval_profile_ref`/`validation_profile_ref` as optional fields on `document_type_def`/`document_class_def`; `extraction_profile_ref` resolves to I281 `extraction_profiles` ids in `eks_processing_config.json`. **T1.219** re-scoped: populate `structural_profile` for 8 `document_classes` / 28 `document_types` (was 6 legacy concepts). **T1.220** re-scoped: `structural_profile_for(type_id, class_id)` with class-level fallback. **T1.221** re-scoped: §B3.2→§B2.1 (content merged by I282); per-class/type table. **T1.222** re-scoped: 8 classes/28 types + `extraction_profiles` ref validation. Appendix B §B2.1 scope note already records Q1.1/Q1.3 answers. Tasks remain 🔷 PLANNED. Issue log v73→v74 (I280 row + priority row 8). | opencode | 🔷 PLANNED |
| U266 | 2026-08-06 | Phase 1 | I287, T1.238–T1.243, TL044 | **I287 IMPLEMENTED — Phase 1 file-processing consolidation (T1.238–T1.243 ✅)**: **T1.238** — `eks_base_schema.json` v1.16.0→v1.17.0: `filename_profile_def` + `file_property_profile_def` + `os_properties_def` added; `processing_profile_registry_def.profile_type` enum 11→13 (`filename`/`file_property` after `graph_mapping`). **T1.239** — `eks_setup_schema.json` v1.10.0→v1.11.0: 3 new `processing_profiles` sections (`filename_profiles`, `file_property_profiles`, `os_properties`) inserted before `extraction_profiles`. **T1.240** — `eks_processing_config.json` v1.0.0→v2.0.0: `filename_profiles` (twrp_standard + default, each gains profile_id + profile_type "filename" per Q2) migrated from doc config; `file_property_patterns`→`file_property_profiles` reshaped (per-profile `supported_extensions` + `property_mapping` + explicit `bound_extraction_profile` per Q3; per-entry `extraction_method` DROPPED — inherited from bound extraction profile, I277 gate); top-level `os_properties` shared block (Q1 — NOT nested; property profiles reference via `uses_os_properties`); 5 `technip_*` extraction profiles kept unchanged as parser_class SSOT; 10 empty deferred sections. **T1.241** — one-time migration: `filename_profiles`/`file_property_patterns` removed from `eks_doc_config.json` v1.10.0→v1.11.0 (old shapes kept as DEPRECATED no-op in `eks_doc_setup_schema.json` v1.11.0); `parser_class` removed from `file_type_registry` rows (doc config + doc setup schema + required); `parsers` removed from `eks_config.json` v1.11.0 + top-level required list. **T1.242** — full consumer repoint (§11): `ParserFactory` derives `ext→parser_class` from `extraction_profiles[].supported_extensions` (hardcoded `_parser_mappings` removed); `ParserRouter` routes via `extraction_profiles` + passes `processing_config`; `FileScanner` gains `processing_config` kwarg + `_build_ext_parser_map()`; `PipelineOrchestrator._build_file_property_config()` projects processing config into legacy `{os_properties, by_file_type}` shape (extraction_method derived from bound profile's `extraction_methods`); `SchemaLoader._extract` filename reconstruction reads `processing_config.get("filename_profiles")`, `_validate_processing_config` cross-checks `bound_extraction_profile`↔`{...}_props` + enum 13 + `os_properties`, parser import check repointed to `extraction_profiles.parser_class`; `phase1_server.py` passes `processing_config`. **T1.243** — 13 new tests (`TestI287FileProcessingChain` + chain-defs test in test_processing_config.py; `file_type_registry` parser_class-dropped assertions + ParserRouter processing_config in test_phase1.py; docx mapping via processing config in test_t132_modules.py); full suite 567 passed / 3 pre-existing (was 554). **TL044**. Issue log v73 (I287 ✅ Resolved; 267 total / 13 open). | Franklin | ✅ Done |
| U265 | 2026-08-05 | Phase 1 | I281, T1.223–T1.225, TL043 | **I281 IMPLEMENTED — Processing Profile Registry formalised (T1.223–T1.225 ✅)**: **T1.223** — `eks_base_schema.json` v1.15.0→v1.16.0: generic `processing_profile_registry_def` (profile_id, profile_type enum of 11, version, capabilities, constraints) + 11 per-type defs — `extraction_profile_def` (superset of removed parsing_profile_def: profile_id/parser_class/description/supported_extensions/supported_document_profiles/requires_ocr/extraction_methods + profile_type/version/capabilities/constraints), `validation_profile_def` (validation_layers; I284 scoring_tier = column-value layer), stubs `chunking`/`retrieval`/`indexing`/`ai_reasoning`/`graph_mapping` (recommended fields entity_mapping/relationship_mapping/tag_type_mapping/document_type_mapping/node_label_strategy/edge_definition)/`embedding`/`asset`/`ontology`/`prompt`; `eks_setup_schema.json` v1.9.0→v1.10.0: `processing_profiles` property (11 `{type}_profiles` sections). ONE-TIME migration (Q1): removed `parsing_profile_def` from `eks_doc_base_schema.json` v1.14.0→v1.15.0 + `parsing_profiles` property from `eks_doc_setup_schema.json` v1.9.0→v1.10.0. **T1.224** — new `eks_processing_config.json` v1.0.0 = values SSOT (11 sections: 5 `technip_*` extraction profiles migrated, 10 empty landing zones); removed `parsing_profiles` from `eks_doc_config.json` v1.9.0→v1.10.0; SchemaLoader `_STEM_TO_ATTR` + `_validate_processing_config()` (per-section validation, doc-base ref resolution) wired into `_validate()`; FULL repoint (Q2, no projection): `column_processor.from_doc_config` (extraction_profiles), `parser_router` (processing_config param), `project_definition` (processing_config param; registries dict + L.13.4 + duplicate check → all 11 sections), `bootstrap` (P3 captures processing_config, resolver injection), `pipeline_orchestrator` (processing_config param); `eks_project_definition_config.json` v1.3.0→v1.4.0 repointed native profiles (131101 parsing_profile→technip_dwg, 131242→technip_dgn + document_profile.parser→technip_xlsx) → PDEF 4→1 (only technip_docx unreferenced). **T1.225** — new `eks/test/test_processing_config.py` (15 tests: 11-type enum, per-type defs, setup sections, extraction superset, 10 empty sections, legacy absence ×3, validate wiring, router/resolver consumption, PDEF 4→1, Phase 1 binding→profile establishment); repointed test_document_type_ssot + test_parser_two_axis_routing; version assertions in test_t132_modules (doc config 1.10.0, doc base 1.15.0). **TL043**: full suite 554 passed / 3 pre-existing (baseline-verified via git stash). **Appendix B** v2.1.2→v2.1.3 (Domain 4 11-type tree + phase scope note). Issue log: I281 🔴 Open → 📐 Aligned; v69→v70 (aligned 90→91, open 14→13; priority row 7 struck). Task log: T1.223–T1.225 🔷 Planned → ✅ COMPLETE; Status Summary recounted (complete 389→392, planned 41→38). | opencode | ✅ Done |
| U264 | 2026-08-05 | Phase 1 | I281, T1.223–T1.225 | **I281 plan revised review round 2 — Q1–Q7 RESOLVED (2026-08-05)**: **Q1** — ONE-TIME migration: doc-base `parsing_profile_def` + doc-setup `parsing_profiles` property REMOVED (no backward-compat duplicate); core schemas become the sole profile source. **Q2** — FULL repointing: all consumers (column_processor.py:509, parser_router.py:54, project_definition.py:605/739/903/983) + tests switch to `eks_processing_config` — no projection pattern. **Q3** — profile_type enum = ALL 11 registry keys: extraction/chunking/embedding/asset/ontology/retrieval/prompt/validation/indexing/ai_reasoning/graph_mapping — each a separate `{type}_profiles` config section with own sub-keys. **Q4** — I284 column `scoring_tier`/`applies_to_concepts` = the column-value validation LAYER within document validation; `validation_profile` is the container (validation_layers) — complementary, not merged. **Q5** — Phase 1 establishes the COMPLETE per-document profile (extraction/validation live, deferred types reserved); future phases consume. **Q6** — `graph_mapping_profile_def` stub with recommended fields (entity_mapping/relationship_mapping/tag_type_mapping/document_type_mapping/node_label_strategy/edge_definition); details later. **Q7** — SchemaLoader wires `eks_processing_config` (`_STEM_TO_ATTR` + `_validate_processing_config()` in `_validate()`). Architecture: generic `processing_profile_registry_def` + `extraction_profile_def` (superset of parsing_profile_def) + `validation_profile_def` + 9 stubs in `eks_base_schema.json` v1.15.0→v1.16.0; `processing_profiles` property in `eks_setup_schema.json` v1.9.0→v1.10.0; new `eks_processing_config.json` v1.0.0 (11 sections, 5 `technip_*` migrated, 10 empty); `parsing_profiles` removed from `eks_doc_config.json` v1.9.0→v1.10.0; PDEF repoint 4→1. Revised `p1_issue_log.md` v68→v69 (I281 row + priority row 7). Revised `p1_task_log.md` T1.223–T1.225. Tasks remain 🔷 PLANNED — awaiting approval. | opencode | 🔷 PLANNED |
| U263 | 2026-08-05 | Phase 1 | I281, T1.223–T1.225 | **I281 plan revised — Q2.1/Q2.2 RESOLVED (2026-08-05 discussion)**: **Q2.1** — base/setup layers REUSE core schemas (`eks_base_schema.json` v1.15.0 containers; `eks_setup_schema.json` v1.9.0 property); actual VALUES always live in a config schema (SSOT §9) — new `eks_processing_config.json`; no profile values in base/setup. **Q2.2** — profile↔document matching established LIVE in Phase 1 (register files in DB → extract file details → validate details → establish each document's required profile); Extraction/Validation slots live in Phase 1, Chunking/Retrieval/Indexing/AI Reasoning/Graph Mapping are phase-deferred stub containers. **Container correction** — containers for ALL 7 Appendix B §B2.1 Domain 4 profile types (Extraction, Chunking, Retrieval, Validation, Indexing, AI Reasoning, **Graph Mapping** — previously omitted). Generic `processing_profile_registry_def` (profile_id, `profile_type` enum of 7, version, capabilities, constraints) + `extraction_profile_def` (concrete) + `validation_profile_def` (partial, I284) + 5 stub defs added to `eks_base_schema.json` v1.15.0→v1.16.0; `processing_profiles` property added to `eks_setup_schema.json` v1.9.0→v1.10.0; new `eks_processing_config.json` = values SSOT (5 `technip_*` migrated, 6 empty landing zones incl. `graph_mapping_profiles`); `parsing_profiles` removed from `eks_doc_config.json`; ProjectDefinitionResolver registries dict + SchemaLoader `_STEM_TO_ATTR` repointed; PDEF warnings cleared via `eks_project_definition_config.json` repoint. Revised `p1_issue_log.md` v67→v68 (I281 row + priority row 7). Revised `p1_task_log.md` T1.223–T1.225. Tasks remain 🔷 PLANNED. | opencode | 🔷 PLANNED |
| U262 | 2026-08-05 | Phase 1 | I282, I285, T1.226–T1.229, T1.235–T1.236, TL042 | **I282/I285 IMPLEMENTED — class-based document hierarchy Class→Type→Family (T1.226–T1.229 + T1.235–T1.236 ✅)**: T1.226 — `eks_doc_base_schema.json` v1.13.0→v1.14.0: `document_type_concept_def` REMOVED; shape-only `document_class_def` (required class_id/label/ontology_class), `document_type_def` (required type_id/label/class_id, optional family_id), `document_family_def` (required family_id/label/discipline) added — NO value enums (§9/§16); `ontology_class` relaxed enum→plain string; `document_type_entry_def`/`project_document_type_def` migrate `concept_id`→`class_id`; `column_processing_entry_def.applies_to_document_types` `$ref`s the class `class_id`; `document_type_code` enum kept as derived mirror. T1.227 — `eks_document_type_schema.json` v2.0.0→v2.1.0 carrier rewritten to five sections: `document_classes` (8: Drawing/Specification/Datasheet/Calculation/Manual/Register/Report/Procedure), `document_types` (28 B3.1 entries each with class_id + optional family_id), `document_family` (4: Process/Instrument/Electrical/Mechanical Drawing), `project_document_types` bindings migrated `concept_id`→`class_id` (131101: DWG/PI-PID/SPC/DS/MAN/OM/RPT/CAD; 131242: DR/SP/CL/BQ/VI/M3/QA), `document_templates` (6 unchanged); `eks_ontology_config.json` v1.6.0→v1.7.0: DataSheet→Datasheet promoted to top-level Document subclass (D1), Calculation/Register/Procedure added, CAD_Drawing retained. T1.228 — SchemaLoader `_derive_doc_type_projection()` resolves label/ontology_class via binding `class_id`; `_validate_doc_registries` cross-references `document_types[].class_id`∈classes, `family_id`∈families, binding `class_id` valid, `column_processing.applies_to_document_types` refs valid class_ids; added `get_documents_by_class()`/`get_documents_by_family()`/`get_class_ancestry()` (cycle + dangling-parent guards); consumers repointed `concept_id`→`class_id`: `common/library/column_processor/base.py::_applies` (context `class_id`), `column_processor.py::resolve_scope`, `pipeline_orchestrator.py` Phase B context; `eks_doc_config.json` `total_sheets.applies_to_document_types` `["DRAWING"]`→`["Drawing"]`. T1.229/T1.236 — tests: `test_document_type_ssot.py` rewritten for the 5-section carrier (concepts-absent, 8 classes/28 types/4 families integrity, base shape-only, helpers, flat projection carries class_id, §24 audit), `test_column_processing.py` scope tests class-based, `test_phase1.py` enum-vs-ontology subset relation + element-expectations projection, `test_t132_modules.py` version 1.13.0→1.14.0; Appendix B §B3.1/§B3.2 (v2.1.1→v2.1.2) + Appendix C §C4 (v1.8→v1.9) aligned. **TL042**: full suite 539 passed / 3 pre-existing (I107 bootstrap-catalog ×2 + setup-validator folder error code — unrelated, P1-BOOT/SETUP error-code catalog drift, logged separately); 85/85 in ssot+column_processing+two-axis. Issue log v66→v67: I282/I285 🔴 Open→📐 Aligned (aligned 88→90, open 16→14, outstanding 31→29); priority rows 5/6 struck. Task log: T1.226–T1.229 + T1.235–T1.236 → ✅ COMPLETE (complete 383→388, planned 47→42). | opencode | ✅ Done |
| U261 | 2026-08-05 | Phase 1 | I282, I285, T1.226–T1.229, T1.235–T1.236 | **I282/I285 SSOT correction — class/type/family VALUES in carrier registries, NO base-schema enums (2026-08-05)**: Per §9 (config = actual values), §16 (no hardcoded duplicates) and the I031 precedent (base stripped to shape-only, config sole SSOT), `document_class_code`/`document_family_code` enums are REMOVED from the I282/I285 design. `eks_doc_base_schema.json` holds SHAPE-ONLY defs — `document_class_def` (class_id/label/ontology_class/common_rules), `document_type_def` (type_id/label/class_id/family_id), `document_family_def` (family_id/label/discipline), all `*_id` as plain strings; `ontology_class` becomes a plain string validated at runtime against `eks_ontology_config.json` (replaces the stale 5-value hardcoded enum at base lines 44/62). The carrier `eks_document_type_schema.json` is the single source of values (`document_classes` 8 entries / `document_types` B3.1 tree / `document_family` sub-discipline groups — T1.227). Validation moves from base enums to RUNTIME CROSS-REFERENCE in SchemaLoader `_validate_doc_registries` (T1.228): every `document_types[].class_id` ∈ `document_classes`, every `family_id` ∈ `document_family`, every binding `class_id` valid, `applies_to_document_types` refs resolve to valid class_ids. `document_type_code` enum is KEPT as a derived mirror of the `local_code` union (unchanged — it validates the stored `document_metadata_def.document_type` column). Revised `p1_issue_log.md` v65→v66 — Q6.1 reworded "validated enum" → "runtime cross-reference validation"; I282 step (5) and I285 steps (1)/(5) rewritten; priority row 6 retitled. Revised `p1_task_log.md` T1.226–T1.229 + T1.235–T1.236 — retitled "SHAPE-ONLY / NO value enums", enum tests replaced with cross-reference validation + grep-absent assertions. Tasks remain 🔷 PLANNED. | opencode | 🔷 PLANNED |
| U260 | 2026-08-05 | Phase 1 | I282, I285, T1.226–T1.229, T1.235–T1.236 | **I282 design review round 2 — CONCEPT REMOVAL confirmed (7 design decisions D1–D7 + 2 confirmations, 2026-08-05)**: **D1** — Datasheet and Specification are two DIFFERENT document classes → 8 classes (Drawing, Specification, Datasheet, Calculation, Manual, Register, Report, Procedure). **D2** — Document Class may be defined for a project but does not block sharing the same class across projects. **D3** — Document Type is as defined in Appendix B §B3.1 (type tree). **D4** — Concept and Class are highly duplicated → concept layer FULLY REMOVED; class carries its own `class_id`; `document_type_concepts` section deleted from the v2.0.0 carrier. **D5** — Document Type is extendable — new types added to the schema. **D6** — Document Family = grouping smaller than discipline; members share common properties and activities. **D7** — Code↔type mapping may reference DCC pipeline document codes (`dcc/config/schemas/document_type_schema.json`). Confirmed: 8-class set now, new classes addable later; full concept removal. Revised `p1_issue_log.md` v64→v65 — I282 resolution rewritten to class-based design (remove concepts, add `document_classes`/`document_types`/`document_family` registries, migrate `concept_id`→`class_id`, extend `ontology_class` incl. Datasheet top-level, DCC code alignment, SchemaLoader class/family/ancestry helpers); I285 reduced to class enum + helper + docs; priority sequence rows 5/6 reworded. Revised `p1_task_log.md` T1.226–T1.229 + T1.235–T1.236. Tasks remain 🔷 PLANNED. | opencode | 🔷 PLANNED |
| U259 | 2026-08-05 | Phase 1 | I282, T1.226–T1.229, T1.235–T1.236 | **I282 Q3.1–Q3.3 RESOLVED — 3-level document type hierarchy designed now (2026-08-05 design review)**: **Q3.1** — ALL 3 levels (Class → Type → Family) modelled in Phase 1 via `parent_concept_id` (Type→Type/Class) + `document_family` (Level 3 grouping). **Q3.2** — All 8 Document Classes get FULL concept definitions (CALCULATION/REGISTER/PROCEDURE full concepts, not stubs; only project bindings deferred). **Q3.3** — Document Family defined now (not deferred to Phase 3). Revised `p1_issue_log.md` v63→v64 — I282 proposed resolution now 9 steps (adds `document_family`, `document_class_code`/`document_family_code` enums, `ontology_class` extension, 3 SchemaLoader helpers with cycle guards); I285 Q3.3 reference + family-deferral wording aligned. Revised `p1_task_log.md` T1.226–T1.229 (3-level schema, full concepts, class/family helpers, tests) and T1.235–T1.236 (Document Family no longer deferred). Tasks remain 🔷 PLANNED. | opencode | 🔷 PLANNED |
| U258 | 2026-08-04 | Phase 1 | I278, T1.211–T1.212, TL040 | **I278 CLOSED — cover-type absence branching (T1.211–T1.212 ✅)**: T1.211 — `StructureDetector.detect()` gains `skip_cover_page` (no-cover `C` templates emit no `cover_page` element); `EKSColumnProcessor.resolve_cover_type(document_type)` resolves the binding template's `cover_type` from the injected `document_templates` (I279 carrier, default `C` for unknown → safe no-cover); `resolve_extraction_methods()` discards `cover_page_element` when `resolve_cover_type() == "C"` — reusing the I277 gate for both direct `cover_page_element` columns and priority-chain cover sources; `from_doc_config()` injects `document_templates`; PipelineOrchestrator passes `skip_cover_page=(cover_type == "C")` into `detect()`. No hardcoding — template `cover_type` is the SSOT (carrier). T1.212 — 9 new tests: `TestCoverTypeBranching` in `test_column_processing.py` (resolve_cover_type cover-bearing/no-cover/unknown; C discards cover_page_element; A keeps it; cover column skipped for C / runs for A; priority-chain cover source skipped for C; no-cover keeps parser_metadata) + `TestStructureDetector.test_skip_cover_page_no_cover_template` in `test_t132_modules.py`. Extraction-method gating (I277) + two-axis routing (I276) unaffected. **TL040**: full suite 532 passed / 4 pre-existing (unchanged; 522→532, +9 tests, zero new regressions). Issue log: I278 🔴 Open → 📐 Aligned; v61→v62 (aligned 87→88, open 10→9; priority sequence — last P1 row removed, outstanding 25→24). | Franklin | ✅ Done |
| U257 | 2026-08-04 | Phase 1 | I277, T1.209–T1.210, TL039 | **I277 CLOSED — Phase B extraction-method gating by profile `extraction_methods` ∩ `format_category` (T1.209–T1.210 ✅)**: T1.209 — `BaseColumnProcessor._extraction_applies()` (context `extraction_methods`; absent → unrestricted); `EKSColumnProcessor._required_extraction_method()` gates direct parser_metadata/cover_page_element columns, `resolve_extraction_methods()` resolves profile methods ∩ format (print drops parser_metadata), `_resolve_priority_chain()` skips gated sources individually, `from_doc_config()` injects `parsing_profiles`; PipelineOrchestrator Phase B context gains `extraction_methods`. Zero hardcoded method lists (names from `parsing_profiles` config). T1.210 — 10 new gating tests in `test_column_processing.py::TestExtractionMethodGating`. COLUMN_ALLOWLIST (I274) + two-axis routing (I276) unaffected. **TL039**: full suite 522 passed / 4 pre-existing (unchanged; 512→522, +10 tests, zero new regressions). Issue log: I277 🔴 Open → 📐 Aligned; v60→v61 (aligned 86→87, open 11→10). | Franklin | ✅ Done |
| U256 | 2026-08-04 | Phase 1 | I276, T1.206–T1.208, TL038 | **I276 CLOSED — two-axis parser routing by project binding profile × file_type reader (T1.206–T1.208 ✅)**: T1.206 — `default_parsing_profile` carried into the flat `document_type_registry` projection by SchemaLoader `_derive_doc_type_projection()` (was dropped); `document_type_entry_def` extended with `default_parsing_profile` so the projected config validates. T1.207 — `ParserRouter.resolve_parsing_profile(document_type)` (axis 1: code→profile id) + `resolve_reader(file_type, document_type)` (axis 2: profile→parser_class verified against `supported_extensions`, else fallback); `route()` accepts + records `document_type`, prefers the profile reader before file-type-only factory fallback; native profiles technip_dwg/dgn/xlsx wired (GAP-N4); PipelineOrchestrator `_process_file` passes registry document_type. T1.208 — 14 new tests in `test_parser_two_axis_routing.py` (§24 capability consistency + per-binding + fallback + unknown code). COLUMN_ALLOWLIST (I274) unaffected. **TL038**: full suite 512 passed / 4 pre-existing (unchanged; 498→512, +14 tests, zero new regressions). Issue log: I276 🔴 Open → 📐 Aligned; v59→v60 (aligned 85→86, open 12→11). | Franklin | ✅ Done |
| U255 | 2026-08-04 | Phase 1 | I275, T1.203–T1.205, TL037 | **I275 CLOSED — `column_processing` scoped by concept × format_category (T1.203–T1.205 ✅)**: T1.203 — `column_processing_entry_def` extended with `applies_to_document_types` (array of $ref to `concept_id`; absent = all concepts) + `native_only` (boolean; PDF-print delivery cannot populate); `eks_doc_base_schema.json` v1.11.0→1.12.0. T1.204 — `BaseColumnProcessor._applies()` (common/library/column_processor/base.py) filters the process() loop by resolved `concept_id` × `format_category`; `EKSColumnProcessor` gains `resolve_scope(document_type)` (lookup in the I279-projected `document_type_registry`) + `document_type_registry` param via `from_doc_config`; `pipeline_orchestrator.py` Phase B injects `concept_id`/`format_category` into the process() context. Unresolved scope defaults to unrestricted (never raises). T1.205 — config: 10 `embedded_*` columns `native_only: true`, `total_sheets` scoped `applies_to_document_types: ["DRAWING"]`; 7 new tests in `test_column_processing.py::TestDocumentTypeScopeFilter`; §24 audit. COLUMN_ALLOWLIST (I274) untouched — per-type availability is a processing concern only. **TL037**: full suite 498 passed / 4 pre-existing (unchanged; 491→498, +7 tests, zero new regressions). Issue log: I275 🔴 Open → 📐 Aligned; v58→v59 (resolved 142, aligned 84→85, open 13→12). | Franklin | ✅ Done |
| U254 | 2026-08-04 | Phase 1 | I279, T1.213–T1.217, TL036 | **I279 CLOSED — three-section `eks_document_type_schema.json` v2.0.0 carrier is the single runtime SSOT for document types (T1.213–T1.217 ✅)**: `eks_document_type_schema.json` v2.0.0 restructured into three sections — `document_type_concepts` (6: DRAWING/PID/SPECIFICATION/DATASHEET/MANUAL/REPORT), `project_document_types` keyed by project_code (131101: DWG,PI-PID,SPC,DS,MAN,OM,RPT,CAD; 131242: DR,SP,CL,BQ,VI,M3,QA — 15 codes), `document_templates` (6: twrp_drawing, twrp_pandid, twrp_spec_c, twrp_datasheet_e, twrp_manual_d, twrp_report_e). T1.214 — `eks_doc_base_schema.json` v1.10.0→1.11.0 (3 new defs + extended doc entry def with concept_id/template/format_category/native_source); `document_type_code` enum synced to union of all 15 local_codes. T1.215 — `format_category` (pdf=print, dgn/docx/xlsx/dwg=native) added to all 5 `file_type_registry` entries; native profiles `technip_dwg`/`technip_dgn`/`technip_xlsx` added (GAP-N4). T1.216 — committed `element_expectations` removed from `eks_doc_config.json` (migrated into `document_templates` re-keyed by template_id); `document_templates` + `document_type_schema_ref` properties added to setup schema. T1.213 — SchemaLoader `_derive_doc_type_projection()` projects `document_type_registry` (15) + `document_templates` + `element_expectations` + `document_type_schema_ref` into doc_config at load from the carrier; `_validate_doc_registries` rewritten to validate carrier sections; HealthScorer accepts injected `document_templates` (derived expected-elements map; `"sections"`→`"section"`); PipelineOrchestrator passes document_templates; `document_type_registry` array removed from `eks_doc_config.json` v1.8.0→v1.9.0. T1.217 — 14 new SSOT tests (`test_document_type_ssot.py`: projection integrity, format_category agreement, enum==union-of-local-codes drift-guard, registry-array removal, §24 seven-source audit); version assertions in `test_t132_modules.py` updated (1.9.0/1.11.0). **TL036**: full suite 491 passed / 4 pre-existing (unchanged; 477→491, zero new regressions). Issue log: I279 🔴 Open → 📐 Aligned; v57→v58 (resolved 142 unchanged, aligned 83→84, open 14→13). | Franklin | ✅ Done |
| U253 | 2026-08-03 | Phase 1 | I264, T1.182–T1.188, TL035 | **I264 CLOSED — Schema-driven ColumnProcessor fully confirmed and verified**: Ran `test_column_processing.py` (25/25 pass) and full suite (477 passed / 4 pre-existing failures unchanged). All 7 tasks complete: T1.182 ✅ (6 defs in eks_doc_base_schema.json v1.9.0), T1.183 ✅ (column_processing property in setup schema v1.7.0), T1.184 ✅ (42 column entries in eks_doc_config.json v1.6.0), T1.185 ✅ (BaseColumnProcessor + HandlerRegistry in common/library/column_processor/; EKSColumnProcessor), T1.186 ✅ (all 9 handlers with real logic: priority_chain, filename_segment, file_property, parser_metadata, cover_page_element, code_to_title_lookup, health_score, auto_increment, existing_record — column_processor.py rev 0.2→0.3), T1.187 ✅ (EKSColumnProcessor wired into PipelineOrchestrator Phases A/B/C), T1.188 ✅ (25 regression tests). Issue log updated: I264 status ⏳→✅, Resolution filled with U253/TL035, Priority Resolution Sequence row 3 removed. Status Summary: In Progress 1→0, Resolved 141→142, outstanding 30→29. Issue log v56→v57. | Franklin | ✅ Done |
| U252 | 2026-08-03 | Phase 1 | I275–I279, T1.203–T1.217 | **I275–I279 revised to the two-layer project-bound document-type model + T1.203–T1.217 updated/added (awaiting approval)**: Per the 2026-08-03 project review, the earlier enriched-single-SSOT proposal is superseded by a **two-layer model** where a document type is always bound to a project. `eks_document_type_schema.json` v2.0.0 becomes the single carrier with **three sections**: (1) `document_type_concepts` — global SSOT `{concept_id, label, ontology_class, common_rules}`; (2) `project_document_types` keyed by project_code — `{local_code, concept_id, template, format_category, native_source, expected_file_types, default_parsing_profile, project_rules}` — different projects may use different local codes for one concept (131101 `DWG` ↔ 131242 `DR` → concept `DRAWING`, templates `twrp_dwg` vs `platform_dwg`); (3) `document_templates` — `{template_id, label, cover_type(A–E), expected_elements, threshold, detection:{native=embedded_structure, print=page1_ocr}}` — absorbs `element_expectations`. `document_metadata_def.document_type` stores the **project-local code** (concept derived via binding). **I279 (enabling, 🟠)** — repoint all consumers (`file_scanner.py:46,104,323`, `schema_loader.py:376,414`, `filename_parser.py`, `column_processor`, `project_definition.py:615`, `health_scorer.py:38,47` hardcodes → `document_templates` reads), remove `document_type_registry` **and** `element_expectations` from `eks_doc_config.json`, migrate 15 codes → concepts + bindings, add `format_category` to `file_type_registry`, §24 multi-source audit. **I275** — per-concept × binding × `format_category` column scope (`applies_to_document_types` refs `concept_id`; `native_only`). **I276** — routing unit = project binding (`local_code → template → default_parsing_profile`) × (file_type → reader). **I277** — Phase B gating = binding `format_category` ∩ template detection ∩ profile `extraction_methods`. **I278** — `cover_type` now `document_templates[template_id].cover_type`, format-independent. Tasks: **T1.213** (repoint consumers), **T1.214** (three-section schema v2.0.0), **T1.215** (file_type format_category + native profiles), **T1.216** (template registry + element_expectations migration), **T1.217** (SSOT/no-drift + §24 audit tests). All 5 🔴 Open, all tasks 🔷 Planned — awaiting approval. Issue log v56 (259 total, 14 open). Task log: Planned 41→42, total 413→414. | Franklin | ✅ Done |
| U251 | 2026-08-03 | Phase 1 | I275–I279, T1.203–T1.216 | **I275–I278 revised + I279 logged + T1.203–T1.216 updated/added (awaiting approval)**: Per the 2026-08-03 review, document type is now modeled on the DCC pattern — `eks_document_type_schema.json` becomes the single enriched runtime SSOT (data array + `allOf` shape ref), and the native/PDF-print distinction is made explicit. **New I279 (🟠 High, enabling)** — `eks_document_type_schema.json` is a dead duplicate: runtime consumers (`file_scanner.py:46`, `schema_loader.py:376`, `filename_parser.py`) read `eks_doc_config.json#/document_type_registry`, not the schema file (I250/I251 only registered the schema, never repointed runtime; §10/§16 violation). Enrich `document_type_entry_def` + schema file v2.0.0 with `format_category` (native/print/either), `native_source`, `default_parsing_profile`; add `format_category` to `file_type_registry`; add missing native reader profiles (GAP-N4: technip_dwg/dgn/xlsx). Tasks: **T1.213** (repoint consumers, remove inline registry), **T1.214** (enrich schema v2.0.0), **T1.215** (file_type_registry format_category + native profiles), **T1.216** (SSOT + §24 six-source audit tests). **I275 revised** — scope = `document_type` × `format_category` (native-only metadata columns can't populate from PDF print); T1.203 adds `applies_to_document_types` + `native_only`. **I276 revised** — two-axis routing (document_type→default_parsing_profile, file_type→reader); T1.206/T1.207 updated + GAP-N4. **I277 revised** — extraction gating bounded by `format_category` ∩ `extraction_methods`; T1.209/T1.210 updated. **I278 revised** — cover-type is template-driven, format-independent (approach unchanged). All 5 🔴 Open, all tasks 🔷 Planned — awaiting approval. Issue log v55 (total 258→259, open 13→14, outstanding 29→30). Task log: Planned 37→41, total 409→413. | Franklin | ✅ Done |
| U249 | 2026-08-03 | Phase 1 | I275–I278, T1.203–T1.212 | **I275–I278 logged + T1.203–T1.212 added (awaiting approval)**: Per-type/template processing gap-analysis from the I274 review (2026-08-03). I274 kept unchanged. **I275** — `column_processing` not document-type-scoped (per-type available columns; T1.203 schema scope key `applies_to_document_types`, T1.204 BaseColumnProcessor doc-type filter, T1.205 config+tests+§24 audit). **I276** — parser selection is file-type-only, no per-document-type/template routing (T1.206 document-type→parsing-profile mapping, T1.207 ParserRouter/ParserFactory routing by document_type+file_type, T1.208 capability validation+tests). **I277** — `extraction_methods` declared but never gates Phase B extraction (T1.209 gating, T1.210 tests). **I278** — cover-page absence doesn't branch the parsing process (T1.211 cover-type branching, T1.212 tests). All 4 🔴 Open, all tasks 🔷 Planned — awaiting approval. Task log: Planned 30→40, total 399→409. Issue log v53 (total 254→258, open 10→14). | Franklin | ✅ Done |
| U250 | 2026-08-03 | Phase 1 | I274, T1.200–T1.202, TL034 | **I274 COMPLETE — COLUMN_ALLOWLIST SSOT fallback removed (T1.200–T1.202 ✅)**: `registry.py` rev 0.9. **T1.201** — CWD-independent doc base schema resolution via new `_resolve_doc_base_config_dir()` (ConfigRegistry `_loader.config_dir` first, then `default_base_path("eks")` + `resolve_paths()`); all `Path("eks/config")` literals removed (§15). **T1.200** — hardcoded 54-column fallback set deleted; schema-derived set is the sole SSOT; genuine schema absence now raises a descriptive error instead of silently degrading (§16). **T1.202** — drift-guard tests: `test_register_document_persists_file_type` extended (non-root CWD derivation == `COLUMN_ALLOWLIST`), new `test_column_allowlist_raises_when_schema_absent`, new `test_column_allowlist_equals_schema_derived_set` (54 = 5 project + 48 document props + id). Fix-breadth (§12): `_load_doc_schema()` and `_get_boilerplate_prefixes()` repointed to the resolver. **TL034**: focused 4/4 green; full suite 477 passed / 4 failed (pre-existing, git-stash baseline verified). Task log: Planned 40→37, Complete 365→368, total 409. Issue log: I274 🔴 Open → ✅ Resolved (close criteria met). | Franklin | ✅ Done |
| U241 | 2026-07-31 | Phase 1 | T1.195 | **T1.195 task revised with approved design decisions (V1/V2/V3)** for Implementation Configuration Validation: **(V1)** System errors hard-fail bootstrap via `resolver.errors`; data-related errors logged via new `resolver.data_errors`, never fail. **(V2)** L.13.6 capability-driven consistency — NO hardcoded pairs/matrix; each profile declares capabilities in its owning schema (`parsing_profile_def` in doc schema set, `parsing_profiles` in `eks_doc_config.json`); resolver `_resolve_profile()` uses exact-key lookup; generic `_evaluate_capability_compat()`. **(V3)** Error codes `S-C-S-{id}` (system, Config category) + `P1-C-V-{id}` (data, P1/C/V) — both fit existing `eks_error_code_base.json` regexes. Task row updated in p1_task_log.md + appendix_l_project_definition.md. | Franklin | ✅ Done |
| U242 | 2026-07-31 | Phase 1 | T1.195, I265 | **T1.195 COMPLETE — Configuration Validation implemented and verified**: Schemas — `eks_doc_base_schema.json` v1.10.0 (`parsing_profile_def` with supported_extensions/supported_document_profiles/requires_ocr), `eks_doc_setup_schema.json` v1.9.0 (`parsing_profiles`), `eks_doc_config.json` v1.8.0 (technip_pdf + technip_docx profiles). Error registry — `eks_error_config.json` v1.7.0 (128 codes; S-C-S-0901..0904 + P1-C-V-0001..0003; ranges `project_definition` + `phase_1_config_validation`), `eks_error_setup_schema.json` v1.3.0 (new range keys). Message catalog — `eks_message_config.json` v1.2.0 (PDEF_RESOLVE_START/COMPLETE, PDEF_DATA_ERROR, PDEF_SYSTEM_ERROR; total 49→52 actual keys). Resolver — `project_definition.py` rev 1.1.0: V1 failure semantics (`resolver.data_errors` never block), V2 exact-key profile lookup + generic `_evaluate_capability_compat()`, per-category validators (L.13.3/.4/.5/.6/.7/.9/.10), validation_report L.13.12, `_known_runtime_profiles()`; bootstrap surfaces data_errors. Tests — test_project_definition.py 28→75 (47 new), test_t132_modules.py 12 new (codes + messages). Full suite 472 passed / 5 pre-existing failures (unchanged). Integration: 2 projects resolved, errors `[]`, data_errors `[P1-C-V-0003 technip_docx unused]`. Appendix D v2.1 re-synced with config SSOT (D4 75/75, D5 53/53, D6 52/52, incl. pre-existing S-B-S-0609..0618 gap closure). | Franklin | ✅ Done |
| U243 | 2026-07-31 | Phase 1 | I266–I272, T1.196 | **T1.196 scope revised to cover all migration gaps (L.11 Stage 4 + Stage 5)** — 7 new issues logged from the I265/T1.195 gap audit: I266 (ConfigRegistry runtime consumers read legacy `project_rules_registry`; no `fragment_required_fields` accessor in resolver), I267 (`project_rules_registry` still required in `eks_setup_schema.json` + `$ref` in `eks_config.json` + `project_rules_def` in `eks_base_schema.json`), I268 (`legacy_project_rules` flag is dead config; Stage 5 compat-layer removal), I269 (`eks_project_definition_schema.json` named in I265/L.1/T1.190 but never created), I270 (Appendix L L.13 contradicts T1.195 V1/V2/V3 — L.13.6/L.13.1/L.13.10 wording + missing error codes), I271 (L.6.2 profile-library naming desync + 7 profile sections missing from doc_config), I272 (L.10.6 omits ColumnProcessor). Updated p1_task_log.md T1.196 row + header, p1_issue_log.md (v47, 245 total, 16 open, 2 in progress), p1_update_log.md. | Franklin | ✅ Done |
| U244 | 2026-07-31 | Phase 1 | T1.196, I266–I272 | **T1.196 COMPLETE — Configuration Migration (L.11 Stage 4 + Stage 5)**: Schemas — `eks_setup_schema.json` v1.9.0 (project_rules_registry property + required removed), `eks_config.json` v1.10.0 ($ref removed), `eks_base_schema.json` v1.15.0 (project_rules_def + `project_definition_entry_def.compatibility` removed), `eks_project_definition_config.json` v1.3.0 (compatibility blocks removed); `eks_project_rules_config.json` archived to `eks/archive/config/` (I267). Runtime consumers — `config_registry.py` rev 0.3: `get_project_rules` / `get_fragment_required_fields` / `resolve_required_fields` repointed to the Project Definition (I266); `project_definition.py` rev 1.2.0: `fragment_required_fields` resolved per project and carried into the AssetExtractor slice via `AssetsDomain.resolved`, validation_report schema_version sourced from config version (I266). Stage 5 — `schema_loader.py` rev 1.3.0: `eks_project_rules_config` mapping/attr/`_validate_project_rules()` removed, dead `revision_validation` reconstruction removed; functional `filename_patterns` reconstruction kept (T1.191 — filename_parser slice does not yet carry resolved patterns); `legacy_project_rules` flag dropped (I268). Tests — TL031: full suite 473 passed / 5 pre-existing (unchanged); test_project_definition.py 76/76 (new `test_asset_slice_carries_fragment_required_fields`); legacy assertions in test_phase1.py repointed to the Project Definition. Docs (I269–I272) — Appendix L naming note + L.13 V1/V2/V3 wording + L.10.6 ColumnProcessor + L.6.2 implementation note + T1.196 row; knowledge.json, eks_system_workplan.md, Appendix E updated. | Franklin | ✅ Done |
| U245 | 2026-07-31 | Phase 1 | — | **Log consolidation audit — 7 archived issues recovered into p1_issue_log.md**: Compared `eks/archive/log/issue_log.md` (I001–I249, consolidated) against all phase issue logs (p1/p2/p3/p5). Found I198 (GAP-D4), I203–I207 (GAP-D9..D13), I208 (GAP-A1) — all Phase 1, 🔷 Deferred for further review — present in the archive but in NO phase log. Added all 7 to `eks/log/phase1/p1_issue_log.md` (9-column format, Tasks `—`, severity marker normalized 🔵→🟢). Status Summary: Deferred 8→15, total 245→252. Priority table row 6 added (Appendix D docs-sync gaps). Root logs verified synced: `eks/log/update_log.md` (retired stub U202–U205) ⊆ p1_update_log.md; `eks/archive/log/update_log.md` and `test_log.md` U-/TL-IDs ⊆ phase logs; no root task_log.md exists (p1_task_log.md holds all 392 Phase 1 task tags). | Franklin | ✅ Done |
| U246 | 2026-07-31 | Phase 1 | T1.197, I265 | **T1.197 COMPLETE — Documentation, Traceability & Regression Testing (I265 chain closed)**: L.9.3 wording amendment (T1.194 D2 — Phase A auto-detect over `registry.project_codes`, no committed assignment in Phase A). Cross-workplan audit — P1.1 (load_all output → `project_definition_config`; module inventory + ProjectDefinitionResolver row), Appendix F/G/H (I265 alignment notes), knowledge.json (key_modules + data_flows), Appendix L (L.9.3 amendment). Migration guide created — `docs/project_definition_migration_guide.md` (+ §10 index routing row). Regression cleanup — `test_config_version_bumped` stale assertions corrected (doc config v1.8.0, doc base v1.10.0); pre-existing failure count 5→4. Task log status summary recounted (365 ✅ / 27 🔷 / 3 ⛔ / 1 🔴 = 396). I265 → 📐 Aligned — Project Definition is the authoritative project SSOT; RuntimeProjectConfiguration is the only runtime configuration. Tests: TL032 — full suite 474 passed / 4 pre-existing. | Franklin | ✅ Done |
| U247 | 2026-07-31 | Phase 1 | T1.197, I273 | **I273 RESOLVED — file_type allowlist regression (I253 recurrence)**: Root cause — `registry.py` `_get_column_allowlist()` static fallback (used when the doc base schema cannot be loaded, e.g. CLI run from a non-root CWD) omitted `file_type`; `register_document()` filters INSERT columns by the allowlist, silently dropping `file_type` → every row stored NULL → Phase B "No parser registered for file type: " → `failed (conf=None)`. Reproduced: 711 docs registered in one run, 680 failed. I253's 2026-07-27 "delete stale DB" fix only masked it. **Fix**: (1) added `file_type` to the static fallback allowlist (registry.py); (2) `_resolve_phase_b_files()` (pipeline_orchestrator.py) derives file_type from the file-path extension when the stored value is NULL/empty — repairs legacy rows without DB migration; (3) regression test `test_register_document_persists_file_type` (round-trip + forced-fallback assertion, test_phase1.py). Tests: TL033 — full suite 475 passed / 4 pre-existing (unchanged). Note: TestPhase1 setUpClass deletes `eks/output/eks_registry.db` (test artifact per §21) — the run containing today's real-data rows was re-initialized; rows are re-derivable by re-running Phase A against `eks/data/twrp` (files on disk) and will now persist file_type correctly. | Franklin | ✅ Done |
| U248 | 2026-07-31 | Phase 1 | I274, T1.200–T1.202 | **I274 logged + T1.200–T1.202 added (awaiting approval)**: Follow-up to I273 — `COLUMN_ALLOWLIST` hardcoded static fallback violates AGENTS.md §16 (hardcoded duplicate of the schema-derived list; drifted once → I273). Approved approach (Option A): T1.201 — CWD-independent doc base schema path via `global_paths`/`resolve_paths()` (replace hardcoded `Path("eks/config")`, §15); T1.200 — remove the hardcoded fallback, schema-derived set is sole SSOT, descriptive error on genuine schema absence (§16); T1.202 — drift-guard regression tests (allowlist == schema-derived set from non-root CWD + descriptive-error assertion). **Empty file_type fix already done** (I273/U247/TL033 — fallback + Phase B derivation). Task log: Planned 27→30, total 396→399. Issue log v52 (I274 🔴 Open, total 254). All T1.200–T1.202 🔷 Planned — awaiting approval before implementation. | Franklin | ✅ Done |
| U240 | 2026-07-31 | Phase 1 | T1.194, I265 | **Runtime modules migrated to ProjectConfigurationRegistry slice injection**: FilenameParser (rev 1.3.0) — `project_code` on FilenameParseResult via `_active_code`, excluded from metadata dict. FileScanner (rev 1.8.0) — `project_config_registry` DI; D2 `project_code_registry` from `registry.project_codes` (auto-detect preserved, registry wins), `_registry_code_titles()` merges registry `project_name`. PipelineOrchestrator (rev 1.0) — `project_config_registry` param, `_slice_for_orchestrator()` single-project shortcut, `_resolve_project_context()` per-file Phase B context, forwards slices to ColumnProcessor + RevisionManager. EKSColumnProcessor (rev 0.3) — `runtime_slice` param + `from_doc_config()`, code_to_title slice fallback. FilePropertyExtractor (rev 0.3), ParserRouter — optional `runtime_slice`. RevisionManager (rev 1.1) — `runtime_slice` + per-call override + revision_scheme logging. Call sites wired: runner.py, discovery_cli.py, phase1_server.py. Created eks/test/test_runtime_slice_injection.py — 21 tests. Full suite 413 passed, 5 pre-existing failures unchanged. | Franklin | ✅ Done |
| U239 | 2026-07-31 | Phase 1 | T1.193 | **ProjectDefinitionResolver implemented**: Created `eks/engine/core/project_definition.py` — RuntimeProjectConfiguration (17 immutable domain dataclasses), ProjectConfigurationRegistry (frozen dict), ProjectDefinitionResolver (6-step workflow: load → resolve → validate → merge → construct → register), _FrozenDict wrapper, Configuration Slice Principle accessor for 13 modules. Wired into EKSBootstrapManager — _resolve_project_definitions() called after P3_registry, stored as project_config_registry, exposed via to_dict() and to_pipeline_context(). Updated eks/engine/core/__init__.py — exports all 20 new symbols. Created eks/test/test_project_definition.py — 28 tests across 5 test classes, all passing. | Franklin | ✅ Done |
| U229 | 2026-07-29 | Phase 1 | — | **Appendix E v0.11 — E13 4-stage schema lifecycle documented**: Added E13 (Discover → Load → Validate → Extract) with 3-tier discovery cascade per file, bootstrap P7_schema integration plan, common/ library references, and per-project Stage 4 extraction rules. Updated E7 compliance checklist (discovery tier, Stage 4, bootstrap). Updated E8 sequence diagram to show bootstrap + common/ orchestration. Updated E9 step 6 for _STEM_TO_ATTR mapping. | opencode | ✅ Done |
| U227 | 2026-07-28 | Phase 1 | I256 / T1.162 | **I256 RESOLVED — Regression test for project_title population**: **(T1.162 — §72)** Added `test_filename_parser_populates_project_title` in `test_phase1.py`. Three sub-tests: (1) known project code `131101` → `project_title="WSD11 — Project Specifications"`. (2) another code `999999` → `project_title="Unknown Project"`. (3) fallback pattern → `project_title=None`. Also validates `document_type` still extracted (no regression). All 3 sub-tests pass. Full suite: 327/332 pass (5 pre-existing failures). Updated test_log (TL021 added). | opencode | ✅ Done |
| U228 | 2026-07-28 | Phase 1 | I256 / T1.161 | **I256 — cover_page element project_title extraction added as highest priority**: Extended T1.161 project_title write-back to extract from cover_page structural element first (before parser metadata and code→title lookup). StructureDetector._detect_cover_page() already extracts project_title via regex from first page. Added `cover_project_title` variable to element loop in `_process_file()`. Priority chain: cover page element (regex) > parser metadata > project_code_titles lookup > Phase A value. 4 documents now have "TUAS WATER RECLAMATION PLANT" extracted. project_title column confirmed in extraction_results.csv export. | opencode | ✅ Done |
| U226 | 2026-07-28 | Phase 1 | I256 / T1.160, T1.161 | **I256 — PipelineOrchestrator passes project_code_titles + I252 block extended**: **(T1.160 — §72)** `PipelineOrchestrator` now reads `project_code_titles` from `doc_config.get("project_code_titles", {})` and passes to `FilenameParser` constructor call. **(T1.161 — §72)** Extended the I252 identity write-back block with 3-tier priority for `project_title`: (1) cover sheet metadata (`metadata.get("project_title")`), (2) code→title lookup from `doc_config["project_code_titles"]` when `project_number` is in `registry_props`, (3) existing Phase A value from `doc.get("project_title")`. `pipeline_orchestrator.py` rev 0.8→0.9. Related: `filename_parser.py` rev 1.1.0→1.2.0. | opencode | ✅ Done |
| U225 | 2026-07-28 | Phase 1 | I256 / T1.160 | **I256 — FileScanner passes project_code_titles to FilenameParser**: **(T1.160 — §72)** `FileScanner.__init__()` now reads `project_code_titles` from `self.doc_config.get("project_code_titles", {})` and passes as fourth param to `FilenameParser()`. `file_scanner.py` rev 1.6.0→1.7.0. | opencode | ✅ Done |
| U224 | 2026-07-28 | Phase 1 | I256 / T1.160 | **I256 — FilenameParser project_title population**: **(T1.160 — §72)** (1) Added `project_title: Optional[str] = None` to `FilenameParseResult` dataclass. (2) Included `"project_title": self.project_title` in `to_metadata_dict()` output. (3) Added `project_code_titles: Optional[Dict[str, str]] = None` param to `FilenameParser.__init__()`, stored as `self._project_code_titles`. (4) In `_extract_segments()`, after `setattr(result, "project_number", raw_value)` (when `maps_to == "project_number"`), looks up `self._project_code_titles.get(raw_value)` and sets `result.project_title` if found. (5) Updated `parse_filename()` wrapper to pass through `project_code_titles`. `filename_parser.py` rev 1.1.0→1.2.0. | opencode | ✅ Done |
| U223 | 2026-07-28 | Phase 1 | I256 / T1.159 | **I256 — SchemaLoader registers eks_project_code_schema**: **(T1.159 — §72)** Added `"eks_project_code_schema": "project_code_schema"` to `_STEM_TO_ATTR` in `schema_loader.py`. Added `self.project_code_schema: Dict[str, Any] = {}` in `__init__`. In `load_all()` post-load section, injects `project_code_titles` dict into `doc_config` — maps each `projects[].code` → `projects[].description` (3 entries: `131101`, `131242`, `999999`). Also updated `eks_doc_setup_schema.json` to allow the runtime-injected `project_code_titles` as an optional `object` property (6-digit code keys → string values). | opencode | ✅ Done |
| U215 | 2026-07-27 | Phase 1 | I238 / T1.124–T1.126 | **I238 RESOLVED — Phase A batch milestones**: **(T1.124 — §61)** Downgraded per-document STATUS to INFO in `registry.py:629` — `logger.status`→`logger.info`. Per-doc messages now only show at `--level 2+`. **(T1.125 — §61)** Added 4 batch milestones (25%/50%/75%/100%) in `file_scanner.py:register_placeholders()` loop — emits `logger.status("[TELEMETRY] A-registration: milestone=X% files=Y")` at each threshold, matching Phase B pattern. **(T1.126 — §61)** Added 2 tests in `test_phase1.py`: `test_phase_a_batch_milestones_emitted` (asserts all 4 milestones appear for 8 files) and `test_phase_a_per_document_info_not_status` (asserts `registered successfully` at INFO level, not STATUS). Full suite: 323/323 pass. Updated issue_log (I238→✅ Resolved, approved 1→0, resolved 117→118), task_log (T1.124–T1.126→✅ COMPLETE), test_log (TL018 added). | opencode | ✅ Done |
| U216 | 2026-07-27 | Phase 1 | I248 / T1.148 | **I248 RESOLVED — Pipeline batch health scoring wired**: Added `self.scorer.score_batch(all_docs)` call at end of `run_phase_b()` in `pipeline_orchestrator.py`. Queries registry for all documents, computes `avg_document_health` and status breakdown (success/partial/failed), appends to Phase B summary dict. Also closes I244 (verbosity noise — all 7 per-doc info→debug, severity bumps, record-before-gate), I247 (metadata counts corrected via I112), I249 (logger reconcile + record-before-gate). I245 and I246 marked ⛔ Won't Implement. Updated issue_log (I244+I247+I249+I248→✅ Resolved, I245+I246→⛔, resolved 121→124, open 14→9, wont-imp 3→5), task_log (T1.148→✅ COMPLETE). | opencode | ✅ Done |
| U217 | 2026-07-27 | Phase 1 | I250 / T1.149 | **I250 LOGGED — Document type schema extraction**: Proposed extraction of inline `document_type_registry` to standalone `eks_document_type_schema.json` following facility/discipline/department pattern. Add `document_type_entry_def` to `eks_doc_base_schema.json`, create schema with 15 codes, update `eks_doc_config.json` to `$ref`. Blocked on approval. | opencode | 🔷 PLANNED |
| U218 | 2026-07-27 | Phase 1 | I251 / T1.150–T1.152 | **I251 LOGGED — Document type schema pipeline wiring**: Three tasks: (1) Register new schema in SchemaLoader load chain (T1.150). (2) Replace inline `document_type_registry` in config with `$ref` (T1.151). (3) Update FilenameParser to use schema-sourced `_doc_type_codes` (T1.152). Blocked on I250 completion. | opencode | 🔷 PLANNED |
| U219 | 2026-07-27 | Phase 1 | I252 / T1.153–T1.155 | **I252 LOGGED — Phase B identity field write-back**: Phase B never writes back project_number, area, discipline, document_type to DB. Three tasks: (1) Extract identity fields from PDF parser metadata in Phase B (T1.153). (2) Add identity fields to `_update_doc_status()` extra_properties with COLUMN_ALLOWLIST check (T1.154). (3) Priority chain for document_type: cover sheet > filename segment > extension inference (T1.155). | opencode | 🔷 PLANNED |
| U222 | 2026-07-28 | Phase 1 | I255 / T1.157, T1.158 | **I255 RESOLVED — FilenameParser auto-pattern detection**: **(T1.157 — §71)** Removed `project_code` param from `FilenameParser.__init__()`; added `project_code_registry: Optional[List[str]]` param. On each `parse()` call, `_detect_pattern(stem)` iterates all registered project codes, splits stem by common separator, checks if first segment matches a code — uses matching pattern or falls back to `"*"` (0 segments). `filename_parser.py` rev 1.0.0→1.1.0. Updated call sites: `FileScanner` (file_scanner.py rev 1.5.0→1.6.0) and `PipelineOrchestrator` (pipeline_orchestrator.py rev 0.7→0.8) — both derive `project_code_registry` from `filename_patterns` keys (minus `"*"`). Fixed pre-existing finalization bug in `parse()` where 0-segment `"*"` pattern produced `parse_status="ok"` instead of `"unresolvable"` (T1.157 addendum). **(T1.158 — §71)** Added 2 regression tests in `test_phase1.py`: `test_filename_parser_auto_detects_131101_pattern` (asserts all 4 identity fields extracted when first segment matches `131101`) and `test_filename_parser_falls_back_to_star_pattern` (asserts all identity fields `None` and `parse_status="unresolvable"` when filename has no matching project code). Both pass. Full suite: 326/331 pass (5 pre-existing unrelated failures). Updated issue_log (I255→✅ Resolved, open 10→9, resolved 129→130), task_log (T1.157→✅ COMPLETE, T1.158→✅ COMPLETE), test_log (TL020 added). | opencode | ✅ Done |
| U221 | 2026-07-28 | Phase 1 | I254 / T1.156 | **I254 RESOLVED — Path doubling fix**: In `bootstrap.py:_bootstrap_params()`, strip `eks_root` prefix from relative CLI `--data-dir` paths before combining. If `cli_str` starts with `"eks/"`, strip it; if equal to `"eks"`, use empty string. Absolute paths unchanged. 3 regression tests added (`test_path_doubling_prevents_eks_eks_data_dir`, `test_path_doubling_handles_bare_data`, `test_path_doubling_handles_absolute_path`). `bootstrap.py` rev 0.4→0.5. Updated issue_log (I254→✅ Resolved, open 10→9, resolved 128→129), task_log (T1.156→✅ COMPLETE), test_log (TL019 added). | opencode | ✅ Done |
| U220 | 2026-07-27 | Phase 1 | I250+I251+I252 / T1.149–T1.155 | **I250+I251+I252 RESOLVED**: Created `eks_document_type_schema.json` (15 codes) + `document_type_entry_def` in doc base. Registered in SchemaLoader. Updated setup schema to `$ref` new definition. Identity fields (project_number, area, discipline, document_type) now written back in Phase B via `extra_properties` with priority chain: cover sheet > filename > extension. `_infer_doc_type()` made conditional. | opencode | ✅ Done |
| U214 | 2026-07-27 | Phase 1 | I237 / T1.120–T1.123 | **I237 RESOLVED — Schema-driven telemetry verbose**: **(T1.120 — §60)** Added `telemetry_verbose` to `system_parameters_def` in `eks_base_schema.json` v1.11.0 with default `true` and `required` entry. **(T1.121 — §60)** Added `"telemetry_verbose": true` to `eks_config.json` v1.9.0. **(T1.122 — §60)** Added `telemetry_verbose` param to `PipelineOrchestrator.__init__` (default True), passed to `TelemetryHeartbeat(verbose=telemetry_verbose)`. Updated all 3 call sites: `runner.py` (context + non-context paths) and `discovery_cli.py`. **(T1.123 — §60)** Added 2 tests in `test_phase1.py` — `test_telemetry_verbose_true_prints_milestones` and `test_telemetry_verbose_false_suppresses_milestones` — verifying milestone `[TELEMETRY]` output appears/suppresses per setting. Full suite: 321/321 pass. Updated issue_log (I237→✅ Resolved, open 14→13, approved 1→0, resolved 117→118), task_log (T1.120–T1.123→✅ COMPLETE), test_log (TL017 added). | opencode | ✅ Done |
| U213 | 2026-07-27 | Phase 1 | I237 / T1.120–T1.123 | **I237 TASKS LOGGED — Schema-driven telemetry verbose**: **(T1.120 — §60)** Schema: add `telemetry_verbose` to `system_parameters_def` in `eks_base_schema.json`. **(T1.121 — §60)** Config: add `telemetry_verbose: true` to `eks_config.json`. **(T1.122 — §60)** Code: pass `telemetry_verbose` through bootstrap → runner → PipelineOrchestrator → TelemetryHeartbeat. **(T1.123 — §60)** Testing: add milestone visibility regression tests. Updated issue_log (I237→🟢 Approved, open 15→14, approved 0→1), task_log (T1.120–T1.123→🔷 PLANNED, §33 added). | opencode | 🔷 PLANNED |
| U212 | 2026-07-27 | Phase 1 | I236 / T1.118, T1.119 | **I236 RESOLVED — ERROR_FILE_PROCESSING kwarg mismatch fix + 2 regression tests**: **(T1.118 — §59)** Changed `pipeline_orchestrator.py` line 381 from `error=str(e)` to `detail=str(e)`. Grep confirmed sole `mm.show("ERROR_FILE_PROCESSING"` call site. Grep for `.show(.*error=` across engine found zero other mismatches. Template in `eks_message_config.json` kept as SSOT. **(T1.119 — §59)** Added 2 tests to `test_t132_modules.py::TestMessageManager`: `test_error_file_processing_hydrates_detail` (asserts hydrated message contains actual exception text and no literal `{detail}`), `test_error_file_processing_wrong_kwarg_fallback` (regression guard — asserts `error=` still produces raw `{detail}`). `pipeline_orchestrator.py` rev 0.6→0.7. Full test suite: 142/142 pass (91 + 51 — zero regressions). Updated issue_log (I236→📐 Aligned, open 10→9, aligned 72→73), task_log (T1.118/T1.119→✅ COMPLETE), test_log (TL016 added). | opencode | ✅ Done |
| U211 | 2026-07-27 | Phase 1 | I235 / T1.103, T1.116, T1.117 | **I235 RESOLVED — Milestone ordering fix + 3 regression tests**: **(T1.116 — §58)** Folded `1.0` into `BATCH_MILESTONES = {0.25, 0.50, 0.75, 1.0}`, removed separate `pct >= 1.0` block, updated label to `"100%" if m == 1.0` and `files: idx + 1` for 100% milestone inside the sorted loop. `last_milestone_pct` now set to `1.0` inside loop — no stale tracker. **(T1.103/T1.117 — §53/§58)** Added 3 tests via `_run_phase_b_with_milestone_mock` helper: 4-file batch (asserts 25%→50%→75%→100% order), 1-file batch (asserts no duplicates), 2-file batch (asserts 75% before 100%). Full test suite: 91/91 pass (88 existing + 3 new). Updated issue_log (I235→✅ Resolved, open 11→10), task_log (T1.103/T1.116/T1.117→✅ COMPLETE), test_log (TL015 added). | opencode | ✅ Done |
| U210 | 2026-07-24 | Phase 1 | I235 + I236 / T1.103, T1.116–T1.119 | **I235 + I236 TASKS LOGGED**: Added 4 new tasks and sharpened T1.103. **(T1.103 updated — §53)**: Spec sharpened to target `_forward_telemetry` (not `TelemetryHeartbeat.track()`); added three-case assertions (4-file, 1-file, 2-file); linked to I235; dependency changed to T1.116. **(T1.116 — §58 new)**: [Code] Fix milestone ordering — fold `1.0` into `BATCH_MILESTONES = {0.25, 0.50, 0.75, 1.0}`, remove separate `pct >= 1.0` block (lines 397–401), update label/files in loop. **(T1.117 — §58 new)**: [Testing] Full suite regression check + implement T1.103 milestone-order assertions (3 cases). **(T1.118 — §59 new)**: [Code] Fix `ERROR_FILE_PROCESSING` call site — rename `error=str(e)` → `detail=str(e)` at `pipeline_orchestrator.py` line 378; grep-confirm sole occurrence. **(T1.119 — §59 new)**: [Testing] Add test asserting hydrated message contains actual exception text, not literal `"{detail}"`. Status Summary: Planned 44→48, Total 315→319. | opencode | ✅ Done |
| U209 | 2026-07-24 | Phase 1 | I235 + I236 | **I235 + I236 LOGGED — I229 evaluation and messaging audit findings**: **(I235)** Logged logic order bug in `run_phase_b()` (pipeline_orchestrator.py lines 395–408): the `pct >= 1.0` block fires the 100% telemetry checkpoint before the `BATCH_MILESTONES` loop runs, causing all 3 intermediate milestones (25%/50%/75%) to also fire on the last file for small batches — up to 4 redundant checkpoints per final tick, defeating I229. `last_milestone_pct` also never updated to 1.0 after 100% fires. T1.103 (dedicated milestone test) listed in task_log but absent from `test_phase1.py`. **(I236)** Logged kwarg mismatch in `run_phase_b()` line 378: `mm.show("ERROR_FILE_PROCESSING", filename=..., error=str(e))` passes key `error` but template `"Error processing {filename}: {detail}"` expects `detail`. `BaseMessageManager` silently falls back to raw template — every file error prints literal `"{detail}"` instead of the exception message. Level=0 so this broken output fires at all verbosity levels. Updated `p1_issue_log.md` v16→v17 (Open 9→11, Total 207→209). | opencode | ✅ Done |
| U208 | 2026-07-24 | Phase 1 | I234 Alignment | **I234 DOCUMENTATION ALIGNMENT COMPLETE**: Aligned all details of issue `I234` across documentation files. Recalculated `p1_issue_log.md` Status Summary counts for actual 207 issue rows (repaired stale layout counts). Updated `appendix_p1.3_phase1_data_export.md` v1.4, `appendix_p1.2_phase1_scope.md` v1.3, and `appendix_p1.1_phase1_architecture.md` v1.7 to include `I234` in caveat tables, issue indexes, and cross-references. Created task-specific workplan `phase_1_alignment_workplan.md` v1.1 and retired it to `eks/archive/workplan/` folder upon completion. | opencode | ✅ Done |
| U207 | 2026-07-24 | Phase 1 | I234 / T1.112–T1.115 | **I234 COMPLETE — CLI default pipeline output: schema-driven export, pipeline_output.json, debug_log.json**: **(T1.113)** Added `export_default` to `system_parameters_def` in `eks_base_schema.json` v1.10.0 with enum `["csv", "xlsx", "both", "none"]` default `"both"`. Added `"export_default": "both"` to `eks_config.json`. Removed hardcoded `default="none"` from `_EKS_CORE_ARG_SPECS` and `build_parser()` in `cli.py` — CLI now reads default from `mgr.effective_parameters` at runtime. **(T1.112)** Writes `pipeline_output.json` (single-overwrite) from CLI `main()` after pipeline completes — job_id, timestamp, status, summary, exported_files. **(T1.114)** Writes `debug_log.json` (single-overwrite) from `logger.debug_object`. **(T1.115)** All 5 output files (`pipeline_output.json`, `debug_log.json`, 3 CSVs) generated by default. Updated issue_log (I234→🟢 Approved), task_log (T1.112–T1.115→✅ COMPLETE). | opencode | ✅ Done |
| U206 | 2026-07-24 | Phase 1 | I225 / T1.99.191 | **T1.99.191 COMPLETE — SchemaToDDL auto-DDL + schema version tracking**: Bootstrap P7 stores pre-generated DDL (documents_ddl, elements_ddl, indexes, doc_base_schema). DocumentRegistry accepts pre_generated_ddl param to skip schema re-load. _ensure_schema_version() tracks DDL hash in _eks_schema_meta table. runner.py + CLI tools pass DDL through. 4 new tests (test_registry_with_pre_generated_ddl, test_schema_version_tracking, test_bootstrap_pre_generated_ddl, test_registry_pre_generated_ddl_uses_bootstrap_ddl). 88/88 pass, no regressions. Updated issue_log (I225→✅ Resolved), task_log (T1.99.191→✅ COMPLETE), test_log (TL013 added). | opencode | ✅ Done |
| U205 | 2026-07-24 | Phase 1 | I212 / T1.99.179 | **T1.99.179 COMPLETE — detect_supersession() implementation in RevisionManager**: Implemented detect_supersession(document_number, revision) in revision.py — queries registry for same document_number, identifies is_latest, compares revisions via _compare_revisions() (numeric/alphabetic), returns structured dict. Wired into PipelineOrchestrator._process_file() after successful parse+score, using parsed revision from metadata (fallback to Phase A revision). 5 new tests. 296/310 pass (14 pre-existing rdflib failures). Updated issue_log (I212→✅ Resolved), task_log (T1.99.179→✅ COMPLETE), test_log (TL012 added). | opencode | ✅ Done |
| U204 | 2026-07-24 | Phase 1 | I211 residual, I214 / T1.99.198–199 | **T1.99.198 COMPLETE — Dead factories removed from factories.py**: HealthScorerFactory and StructureDetectorFactory had zero instantiation sites. Both removed. ParserFactory retained (actively used by ParserRouter). Dead `_engine_map`/`_parser_map` declarations cleaned up. Project-wide grep: zero remaining imports of removed classes. **T1.99.199 COMPLETE — HealthInput/Output contract wiring activated**: PipelineOrchestrator._process_file() now constructs HealthInput with doc/elements and calls self.scorer.score_from_input(health_input) instead of raw self.scorer.score(). Full HealthOutput consumption with backward-compatible score dict via hout.metadata/hout.overall. 291/305 pass (14 pre-existing rdflib failures). Updated issue_log (I212/I214 factory assessment), task_log (T1.99.198–199→✅ COMPLETE), test_log (TL011 added). | opencode | ✅ Done |
| U203 | 2026-07-24 | Phase 1 | I211 / T1.182.1–8, T1.99.183 | **I211 RESOLVED — DI factory SSOT compliance + PipelineOrchestrator factory wiring**: Removed duplicate `Factory` ABC from `factories.py` (lines 22–41); all 4 factory classes now inherit from `common.library.utility.factories.base_factory.Factory`. Manual `importlib` replaced with `self._load_class()` from common across `ParserFactory`, `StructureDetectorFactory`, `EngineFactory`. `HealthScorerFactory` replaced direct import with `self._load_class()`. `EngineFactory` gained `FileScanner`/`HealthScorer`/`StructureDetector` mappings for DI. `PipelineOrchestrator.__init__` now creates `EngineFactory(config_registry=config)` and uses it: `self.scanner = self._engine_factory.create("FileScanner", ...)`, `self.scorer = self._engine_factory.create("HealthScorer", ...)`, `self.detector = self._engine_factory.create("StructureDetector", ...)`. Fixed case-sensitivity bug in `EngineFactory.create()` (removed spurious `.lower()` call). All 114 test_phase1 + test_io_contracts tests pass; 19 schema/discovery/path tests also pass. Updated issue_log (I211→✅ Resolved), task_log (all 9 tasks→✅ COMPLETE), test_log (TL010 added). | opencode | ✅ Done |
| U197 | 2026-07-22 | Phase 1 | P1.3 SSOT cleanup | **P1.3 §5.3–§5.5 root cause/fix narrative stripped**: I188 Discovery + Root Causes & Fixes (Gap A/B/C narrative), I189 Discovery + Root Causes table + Fix Design table, I192 Problem/Fix/Result removed from P1.3 detail blocks. Only task tables + SC checklists retained per SSOT rule (issue detail lives only in `issue_log.md`). §5 intro updated to redirect to `issue_log.md`. I189 master table description simplified. Bumped P1.3 v1.1→v1.2. | opencode | ✅ Done |
| U198 | 2026-07-23 | Phase 1 | I227 / T1.100, T1.101 | **I227 APPROACH B IMPLEMENTED — Phase B reads file list from DuckDB instead of re-scanning filesystem**: Added `_resolve_phase_b_files()` method to `pipeline_orchestrator.py` — queries `self.registry.list_documents(latest_only=False)` for DuckDB SSOT, falls back to `self.scanner.scan()` when registry empty. Modified `run_phase_b()` to call `_resolve_phase_b_files()` instead of `self.scanner.scan()` directly. Bumped revision header 0.3→0.4. Added 2 regression tests: `test_phase_b_reads_from_registry_instead_of_rescan` and `test_phase_b_falls_back_to_scan_when_registry_empty`. Full test suite: 288 passed, 14 pre-existing failures (rdflib missing — unrelated). | opencode | ✅ Done |
| U199 | 2026-07-23 | Phase 1 | I232 / T1.106, T1.107 | **I232 RESOLVED — Legacy doc_id fallback removed; doc_id resolved once via file_path SSOT**: Added `get_document_by_file_path()` to `registry.py` (rev 0.7). In `_process_file()`, resolved `doc`/`doc_id` once at entry via registry lookup — removes stem-based fallback that diverged from Phase A synthetic keys. `_update_doc_status()` now requires `doc_id: str` (legacy filename-parse path deleted). All call sites updated. Added 3 tests: `test_get_document_by_file_path_found`, `test_get_document_by_file_path_not_found`, `test_get_document_by_file_path_synthetic_key_roundtrip`. Full Phase 1 test suite: 79/79 pass. | opencode | ✅ Done |
| U200 | 2026-07-23 | Phase 1 | I229 + I230 / T1.102, T1.104 | **I229 RESOLVED — Batch-level telemetry replaces per-file checkpoints in run_phase_b()**: Added BATCH_MILESTONES={0.25,0.50,0.75} with last_milestone_pct tracker. Telemetry fires at 25%/50%/75%/100% only — 7000+ file-level checkpoints reduced to 4 per run. Per-file error logging via ErrorManager unchanged. **I230 RESOLVED — validate_phase_transition() wired into run_full_pipeline()**: Method checks A→B (registry has docs with file_path) and B→C (extraction_confidence scores present). Non-blocking by default; logged as warnings; results in summary["gates"]. Updated issue_log (I229→Resolved, I230→Resolved), task_log (T1.102→COMPLETE, T1.104→COMPLETE), workplan (added §53/§54 index entries). Full test suite: 291/305 pass (14 pre-existing rdflib env failures). | opencode | ✅ Done |
| U201 | 2026-07-23 | Phase 1 | I231 / T1.108 | **I231 RESOLVED — Version SSOT: single `__version__` in eks/__init__.py**: Declared `__version__ = "2.6.0"` in `eks/__init__.py`. All 8 subpackage `__init__.py` files import `__version__` from `eks` instead of hardcoding. pyproject.toml and knowledge.json already at 2.6.0. Updated issue_log (I231→Resolved, counts: ✅115→116, 🔴16→15), task_log (T1.108→COMPLETE, §28 added), test_log (TL008 added), workplan (added §55 index entry). Pre-bootstrap `P2_paths` print now reads `eks.__version__`. Full test suite: 291/305 pass (14 pre-existing rdflib env failures — unchanged). | opencode | ✅ Done |
| U202 | 2026-07-23 | Phase 1 | I233 / T1.109, T1.110, T1.111 | **I233 RESOLVED — Pipeline monolith split into pipeline_engine/**: Created `pipeline_engine/` subfolder under `eks/engine/` with `cli.py`, `runner.py`, `exporter.py`. `eks_engine_pipeline.py` reduced from 1,284 to 295 lines (thin entry-point shell with re-exports). Zero module-level globals — all paths flow from `main()` via `_preload_infrastructure()` returned `project_root`. DCC-faithful `*_engine/` convention. Full test suite: 291/305 pass (14 pre-existing rdflib failures — unchanged). Updated issue_log (I233→Resolved, ✅116→117, 🟢1→0), task_log (T1.109/T1.110/T1.111→COMPLETE), test_log (TL009 added). | opencode | ✅ Done |
| U196 | 2026-07-22 | Phase 1 | P1.5 retirement | **Appendix P1.5 retired — SC+deliverables migrated to phase 1 logs**: `appendix_p1.5_phase1_checklists.md` archived to `eks/archive/workplan/` via `git mv` (history preserved). Foundation workplan §9 body rewrites redirects: Success Criteria → `p1_sc_log.md`, Deliverables → P1.2 §4. §10.2 table updated (SC→SC Log, deliverables→P1.2 §4). §11 P1.x table marks P1.5 retired with redirect paths. TOC link updated. `appendix_p1.6` cross-refs updated. All 3 retired appendices (P1.4, P1.5) now archived; no remaining appendix P1.x files in active `workplan/` directory. Bumped workplan v5.3→v5.4. | opencode | ✅ Done |
| U195 | 2026-07-19 | Phase 1 | I208–I225 / T1.99.179–193 | **Appendix E+F vs. Pipeline Architecture cross-source audit — 18 gaps documented**: Comprehensive comparison of Appendix E (Schema Design) + Appendix F (Pipeline Architecture Design) against current `eks/engine/` codebase revealed 18 gaps (I208–I225) across 5 categories: **(Wave 1 — Critical, 3 tasks)** I212 `RevisionManager` never wired (T1.99.179), I216 checkpoint/resume broken (T1.99.180), I224 Phase C read-only no review persistence (T1.99.181). **(Wave 2 — Architecture, 4 tasks)** I209 no EKS engine inherits `BaseEngine` (T1.99.182), I211 DI factories unused in orchestrator (T1.99.183), I215 dual telemetry unsynchronized (T1.99.184), I221 psutil import unguarded (T1.99.185). **(Wave 3 — IO/Data, 4 tasks)** I210 dual EngineInput/Output (T1.99.186), I214 HealthInput/Output unused (T1.99.187), I218 ParserInput empty defaults (T1.99.188), I219 extracted_content never written (T1.99.189). **(Wave 4 — Structure, 2 tasks)** I208+I220 folder migration to domain subdirs (T1.99.190), I225 SchemaToDDL unused in bootstrap (T1.99.191). **(Wave 5 — Docs/UI, 2 tasks)** I217 UI contracts unimplemented (T1.99.192), I222 Appendix E version staleness audit (T1.99.193). **(Deferred)** I223 per-engine CLI entry points → Phase 2. **Files**: `issue_log.md` +18 rows, `phase_1_foundation_workplan.md` +§49 (150 lines), `update_log.md` +U195. 21 existing files + 8 new files planned for implementation. | CodeBuddy | 🔷 Planned |
| U194 | 2026-07-19 | Phase 1 | I195–I201 / T1.99.163–168 | **§48 Waves 1-2 RESOLVED — 7 gaps closed across 6 files (Appendix D vs. Pipeline cross-source audit)**: **(GAP-D1 / T1.99.163)** `pipeline_orchestrator.py:643`: Fixed `self.scorer.score(doc, elements)` → `score(doc, structural_elements=elements)` — structural elements were silently misrouted as extraction_results. `review_manager.py:132`: Same fix. `test_t132_modules.py:104,115,134,135`: Updated all 4 `scorer.score()` calls with `structural_elements=` kwarg. **(GAP-D2 / T1.99.164)** `eks_message_config.json`: Added 10 missing message IDs: `STATUS_PHASE_A_START`, `STATUS_PHASE_A_COMPLETE`, `STATUS_PHASE_B_START`, `STATUS_PHASE_B_COMPLETE`, `STATUS_PHASE_C_START`, `STATUS_PHASE_C_COMPLETE`, `STATUS_PIPELINE_START`, `STATUS_PIPELINE_COMPLETE`, `ERROR_FILE_PROCESSING`; bumped total 39→49. Pipeline transitions now visible. **(GAP-D3 / T1.99.165)** `eks_error_config.json`: Registered 6 ad-hoc codes with correct severity: S-R-S-0407 (ERROR), S-R-S-0408 (ERROR), S-R-S-0409 (ERROR), P1-D-P-0003 (HIGH), P3-E-E-0018 (WARNING), P3-E-E-0019 (WARNING). Updated range counts (runtime 6→9, P1 discovery 2→3, P3 extract 17→19). Total 103→109. `pipeline_orchestrator.py`: Updated all 6 call sites from old codes to new registered codes. `test_t132_modules.py`: Updated `test_loads_catalog` assertion 103→109. **(GAP-D5 / T1.99.166)** `health_scorer.py`: Added 15 columns to ALL_SCOABLE tiers: T2 (+10), T3 (+5). Completeness scoring now covers all 39 schema columns. **(GAP-D6 / T1.99.167)** `health_scorer.py`: Added `"F": 0.0` to COVER_TYPE_SOURCE_SCORES — failed parses get zero source quality. **(GAP-D7 / T1.99.168)** `pipeline_orchestrator.py:_process_file()`: Wired `self.error_manager.get_health_impact(doc["id"])` after scoring; penalty applied via `adjusted = max(0.0, raw + penalty/100.0)`. **(GAP-D8 / I202)** `health_scorer.py`: Added `table` to Type A/B EXPECTED_ELEMENTS_BY_TYPE (4→5 expected elements). **Result**: 6 files modified, zero regressions (24 passed / 0 new failures), 2 Critical + 2 High + 3 Medium gaps closed. Docs-only gaps D4/D9-D13 deferred to T1.99.169–175. | CodeBuddy | ✅ Done |
| U193 | 2026-07-19 | Phase 1 | I194 / T1.99.161–167 | **I194 RESOLVED — 11-gap closure sweep across 6 files (Appendix B ↔ codebase cross-source consistency)**: **(GAP-1, GAP-2, GAP-5)** Appendix B updated: `id` format → UUID v4 (I186), PK description + B2 diagram corrected, `CAD` row added to B3.2, B6.2 scope rewritten. **(GAP-3)** `pipeline_orchestrator.py`: Added `registry.store_elements()` after detection+scoring. **(GAP-6)** `structure_detector.py`: Added `asset_tags` regex; `pipeline_orchestrator.py` extracts+persists asset_tags from cover page. **(GAP-7+GAP-10)** `pipeline_orchestrator.py`: `_update_doc_status()` now accepts optional `doc_id` — fast path avoids redundant filename parse. **(GAP-11)** `schema_to_ddl.py`: Applied `always_nullable` subtraction in migration DDL. **(GAP-4)** `registry.py`: Added PRAGMA-based NOT NULL diagnostic. **(GAP-9)** `registry.py` + `eks_doc_config.json`: Moved hardcoded `_BOILERPLATE_PREFIXES` to config SSOT. **(GAP-8)** `eks_engine_pipeline.py`: Wrapped copy in standalone try/except. **Result**: 6 files modified, 0 linter errors, 3 HIGH/MEDIUM gaps closed. | CodeBuddy | ✅ Done |
| U192 | 2026-07-19 | Phase 1 | T1.99.141–T1.99.146 | **All 15 Metadata Columns Implemented — 6 tasks complete**: Implemented all Phase 1 document metadata completeness tasks: **(T1.99.141)** Added `supersedes`/`superseded_by` columns + revision-chain logic in `register_document()` — queries previous-latest before INSERT, sets new.supersedes=prev.id and prev.superseded_by=new.id after INSERT. **(T1.99.142)** Added `document_title` column + derivation: embedded_title (skip boilerplate) → filename stem fallback → explicit override preserved. **(T1.99.143)** Added `lifecycle_stage` (enum: draft/issued_for_review/issued_for_construction/as_built/superseded/archived, default draft) + `revision_date` + `revision_description` columns. Pipeline orchestrator extracts revision_description from revision_table elements. **(T1.99.144)** Added `embedded_revision_number` column + DOCX `revision`→`embedded_revision_number` config mapping + `_REGISTRY_MAP` entry in FilePropertyResult. **(T1.99.145)** Added `references_documents` JSON column (default []) + JSON serialization in register_document() + ontology trigger `REFERENCES_DOC` in config. **(T1.99.146)** Added 7 columns: `project_phase`, `contract_package`, `issued_date`, `responsible_engineer`, `total_sheets` (defaults to page_count), `language` (default "en"), `vendor_name` — all nullable, config/code-level defaults. **Files modified**: `eks_doc_base_schema.json` (v1.4.0→1.6.0, +lifecycle_stage_code enum, +15 properties), `eks_doc_config.json` (v1.4.0→1.5.0, +DOCX revision mapping, +REFERENCES_DOC trigger), `registry.py` (v0.3→0.4, supersedes chain, title derivation, refs_docs serialization, total_sheets/language defaults, updated fallback allowlist), `file_property_parser.py` (+embedded_revision_number in _REGISTRY_MAP), `pipeline_orchestrator.py` (revision_desc extraction from revision_table elements), `test_t132_modules.py` (TestDocumentMetadataCompleteness: 20 tests covering all 15 columns + config validation). | CodeBuddy | ✅ Complete |
| U191 | 2026-07-19 | Phase 1 | I189 / T1.99.153–156 | **I189 RESOLVED — Stale output + test-production DB pollution fixed (4-layer fix)**: **(T1.99.153/F1)** Added optional `db_path` parameter to `DocumentRegistry.__init__` (registry.py v0.6) for test-isolated databases. **(T1.99.154/F2)** Export scoped to current-run docs: `main()` captures pre-run `document_number` set, post-run filters export to new docs only. **(T1.99.155/F3)** Per-run output subdirectories: exports write to `output/<run_id>/` (UUID subdir) instead of shared root. **(T1.99.156/F4)** Integration test uses `mock.patch.object` with temp DB `_IsolatedRegistry` — no production DB pollution, no production output overwrite. Cleaned up stale `eks/output/` files. Updated issue_log (I189→Resolved), workplan (§32.8→COMPLETE), update_log (U191). Full test suite: 36/36 green. | CodeBuddy | ✅ Done |
| U190 | 2026-07-19 | Phase 1 | I188 / T1.99.147–151 | **I188 RESOLVED — CSV/Excel export gap fix: 3 files now generate correctly**: **(T1.99.147)** `discovery_inventory` empty → removed `["pending"]` filter at L987 (`_build_export_rows(all_docs, None, discovery_cols)`). **(T1.99.148)** `review_flags` empty → changed L1126 `elif status != "success"` to `else:` (unconditional confidence-missing flag). **(T1.99.149)** Verified: `--export both` now produces all 3 CSV + 3 XLSX files. **(T1.99.150)** Added 7 new export tests (3 `_build_export_rows` variants, 2 `_build_flagged_rows` variants, 1 integration `main([--export both])`). **(T1.99.151)** Updated issue_log (I188→Resolved), workplan (§32.7→COMPLETE), update_log (U190). Full test suite: 36/36 green. | CodeBuddy | ✅ Done |
| U189 | 2026-07-18 | Phase 1 | I147–I162 / T1.99.128–140 | **I147–I162 RESOLVED — Appendix J File Property Extraction implemented across 4 layers / 13 tasks**: **(Layer 1 — Schema+Config)** T1.99.128: Added 3 new definitions (`file_property_source_def`, `file_property_pattern_def`, `file_property_os_def`) + 13 new optional columns to `eks_doc_base_schema.json` v1.4.0 `document_metadata_def`. T1.99.129: Replaced stub `file_property_patterns` in `eks_doc_setup_schema.json` v1.5.0 with full schema (`os_properties` + `by_file_type`). T1.99.130: Populated `file_property_patterns` in `eks_doc_config.json` v1.4.0 with OS config + 5 file-type mappings (pdf/docx/xlsx with `parser_metadata`, dgn/dwg with `os_only`). **(Layer 2 — Module+Errors)** T1.99.131: Created `eks/engine/core/file_property_parser.py` (rev 0.1) — `FilePropertyResult` dataclass (22 fields, `.to_registry_dict()`), `FilePropertyExtractor` class (two-layer extraction: `Path.stat()` + `hashlib` for Layer 1, per-config `property_mapping` for Layer 2), `extract_file_properties()` convenience function. Updated `__init__.py` v1.4.0 with exports. **This is the first code in EKS to use `os.stat`/`Path.stat`/`hashlib`.** T1.99.132: Extended parser `extract_metadata()` — `docx_parser.py`: added `last_modified_by`; `xlsx_parser.py`: added `last_modified_by`; `pdf_parser.py`: added `keywords`. T1.99.133: Registered 5 `P5-F-PROP-*` error codes in `eks_error_config.json` v1.3.0 (0001: file not found, 0002: stat failed, 0003: no metadata, 0004: mapping failure, 0005: hash failed); total_codes 98→103, data_logic_codes 40→45. **(Layer 3 — Pipeline Integration)** T1.99.134: Instantiated `FilePropertyExtractor` in `PipelineOrchestrator.__init__()`. T1.99.135: Inserted property extraction in `_process_file()` after metadata capture — calls `self._property_extractor.extract()`, passes `registry_props` as `extra_properties` to `_update_doc_status()`. **Closes the data-dropping gap.** T1.99.136: Replaced hardcoded 24-column INSERT in `registry.register_document()` with dynamic builder; added `extra_properties` parameter to `update_document_status()` with dynamic SET clause builder. **(Layer 4 — Health+Verify)** T1.99.137: Added `file_size`, `file_hash`, `embedded_title`, `embedded_subject`, `embedded_creator_app`, `embedded_producer` to `TIER_3_COLUMNS` and `ALL_SCOABLE`; `_score_source_quality()` receives +0.05 bonus when `embedded_creator_app` is non-null; `_score_consistency()` adds +1 violation when `file_modified_at` and `embedded_modified_date` diverge >24h. T1.99.138: Added `TestFilePropertyExtractor` class (12 tests: OS extraction, embedded mapping, null handling, file-not-found, os_only, to_registry_dict, no-op mode, class reuse, convenience function). T1.99.139: Pipeline verification — full build compiles, JSON valid. T1.99.140: Updated `update_log.md` (U189), `issue_log.md` (I147–I162→Resolved), `phase_1_foundation_workplan.md` (§43 added). **18 files changed** (1 new + 17 modified). | CodeBuddy | ✅ Done |
| U187 | 2026-07-18 | Phase 1 | T1.99.108–127 | **Option A2 implemented — Unified P5-Prefix Error Codes + Appendix I FilenameParser**: **(Phase A) T1.99.108**: Renamed 3 existing D5-PARSE string literals in `pipeline_orchestrator.py` to P5-format (`D5-PARSE-001→P5-F-V-0001`, `D5-PARSE-002→P5-F-S-0002`, `D5-PARSE-003→P5-R-P-0003`). **(Phase B) T1.99.110–112**: Added 4 shared definitions (`filename_pattern_def`, `filename_segment_def`, `filename_output_def`, `filename_error_subcodes_def`) to `eks_doc_base_schema.json` v1.3.0; added `filename_patterns` + `file_property_patterns` properties to `eks_doc_setup_schema.json` v1.4.0; added `filename_patterns` config block (2 entries: `"131101"` with 5-segment delimited TWRP pattern + `"*"` backward-compat catch-all) to `eks_doc_config.json` v1.3.0. **(Phase C) T1.99.113–114**: Created `eks/engine/core/filename_parser.py` — `FilenameParseResult` dataclass (9 fields) + `FilenameParser` class (schema-driven, caches compiled validators, 8-step algorithm, never raises) + `parse_filename()` convenience function; exported from `eks/engine/core/__init__.py` v1.3.0. **(Phase D) T1.99.115–117**: Migrated `FileScanner.build_placeholder_metadata()` to use `FilenameParser` instance, deleted old `_parse_filename()` method (L145–174); replaced 2 inline one-liners in `PipelineOrchestrator._process_file()` L577 and `_update_doc_status()` L655 with `self._parser.parse()`; verified `phase1_server.py` flows through migrated FileScanner. **(Phase E) T1.99.118–120**: L1 — `FilenameParseResult` all fields `Optional[str]` (design); L2 — `FileScanner.register_placeholders()` + `phase1_server.py` `_register()` generate synthetic `UNRESOLVED-{md5[:8]}` key instead of skipping; L3 — `registry.register_document()` generates synthetic key instead of raising `KeyError`. **(Phase F) T1.99.121–123**: Registered 5 new P5-F-* codes (`P5-F-V-0004..P5-F-P-0007`) in `eks_error_config.json` v1.2.0; added `phase_5_file_ops` range to `data_error_ranges` (count:12); updated `eks_error_setup_schema.json` `data_error_ranges.properties`; all error_subcodes in config use P5-format. **(Phase G) T1.99.124–125**: Added full `TestFilenameParser` test class (17 test methods covering A1–A4 subtypes, default _rev/dash-suffix, regex validation, segment count bounds, fallback_revision, schema_reference, to_metadata_dict, class reuse); updated `test_loads_catalog` assertion 93→98; all smoke tests pass. **(Phase H) T1.99.126–127**: Updated `update_log.md` U187; `issue_log.md` I133–I146+I155+I157+I163→✅ Resolved; workplan §42 status updated; `eks_error_setup_schema.json` added `phase_5_file_ops` property. **Files created**: `filename_parser.py` (1 new). **Files modified**: `pipeline_orchestrator.py`, `file_scanner.py`, `registry.py`, `phase1_server.py`, `__init__.py`, `eks_doc_base_schema.json`, `eks_doc_setup_schema.json`, `eks_doc_config.json`, `eks_error_config.json`, `eks_error_setup_schema.json`, `test_t132_modules.py`, `update_log.md`, `issue_log.md`, `phase_1_foundation_workplan.md` (16 files total). | CodeBuddy | ✅ Done |
| U186 | 2026-07-18 | Phase 1 | I133–I137 (Appendix I) | **Appendix I created — Schema-Driven Filename Parser (v0.1)**: Comprehensive design document created at `eks/workplan/appendix_i_filename_parser.md`. Covers: **(I1)** Overview & design principles; **(I2)** Problem analysis — 4 call sites with 2 divergent algorithms, root cause of D5-PARSE-003; **(I3)** TWRP filename analysis — all 171 files categorized into 3 families (A=standard 131101, B=cover pages, C=SOP/preamble), 4 known non-revision suffixes documented (_Add1/3, _2-Stage/stage, _HAC); **(I4)** Schema design — `filename_patterns` block with per-project (`131101` + `"*"`) entries, 6 configurable fields (strip_suffixes, revision_separators, dash_revision_max_len, fallback_doc_number, fallback_revision), JSON Schema validation rules for both setup and base schemas; **(I5)** Parsing algorithm — 6-step flow with worked examples for all TWRP filename subtypes; **(I6)** Shared parser module — `eks/engine/core/filename_parser.py` design with `parse_filename()` function signature and full implementation; **(I7)** Null-tolerant registration — 3-layer relaxation (parser → null returns, application → no skip, store → synthetic UNRESOLVED- key); **(I8)** Call-site migration map — 4 sites to update, 2 inline one-liners to remove; **(I9)** Per-project configuration guide with new-project add checklist; **(I10)** 10-task implementation plan with success criteria and risk table. Issues I133–I137 remain 🔴 Open pending review/approval of Appendix I. | opencode | 🔷 Planned |
| U185 | 2026-07-18 | Phase 1 | I127 / T1.99.95–100 | **I127 RESOLVED — Preload import gate hardening COMPLETE**: **(T1.99.95)** Extended existing `common.library.core.pipeline` guard in `_preload_infrastructure()` to also preload `EngineInput`/`EngineOutput`. **(T1.99.96)** Added new `common.library.cli` guard for `parse_cli_args`; stored on `EKSBootstrapManager._preloaded_parse_cli_args`; forwarded via `_eks_cli_parser()` → `parse_eks_cli(_parse_cli_args_fn=...)`. **(T1.99.97)** Added new `eks.engine.core.pipeline_orchestrator` guard for `PipelineOrchestrator`. **(T1.99.98)** Added new `eks.engine.core.registry` guard for `DocumentRegistry`. **(T1.99.99)** Added new `common.library.export` guard for `DataExporter`. **(T1.99.100)** Replaced all 6 bare `from ... import` in `main()` / `run_pipeline()` with preloaded `infra[key]` references; `run_pipeline()` accepts optional `_PipelineOrchestrator_cls`/`_DocumentRegistry_cls` with fallback to deferred import for non-preload callers; export `try/except ImportError` → `if None` gate. Updated all 3 return dicts in `_preload_infrastructure()`, docstring, and file revision (0.9→1.0). Updated `eks/engine/core/bootstrap.py` (rev 0.2→0.3): `EKSBootstrapManager.__init__` adds `_preloaded_parse_cli_args=None`; `_eks_cli_parser` forwards it. Test suite: 12/13 passed (1 pre-existing failure unrelated). | opencode | ✅ Done |
| U184 | 2026-07-18 | Phase 1 | I127 / T1.99.95–100 | **I127 LOGGED — Preload import gate audit: 6 bare imports in `main()` call chain not preload/bootstrap-guarded (🔷 PLANNED)**: Full audit of every `from ...` in `eks_engine_pipeline.py::main()` call graph vs `_preload_infrastructure()` + `bootstrap_all()` guards. Found 6 imports not in preload or bootstrap: **(G1)** L821 `EngineInput`/`EngineOutput` from same module as preloaded `TelemetryHeartbeat` — post-bootstrap crash risk. **(G2)** L515 `common.library.cli` `parse_cli_args` — in bootstrap but not preload. **(G3)** L246 `PipelineOrchestrator`, **(G4)** L247 `DocumentRegistry` — bare in `run_pipeline()`. **(G5)** L868 `DataExporter`, **(G6)** L874 `DocumentRegistry` (export) — locally guarded but inconsistent. Proposed T1.99.95–T1.99.100 in workplan §33: add all 6 to `_preload_infrastructure()`, replace bare imports with preloaded references. All 🔷 PLANNED pending review. No code changes — issue-log + workplan entries only. | opencode | 🔷 PLANNED |
| U183 | 2026-07-18 | Phase 1 | I126 / T1.99.87–94 | **I126 RESOLVED — L22 universal export module + EKS wiring**: **(T1.99.87–89)** Created `common/library/export/` (L22) — `DataExporter` with `export_to_csv()` (csv.DictWriter + BOM), `export_to_excel()` (openpyxl, auto-column-width, bold+frozen headers), `export_multi_sheet()` (multi-sheet workbook); error codes in `S-DE-*` range. Registered in `common/library/__init__.py`. **(T1.99.90)** Added 20 tests (csv/excel/multi-sheet round-trips, CJK/Unicode, edge cases). **(T1.99.92)** Added `--export {csv,xlsx,both,none}` flag to both `_EKS_CORE_ARG_SPECS` and `build_parser()` in `eks_engine_pipeline.py` (rev 0.9). **(T1.99.93)** Wired 3 export calls in `main()` after `run_pipeline()` returns — `discovery_inventory`, `extraction_results`, `review_flags` via `DataExporter` + `_build_export_rows`/`_build_flagged_rows` helpers. Export stays in `main()` (not PipelineOrchestrator) — design rationale in workplan §32.6. Zero changes to `_preload_infrastructure()`, `bootstrap.py`, `run_pipeline()`, `PipelineOrchestrator`. **(T1.99.94)** Added `GET /api/v1/export/{phase}/{format}` endpoint to `phase1_server.py` (rev 0.11) — phases: a/b/c/all, formats: csv/xlsx; returns file download with Content-Disposition. Updated `eks/log/issue_log.md` (I126→Resolved), `eks/workplan/phase_1_foundation_workplan.md` (v3.93). Zero new dependencies (openpyxl already in eks_config.json; csv is stdlib). | opencode | ✅ Done |
| U182 | 2026-07-18 | Phase 1 | I128 / I129 | **I128 + I129 RESOLVED — End-to-end pipeline run: 3 blocking bugs fixed**: **(I128a)** Added `bootstrap_universal` to `eks_error_setup_schema.json` `system_error_ranges.properties` (missing property caused `P1-BOOT-CONFIG` schema validation error at bootstrap P3). **(I128b)** Added expanded format string `S-{cat}-S-{id} \| P1-SETUP-{type}{id} \| P1-BOOT-{reason} \| B-{module}-{id}` to `eks_error_code_base.json` `error_catalog_metadata.system_format` enum (missing enum value caused secondary validation error). **(I129)** Modified `EKSBootstrapManager._bootstrap_registry()` in `eks/engine/core/bootstrap.py`: after loading main config, separately loads `doc_config` from SchemaLoader (previously only `self.config` was loaded; `self.doc_config` stayed `{}`, causing `file_type_registry` to be empty → FileScanner discovered 0 files). Updated P3 log message to include doc_config key count. **(I128c)** Updated `test_loads_catalog` expected `total_codes` from 72→93 (matches actual catalog after bootstrap_universal 15 codes). **Result**: Pipeline runs end-to-end — bootstrap P1–P8 all pass; Phase A discovers 2/2 test files (PDF+DGN); Phase B processes both; Phase C flags 10 documents; 283/288 tests pass (5 pre-existing failures unaffected). | opencode | ✅ Done |
| U181 | 2026-07-18 | Phase 1 | T1.99.64–69 | **(T1.99.64) Appendix D v0.4** — added Bootstrap (`B`) category to D3 with range `S-B-S-0600–0699`; documented `S-B-S-0601`/`0602` in D4; added `P1-BOOT-*` as setup/bootstrap hybrid format and `B-*` as universal bootstrap format in D2; added `P1-BOOT` (6 codes) + `B-*` (15 codes) data error catalog entries to D5; added 7 bootstrap messages to D6 (milestone, status, warning); updated D9 with bootstrap module references; bumped version 0.3→0.4. **(T1.99.65) `eks_error_config.json` v1.1.0** — added `bootstrap_universal` range; registered all 15 universal `B-*` codes (B-CLI-001 through B-UNK-002) under `system_errors` with FATAL severity / Bootstrap category; `total_codes` 78→93, `system_codes` 43→58; updated `migration_log`. **(T1.99.65b) `eks_error_code_base.json` v1.3.0** — updated `system_error_code_format` pattern to accept `B-[A-Za-z0-9_]+-[A-Za-z0-9]+` (covers dynamic `B-{phase_id}-ERR` patterns). **(T1.99.66) `eks_message_config.json` v1.1.0** — added 7 bootstrap messages: `MILESTONE_BOOTSTRAP_START`, `MILESTONE_BOOTSTRAP_COMPLETE`, `STATUS_CONFIG_LOADED`, `STATUS_PATHS_RESOLVED`, `STATUS_READINESS_PASSED`, `STATUS_MANAGERS_INITIALIZED`, `WARNING_BOOTSTRAP_PHASE_FAILED`; `total_messages` 33→39. **(T1.99.67) Format decision** — Option B selected: keep `P1-BOOT-*` as documented hybrid format (less churn, backward-compat, 6 codes already registered + tested). **(T1.99.68) `EKSBootstrapManager` (`bootstrap.py` rev 0.2)** — overrode P1–P5, P7 phases to use `P1-BOOT-CONFIG`/`P1-BOOT-PATHS` instead of universal `B-*` codes; overrode `bootstrap_all()`/`bootstrap_for_ui()` catch-alls (`B-UNK-*` → `P1-BOOT-READINESS`); overrode `preload_trace`/`postload_trace` properties (`B-BOOT-0601`/`B-CTX-001` → `P1-BOOT-CTX`). All 21 bootstrap error codes now resolve via `ErrorManager`. **(T1.99.69)** Logs/docs — `issue_log.md` I112 → ✅ Resolved; `update_log.md` U181; Appendix D fully updated; status legend counts in `issue_log.md` updated. | opencode | ✅ Done |
| U180 | 2026-07-18 | Phase 1 | I124 / T1.99.84–86 | **Pipeline output consolidation — single overwrite `pipeline_output.json`**: **(a)** `phase1_server.py` rev 0.10 — collapsed three per-job JSON writes (`pipeline_status_{job_id}.json` at L623, `pipeline_messages_{job_id}.json` at L633, `debug_log.json` via logger) into single overwrite `pipeline_output.json` at `eks/output/`. Contains `{job_id, timestamp, status, messages, errors, debug}`. No `{job_id}` in filename. Overwritten on every pipeline run — zero accumulation. **(b)** `eks_engine_pipeline.py` — commented out per-phase `save_checkpoint()` call in `_after()` closure (L314–317); checkpoint unused by resume logic, context held in-memory via `orchestrator.checkpoint_states`. **(c)** `pipeline_orchestrator.py` `run_full_pipeline()` — same treatment for internal `_after()` checkpoint writes (L455–458). **(d)** Deleted stale `debug_log.json` from `eks/output/`. **(e)** `issue_log.md` I124 → Resolved; workplan v3.88 → v3.89 with T1.99.84–86 marked ✅ COMPLETE. | opencode | ✅ Done |
| U179 | 2026-07-17 | Phase 1 | I123 | **P6 env check fixed — pip→import name mapping + rdflib installed**: Bootstrap P6 raised `P1-BOOT-ENV` with 3 failures: `python-docx`, `rdflib`, `qdrant-client`. Root causes: **(a)** `test_environment()` passed pip distribution names directly to `importlib.import_module()` — `python-docx` and `qdrant-client` are not valid Python module names (correct names: `docx`, `qdrant_client`). **(b)** `rdflib` was genuinely not installed (also absent from `eks.yml`). **Fixes**: **(1)** Installed `rdflib==7.6.0` (`pip install rdflib`). **(2)** Added `rdflib==7.6.0` to `eks.yml` under Phase 3: Knowledge Graph section. **(3)** Added `_PIP_TO_IMPORT` mapping dict and `_resolve_import_name()` helper to `common/library/core/system/tester.py` (rev 0.1 → 0.2); covers `python-docx→docx`, `qdrant-client→qdrant_client`, `pymupdf→fitz`, `pillow→PIL`, `scikit-learn→sklearn`, `beautifulsoup4→bs4`, `python-dateutil→dateutil`, `python-multipart→multipart`, `rank-bm25→rank_bm25`, `sentence-transformers→sentence_transformers`, `psycopg2-binary→psycopg2`; fallback replaces hyphens with underscores. All three import-test loops now call `_resolve_import_name()`. Bootstrap P1–P8 all pass; pipeline runs to Phase C completion. | opencode | ✅ Done |
| U178 | 2026-07-17 | Phase 1 | I122 | **Stale `eks/eks/` ghost directory archived — fixes B-REG-001 path-doubling root-discovery bug**: Bootstrap P3 raised `B-REG-001: Schema file not found: eks_base_schema.json. Searched: [.../eks/eks/config/schemas]`. Root cause traced to a legacy empty directory `eks/eks/` (contents: only `output/` subdir, no files). When running `python eks_engine_pipeline.py` from `eks/engine/`, `resolve_pipeline_base_path("engine")` strips the `engine` segment → `start = /Engineering-and-Design/eks`; `discover_project_root` checks `(start / "eks").is_dir()` → `/Engineering-and-Design/eks/eks` — **which existed** — so it incorrectly returned `start` (`/Engineering-and-Design/eks`) as the project root instead of falling through to `default_base_path`. Every downstream path resolved doubled the `eks` prefix (e.g. `eks/eks/config/schemas`). Fix: archived `eks/eks/` → `eks/archive/eks_stale_dir_2026-07-17`. With the ghost directory gone, `discover_project_root` correctly falls through to `default_base_path()` → returns `/Engineering-and-Design`. Bootstrap P1–P5 now pass; P6 fails on missing optional deps (python-docx, rdflib, qdrant-client — pre-existing I114 / P1-BOOT-ENV). | opencode | ✅ Done |
| U177 | 2026-07-17 | Phase 1 | I119 / I120 / I121 | **Runtime entry-point fixes — stdlib sys.path, absolute imports, guarded bootstrap manager**: Three issues discovered and resolved during direct `python eks_engine_pipeline.py` execution from `eks/engine/`. **(I119)** Added `_stdlib_find_repo_root()` — pure-pathlib walk using `eks/` + `common/` twin anchors — inserted at module level before any `common.library` try/except. Repository root now unconditionally on `sys.path`; the chicken-and-egg loop that caused 4 FATAL `No module named 'common'` errors is broken. **(I120)** Converted all 5 relative imports (`from .core.*`) in `bootstrap_pipeline()`, `run_pipeline()`, `_read_system_params()`, and `main()` to absolute form (`eks.engine.core.*`). Created `eks/__init__.py` so the `eks` top-level package is recognised. Fixes `ImportError: attempted relative import with no known parent package`. **(I121)** Moved `EKSBootstrapManager` import from bare line 756 in `main()` into `_preload_infrastructure()` **Step 1c** with its own `try/except ImportError` guard; returned as `_EKSBootstrapManager` in result dict; `main()` retrieves from `infra`. Updated docstring, all three return dicts, and `_EKSBootstrapManager = None` pre-binding. Fixed latent `NameError` on line 802: `TelemetryHeartbeat` → `_TelemetryHeartbeat`. Script now runs cleanly from `eks/engine/` directory with no FATAL or import errors. | opencode | ✅ Done |
| U176 | 2026-07-17 | Phase 1 | I118 / T1.99.84 | **`_preload_infrastructure()` v2 — true pure-stdlib + error safety net**: User review found two critical defects in v1. **(a) Silent errors**: import errors collected in `errors: list` but only printed in `main()`'s gate — if another caller used the function, errors silently lost. Also `debug_mode = early_verbosity.get(...)` at L613 ran unconditionally even if verbosity parse threw (`NameError` crash). **(b) Not pure-stdlib**: v1 called `_UniversalLogger(...)` + `_TelemetryHeartbeat(...)` + `hb.start()` inside the function — if `common.library` failed to import, the function crashed trying to instantiate classes it just guarded. **Fixes**: **(a)** All 6 failure points now `print(f"FATAL: {msg}", file=sys.stderr)` immediately at point of failure. All variables (`early_level`, `debug_mode`, `_safe_posix`, `_should_auto_create`, `_discover_root`, `_UniversalLogger`, `_TelemetryHeartbeat`) pre-bound to safe defaults BEFORE any step runs — no `NameError` possible. **(b)** `_preload_infrastructure()` no longer instantiates logger/heartbeat — returns imported **classes** (`_UniversalLogger`, `_TelemetryHeartbeat`). `main()` instantiates locally after preload gate: `logger = _UniversalLogger("eks-pipeline", level=early_level)` / `hb = _TelemetryHeartbeat(...)` / `hb.start()`. Removed `logger_name` parameter. **(c)** `issue_log.md` — I118 logged + resolved; I117 entry updated with v2 refinements. **(d)** `update_log.md` — U176 entry. | opencode | ✅ Done |
| U175 | 2026-07-17 | Phase 1 | I117 / T1.99.81–83 | **I117 documentation & log updates**: **(a)** `test_log.md` v0.2 — added TL003 (I108–I117 bootstrap/entry-point focused tests: 17 passed) and TL004 (full EKS regression: 265/277 pass — 12 expected failures from P1-BOOT-ENV missing optional deps python-docx/rdflib/qdrant-client). **(b)** `update_log.md` — U175 entry documenting this update cycle. **(c)** `appendix_h_bootstrap_module_design.md` v0.4 — added §H.12.1 Preload Infrastructure Guard (I117) with problem statement (chicken-and-egg), `_preload_infrastructure()` design, return dict schema, `main()` integration flow, and universal preload pattern note; updated H.1 index, H.3 file inventory (eks_engine_pipeline.py line count), H.12 main() flow diagram (added step 0.5), and revision header. **(d)** `test_eks_engine_pipeline.py` v0.7 — fixed `discover_project_root` import path (moved from `eks_engine_pipeline` to `common.library.paths.root_discovery` after lazy-import refactor). All documentation now consistent with `eks_engine_pipeline.py` v0.6. | opencode | ✅ Done |
| U174 | 2026-07-17 | Phase 1 | I114 / T1.99.80 v2 | **T1.99.80 v2 — Complete lazy-import refactor**: User review found remaining `common.library` imports at module level in `eks_engine_pipeline.py` (L52-70): `from common.library.core.paths.path_utils import detect_os, safe_posix, should_auto_create_folders`, `from common.library.paths.root_discovery import discover_project_root`, `from common.library.core.system import test_environment`. These lightweight stdlib-only imports were still at module level — if `common.library` is not importable (e.g. missing from PYTHONPATH), they fail before `test_environment()` runs. Fix: **(a)** Removed all three `from common.library.*` module-level import statements; module now only has stdlib imports (`argparse`, `json`, `sys`, `uuid`, `pathlib`, `typing`). **(b)** `safe_posix` + `should_auto_create_folders` + `discover_project_root` moved inside `main()` (alongside existing `UniversalLogger` + `TelemetryHeartbeat` deferred imports). **(c)** `discover_project_root` import-time usage (L77-98 try/except) changed to use a deferred import inside the try block (`from common.library.paths.root_discovery import discover_project_root as _dr`); falls back to `Path.cwd()` on failure; `main()` always calls `discover_project_root()` again inside the function body. **(d)** `detect_os` and `test_environment` were never directly called in `eks_engine_pipeline.py` (used only via `EKSBootstrapManager` which has its own imports) — removed from module level entirely. Result: truly no `common.library` import at module level; `test_environment()` in bootstrap P6 is the first common.library call path. | opencode | ✅ Done |
| U173 | 2026-07-17 | Phase 1 | I114 / T1.99.75–80 | **I114 RESOLVED — T1.99.75–80 implemented**: **(T1.99.75)** Created `common/library/core/system/` with universal `test_environment(dependencies: dict)` — stdlib-only (`importlib`, `platform`, `pathlib`), runs per-entry `importlib.import_module()`, returns `{ready, errors, required_modules, optional_modules, engine_modules, python_version, platform}`. **(T1.99.76)** Added `env_tester: Optional[EnvTester]` strategy hook to universal `BootstrapManager.__init__()` + `_bootstrap_env()` — calls after OS detection, backward-compat when not injected; added `_env_test_results` state attribute. **(T1.99.77)** Wired `EKSBootstrapManager._bootstrap_env()` to universal `test_environment()` — reads `dependencies` from `eks_config.json`, raises `BootstrapError("P1-BOOT-ENV", "Required dependencies missing: {list}. Run: conda activate eks", "env")`. **(T1.99.78)** Registered `P1-BOOT-ENV` (`BOOT_ENVIRONMENT_FAILED`, FATAL, Bootstrap) in `eks_error_config.json`; updated `bootstrap_p1` count 5→6, `total_codes` 77→78, `system_codes` 42→43; updated `migration_log`. **(T1.99.79)** Logs/docs — `issue_log.md` I114→Resolved, `update_log.md` U173, workplan v3.84. **(T1.99.80)** Lazy-import refactor in `eks_engine_pipeline.py` — only stdlib + `path_utils` + `discover_project_root` + `test_environment` at module level; `UniversalLogger`/`TelemetryHeartbeat` → `main()` body; `EngineInput`/`EngineOutput` → `main()` try block; `PipelineOrchestrator`/`DocumentRegistry` → `run_pipeline()`; `build_parser_from_schema`/`parse_cli_args` → respective functions; `ConfigRegistry`/`SchemaLoader` → `_read_system_params()`; `_PRJ_DIR` discovery wrapped in try/except. Result: `test_environment()` (P6) runs first, reports ALL missing deps, no bare `ModuleNotFoundError` reaches the user. (Superseded by U174 for remaining module-level common.library imports.) | opencode | ✅ Done |
| U172 | 2026-07-17 | Phase 1 | I114 / T1.99.80 | **I114 revised (v2) + T1.99.80 added — lazy-import refactor in `main()` to close top-level-import bypass gap**: Analysis found a second, more critical gap: EKS `eks_engine_pipeline.py` L47-63 imports ~15 heavy modules at module level, which execute before `main()` or `test_environment()` runs. If any transitive dependency (pandas, duckdb, jsonschema, etc.) is missing, Python throws bare `ModuleNotFoundError` — `test_environment()` in P6 never runs. **Solution (Option A — lazy imports)**: T1.99.80 restructures `main()` to defer heavy imports until after `test_environment()` passes. L20 `test_environment()` is stdlib-only, runs first, reports ALL missing packages with friendly guidance. Only if `ready=True` are heavy modules imported. No bare `ModuleNotFoundError` reaches the user. (Superseded by U173 — implementation complete.) | opencode | ✅ Done |
| U171 | 2026-07-17 | Phase 1 | I114 / T1.99.75–79 | **I114 revised + T1.99.75–79 redesigned (L20 universal-first approach)**: Cross-project analysis found `test_environment()` is a universal candidate — both DCC and EKS need identical `importlib.import_module()` logic on schema-driven dependencies. Original T1.99.75–79 would have added `test_environment()` directly to `EKSBootstrapManager._bootstrap_env()`, duplicating DCC. **Revised tasks now add `test_environment()` to `common/library/` first as L20**: T1.99.75 creates `common/library/core/system/` with `test_environment(dependencies: dict)` (universal `importlib.import_module()` loop); T1.99.76 adds `env_tester` strategy hook to universal `BootstrapManager._bootstrap_env()`; T1.99.77 wires EKS `EKSBootstrapManager._bootstrap_env()` to call universal `test_environment()` via hook; T1.99.78 registers `P1-BOOT-ENV` error code + message; T1.99.79 tests (universal + EKS) + docs/logs. (Superseded by U173 — implementation complete.) | opencode | ✅ Done |
| U170 | 2026-07-17 | Phase 1 | I114 / T1.99.75–79 | **I114 logged + tasks proposed (for review)**: Logged I114 — EKS bootstrap has no environment/dependency check (DCC `test_environment()` verifies all schema-driven dependencies in P6; EKS `_bootstrap_env()` only does OS detection). Proposed T1.99.75–79: add `test_environment()` to `_bootstrap_env()` loading dependencies from `eks_config.json`, schema-driven dependency check with `importlib.import_module()`, raise `BootstrapError("P1-BOOT-ENV", ...)` with missing-package guidance, register error code + message, optional/engines checking, tests + docs/logs. All 🔷 PLANNED pending review. No code changes — issue-log + workplan entries only. | opencode | 🔷 Planned |
| U169 | 2026-07-17 | Phase 1 | I113 / T1.99.70–74 | **I113 resolution — Pre-bootstrap logger + heartbeat (DCC alignment)**: **(T1.99.70)** Added `_parse_early_verbosity(args)` — lightweight argparse for `--level`/`--debug` only (no schema/config dependency), mirrors DCC's `parse_cli_args()` → `set_debug_level()` before `BootstrapManager.bootstrap_all()`. **(T1.99.71)** `UniversalLogger("eks-pipeline", level=early_level)` created pre-bootstrap, passed into `EKSBootstrapManager(logger=logger)` — constructor already accepted `logger` param. **(T1.99.72)** `TelemetryHeartbeat` moved pre-bootstrap (before `discover_project_root()`), covers all 8 bootstrap phases; level reconciliation recreates heartbeat if bootstrap resolves a different level. **(T1.99.73)** Hooks verified — `_eks_error_factory`/`_eks_message_factory` already pass `self.logger` through; `BootstrapManager._log()` uses `self.logger` for all phase logging. **(T1.99.74)** Issue log + workplan updated; I113 → Resolved. 29/29 pipeline tests pass. See `eks/engine/eks_engine_pipeline.py` L470–594. | opencode | ✅ Done |
| U168 | 2026-07-17 | Phase 1 | I111 / T1.99.62–63 | **I111 closure — Structured BootstrapError + P1-BOOT-* codes**: Registered 5 `P1-BOOT-*` error codes (`P1-BOOT-READINESS`, `P1-BOOT-CONFIG`, `P1-BOOT-PATHS`, `P1-BOOT-OS`, `P1-BOOT-CTX`) in `eks_error_config.json` under `system_errors` with FATAL severity; added `bootstrap_p1` range to `system_error_ranges`; updated `eks_error_setup_schema.json` with `bootstrap_p1` property; updated `eks_error_code_base.json` pattern to accept `P1-BOOT-[A-Z0-9]+`; total_codes 72→77, system_codes 37→42. Replaced bare `RuntimeError` in `bootstrap_pipeline()` readiness gate with `raise BootstrapError("P1-BOOT-READINESS", ...)` wired to `common.library.bootstrap.BootstrapError`. Added 6 new error-path tests in `test_eks_engine_pipeline.py`: readiness-failure raises BootstrapError, code/phase attributes, to_system_error(), to_dict(), from_system_error(), catalog registration check. 37 bootstrap + 29 EKS pipeline = 66/66 tests pass. I111 → Resolved. | opencode | ✅ Done |
| U167 | 2026-07-17 | Phase 1 / Master | I108–I110 / T1.99.50–61 | **I108–I110 closure — Universal BootstrapManager (L19) + EKS wiring + main() simplification**: Created `common/library/bootstrap/` as L19 with `manager.py` (~500 lines, stateful 8-phase orchestrator), `errors.py` (structured `BootstrapError` with `code`/`message`/`phase` + `to_system_error()`), `phases.py` (`BootstrapPhaseRegistry` with fluent API), 37 universal tests. Updated `common/library/__init__.py`. Created `EKSBootstrapManager` subclass in `eks/engine/core/bootstrap.py` injecting EKS hooks (`ConfigRegistry`, `ProjectSetupValidator`, `parse_eks_cli`, `resolve_paths`, `detect_os`, `ErrorManager`/`MessageManager` factories). Refactored `bootstrap_pipeline()` as thin wrapper delegating to `EKSBootstrapManager`; `main()` simplified via `EKSBootstrapManager().bootstrap_all().to_pipeline_context()` chain collapsing ~30 lines of manual context assembly; `EngineInput` derived from context. Backward-compat `to_dict()` returns same 13-key shape. 66/66 tests pass. I108–I110 → Resolved. | opencode | ✅ Done |
| U166 | 2026-07-16 | Phase 1 | T1.99.49 | **T1.99.49 — Integration tests + knowledge base + logs for bootstrap completeness**: Added 4 integration tests (TestI107BootstrapCompleteness): `test_bootstrap_single_resolved_paths`, `test_context_paths_from_single_resolved_dict`, `test_main_context_consistent_paths`, `test_run_pipeline_result_has_phase1_keys`. Fixed 4 pre-existing bugs discovered during testing: (1) `schema_cli.py` `anchor` kwarg → `pipeline_root_dir`; (2) `main()` missing `project_root=` arg to `bootstrap_pipeline()`; (3) `run_pipeline()` `args=None` causing `sys.argv` bleed into CLI parser; (4) `main()` missing `EKSPipelineContext` import. 23/23 pipeline tests pass. `eks/knowledge.json` v2.7.0, `issue_log.md` I107 → ✅ Resolved, workplan Master Index + §30 updated. | opencode | ✅ Done |
| U165 | 2026-07-16 | Phase 1 | I107 / T1.99.45–48 | **I107 closure — Bootstrap completeness (T1.99.45–48)**: `bootstrap_pipeline()` now handles all init — OS detection (L133), CLI parse (L136), config load once (L142–152), log-level precedence (L157–163), eks_root (L160), single `resolve_paths()` (L166), data-dir precedence (L168–175), single MessageManager (L178). `main()` delegates everything to bootstrap (L542–547) and reads single `resolved_paths` + `boot["mm"]` (Defect A+B fixed). `run_pipeline()` fixed: context path wires `em`/`mm` into orchestrator (L273–274); non-context path passes `project_root` to bootstrap (L284). | opencode | ✅ Done |
| U164 | 2026-07-16 | Phase 1 | I107 | **I107 logged — Bootstrap incomplete vs DCC (post-I106 residual)**: Audit of `eks/engine/eks_engine_pipeline.py::main()` against DCC `dcc_engine_pipeline.py::main()` + `BootstrapManager` (8-phase `bootstrap_all`) found init concerns still outside EKS `bootstrap_pipeline()`: (1) `detect_os()` at L498 (DCC: P6 env); (2) `_read_system_params()` + `get_system_param(log_level)` at L509–510 — 2nd ConfigRegistry load; (3) `data_dir` CLI>Schema>Native precedence at L521–527 (DCC: P8 params); (4) double `MessageManager` (main L535 + bootstrap L149). Most serious: split path-resolution source — `schema_dir`/`output_dir` from `cli.resolved_paths` (L529–530) vs `archive_dir`/`log_dir` from `boot["resolved_paths"]` (L570–572). Confirmed `project_root` resolution in `main()` (L505) is correctly outside bootstrap (DCC-faithful, no change). Issue logged as I107 (🔴 Open) with remediation plan (T1.99.45–49). | opencode | ✅ Done |
| U163 | 2026-07-16 | Phase 1 | I106 / T1.99.40–43 | **I106 closure — Context threading (L06)**: `EKSPipelineContext` now extends `common.library.core.pipeline.BasePipelineContext` (L06) implementing the universal contract (`to_dict()`, `_from_dict()`, lifecycle methods inherited). `PipelineOrchestrator.initialize_context()` accepts bootstrap data (parameters, config_registry, schema_registry, checkpoint_state) and populates context fields (T1.99.41). `run_pipeline()` accepts optional `context` parameter; when provided, skips bootstrap and uses pre-seeded context directly; surfaces context in return dict (T1.99.42). `main()` builds `EKSPipelineContext` from `EngineInput` + bootstrap, passes to `run_pipeline(context=ctx)`, extracts `EngineOutput` from returned context (T1.99.43). Advances DCC-faithful pipeline architecture with context threading through all phases. | opencode | ✅ Done |
| U162 | 2026-07-16 | Phase 1 / Master | I105 / T1.99.35–39 | **I105 closure — MessageManager SSOT consolidation**: EKS `MessageManager` rewritten as thin subclass of `common.BaseMessageManager` (L11) with `_catalog_filename="eks_message_config.json"`. Base hardened with: optional icon prepend in `show()`, `_make_default_logger()` hook, `_messages_dict()` helper supporting flat/structured formats, verbosity clamp preserved. Fixed wrong-catalog bug at `eks_engine_pipeline.py:505` (was using BaseMessageManager default `message_config.json` → silent no-op; now uses EKS MessageManager → reads `eks_message_config.json` correctly). Added `icon` property to `eks_message_base.json` message_entry schema. Dropped unused `from common.library.messages import BaseMessageManager` import. 10/10 new common BaseMessageManager tests pass (icon routing, verbosity clamp, fallback print, missing-format-key safety). All 7 `test_t132_modules` MessageManager tests green. 272/277 EKS suite green (5 pre-existing `discover_project_root` anchor failures, unrelated). `eks/knowledge.json` status updated. I105 → Resolved. | opencode | ✅ Done |
| U161 | 2026-07-15 | Phase 1 | I104 / T1.99.34 | **I104 closure (T1.99.34)**: EKS `eks/engine/eks_engine_pipeline.py::main()` now declares `pipeline_root_dir = "eks"` and `pipeline_dir = "engine"` as local literals at the top of the function (DCC-faithful — mirrors DCC `pipeline_dir = "workflow"` inside `main()`) and passes them explicitly to `parse_eks_cli(anchor=pipeline_root_dir, pipeline_dir=pipeline_dir)`. The module-level `_EKS_ANCHOR` / `_EKS_PIPELINE_DIR` constants were removed entirely; `parse_eks_cli` / `build_schema_driven_parser` default to the same literals and the import-time `_PRJ_DIR` discovery uses inline literals. `main()`'s I/O dependencies are now explicit/self-documenting; advances AGENTS.md §10 SSOT + I/O clarity. 26/26 EKS-engine + schema-CLI tests pass. I104 -> Resolved. Workplan v3.72 -> v3.73. | opencode | ✅ Done |
| U160 | 2026-07-15 | Phase 1 | I100 / T1.99.31 | **I100 closure (T1.99.31)**: Root cause was a config-resolution bug, not a missing `project_setup` section (eks_config.json already carries top-level setup keys). `common/library/cli/schema_cli.py::_schema_config_candidates` looked for `eks_config.json` at `eks/config/eks_config.json` but EKS keeps it at `eks/config/schemas/eks_config.json`; so `parse_eks_cli` loaded `{}`, `resolve_paths` fell back to `config_dir="config"`, `ConfigRegistry` loaded empty `{}`, and `ProjectSetupValidator` found no setup values → 15 pre-existing failures. Fixes: (1) `_schema_config_candidates` now probes `eks/config/schemas/eks_config.json` (+ `dcc/config/schemas/project_config.json`); (2) `ConfigRegistry.__new__` promotes the singleton only after a successful `load_all()`, preventing a poisoned empty singleton from blocking later calls. Added 2 regression tests (`test_load_schema_config_finds_eks_config_in_schemas`, `test_parse_cli_args_resolves_eks_config_dir`). Full EKS suite 277/277 green. I100 -> Resolved. Workplan v3.71 → v3.72. | opencode | ✅ Done |
| U159 | 2026-07-15 | Phase 1 | I101 / T1.99.30 | **DCC-not-to-implement directive (user)**: Per explicit user instruction, any issue related to DCC within the EKS pipeline is marked NOT to be implemented. Applied to I101 (DCC not wired to universal L18 CLI parser) and its task T1.99.30 — both set to ⛔ Won't Implement in `issue_log.md` and `phase_1_foundation_workplan.md` (§8 + §30). Rationale: I101 would rewire DCC's local parsers (`dcc/workflow/utility_engine/cli/cli_parser.py`, `cli_registry.py`) to the universal L18 parser, a DCC-side SSOT change that brings DCC code/conditioning into the EKS pipeline scope. Deferred; source-of-truth parser unification for DCC remains open for a future DCC-only workstream. | opencode | ✅ Done |
| U158 | 2026-07-15 | Phase 1 | I103 / T1.99.33 | **I103 closure (T1.99.33)**: Merged `run()` into `main(args: Optional[list] = None) -> int` in `eks/engine/eks_engine_pipeline.py` so `main()` holds every pipeline step (DCC-faithful, `dcc/workflow/dcc_engine_pipeline.py::main`); deleted the separate `run()`; `if __name__ == "__main__": sys.exit(main())`. Kept `run_pipeline()` / `bootstrap_pipeline()` funnels (reused by `phase1_server.py` + tests, analogous to DCC's `run_engine_pipeline(context)`). `eks/test/test_eks_engine_pipeline.py` now calls `main()` (`test_detect_os_called_in_run` -> `test_detect_os_called_in_main`). I103 → Resolved; advances AGENTS.md §10 SSOT. Workplan v3.70 → v3.71. | opencode | ✅ Done |
| U157 | 2026-07-15 | Phase 1 | I102 / T1.99.32 | **I102 closure (T1.99.32)**: Removed EKS-specific `DEFAULT_PIPELINE_DIR = "engine"` from `common/library/paths/root_discovery.py` (L17) and its re-export in `common/library/paths/__init__.py`; `common/library/cli/schema_cli.py` (L18) no longer imports it and defaults `pipeline_dir` to `None` (strip skipped when `None`); EKS defines local `_EKS_PIPELINE_DIR = "engine"` and passes it explicitly to `discover_project_root` / `resolve_pipeline_base_path` / `parse_eks_cli`. Added regression test asserting the common library exposes no project-specific `pipeline_dir` default. I102 → Resolved; advances AGENTS.md §10 SSOT. Workplan v3.69 → v3.70. | opencode | ✅ Done |
| U156 | 2026-07-15 | Phase 1 / Master | I099 / I100 / I101 | **I099 follow-up tasks + issues logged**: Added L18 follow-up tasks T1.99.30 (wire DCC to universal L18 CLI parser) and T1.99.31 (fix EKS project_setup/ConfigRegistry config drift) to the §8 Master Task Index and §30 task table (both 🔷 PLANNED). Logged I100 (EKS config drift - missing project_setup section causes 15 pre-existing suite failures) and I101 (DCC not yet wired to L18 parser - SSOT gap remains). I099 COMPLETE (T1.99.27-ac); I100/I101 OPEN with planned remediation. Bumped workplan to v3.69. | opencode | 🔷 PLANNED |
| U155 | 2026-07-15 | Phase 1 / Master | I099 | **I099 + universal schema-driven CLI parser (L18)**: Created common/library/cli/schema_cli.py implementing the four review principles for pipeline CLI parsing: (1) schema-driven argument generation from system_parameters/global_paths (L15/L16) via build_parser_from_schema(); (2) root-folder-based schema retrieval via the L17 entry sequence (discover_project_root) inside parse_cli_args(); (3) CLI>Schema>Native precedence encoded as an explicit-only overrides dict + overrides_provided flag (detected by scanning raw argv, faithful to DCC parse_cli_args); (4) a structured CliResult (namespace + overrides + pipeline_input with resolved paths + schema params) returned to the pipeline. Added common/library/cli/__init__.py (package). Wired EKS via build_schema_driven_parser() + parse_eks_cli(); refactored run() to consume the universal parser while preserving behavior (config_dir = root/eks/config unchanged). 15 new tests (common/library/cli/tests/test_schema_cli.py + TestSchemaDrivenCli) all green; EKS run()-based tests pass. I099 -> Resolved; advances I078. Report RP-EKS-P1-CLI-001. Pre-existing project_setup/ConfigRegistry config-drift suite failures are unrelated. | opencode | ✅ Resolved |
| U154 | 2026-07-15 | Phase 1 / Master | I098 | **I098 + T1.99.17–26 COMPLETE (entry-point cross-platform discovery / universal L17)**: Created `common/library/paths/root_discovery.py` implementing the L17 ordered entry sequence — `detect_os()` [L12] → cwd/`--base-path` resolver → `==pipeline_dir` strip → `discover_project_root()` (anchor-verified, EKS anchor `eks`) → `resolve_paths()` [L16] honours `eks_root` (fixes default `data_dir` bug #1) → `should_auto_create_folders()` OS-gated auto-create → `safe_posix()` serialization; `default_base_path()` raises when anchor missing. Wired into `eks/engine/eks_engine_pipeline.py` (entry calls `detect_os()`, adds `--base-path` CLI, routes all sub-paths via `resolve_paths`, gates auto-create by OS). Added `should_auto_create_folders` to `common.library.core.paths.path_utils` (L12), de-duplicating DCC's triple `detect_os`/`should_auto_create_folders`. Bumped `eks_engine_pipeline.py` → v0.3. 275/275 EKS tests green. I098 → Resolved; advances I078. | opencode | ✅ Resolved |
| U153 | 2026-07-15 | Phase 1 / Master | I098 | **Universal design revision-table sequence corrected**: Reordered `common/universal_pipeline_architecture_design.md` Revision History to descending version order (1.8 -> 1.0); previously 1.3/1.1/1.0 trailed after 1.8. Ordering-only fix, no content change. | opencode | 🔴 Open |
| U152 | 2026-07-15 | Phase 1 / Master | I098 | **Phase 1 workplan tasks added (I098 / L17)**: Added T1.99.17–26 (entry-point cross-platform discovery) to `phase_1_foundation_workplan.md` — revision 3.66 row, status-line note, §8 Master Task Index rows, §30 header + task table (10 task definitions) + success-criteria bullets. All 🔷 PLANNED, pending review/approval. No code changes. Companion to U151 (universal design L17) and I098. | opencode | 🔴 Open |
| U151 | 2026-07-15 | Phase 1 / Master | I098 | **I098 + universal design extended**: Added 7-dimension entry-point gap table (pipeline_dir, pipeline_start, cli_arg, schema default, project_root, folder/file discovery, OS detection) to I098 and cross-referenced the proposed universal pattern L17 (Universal Pipeline Architecture Design §3.19 — Pipeline Entry-Point & Cross-Platform Discovery). Added L17 to `common/universal_pipeline_architecture_design.md`: revision 1.8, pattern-table row, `root_discovery.py` package entry, new §3.19 with ordered entry contract + canonical helper + 7-concern table, index + §4.1/§4.2 entries, and reconciled §3.18 (__file__ walk = fallback). I098 maps 1:1 onto L17. Both files updated for review; no code changes. | opencode | 🔴 Open |
| U150 | 2026-07-15 | Phase 1 / Master | I098 | **I098 logged**: Reviewed the `pipeline_dir` anchor-folder discovery feature for EKS vs DCC. Documented that EKS `resolve_pipeline_base_path(anchor="engine")` (T1.99.13–16, I097) diverged from DCC `default_base_path("workflow")`: EKS hardcodes `parent.parent.parent`, drops `eks_root` on the `data_dir` default in `run()` (so default CLI scans `project_root/data` instead of `project_root/eks/data`), duplicates path logic vs the universal `resolve_paths()`, uses a silent `cwd` fallback, and lacks a test asserting the default data_dir. Issue I098 opened as 🔴 Open, pending review; candidate remediation captured in the issue entry. | opencode | 🔴 Open |
| U149 | 2026-07-14 | Phase 1 | I097 / T1.99.13–T1.99.16 | **I097 + T1.99.13–16 COMPLETE (anchor-folder path resolution + schema-driven path defaults)**: Added `resolve_pipeline_base_path()` to `eks/engine/eks_engine_pipeline.py` using DCC-faithful anchor-folder walk (`engine/` anchor, `Path.cwd()` fallback) — replaces hardcoded `parent.parent.parent`. Added `__main__` sys.path guard (formalized). Made `--data-dir` optional with schema-driven default from `global_paths.data_dir` (precedence CLI > Schema > Native). Routed all hardcoded path literals (`output_dir`, `schema_dir`, `config_file`) through `global_paths` schema lookups. Added 7 new tests covering anchor finding, fallback, parser defaults, and CLI override. Full EKS suite 271/271 green. I097 → Resolved. `phase_1_foundation_workplan.md` → v3.64. | opencode | ✅ Done |
| U148 | 2026-07-14 | Phase 1 | I096 / T1.99.8–T1.99.12 | **I096 + T1.99.8–T1.99.12 COMPLETE (entry-point relocation + common.library reuse)**: Created `eks/engine/eks_engine_pipeline.py` as the unified DCC-faithful main entry (`main()` + `--phase {A,B,C,full}` + `bootstrap_pipeline()`/`run_pipeline()` funnel on `common.library` building blocks: BaseEngine/BasePipelineContext/EngineInput/EngineOutput/TelemetryHeartbeat, get_system_param, resolve_paths, BaseMessageManager, UniversalLogger, ValidationManager) — advances I078. Deleted `parsers/cli.py`; archived `pipeline_runner.py`; repointed 7 import sites (pyproject, phase1_server, discovery_cli, health_cli, test_pipeline_runner→test_eks_engine_pipeline, test_discovery_cli, test_health_cli) and fixed a pre-existing over-indentation defect in the two CLI test files. Extended `PipelineOrchestrator.run_full_pipeline` with on_phase/checkpoint_dir/job_id. Full EKS suite 264/264 green. Docs updated (workplan §9/§30, report §21.7, appendix_f §2.3.3, common §8.2). `phase_1_foundation_workplan.md` → v3.63. I096 → Resolved. | opencode | ✅ Done |
| U146 | 2026-07-13 | Phase 1 | I093 | **I093 closure (T1.56.1–T1.56.5)**: Wired `eks/engine/core/discovery_cli.py` `run()` to `PipelineOrchestrator.run_phase_a()` via the shared `bootstrap_pipeline()` funnel (T1.56.1); wired `eks/engine/core/health_cli.py` `run()` to `HealthScorer.score()`/`score_batch()` over the DuckDB `DocumentRegistry` (T1.56.2); added `eks/test/test_discovery_cli.py` + `eks/test/test_health_cli.py` (7 tests) closing the AGENTS.md §21 coverage gap (T1.56.3–T1.56.4); flipped `T1.56` 🔶 PARTIAL → ✅ in workplan §8/§9/§14 (T1.56.5). Full EKS suite 264/264 green. Issue I093 → Resolved. | opencode | ✅ Done |
| U144 | 2026-07-13 | Phase 1 | I094 | **I094 fix**: Updated `phase_1_foundation_workplan.md` §9 Mermaid diagram — moved `initialize_context()` out of Phase A subgraph into an independent "Phase 1 Context Setup" subgraph between Bootstrap and Phase A, matching actual call order in `pipeline_runner.py:180`. Bumped to v3.56. | opencode | ✅ Done |
| U143 | 2026-07-11 | Phase 1 | Docs | **Canonical workplan promotion (v3.55)**: Archived pre-restructure `phase_1_foundation_workplan.md` v3.52 → `eks/archive/phase_1_foundation_workplan_v3.52.md`. Promoted restructured workplan (master-index layout + §11–§35 topical sections) as canonical `eks/workplan/phase_1_foundation_workplan.md` v3.55; removed interim `phase_1_foundation_workplan_b2.md` draft filename. | opencode | ✅ Done |
| U142 | 2026-07-11 | Phase 1 | §1 / §3 | **§1 Title & Description and §3 Objective expanded**: The narrative §1 description and §3 objective bullets only covered the original foundation scope and omitted the capability areas Phase 1 actually delivered (present in §4/§8). Added coverage for: universal plant-item asset schema (R36/R39), ISO 15926 ontology integration (R44), schema-driven error/message catalog + 6-dim health scoring (R51), Document Schema v2 / reorganization (R52/R53), end-to-end pipeline orchestration (R54–R58), and universal path resolution + converged entry point funneling through `run_pipeline(context)`/`bootstrap_pipeline()` (T1.97–T1.99). Also corrected stale header `Current Version: 3.50` → `3.52` to match the latest revision row. Doc-only change to `phase_1_foundation_workplan.md`. | opencode | ✅ Done |
| U141 | 2026-07-11 | Phase 1 | §2 Revision History | **Revision sequence corrected**: The §2 revision table was out of sequence — v3.46 sat below v3.52, the 3.40–3.45 block was ascending, the 3.23–3.29 block was internally jumbled, and the entire 0.1–3.22 history block was ascending. Re-sorted all 68 revision rows into strict descending order (newest v3.52 at top → oldest v0.1 at bottom) via numeric version sort. No content changed; ordering only. Doc-only change to `phase_1_foundation_workplan.md`. | opencode | ✅ Done |
| U140 | 2026-07-11 | Phase 1 | I093 / G1–G4 | **Phase 1 workplan audit & gap remediation**: Re-audited all Phase 1 tasks vs code. G3 — §8 status column for T1.69/T1.70/T1.72/T1.73/T1.75/T1.76 corrected from severity emojis (🟠/🔴/🟡) to ✅ (all six verified implemented). G1 — T1.56 (CLI Entry Points) reclassified 🔶 PARTIAL: only `eks/engine/parsers/cli.py` is real (T1.99.2); `eks/engine/core/discovery_cli.py` + `health_cli.py` are stubs returning placeholder SUCCESS (no standalone discovery/health engines built) — I093. G2 — T1.57 detail corrected to `phase1_server.py` (engine_endpoints.py archived T1.99.4). G4 — §9 files table fixed (project_setup.json Create→Deleted T1.67; engine_endpoints.py→phase1_server.py; discovery/health CLI paths → engine/core/). Color key — §14 Mermaid ENTRY nodes ECLI/EWEB/ERUN recoloured green (were amber while labeled ✅ COMPLETE); legend tidied; §4 Status Legend aligned to "✅ PASS/COMPLETE \| 🔶 PARTIAL \| ❌ FAIL \| 🔷 PLANNED" with note that severity emojis are not status. Workplan → v3.52. | opencode | ✅ Done |
| U139 | 2026-07-11 | Phase 1 | I092 / R60 | **T1.99.1–7 COMPLETE — Phase 1 entry-point convergence**: Implemented shared `bootstrap_pipeline()`/`run_pipeline(context)` in new `eks/engine/core/pipeline_runner.py` (ConfigRegistry → SchemaLoader.load_all → DocumentRegistry → ErrorManager/MessageManager → ProjectSetupValidator readiness gate → PipelineOrchestrator.run_full_pipeline → checkpoint + on_phase callback). Rewrote `eks/engine/parsers/cli.py` as real end-to-end CLI; added `eks/pyproject.toml` `eks-pipeline` console_scripts. Wired `phase1_server._run` to `run_pipeline()` (409 guard + resolve_paths preserved). Archived orphan `ui/backend/engine_endpoints.py` → `archive/ui/backend/` (no references). Added canonical `eks/serve.py` (§18.12); `server.py` is now a thin re-export shim. `bootstrap_pipeline()` uses `ConfigRegistry` singleton SSOT (resets singleton on differing config_dir to avoid test pollution; soft readiness gate logs + continues despite `fail_fast:true`). Added `eks/test/test_pipeline_runner.py`. Fixed `project_root` depth bug (`parent×4`) in cli.py + pipeline_runner.py; fixed `phase1_server._run` cancellation bug — a cancelled job previously had its `"cancelled"` status overwritten with `"failed"` when the running thread caught the `Pipeline cancelled` RuntimeError, making `test_pipeline_cancel` flaky — cancellation now preserves `"cancelled"` status and logs as STATUS not ERROR. Full EKS suite 257/257 green (stable across repeated runs). `phase_1_foundation_workplan.md` → v3.51 (§21 T1.99.1–7 ✅, §14 entry nodes ✅). I092 narrowed to remaining Phases 2–5. | opencode | ✅ Done |
| U138 | 2026-07-11 | Phase 1–5 | I092 / R60 | **Logged proposed I092/R60 convergence tasks in all phase workplans (for review)**: Phase 1 `phase_1_foundation_workplan.md` v3.49 — new §21 (T1.99.1–7: shared `run_pipeline(context)`/`bootstrap_pipeline()` helper, unified CLI + `console_scripts`, wire `phase1_server._run` to `run_full_pipeline`, delete orphan `engine_endpoints.py`, add `eks/serve.py`, `ConfigRegistry` SSOT at entry, tests). Phases 2–5 backend tasks added — Phase 2 v0.7 (T2.25–T2.26), Phase 3 v1.1 (T3.36–T3.37), Phase 4 v0.8 (T4.26–T4.27), Phase 5 v0.7 (T5.21–T5.22): each a standalone `phase{N}_server.py` + `run_phase{N}_pipeline(context)` reusing the Phase 1 shared helper + `serve.py` proxy wiring. All 🔷 PLANNED. I092 resolution updated to reference the logged task IDs. No implementation. | opencode | ✅ Done |
| U137 | 2026-07-11 | Phase 1 | I092 / R60 | **Widened I092 + added R60**: Added R60 (Independent Phase Sub-Pipelines) to EKS master workplan scope table (WP-EKS-001 v1.10) per AGENTS.md 18.13 — each phase (1–5) must run as an independent sub-pipeline with its own `phase{N}_server.py` backend + `run_pipeline(context)`; Phase 1 satisfied (phase1_server.py), Phases 2–5 pending. Widened I092 description/resolution to the per-phase convergence pattern (entry-point funnel for Phase 1 + per-phase backend servers for Phases 2–5). Wait for review — no implementation. | opencode | ✅ Done |
| U136 | 2026-07-11 | Phase 1 | I092 | **Logged I092 — EKS pipeline entry-point convergence gap**: EKS pipeline entry points (CLI, UI, web) do not converge on a shared `run_pipeline(context)` the way DCC does (CLI + `run_engine_pipeline_with_ui` + web all call one `BootstrapManager` + `PipelineContext` + `run_engine_pipeline`). Documented the gaps (HTTP-only real runner, stub CLIs, orphan FastAPI, dead `run_full_pipeline`, bypassed `ConfigRegistry`, `server.py` not `serve.py`, no console_scripts). Status 🔴 Open, pending review. | opencode | ✅ Done |
| U135 | 2026-07-11 | Phase 1 | T1.98 / I089 / I090 | **Universal Path Resolution & Schema-Driven Initialization COMPLETE**: Adopted EKS `global_paths` as the universal canonical path pattern (L16 in `common/universal_pipeline_architecture_design.md`). Added `common/library/paths/resolver.py` (`resolve_paths`, `ResolvedPaths`) normalizing EKS `global_paths` and DCC `folder_creation`/`discovery_rules` shapes; exported from `common/library/paths/__init__.py`. Wired `ConfigRegistry` (6 path properties) + `phase1_server.py` to the resolver (replaced inline `PRJ_DIR / _eks_root / gp.get(...)`). Added `workflow_file_entry_def`/`tool_file_entry_def` to `eks_base_schema.json` v1.9.0; added `workflow_files`/`tool_files` to `eks_setup_schema.json` v1.7.0 + `eks_config.json` v1.7.0` (DCC parity); `setup_validator.py` validates them; `folder_creation` satisfied by canonical `global_paths` (no separate DCC-style block). Universal architecture doc L16 + §3.18 + §2.2/§2.3/§2.4/§4.1/§4.2/§9/§10. `eks/knowledge.json` → v2.6.0. Added `eks/test/test_path_resolver.py` (9 tests). Full EKS suite 252/252 pass (243 + 9 new). I089 + I090 closed. | opencode | ✅ Done |
| U134 | 2026-07-11 | Phase 1 | T1.97.10–T1.99.14 / I091 | **Universal Architecture Elevation COMPLETE**: Registered `config/` as architecture-aligned sub-package in `common/library/__init__.py` (docstring, import, `__all__`). Added L15 — System Parameters / Runtime Behavior Config to `common/universal_pipeline_architecture_design.md`: §2.2 inventory row, §2.3 package structure, §2.4 per-library detail, §3.17 System Parameters Pattern, §4.1/§4.2 application guidelines, §9 checklist, §10 success criteria; doc version → 1.6. Updated `eks/knowledge.json` → v2.5.0 with L15 elevation status and revision entry. I091 resolved. Full EKS suite 243/243 pass. | opencode | ✅ Done |
| U133 | 2026-07-11 | Phase 1 | T1.97 / I088 | **System Parameters SSOT COMPLETE**: Added `common/library/config` with `normalize_system_parameters()` and `get_system_param()` for flat and DCC-style array shapes. Added `system_parameters_def` to `eks_base_schema.json` v1.8.0, `system_parameters` property to `eks_setup_schema.json` v1.6.0, and `system_parameters` block to `eks_config.json` v1.6.0; removed standalone `registry.timeout`. Wired `phase1_server.py`, `error_manager.py`, `registry.py`, and `eks/server.py` to read runtime knobs from config. Added `test_system_parameters.py`. Full EKS suite 243/243 pass. I088 resolved. | Codex | ✅ Done |
| U132 | 2026-07-10 | Phase 1 | T1.96 | **Schema Discovery & Registration COMPLETE**: Extracted `discover_schema_files()` from DCC `ref_resolver.py` into `common/library/loader/schema_discovery.py` (T1.96.1). Added 5 discovery rules to `eks_config.json` (T1.96.2). Refactored `schema_loader.py` to replace 22 hardcoded filenames with config-driven loading (T1.96.3). Wired `validate_discovery_rules()` in `setup_validator.py` (T1.96.4). Updated `common/universal_pipeline_architecture_design.md` §3.16 (T1.96.5). Fixed `*_base.json` pattern gap for outlier files — full EKS suite 236/236 green (T1.96.6). Phase 1 foundation marked ✅ COMPLETE. Workplan v3.43. | opencode | ✅ Done |
| U131 | 2026-07-09 | Phase 1 | T1.90-T1.95 | **Initiation Config Flattening**: Flattened `project_setup` from `eks_config.json` to top-level keys (folders/root_files/schema_files/environment/dependencies/project_metadata) to match DCC `project_config.json` (T1.90). Updated `eks_setup_schema.json` v1.5.0 to drop the `project_setup` wrapper (T1.91). Made `setup_validator.py` + `phase1_server.py` flatten-aware with `project_setup` backward-compat fallback (T1.92/T1.93). Archived + deleted orphan `eks_project_setup_config.json` (T1.94). Updated tests; full EKS suite 236/236 pass (T1.95). | opencode | ✅ Done |
| U130 | 2026-07-09 | Phase 1.3 | T1.84–T1.89 | Initiation harmonization complete: (T1.84) Added validate_folders/validate_named_files/validate_environment/validate_dependencies/validate_discovery_rules/validate_project_setup to universal ValidationManager in common/library/utility/validation/manager.py; (T1.85) Replaced flat-array project_setup defs in eks_base_schema.json v1.7.0 with DCC-aligned object definitions (folder_entry_def, root_file_entry_def, schema_file_entry_def, discovery_rule_def, environment_entry_def, dependency_config_def, validation_rule_entry_def, project_metadata_def); (T1.86) Created eks_project_setup_config.json v1.0.0 — instance data in DCC object model; (T1.87) Refactored setup_validator.py v0.7 as thin adapter delegating to universal ValidationManager, preserves P1-SETUP-* codes + ErrorManager wiring; (T1.88) Updated test_setup_validator.py (19 tests), created test_validation_manager.py (20 tests); (T1.89) Updated knowledge.json v2.3.0, update_log, issue_log, workplans. 235/235 tests pass. I085 resolved. | opencode | ✅ Done |
| U130 | 2026-07-09 | Phase 1 | Docs | Consolidated Phase 1.3 workplan and initiation hardening/harmonization test reports into the main Phase 1 foundation workplan (v3.40) and test report (v2.7). Retained the stand-alone Phase 1.3 workplan as an archived reference document. | opencode | ✅ Done |
| U129 | 2026-07-09 | Phase 1.3 | T1.84–T1.89 | **Phase 1.3 COMPLETE**: Initiation schema/code harmonization. Added universal `ValidationManager` in `common/library/utility/validation/manager.py` (validate_folders, validate_named_files, validate_environment, validate_dependencies, validate_discovery_rules, validate_project_setup). Reshaped EKS `project_setup` schema (8 new defs in `eks_base_schema.json` v1.7.0, `project_setup` in `eks_setup_schema.json` v1.4.0). Extracted setup instance data to `eks_project_setup_config.json` v1.0.0. Refactored `setup_validator.py` v0.7 as thin adapter. Migrated/added unit tests (235/235 pass). I085 resolved. | opencode | ✅ Done |
| U128 | 2026-07-09 | Phase 1.3 | Planning | Created follow-up Phase 1.3 workplan (`phase_1.3_initiation_harmonization_workplan.md`, WP-EKS-P1.3-001) for initiation schema/code harmonization with DCC's universal pattern. Decisions: (1) shared `ValidationManager` in `common/library/validation/`; (2) per-project schemas shape-aligned to DCC (no repo-root shared schema); (3) DCC deferred. Tasks T1.84–T1.89. Logged I085 (EKS vs DCC `project_setup` schema divergence). Parent Phase 1 workplan v3.38 cross-references Phase 1.3; Phase 1 remains ✅ COMPLETE. | opencode | ✅ Done |
| U127 | 2026-07-09 | Phase 1 | Re-audit (T1.79–T1.83 follow-up) | Re-audited Phase 1 code for schema-driven violations after T1.83. Found 4 residual issues and fixed all: (1) `setup_validator.py:314` hardcoded `project_root / "eks"` — now uses `global_paths.eks_root` (T1.83 miss); (2) `P1-SETUP-*` error code constants in `setup_validator.py` duplicated `eks_error_config.json` — now loaded from the catalog at import (SSOT); (3) `initialize_context()` leaf literals (`archive`/`config`/`log`) — now from `global_paths`; (4) `log_dir` added to `global_paths` schema+config; `debug_log.json` filename now from `logging.debug_file_path`. 215/215 tests pass. | opencode | ✅ Done |
| U126 | 2026-07-09 | Phase 1 | T1.79–T1.83 | **T1.79–T1.83 COMPLETE**: Initiation schema-driven hardening. 215/215 pass. I079–I084 closed. Phase 1 ✅ COMPLETE. Created report `phase_1_t179_t183_report.md`. Updated workplan v3.37, knowledge.json v2.3.0. | opencode | ✅ Done |
| U125 | 2026-07-09 | Phase 1 | T1.79–T1.83 | Reviewed T1.77/T1.78 initiation flow for schema-driven (SSOT) violations. Logged 6 issues: I079–I084. Added T1.79–T1.83 to workplan v3.36; phase status PARTIAL. Updated knowledge.json v2.2.0. No code change. | opencode | ✅ Done |
| U124 | 2026-07-09 | Phase 1 | T1.78 | **T1.78 COMPLETE**: Implemented all 6 DCC initiation-integrity gap items. G1: `eks.yml`→`eks/eks.yml` path fix in config + validator + tests — readiness gate now passes. G2: `data_dir` readability check via `os.access()`. G3: `_validate_dependencies()` probe added. G4: Output-path writability check + `_validate_output_paths()`. G5: `--skip-readiness` CLI override. G7: `ERR_PREFIX`/`P1-SETUP-*` error code constants. Fixed pre-existing `_LogCapture.level` bug. 5 new tests (3 validator + 2 server). 207/207 tests pass. Updated knowledge.json v2.1.0, phase1_server.py v0.4, setup_validator.py v0.3. Workplan v3.35 ✅ COMPLETE. | opencode | ✅ Done |
| U123 | 2026-07-09 | Phase 1 | T1.78 | Added T1.78 (Initiation Integrity Remediation) to workplan v3.34 after review of T1.77 vs DCC `initiation_engine`: documents readiness-gate `eks.yml` path regression (blocks all runs) + 8 DCC-aligned gap items (input readability, schema-driven debug default, env probe, readiness summary, ErrorManager wiring, output-path validation, 6-category validator). Phase status reverted ✅ COMPLETE → 🔶 PARTIAL (planned task present, AGENTS.md §15). Updated §4/§6/§7/§8/§9/§10/§14/§16. Updated `common/universal_pipeline_architecture_design.md` to v1.4: fixed stale T1.77 "PLANNED" refs (§8.2, §3.9, §2.2 L13, §7.5.2) to ✅ DONE; added §3.9.1 Initiation Integrity Layers; added checklist/success items; T1.78 referenced as PLANNED. No code change. | opencode | ✅ Done |
| U122 | 2026-07-09 | Phase 1 | T1.77 | **T1.77 implemented**: (1) `ProjectSetupValidator.validate_all()` readiness gate at start of `_run()` thread in `phase1_server.py`; raises `RuntimeError` if readiness != "YES". (2) `--debug`/`--level` CLI args with effective-level logic; reported in `/api/v1/status`. (3) `data_dir` existence + `recursive` bool validation before concurrency guard in `_handle_pipeline_start()`. Exported `ProjectSetupValidator` from `eks/engine.core.__init__`. Created `test/test_setup_validator.py` (8 tests) + 3 T1.77 server tests. Updated workplan v3.33 → ✅ COMPLETE. Updated report v2.5. 202/202 tests pass. | opencode | ✅ Done |
| U121 | 2026-07-09 | Phase 1 | T1.77 | Added DCC `initiation_engine` cross-references to workplan v3.32 §6 Evaluation and Alignment (initiation integrity mirrors DCC) and §7 Dependencies (added DCC `initiation_engine` + `common/universal_pipeline_architecture_design.md` as dependencies for T1.77). No code change. | opencode | ✅ Done |
| U120 | 2026-07-09 | Phase 1 | T1.77 | Workplan cross-section consistency for T1.77: changed phase status ✅ COMPLETE → 🔶 PARTIAL (planned task present, per AGENTS.md §15); added `ProjectSetupValidator` readiness node (amber) to §14 Mermaid BOOT flow + legend; added §14.1.12 function table; added §10 risk row; added DCC `initiation_engine` §16 reference. Workplan v3.32. No code change. | opencode | ✅ Done |
| U119 | 2026-07-09 | Phase 1 | T1.77 | Added T1.77 (Initiation Integrity Checks, mirroring DCC `initiation_engine`) to workplan v3.32: wire `ProjectSetupValidator` readiness gate into pipeline start, add `--debug`/`--level` CLI flag + schema-driven default, validate pipeline args (`data_dir` exists, `recursive` bool) before thread spawn. Evaluated against DCC validator/parameters/init_overrides. Status 🔷 PLANNED. No code change. | opencode | ✅ Done |
| U118 | 2026-07-09 | Phase 1 | T1.73 | Reconciled workplan with actual implementation: checkpoint files are 3 per-phase files `eks/output/checkpoint_{job_id}_{A,B,C}.json` (not `checkpoints/{job_id}.json`). Added `eks_registry.db` and the 3 checkpoint files to workplan §9; updated §14 Mermaid CKPT node and T1.73 description. Workplan v3.31. No code change. | opencode | ✅ Done |
| U117 | 2026-07-09 | Phase 1 | T1.72 | Bootstrap closure complete: wrapped run_phase_a() with DiscoveryInput/Output contracts and _process_file() with ParserInput/Output contracts in pipeline_orchestrator.py. Added _validate_discovery_input() method. All 6 Bootstrap tasks (T1.69–T1.76) now ✅. Updated workplan v3.30, report v2.3. 191/191 tests pass. | opencode | ✅ Done |
| U116 | 2026-07-08 | Phase 1 | Docs | Updated phase_1_foundation_workplan.md to v3.29: expanded Bootstrap closure scope with T1.75 (activate ErrorManager/MessageManager in phase1_server — closes silent T1.68 gap) and T1.76 (persist debug/message/status JSON to eks/output per AGENTS.md §7/§19); updated T1.69 (run_id) and T1.73 (checkpoint path). Updated §4 scope, §8 task table, §9 files table, §14 Mermaid. Updated phase_1_foundation_report.md to v2.2. | opencode | ✅ Done |
| U115 | 2026-07-08 | Phase 1 | Fix | Fixed pre-existing bug in registry.py get_document(): fetchone() result iterated as column values instead of single row dict. | opencode | ✅ Done |
| U114 | 2026-07-08 | Phase 1 | T1.74 | Cross-platform path fixes: EKSPaths.to_dict() uses .as_posix(); phase1_server.py anchors relative paths to PRJ_DIR. 2 new tests. | opencode | ✅ Done |
| U113 | 2026-07-08 | Phase 1 | T1.71 | Added update_document_status() with _with_retry to DocumentRegistry. Replaced raw duckdb.connect in PipelineOrchestrator._update_doc_status with registry call. 2 new tests. | opencode | ✅ Done |
| U112 | 2026-07-08 | Phase 1 | T1.68 | Wired ErrorManager/MessageManager into PipelineOrchestrator: run_phase_a, run_phase_b, run_phase_c, run_full_pipeline, _process_file now use error_manager/message_manager for error codes (D5-PARSE-*, D5-DETECT-*, D5-SCORE-*, D5-REG-*, S-PIP-*) and status messages. 3 new tests. | opencode | ✅ Done |
| U111 | 2026-07-08 | Phase 1 / Master | Docs | Updated EKS workplans to reflect the shared common-library package structure under `common/library` for architecture-aligned logging, telemetry, pipeline, errors, messages, paths, validation, UI, and factory modules; recorded the milestone in the Phase 1 foundation, Phase 1.2 UI, and master EKS workplans. | System | ✅ Done |
| U110 | 2026-07-07 | Phase 1.2 | T1.2.17.1–4 | Phase 1.2.17 COMPLETE: Directory Tree Browser Widget — replaced flat browse dropdown with expandable directory tree. Server: `_handle_list_dirs` now returns POSIX paths via `.as_posix()` for cross-platform consistency. HTML: replaced `#browse-dropdown` with `#dir-tree`. CSS: added `.dir-tree`, `.dir-tree-node`, `.dir-tree-toggle`, `.dir-tree-label`, `.dir-tree-selected`. JS: `browseDirs()` → `toggleDirTree()` / `toggleDirTreeNode()` / `selectDirTreeNode()` with async child loading and selected highlight. All 186 tests pass. Workplan v1.21. | opencode | ✅ Done |
| U109 | 2026-07-07 | Phase 1.2 | T1.2.16.1–3 | Phase 1.2.16 COMPLETE: Step 1 Load tab redesign — added step-header with recursive-scan instruction text, numbered step-section cards for (1) Choose Folder and (2) Results, recursive-hint label, load-summary line with ✅ confirmation, "Proceed to Step 2" button that appears after documents are loaded. Updated phase1_ingestion.html, eks.css (9 new rule blocks), eks.js (showLoadSummary, proceed button wiring). Removed duplicate tab-bar. All 186 tests pass. Workplan v1.19. | opencode | ✅ Done |
| U108 | 2026-07-07 | Phase 1.2 | T1.2.15.1–7 | Phase 1.2.15 COMPLETE: EKS UI migration to common design system. Added `/common/` route to `server.py` (COMMON constant + `_serve_common()` traversal-guarded method). Imported common CSS/JS in `phase1_ingestion.html`. Rewrote `eks.css` (380 lines) — removed all non-standard tokens, shell layout, duplicated components; kept EKS-specific overrides with standard `var(--token)` references. Migrated HTML shell to `com-*` classes (`.com-shell`, `.com-titlebar`, `.com-iconbar`, `.com-sidebar`, `.com-content`, `.com-statusbar`, `.com-modal`). Refactored `eks.js` — replaced `applyTheme`/`esc`/`setStatus`/drag-drop/modal/theme-cycle with `comUI.*` equivalents; added theme dropdown via `comUI.theme.initPicker('eks-theme')`. All 186 tests pass. Workplan v1.17. | opencode | ✅ Done |
| U107 | 2026-07-07 | Phase 1.2 | T1.2.14.1–5 | Phase 1.2.14 COMPLETE: created `common/universal_ui_design.css` (842 lines, 29 sections, 5 themes, `com-` prefix shared components), `common/universal_ui_design.js` (~350 lines, 8 modules under `comUI` namespace), `common/universal_ui_design.md` (full AGENTS.md §14 documentation). Renamed from `common/ui/com-design-system.css`. Workplan v1.15. | opencode | ✅ Done |
| U106 | 2026-07-07 | Phase 1.2 | T1.2.13.1–3 | Phase 1.2.13 COMPLETE: upgraded step progress bar from circle+connector to pipe-node style — bordered rounded cards with `›` arrow separators, matching DCC pipe-node pattern from `dcc/ui/excel_explorer_data_pro.html`. Updated eks.css (step-progress, step, step-connector), phase1_ingestion.html (step-connector to span with `›`). Workplan v1.14. | opencode | ✅ Done |
| U105 | 2026-07-06 | Phase 1.2 | T1.2.12.1–8 | Phase 1.2.12 COMPLETE: added `GET /api/v1/config/paths` and `GET /api/v1/files/list-dirs` endpoints; `fetchPaths()` in eks.js replaces 7 hardcoded paths; folder path input + Browse button; traversal-guarded directory listing; pipeline uses user-selected path; 4 new tests, 66 total pass. Updated phase1_ingestion.html, eks.js, eks.css, phase1_server.py, test_phase1_server.py. Workplan v1.13. | opencode | ✅ Done |
| U104 | 2026-07-06 | Phase 1.2 | — | Added Phase 1.2.12 (Folder Picker & SSOT Path Resolution) to workplan — config-driven data directory picker, Browse endpoint, all frontend paths read from schema instead of hardcoded. Workplan v1.12. | opencode | ✅ Done |
| U103 | 2026-07-06 | Phase 1.2 | — | Consolidated all Phase 1.2 test suites into single `reports/phase_1.2_report.md` (63 tests: 35 io_contracts + 28 phase1_server). Archived `phase_1.2.11_report.md` → `eks/archive/`. Updated workplan v1.11. | opencode | ✅ Done |
| U102 | 2026-07-06 | Phase 1.2 | T1.2.11.1–8 | Phase 1.2.11 complete: Removed global toolbar, added step progress bar (4-step Load→Process→Review→Health), scoped action buttons to respective tabs, auto-advance step states. Updated phase1_ingestion.html, eks.css, eks.js. All 28 tests pass. | opencode | ✅ Done |
| U101 | 2026-07-06 | Phase 1.2 | - | Renamed eks/ui/index.html to eks/ui/phase1_ingestion.html to align with Phase 1 pipeline purpose. Updated server.py (removed index stem special case), knowledge.json (v2.2.0), HTML title. All 28 tests pass. | opencode | ✅ Done |
| U100 | 2026-07-01 | Phase 1.2 | T1.2.2–1.2.7 | Phase 1.2.2–1.2.7 complete: T1.2.2.7 (log streaming — _job_logs + _LogCapture + GET /api/pipeline/logs/{job_id}); T1.2.3 (eks/ui/index.html — VS Code layout, 5 themes, tabs, sidebars, resize, help modal); T1.2.3 (eks/ui/eks.css — 5 themes, layout switching, responsive); T1.2.4 (eks/ui/eks.js — Fetch API, doc table, tree, pipeline polling, review form, Chart.js, keyboard shortcuts); T1.2.5 (Chart.js radar chart + color-coded health badges); T1.2.6 (eks/ui/ui_help.json — schema + glossary); T1.2.7 (3 new tests, 178 total). Root serves index.html. | opencode | ✅ Done |
| U099 | 2026-07-01 | Phase 1.2 | T1.2.1 | Phase 1.2.1 complete: Created eks/server.py (main launcher, port 5000, file picker + proxy), eks/ui/backend/phase1_server.py (Phase 1 backend, port 5001, 12 API endpoints), eks/ui/backend/__init__.py. All endpoints: status, files/load, documents CRUD, pipeline start/status/cancel, review summary/flagged/lock. 20 backend integration tests all pass (total 120/120). | opencode | ✅ Done |
| U098 | 2026-07-01 | Phase 1.2 | T1.2.0 | Phase 1.2.0 complete: Created eks/engine/core/io_contracts.py (re-exports base + DiscoveryInput/Output, HealthInput/Output), eks/engine/parsers/io_contracts.py (ParserInput/Output), eks/ui/backend/contracts.py (4 UI contracts per Appendix G §7), eks/ui/backend/contract_manager.py (UIContractManager with validation and serialization), eks/test/test_io_contracts.py (35 tests covering all contracts). All 35/35 tests pass. | opencode | ✅ Done |
| U097 | 2026-07-01 | Phase 1 | Docs | Updated Phase 1 workplan §14 Mermaid diagram with explicit function call annotations per pipeline step. Added §14.1 Function Table (11 module tables) per AGENTS.md §17 — covers all pipeline-critical functions with description, parameters, return values, dependencies, error handling, and tracing. Workplan v3.22. | opencode | ✅ Done |
| U096 | 2026-07-01 | Phase 1.2 | Docs | Updated Appendix G to v0.2: added G10 Server Architecture (two-server pattern, port allocation, proxy routing). Updated G3.1 (proxy note), G3.4 (two-server flow), G9 (G10 reference). Updated Phase 1.2 workplan to v1.2: architecture diagram split into main server + phase backend, T1.2.1.x retargeted to `eks/server.py` + `eks/ui/backend/phase1_server.py`, updated deliverables, dependencies, success criteria. Per server design review. | opencode | ✅ Done |
| U095 | 2026-06-30 | Phase 1 | — | Archived `phase_1_appendix_f_architecture_workplan.md` → `eks/archive/` — stale draft (tasks T1.52-T1.66 already tracked as ✅ in phase_1_foundation_workplan.md v3.19). No cross-references from other files. | opencode | ✅ Done |
| U094 | 2026-06-30 | Phase 1.2 | — | Created `appendix_g_interface_architecture.md` v0.1 (G1–G9): extracted universal UI theme, help schema, API conventions, polling, and UI contract patterns from Phase 1.2 workplan. Updated Phase 1.2 workplan to reference Appendix G (inline theme colors, help JSON, polling details replaced with references). Updated Phase 5 workplan to reference Appendix G for UI conventions and contract ownership. Archived orphaned `appendix_f_gaps_summary.md` → `eks/archive/`. | opencode | ✅ Done |
| U093 | 2026-06-30 | Phase 1 | T1.67 | T1.67 complete: Added 4 defs (`required_folder_setup_def`, `required_engine_subfolder_setup_def`, `required_file_setup_def`, `environment_setup_def`, `validation_options_def`) to `eks_base_schema.json` v1.6.0; added `project_setup` property to `eks_setup_schema.json` v1.3.0 with `$ref` to base defs; added actual values to `eks_config.json` v1.4.0; archived `project_setup.json` → `eks/archive/`; refactored `setup_validator.py` v0.2 to load from ConfigRegistry with backward-compatible fallback. I046 resolved. 118/118 tests pass. Workplan v3.21 COMPLETE. | opencode | ✅ Done |
| U092 | 2026-06-30 | Phase 1 | T1.67 | Added T1.67: Integrate `project_setup.json` into core eks_base/setup/config schemas per AGENTS.md §9 3-layer pattern. Logged I046. Phase 1 status changed to PARTIAL pending review. Updated workplan v3.20. | opencode | ✅ Done |
| U091 | 2026-06-30 | Phase 1.2 | Docs | Migrated Section 16 (I/O Contracts) from phase_1_foundation_workplan.md into phase_1.2_interactive_ui_workplan.md as Phase 1.2.0 and Section 10. Updated phase 1 workplan v3.18, phase 1.2 workplan v1.1. | opencode | ✅ Done |
| U090 | 2026-06-30 | Phase 1 | All | Fixed 3 documentation/status issues: (1) Phase status COMPLETE→PARTIAL; (2) T1.35.1–T1.35.5 statuses corrected; (3) Removed `$schema` from `eks_error_config.json` and `eks_message_config.json` (I045 regression). All 118 tests pass again. | opencode | ✅ Done |
| U089 | 2026-06-25 | Phase 1 | Docs | Integrated full Schema Inheritance Chain from `schema_inheritance_chain.md` (v1.11) into `appendix_e_schema_design.md` (v0.6): added E11 (per-key Base→Setup→Config→Actual Value trace tables for all 6 schema sets + fragments) and E12 (Business Logic vs Schema Layers). Updated E1 (23 files, asset_context), E2 Mermaid (14 fragments), E5.1/5.3 (14 asset fragments, 55 base defs). Archived `schema_inheritance_chain.md` → `eks/archive/`. | opencode | ✅ Done |
| U088 | 2026-06-25 | Phase 1 | T1.51 | Implemented `asset_context` fragment across all 3 asset schema files (base 1.3.0, setup 1.3.0, config 1.4.0). Added extensible location_hierarchy, system_hierarchy, project_context, asset_relationships (13 types), document_relationships (5 types), lifecycle_context. Updated schema_inheritance_chain.md v1.11, tests (118/118 pass). Phase 1 COMPLETE. | opencode | ✅ Done |
| U087 | 2026-06-24 | Phase 1 | Schema | Moved `revision_id` from `eks_base_schema.json` to `eks_doc_base_schema.json` (v1.1.2→v1.2.0). Added `revision_validation` property to `eks_doc_setup_schema.json` (v1.2.0→v1.3.0) and values to `eks_doc_config.json` (v1.1.0→v1.2.0). Removed `revision_pattern` from `project_rules_def` in base (v1.4.0→v1.5.0) and from `eks_project_rules_config.json` (v1.0.0→v1.1.0). I032 resolved. | opencode | ✅ Done |
| U086 | 2026-06-24 | Phase 1 | Schema | Removed `properties`/`required` from `document_relationship_trigger_map` in `eks_base_schema.json` (v1.3.1→v1.4.0). Base now defines only the shape (`type: object`, `additionalProperties`). Actual mapping entries are now SSOT in config files (`eks_asset_config.json`, `eks_doc_config.json`) only. I031 resolved. | opencode | ✅ Done |
| U085 | 2026-06-24 | Phase 1 | Report | Added "Type" column (Domain/Pipeline) to all tables in `schema_inheritance_chain.md` (v1.4). Classified all keys across 23 schema files into Domain (reference registries) or Pipeline (operational config). | opencode | ✅ Done |
| U083 | 2026-06-24 | Phase 1 | Schema audit | Created `eks_project_rules_config.json` v1.0.0 — moved `project_rules` inline values from `eks_config.json` to a dedicated config file with `$ref` from `eks_setup_schema.json` (v1.2.1→v1.2.2) and `eks_config.json` (v1.2.0→v1.3.0). SSOT enforced — new project rules require update to `eks_project_rules_config.json`. Updated `schema_inheritance_chain.md` (v1.3) to include new file (23 total). | opencode | ✅ Done |
| U082 | 2026-06-24 | Phase 1 | Schema audit | Removed `oneOf` from `discipline_registry` in `eks_setup_schema.json` (v1.2.0→v1.2.1) — changed to `$ref`-only to enforce SSOT. New disciplines must be added to `eks_discipline_schema.json` first. No other `oneOf` violations found across 22 schema files. Generated `schema_inheritance_chain.md` v1.2 report tracing all 22 files through base→setup→config→actual value. | opencode | ✅ Done |
| U080 | 2026-06-24 | Phase 1 | Reports | Consolidated `phase_1_t130_t132_report.md` into `phase_1_foundation_report.md` (v1.4). Added detailed T1.30–T1.32 test case tables. Merged success criteria, files, recommendations, and lessons learned. Archived and removed separate report file. Updated `phase_1_foundation_workplan.md` to v3.10. | opencode | ✅ Done |
| U079 | 2026-06-23 | Phase 1 | I023 | Removed standalone `discipline_code` definition from `eks_base_schema.json` (v1.3.1). `eks_doc_base_schema.json` `$ref` now points to `discipline_entry_def.properties/code` (v1.1.2). `discipline_code` is no longer a standalone type — it is the `code` property of `discipline_entry_def`. I023 resolved. Updated Appendix E inventory and summary matrix. | opencode | ✅ Done |
| U078 | 2026-06-23 | Phase 1 | I027 + fixes | Three optional fixes: (1) I027 — aligned error/message base schema URIs to filename-based pattern; (2) Consolidated `verbosity_level` enum into shared `eks_base_schema.json#/definitions/verbosity_level` (message + logging now share one definition); (3) Added shared `document_relationship_trigger_map` definition to `eks_base_schema.json` — both `eks_asset_setup_schema.document_triggers` and `eks_doc_setup_schema.ontology_triggers` now `$ref` it. Added `base_schema` to all validation registries in `schema_loader.py` and tests. Bumped versions: base_schema 1.2.0→1.3.0, asset_setup 1.1.0→1.2.0, doc_setup 1.1.0→1.2.0, error/message base+setup 1.0.0→1.1.0. I027 resolved. I028 updated. All 114 tests pass. | opencode | ✅ Done |
| U077 | 2026-06-23 | Phase 1 | T1.48 | Regression fix: removed `$schema`, `$id`, `version`, `title`, `description` from `eks_error_config.json` and `eks_message_config.json` — these fields broke validation against setup schemas which have `additionalProperties: false`. All 114 tests pass. Updated workplan to v3.8. Logged I028. | opencode | ✅ Done |
| U076 | 2026-06-23 | Phase 1 | T1.48 | Schema audit — fixed 4 issues: (1) Removed duplicate `revision_id` and `discipline_code` from `eks_doc_base_schema.json` (v1.1.1); (2) Aligned parser import paths in `eks_config.json` (`engine.parsers.*` → `eks.engine.parsers.*`); (3) Added dgn/dwg stub parsers to `eks_config.json` (v1.2.0); (4) Added `$schema` to `eks_error_config.json` and `eks_message_config.json` (v1.0.1) — later reverted (U077) because setup schemas have `additionalProperties: false`. Logged I022–I027. Updated workplan to v3.7. | opencode | ⚠️ Partially reverted |
| U075 | 2026-06-23 | Phase 1 | Documentation | Created `appendix_e_schema_design.md` v0.1: E1–E10 covering schema architecture overview, Mermaid diagram (22 files, 6 schema sets, 4 fragments), 3-layer pattern details, fragment schemas, complete file inventory, cross-schema references, AGENTS.md §9 compliance checklist, schema validation flow, how to add new fragment schema. Added Appendix E reference to phase_1_foundation_workplan.md and eks_system_workplan.md. Fixed duplicate appendix_d reference in master workplan. | opencode | ✅ Done |
| U074 | 2026-06-23 | Phase 1 | T1.47 | Added 6 new tests to `test_phase1.py`: test_fragment_schema_files_exist, test_base_schema_has_new_definitions, test_fragment_schemas_have_required_fields, test_config_no_placeholder_data, test_config_has_fragment_references, test_setup_schema_has_new_properties. Updated test_project_scoped_config to use real project codes. Fixed existing test for new config structure. All 59 tests pass. | opencode | ✅ Done |
| U073 | 2026-06-23 | Phase 1 | T1.46 | Updated base schema: added `project_entry_def`, `department_entry_def`, `facility_entry_def` to `eks_base_schema.json` definitions. Updated `eks_config.json`: replaced P123/P456 with real WSD11 codes (131101, 131242), added `$ref` to fragment schemas for project_registry, department_registry, facility_registry. Updated `eks_setup_schema.json`: added property declarations with `oneOf` for discipline_registry (supports `$ref` or inline), added project_registry, department_registry, facility_registry to properties and required. Version bumped to 1.1.0/1.3.0. | opencode | ✅ Done |
| U072 | 2026-06-23 | Phase 1 | T1.42–T1.45 | Created 4 fragment schema files following AGENTS.md §9 and DCC pattern: `eks_project_code_schema.json` (3 projects: 131101, 131242, 999999), `eks_discipline_schema.json` (21 disciplines), `eks_department_schema.json` (11 departments), `eks_facility_schema.json` (12 facilities). All use `$schema: draft-07`, `$id` URIs, `allOf`/`$ref` to base definitions, `additionalProperties: false`, `version: "1.0.0"`. | opencode | ✅ Done |
| U071 | 2026-06-23 | Phase 1 | Documentation | Updated `phase_1_foundation_workplan.md` to v3.5: added §12 success criteria (data challenges documented, DGN gap identified), added §13 deliverable (data challenge analysis). Updated revision history. | opencode | ✅ Done |
| U070 | 2026-06-23 | Phase 1 | Documentation | Updated `phase_1_foundation_report.md` to v1.1: added §10.2 (Data Challenges Identified — 7 issues with descriptions and resolution paths), added §11 items 9–10 (Lessons Learned: real data complexity, data quality variance). Updated revision history. | opencode | ✅ Done |
| U068 | 2026-06-23 | Phase 1 | Documentation | Analyzed `eks/data/twrp/` sample data: 721 documents (design_doc/ + project_spec/), 6,682 asset records (7 datadrop sheets). Identified 7 data challenges (I015–I021): DGN parser gap (48 files), revision folder inconsistency, two project codes, datadrop column variability, data incompleteness (22–64%). Logged to `eks/log/issue_log.md`. | opencode | ✅ Done |
| U067 | 2026-06-22 | Phase 1 | T1.41 | T1.41 complete: Fixed error/message schemas to follow AGENTS.md §9 3-layer pattern. Created `eks_error_setup_schema.json` (allOf + $ref to base, properties, additionalProperties: false) and `eks_message_setup_schema.json` (same pattern). Cleaned config files: removed `$schema/$id/title/description/version` from error and message configs, removed `type: "object"`. Updated `eks_error_code_base.json`: added function codes X (Cross-reference), G (Graph), E (Extract) to enum (data errors used these but enum was incomplete). Updated `schema_loader.py`: added error/message schema loading (6 new instance vars), `_validate_error_config()` and `_validate_message_config()` methods, called in `load_all()`. All 53 tests pass. I014 resolved. | opencode | ✅ Done |
| U064 | 2026-06-22 | Phase 1 | Documentation | Updated test report to v1.0: added T1.36–T1.40 test cases (53 total), updated scope/execution summary, success criteria checklist, files created/modified, lessons learned, references. Updated workplan Section 12 success criteria (all T1.35.x and T1.36–T1.40 checked). Updated workplan Section 13 deliverables. Updated master workplan to v1.1: Phase 1 status confirmed COMPLETE, R54–R58 marked PASS. | opencode | ✅ Done |
| U063 | 2026-06-22 | Phase 1 | T1.40 | T1.40 complete: Created `eks/engine/core/review_manager.py` (ManualReviewManager) — queries flagged documents, supports metadata correction (14 allowed fields), element confirmation, score recalculation, document locking via verified_by. Added 4 new tests (get_flagged, correct_metadata, lock_document, get_summary). 53/53 tests pass. Phase 1 COMPLETE (v3.2). | opencode | ✅ Done |
| U062 | 2026-06-22 | Phase 1 | T1.39 | T1.39 complete: Created `eks/engine/core/pipeline_orchestrator.py` — coordinates 3-phase pipeline (Phase A: scan→register, Phase B: route→parse→detect→score→update, Phase C: flag for review). Adapts parser content blocks to StructureDetector page format. Added 2 new tests (phase_a, phase_c). 49/49 tests pass. | opencode | ✅ Done |
| U061 | 2026-06-22 | Phase 1 | T1.38 | T1.38 complete: Created `eks/engine/parsers/parser_router.py` — maps file_type → parser_class from file_type_registry, dynamically imports/instantiates parser, orchestrates parse() → extract_metadata() sequence, returns structured results with status/error. Fixed DGN/DWG stubs to implement BaseParser interface (extract_metadata). Added 4 new tests (lookup, instantiate, route_no_parser, route_batch). 47/47 tests pass. | opencode | ✅ Done |
| U060 | 2026-06-22 | Phase 1 | T1.37 | T1.37 complete: Created `eks/engine/core/file_scanner.py` — walks project directory, discovers files with recognized extensions, validates against `file_type_registry`, infers document_type from `document_type_registry[].expected_file_types`, builds placeholder metadata from filename heuristics (doc_number + revision), registers placeholders in documents table. Added 4 new tests (discover, validate, build_placeholder, register). 43/43 tests pass. | opencode | ✅ Done |
| U059 | 2026-06-22 | Phase 1 | T1.36 | T1.36 complete: Created `eks/engine/core/schema_to_ddl.py` — auto-generates SQL DDL from JSON schema definitions (`document_metadata_def`, `project_metadata_def`, `document_element_def`). Handles type mapping (string→VARCHAR, boolean→BOOLEAN, integer→INTEGER, number→DOUBLE, array→JSON), `format: date-time`→TIMESTAMP, `id` as PRIMARY KEY, nullable project metadata fields for backward compatibility. Refactored `registry.py`: replaced hard-coded DDL with `SchemaToDDL` output; added `sync_schema()` method; `COLUMN_ALLOWLIST` now derived from schema. Added 8 new tests. 39/39 tests pass. | opencode | ✅ Done |
| U058 | 2026-06-22 | Phase 1 | T1.36–T1.40 | Updated `phase_1_foundation_workplan.md` to v3.0: added T1.36 (SchemaToDDL), T1.37 (FileScanner), T1.38 (ParserRouter), T1.39 (PipelineOrchestrator), T1.40 (Review Workflow); added R54–R58; added 4 new modules to files table and folder tree; updated success criteria and deliverables. Status: PARTIAL. | opencode | ✅ Done |
| U057 | 2026-06-22 | Phase 1 | T1.35 | Updated `appendix_c_ontology.md` to v1.8: added `DataSheet` (subClassOf Specification, mapping DS) and `OpsManual` (subClassOf Manual, mapping OM) classes to support T1.35 document type code alignment. Updated C4 class hierarchy table. | opencode | ✅ Done |
| U055 | 2026-06-22 | Phase 1 | T1.35 | T1.35 complete: Enhanced doc schema v1.1 — added document_type_code, file_type_code, element_type_code enums; file_path/ingested_at/file_type to metadata; 3 registries in config; refactored expectations; _validate_doc_registries() in schema_loader; 6 new tests; DGN/DWG parser stubs; DataSheet/OpsManual ontology classes. 31/31 tests pass. Phase 1 COMPLETE (v2.5). | opencode | ✅ Done |
| U054 | 2026-06-22 | Phase 1 | T1.35 | Added T1.35 to Phase 1 workplan: Enhanced Document Schema v2 — enums (7 doc types, 5 file types, 8 element types), 3 registries (document_type, file_type, element_type), element expectations keyed by doc type, R53 added. Updated workplan v2.5, appendix B v0.9 with B3.2/B3.3/B3.4 sections. Status: PLANNED. | opencode | ✅ Done |
| U053 | 2026-06-22 | Phase 1 | T1.34 | T1.34 complete: Created 3 doc schema files (`eks_doc_base_schema.json`, `eks_doc_setup_schema.json`, `eks_doc_config.json`) with document_metadata_def, project_metadata_def, document_element_def. Removed doc defs from `eks_base_schema.json`. Updated `schema_loader.py` with doc schema loading and validation. Added 6 new tests in `test_phase1.py` verifying doc schema existence, definitions, validation, and pipeline base cleanup. 73/73 tests pass. Phase 1 COMPLETE (v2.4). | opencode | ✅ Done |
| U052 | 2026-06-22 | Phase 1 | T1.34 | Added T1.34 to Phase 1 workplan: Reorganize document schema into dedicated 3-layer pattern (`eks_doc_base_schema.json`, `eks_doc_setup_schema.json`, `eks_doc_config.json`). Separates document definitions from pipeline config following asset schema pattern. Updated workplan v2.3, added R52 to scope, updated task breakdown, files list, success criteria, deliverables, and folder tree. Status: PARTIAL. | opencode | ✅ Done |
| U051 | 2026-06-22 | Phase 1 | T1.33 | Schema consolidation compliance (I010): Confirmed all 13 schema/config JSON files in `eks/config/schemas/`. Updated `test_phase1.py` to use schemas/-first fallback chain (`eks/config/schemas` → `eks/config` → `config/schemas` → `config`) and corrected `ConfigRegistry`/`SchemaLoader` calls to use `config_parent`. Updated section 7b folder tree in `phase_1_foundation_workplan.md`. Logged I010 in `issue_log.md`. Updated workplan to v2.2 COMPLETE; all Phase 1 success criteria now ✅. | opencode | ✅ Done |
| U050 | 2026-06-22 | Phase 1 | T1.30–T1.32 | Implemented T1.30 (error code schema), T1.31 (message schema), T1.32 (error_manager, message_manager, health_scorer, structure_detector, document_elements table). Created 9 files, modified 1 (registry.py). 47 new T1.32 tests + 20 existing Phase 1 tests all passing (67 total). Created test report `phase_1_t130_t132_report.md`. Updated `phase_1_foundation_workplan.md` to v2.0 — Phase 1 complete. | opencode | ✅ Done |
| U049 | 2026-06-19 | Phase 1 | Documentation | Updated `phase_1_foundation_workplan.md` to v1.9: updated T1.30–T1.32 for 6-dimension health scoring, structure_detector.py, document_elements table; updated R51 description; updated deliverables with health_scorer.py and structure_detector.py. | opencode | ✅ Done |
| U047 | 2026-06-19 | Phase 1 | Documentation | Updated `appendix_d_pipeline_messages_errors.md` to v0.3: added D7.10 Structural Elements Table (`document_elements`); revised D7 to 6-dimension composite (added structural completeness 20%); added 8 structural error codes (D5), 8 structural messages (D6); updated D9 implementation files (added `structure_detector.py`); updated D8 status lifecycle. | opencode | ✅ Done |
| U046 | 2026-06-19 | Phase 1 | Documentation | Updated `appendix_d_pipeline_messages_errors.md` to v0.2: revised D7 Health Scoring to all-column design — 18 scorable columns across 3 weight tiers, 5-dimension weighted composite formula, extraction_notes JSON format, 3 worked examples. Moved `health_scorer.py` to Phase 1 (T1.32). Updated R51 description in master workplan. | opencode | ✅ Done |
| U045 | 2026-06-19 | Phase 1 | Documentation | Updated `phase_1_foundation_workplan.md` to v1.8: added T1.30 (error code taxonomy schema), T1.31 (pipeline message catalog schema), T1.32 (error/message manager modules), added Appendix D reference, updated deliverables. | opencode | ✅ Done |
| U043 | 2026-06-19 | Phase 1 | Documentation | Created `appendix_d_pipeline_messages_errors.md` v0.1: D1–D10 covering error code taxonomy (P{phase}-{module}-{function}-{id} + S-C-S-XXXX), system error catalog (30 codes), data error catalog (35 codes), pipeline message catalog (25 messages), health scoring formula, status lifecycle, implementation files. | opencode | ✅ Done |
| U041 | 2026-06-19 | Phase 1 | Documentation | Added revision history table (v0.1–v0.5) to `appendix_a_asset_schema.md` per AGENTS.md §13. Added `Phase` and `Status` metadata fields. Preserved existing inline summary notes. | opencode | ✅ Done |
| U040 | 2026-06-19 | Phase 1 | Documentation | Added revision history table (v0.1–v0.7) to `appendix_b_document_registry.md` per AGENTS.md §13. Bumped version to 0.7. Traces all changes from initial draft through section renumbering and DB path update. | opencode | ✅ Done |
| U039 | 2026-06-19 | Phase 1 | Refactor | Updated 8 files for DB path relocation: `eks_config.json`, `registry.py`, `test_phase1.py`, `.gitignore`, `AGENTS.md`, `appendix_b_document_registry.md` (×2), `phase_1_foundation_workplan.md`. Added `eks/output/` to `.gitignore`. All 20 Phase 1 tests + ontology validation pass. | opencode | ✅ Done |
| U038 | 2026-06-19 | Phase 1 | Refactor | Moved `eks_registry.db` from `eks/data/` to `eks/output/` to separate source input data (TWRP PDFs, Datadrop) from generated artifacts. Updated `connection_string` in `eks_config.json` and default fallback in `registry.py`. | opencode | ✅ Done |
| U037 | 2026-06-19 | Phase 1 | Documentation | Renumbered Appendix B sections for sequential ordering: B7→B6 (Document Registry Establishment Summary), B8→B7 (References). Updated all subsection numbers (B6.1–B6.3). | opencode | ✅ Done |
| U036 | 2026-06-18 | Phase 1/3 | Planning | Implemented Document Ontology & Lifecycle: Updated Appendix B & C, Asset/Ontology schemas, and Phase 3 Workplan to include Document Class Hierarchy (Drawing, Spec, Manual) and lifecycle relations (SUPERSEDES, SUPPLEMENTS, REFERENCES_DOC) per registry-to-graph mapping rules. | Gemini CLI | ✅ Done |
| U035 | 2026-06-18 | Phase 1/3 | Planning | Implemented Specialized Relationship Gap Closure: Updated Appendices A & C, Phase 1 & 3 Workplans, and Asset/Ontology schemas to include 5 new relationship pairs (Flow, Power, Control, Governance, Set Points) and target classes (Infrastructure, Governance) per agent_rule standards. | Gemini CLI | ✅ Done |
| U034 | 2026-06-18 | Phase 1 | Documentation | Updated `phase_1_foundation_workplan.md` to v1.5 COMPLETE. Updated `appendix_c_ontology.md` to v1.1. Synchronized all phase workplans with new naming conventions. | Gemini CLI | ✅ Done |
| U033 | 2026-06-18 | Phase 1 | Testing | Verified refactored architecture with full test suite: `test_phase1.py`, `test_asset_schema.py`, `validate_ontology.py`, and `test_loader_full.py` all PASS. | Gemini CLI | ✅ Done |
| U032 | 2026-06-18 | Phase 1 | Refactor | Refactored all schemas to the Triple-File Pattern (Base, Setup, Config) with explicit `allOf` inheritance. Renamed `eks_ontology.json` to `eks_ontology_config.json` for consistency. | Gemini CLI | ✅ Done |
| U031 | 2026-06-18 | Phase 1 | T1.25 | Extended `SchemaLoader.py` with cross-schema validation: verifies ontology fragments exist in asset base schema and config class map references real ontology classes. | Gemini CLI | ✅ Done |
| U030 | 2026-06-18 | Phase 1 | T1.23-T1.27 | Implemented ISO 15926-aligned ontology: Created `eks_ontology_base_schema.json`, `eks_ontology_setup_schema.json`, and `eks_ontology_config.json`. Categorized 13 asset fragments as functional/physical; linked ontology classes to fragments. | Gemini CLI | ✅ Done |
| U028 | 2026-06-16 | Phase 1 | T1.2 | Created `eks` conda environment from `eks.yml`. Resolved 3 compatibility issues: (1) pymupdf pinned to 1.27.2 (1.24.5 unavailable); (2) tiktoken changed from 0.7.0→0.13.0 (0.7.0 requires Rust compiler, no pre-built Py3.13 wheel); (3) psycopg2-binary relaxed to >=2.9 (2.9.9 has no Py3.13 wheel, 2.9.12 installed). Added `referencing==0.35.1` and `pytest` to env. All 22 Phase 1 tests pass in `eks` env. | System | ✅ Done |
| U027 | 2026-06-16 | Phase 1 | Testing | Full test run: 22/22 unit tests pass (test_phase1.py + test_asset_schema.py), validate_asset_schema.py PASS, verify_all.py ALL PASS (18/18). Fixed stale version assertions in verify_all.py (p3: 0.5→0.6, p4: 0.4→0.5) | System | ✅ Done |
| U026 | 2026-06-16 | Phase 1 | Review | Applied 6 review corrections: fixed v1.1 date typo in phase_1_foundation_workplan.md; added R36/R39 to Section 4 scope table; `pipeline_route.p_and_id_files` changed to array of strings in Appendix A; added `submergence_min` overlap note in A2.12; renumbered B11/B12→B5/B6 in Appendix B; clarified `asset_tags` as VARCHAR JSON string | System | ✅ Done |
| U025 | 2026-06-16 | Phase 1 | T1.22 | Implemented Extended Document Metadata: Added 11 new fields to schema and DB; implemented automatic `ALTER TABLE` migration in `registry.py`; added JSON serialization for `asset_tags` array. | Gemini CLI | ✅ Done |
| U024 | 2026-06-16 | Phase 1 | T1.21 | Implemented Document Registry remediation (G1-G3): Added `source_type` column to registry; implemented SQL column allowlist for `list_documents` (G2); migrated history sorting to SQL `ORDER BY` (G3). Updated `eks_base_schema.json`. | Gemini CLI | ✅ Done |
| U023 | 2026-06-16 | Phase 1 | — | Logged issue I006 (Document Registry Technical Gaps G1-G3) to `eks/log/issue_log.md` following Appendix B recommendation | Gemini CLI | ✅ Done |
| U021 | 2026-06-18 | Phase 1 | — | Updated `phase_1_foundation_workplan.md` v0.9 to COMPLETE. Marked T1.20 ✅, all scope R-items ✅, all success criteria ✅. Updated `eks_system_workplan.md` v0.6: Phase 1 status back to PASS. Updated `phase_1_foundation_report.md` v0.2. | System | ✅ Done |
| U020 | 2026-06-18 | Phase 1 | T1.15 | Added 7 new test cases to `eks/test/test_asset_schema.py` covering T1.20 and R39: schema file existence, 13-fragment definitions, config validation, conditional_fragments structure (AT_EQUIP), motor_control fragment (AT_MOTOR), all fragment names in base, all 14 AT_ types registered. All 7 tests pass. | System | ✅ Done |
| U019 | 2026-06-17 | Phase 1 | — | Added A7 (How to Add a New Plant Asset Type) to `appendix_a_asset_schema.md` v0.3. Three scenarios documented: existing fragments only, conditional fragment, new fragment. Decision guide and validation step included. | System | ✅ Done |
| U018 | 2026-06-17 | Phase 1 | R39 | Added R39 zero-code asset extensibility. Updated `eks_asset_setup_schema.json` with `conditional_fragments` structure and 13-fragment enum. Updated `eks_asset_config.json` with `conditional_fragments` rules for AT_EQUIP and AT_MOTOR; full column normalization across all 7 sheets. Validated all 3 schema files. | System | ✅ Done |
| U017 | 2026-06-16 | Phase 1 | T1.20 | Gap analysis against actual datadrop Excel (7 sheets). Found 14 unmapped Equipment columns, 10 Motor columns, 33 CONTROLVALVE actuator columns, 12 Instrument columns, 3 MANUALVALVE columns, 612 duplicate Pipeline KEYTAGs. Updated `eks_asset_base_schema.json` to 13 fragments (added `specialist_equipment`, `motor_control`; expanded `actuator`, `rotating_equipment`, `instrumentation`, `valve_internals`). Updated `appendix_a_asset_schema.md` v0.2. | System | ✅ Done |
| U016 | 2026-06-15 | Phase 1 | T1.17-T1.19 | Created asset schema files: `eks_asset_base_schema.json` (11 fragment definitions), `eks_asset_setup_schema.json` (asset_type_registry + column normalization declarations), `eks_asset_config.json` (14 AT_ type→fragment mappings + 7-sheet column normalization map). Appendix A extracted to `eks/workplan/appendix_a_asset_schema.md`. Validated config against setup schema. | opencode | ✅ Done |
| U015 | 2026-06-15 | Phase 1 | — | Logged 4 issues (I001-I004) to `eks/log/issue_log.md` | Gemini CLI | ✅ Done |
| U014 | 2026-06-15 | Phase 1 | T1.13 | Generated Phase 1 foundation test report at `eks/workplan/reports/phase_1_foundation_report.md` | Gemini CLI | ✅ Done |
| U013 | 2026-06-15 | Phase 1 | Fix | Removed schema metadata fields (`$schema`, `$id`, `version`, `title`, `description`) from `eks_setup_schema.json` properties; stripped from config before validation | Gemini CLI | ✅ Done |
| U012 | 2026-06-15 | Phase 1 | Fix | Migrated `schema_loader.py` and `verify_schema_metadata.py` from deprecated `RefResolver` to `referencing` library API | Gemini CLI | ✅ Done |
| U011 | 2026-06-15 | Phase 1 | Fix | Created missing `__init__.py` files for engine, core, parsers, logging packages per agent_rule §4.2 | Gemini CLI | ✅ Done |
| U010 | 2026-06-15 | Phase 1 | Refactor | Refactored EKS schema to support project-scoped discipline and rules registries. Updated ConfigRegistry and tests. | Gemini CLI | ✅ Done |
| U008 | 2026-06-15 | Phase 1 | T1.6-T1.14 | Implemented Phase 1 foundation components: schema_loader, registry, revision, config_registry, tiered logger, and abstract/concrete parsers (PDF, XLSX, DOCX) | Gemini CLI | ✅ Done |
| U007 | 2026-06-15 | Phase 1 | — | Validated eks_config.json against eks_setup_schema.json using jsonschema | Gemini CLI | ✅ Done |
| U006 | 2026-06-15 | Phase 1 | T1.3-T1.5 | Designed and implemented canonical schema: eks_base_schema.json, eks_setup_schema.json, and eks_config.json following agent_rule.md standards | Gemini CLI | ✅ Done |
| U005 | 2026-06-11 | Phase 1 | — | Created `eks/log/update_log.md` and `eks/log/issue_log.md` | System | ✅ Done |
| U004 | 2026-06-11 | Phase 1 | T1.2 | Created `eks/eks.yml` conda environment file covering all Phase 1–5 dependencies | System | ✅ Done |
| U003 | 2026-06-11 | Phase 1 | T1.1 | Created full EKS project folder scaffolding: archive, config, data, output, engine (core, logging, parsers, chunking, embedding, vector_store, graph, extractors, retrieval, cache), ui (static, routes, templates), test, docs, log, workplan/reports | System | ✅ Done |

## Notes

- Extracted from `eks/log/update_log.md` — Phase 1 entries only
- Each entry corresponds to a workplan task or phase milestone
- Timestamp format: YYYY-MM-DD
| U230 | 2026-07-29 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: Implemented 4-stage schema lifecycle across common/ and EKS (I259–I263).
| | | | | **I259** (Critical): Added discover_schema_files_tier3() to schema_discovery.py; wired into SchemaLoader._discover(); added regression test.
| | | | | **I260** (Medium): Added 3 _STEM_TO_ATTR entries (department, discipline, facility) + __init__ attributes.
| | | | | **I261** (Medium): Implemented _bootstrap_schema() with schema_loader hook in BootstrapManager; registered S-B-S-0616/S-B-S-0617; added validate_schema_conformance().
| | | | | **I262** (Low): Extracted build_uri_registry() to common/library/loader/ref_resolver.py.
| | | | | **I263** (Low): Refactored load_all() into _discover/_load/_validate/_extract; registered S-B-S-0618.
| | | | | **Files affected**: schema_discovery.py, __init__.py (common/loader), schema_loader.py, bootstrap/manager.py, validation/manager.py, ref_resolver.py (new), eks_error_config.json v1.6.0, p1_issue_log.md v41, p1_task_log.md, p1_update_log.md.
| | | | | **Tests**: 1 new (Tier 3 fallback). Full suite: 328/332 pass. |
| U231 | 2026-07-29 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: Follow-up fixes for I259–I263 implementation (post-merge cleanup).
| | | | | **Fixes**: (a) Added `document_type_schema` attr to `SchemaLoader.__init__()` (missing from I250 original gap — Tier 3 discovered but attr not initialized). (b) Fixed `pipeline_orchestrator.py:908` dead-code guard — merged `else` branch into `elif "project_number" in registry_props:` so `doc.get("project_title")` fallback is reachable when code→title lookup fails. (c) Fixed `\$id`/`\$ref` SyntaxWarning in `validation/manager.py` docstring.
| | | | | **Test fixes**: Updated `test_t132_modules.py` error code count 109→121. Fixed `TestBootstrapDegradation` config_dir references (`_PROJECT_ROOT`).
| | | | | **Files affected**: schema_loader.py (document_type_schema attr), pipeline_orchestrator.py (dead-code guard), validation/manager.py (SyntaxWarning), test_t132_modules.py (count 109→121), test_phase1.py (config_dir), test_tier3_fallback.py (new standalone). |
| U232 | 2026-07-29 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: T1.182–T1.184 complete — column-processing schema definitions, setup property, and config entries for all 42 document registry columns (I264).
| | | | | **T1.182** — Added 6 definitions to `eks_doc_base_schema.json` v1.9.0: column_type_enum, processing_phase_enum, calculation_strategy_def, handler_def, validation_rule_def, column_processing_entry_def.
| | | | | **T1.183** — Added `column_processing` property (object with column-name keys, DCC-aligned key-as-name pattern) to `eks_doc_setup_schema.json` v1.7.0.
| | | | | **T1.184** — Populated 42 column processing entries in `eks_doc_config.json` v1.6.0: 9 Phase A columns (file_path, file_type, document_number, project_number, area, document_type, discipline, sequence_number, revision) + 33 Phase B columns (project_title, document_title, file_size, file_created_at, file_modified_at, file_hash, 9 embedded_* fields, page_count, created_by, checked_by, approved_by, originator_company, asset_tags, references_documents, language, vendor_name, total_sheets, lifecycle_stage, revision_description, revision_date, project_phase, contract_package, issued_date, responsible_engineer).
| | | | | **Files affected**: eks_doc_base_schema.json v1.8.0→1.9.0, eks_doc_setup_schema.json v1.6.0→1.7.0, eks_doc_config.json v1.5.0→1.6.0, p1_issue_log.md v43→v44, p1_task_log.md §75, p1_update_log.md. |
| U233 | 2026-07-29 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: T1.185 complete — common/library/column_processor/ package + EKSColumnProcessor subclass (I264).
| | | | | **common/library/column_processor/**: BaseColumnProcessor (phase-filtered dispatch + fallback), HandlerRegistry (register/lookup by calc type), ColumnProcessorError. Registered in common/library/__init__.py.
| | | | | **eks/engine/core/column_processor.py**: EKSColumnProcessor(BaseColumnProcessor) with 9 pre-registered handler stubs + from_doc_config() factory.
| | | | | **Verified**: All imports, dispatch, registry, fallback, factory, and Phase B processing pass.
| | | | | **Files affected**: common/library/column_processor/{__init__.py,base.py,registry.py} (new), common/library/__init__.py (updated), eks/engine/core/column_processor.py (new), p1_task_log.md §75, p1_update_log.md. |
| U234 | 2026-07-29 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: T1.187 complete — ColumnProcessor wired into PipelineOrchestrator phases (I264).
| | | | | **Core change**: Added EKSColumnProcessor import and `self._column_processor` to `PipelineOrchestrator.__init__()`. In `_process_file()` (Phase B), replaced 3 hardcoded blocks (project_title priority chain, asset_tags cover-page extraction, cover_project_title extraction) with a single `ColumnProcessor.process("B", registry_props, context)` call. Added `ColumnProcessor.process("A")` after Phase A placeholder registration and `ColumnProcessor.process("C")` after Phase C review loop for schema-driven future-proofing.
| | | | | **Ancillary fixes**: (a) Fixed bare `doc_config` bug in `_process_file()` (line used bare name `doc_config` instead of `self.doc_config`). (b) Changed `position` schema type from `"integer"` to `["integer", "null"]` — `revision` column uses `position: null` (separator-based extraction). (c) Changed `min_length` schema minimum from 1 to 0 — `asset_tags` uses `min_length: 0`.
| | | | | **Files affected**: eks/engine/core/pipeline_orchestrator.py (import + __init__ + 3 process() call sites + elements loop simplified), eks/config/schemas/eks_doc_base_schema.json (position type, min_length minimum), p1_task_log.md §75 (T1.187→✅ COMPLETE, Status Summary 293→294), p1_update_log.md. |
| U238 | 2026-07-30 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: T1.192 complete — SchemaLoader compatibility verified for Project Definition (I265).
| | | | | **Changes**: Added `eks_project_definition_config` to `_STEM_TO_ATTR` mapping + `project_definition_config` attr. Added `_validate_project_definition()` validation stage with per-entry jsonschema validation. Added `$ref` resolution in `_discover()` (resolves before `_validate_config()` to satisfy object type check). Backward-compat injection in `_extract()` reconstructs `filename_patterns` and `revision_validation` from Project Definition. Contract boundary documented: SchemaLoader returns raw validated schema objects; no RuntimeProjectDefinition assembly.
| | | | | **Files affected**: `eks/engine/core/schema_loader.py` v1.1.0→1.2.0, `p1_task_log.md` (T1.192→✅ COMPLETE), `p1_update_log.md` (U238). |
| | | | | **Tests**: 364/368 pass (4 pre-existing failures unrelated to these changes). |
| U237 | 2026-07-30 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: T1.191 complete — reusable configuration libraries refactored (I265).
| | | | | **Changes**: Removed `revision_validation` (per-project revision patterns) from `eks_doc_config.json`. Removed `filename_patterns` (per-project filename parsing) from `eks_doc_config.json`. Added `filename_profiles` as reusable profile-keyed section (`twrp_standard`, `default`). Added `revision_validation` field to each project entry in `eks_project_definition_config.json`. Added `revision_validation` property to `project_definition_entry_def` in `eks_base_schema.json` v1.14.0. Updated `eks_doc_setup_schema.json` v1.8.0 — replaced `filename_patterns` with `filename_profiles` property, removed `revision_validation` from required. Backward-compat injection in SchemaLoader._extract() populates `doc_config["filename_patterns"]` and `doc_config["revision_validation"]` from Project Definition data.
| | | | | **Files affected**: `eks/config/schemas/eks_doc_config.json` v1.6.0→1.7.0, `eks/config/schemas/eks_doc_setup_schema.json` v1.7.0→1.8.0, `eks/config/schemas/eks_base_schema.json` v1.13.0→1.14.0, `eks/config/schemas/eks_project_definition_config.json` v1.1.0→1.2.0, `eks/engine/core/schema_loader.py` v1.1.0→1.2.0, `p1_task_log.md` (T1.191→✅ COMPLETE), `p1_update_log.md` (U237). |
| | | | | **Tests**: 364/368 pass (4 pre-existing failures unrelated to these changes). |
| U236 | 2026-07-30 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: T1.199 cancelled — `eks_config.json` already serves as environment configuration.
| | | | | **Rationale**: `eks_config.json` already holds all deployment-specific settings (`vector_store`, `embedding`, `registry`, `global_paths`, `logging`, `system_parameters`). Creating a separate environment config file would duplicate this. The principle of L.6.3 (deployment config separate from Project Definition) is already satisfied. Remaining scope (add `graph_db`, `storage`, `messaging` properties) absorbed into T1.196.
| | | | | **Files affected**: `p1_task_log.md` (T1.199→⛔ Cancelled, Status Summary Planned 34→33, Won't Implement 0→1), `p1_update_log.md` (U236). |
| U235 | 2026-07-29 | opencode | Phase 1 | ✅ Done |
| | | | | **Summary**: T1.188 complete — regression tests for ColumnProcessor central orchestrator (I264).
| | | | | **New file**: `eks/test/test_column_processing.py` (25 tests, 5 test classes).
| | | | | **Test coverage**: (a) `TestColumnProcessorDispatch` — 9 tests verifying each `calculation.type` dispatches to correct handler (priority_chain, filename_segment, file_property, parser_metadata, cover_page_element, code_to_title_lookup, health_score, auto_increment, existing_record). (b) `TestPriorityChainResolution` — 5 tests verifying resolution order (cover > metadata > lookup > existing) and all-sources-none returns None. (c) `TestColumnProcessingFallback` — 1 test for leave_null behavior. (d) `TestFortyTwoColumnConfigValidation` — 3 tests: entry count (42+), required field presence, SchemaLoader validation. (e) `TestColumnProcessingEndToEnd` — 7 tests: Phase A filename pass-through, Phase B project_title from cover, asset_tags split, priority chain order, document_title, total_sheets, health score.
| | | | | **Files affected**: eks/test/test_column_processing.py (new, 518 lines), p1_task_log.md §75 (T1.188→✅ COMPLETE, Status Summary 294→295), p1_update_log.md. |

